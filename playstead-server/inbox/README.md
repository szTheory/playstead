# The default inbox folder for a native `mix phx.server` dev run.
#
# `config/runtime.exs` points `:inbox_path` here in the `:dev` environment,
# and docker-compose bind-mounts this same directory read-only at
# `/app/inbox`. Drop ROMs in here and they show up in the import UI.
