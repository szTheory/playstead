defmodule Playstead.Catalogue do
  @moduledoc """
  The user-scoped logical catalogue context: `Playstead.Catalogue.AssetSet`
  and `Playstead.Catalogue.AssetMember`. `member_fingerprint/1` (task 3)
  is the natural key that makes "no duplicate logical record" a
  database guarantee via the unique index on the user/fingerprint pair.
  """
end
