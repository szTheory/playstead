# Criterion 6 proof — PairingCeremonyTests over real TLS

Produced by `playstead-mac/scripts/dev/prove-pairing-ceremony.sh` on 2026-09-10T01:29:01Z.
Commit: 30a4e503c5ebdb2a7a1b28a39febe5c207de9092

## Run 1 — ceremony against the TLS-terminating mac_ci endpoint

- **PASS** tls handshake verified
  ```
  == tls handshake verified
  ```
- **PASS** ceremony test passed
  ```
  Test Case '-[PlaysteadUITests.PairingCeremonyTests testAHumanCanPairAFreshMacEntirelyFromInsideTheAppAgainstTheRealServer]' passed (27.599 seconds).
  ```
- **PASS** one test, zero failures
  ```
  	 Executed 1 test, with 0 failures (0 unexpected) in 27.599 (27.600) seconds
  ```
- **PASS** no skipped/failed ceremony — count=0 (expected 0)
- **PASS** server saw pairing request — count=2 (expected min:1)
- **PASS** server saw redeem — count=1 (expected min:1)

## Run 2 — live-server layer regression over https

- **FAIL** run `https`: harness exited 65
- **FAIL** four tests, zero failures -- no line matching `Executed 4 tests, with 0 failures` in `/tmp/playstead-ceremony-proof/https/native-services/live-server.log`
- **FAIL** no skipped/failed anywhere — count=1, expected 0, pattern `Test Case .* (skipped|failed)`

## Run 3 — ceremony still green with the WR-01 #if DEBUG gate in place

- **PASS** ceremony test passed
  ```
  Test Case '-[PlaysteadUITests.PairingCeremonyTests testAHumanCanPairAFreshMacEntirelyFromInsideTheAppAgainstTheRealServer]' passed (27.943 seconds).
  ```
- **PASS** not zero tests — count=0 (expected 0)

## Teardown — the run's CA is gone from the System keychain

- **PASS** no `Playstead Mac CI Root` remains in the System keychain.

## Verdict: FAIL — 3 check(s) failed.
