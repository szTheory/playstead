---
phase: 04.5-mac-pairing-ceremony
plan: 05
subsystem: infra
tags: [ci, tls, xcuitest, mac-ci-tls, pairing-ceremony, gate-fail-closed]

requires:
  - phase: 04.5-mac-pairing-ceremony (04.5-04)
    provides: mac-ci-tls.sh (issue/trust/untrust/fingerprint), TLS-only mac_ci Phoenix config, one recorded green PairingCeremonyTests run
provides:
  - Hosted runner issues, trusts, uses, and removes a per-run TLS anchor for its native Phoenix, with no plaintext health-probe fallback
  - PairingCeremonyTests promoted to a --required-test of the live-server CI layer, anchored in three source locations
  - pairing-ceremony-gate-test.sh proving the promoted gate fails closed by construction (pass/failed/skipped/absent)
  - PROT-01 marked complete, citing the recorded green ceremony run
affects: [phase-05-self-hoster-operability, any future mac_ci CI runner changes]

actuals:
  tokens: 9200
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "required_exact restructured from one identifier per layer to a list per layer in the complete-evidence validator, keeping all existing entries"
    - "cleanup functions untrust before removing the directory they read from, so the EXIT trap sequencing stays correct on every failure path"

key-files:
  created:
    - playstead-mac/scripts/ci/tests/pairing-ceremony-gate-test.sh
  modified:
    - playstead-mac/scripts/ci/run-mac-verification.sh
    - .planning/REQUIREMENTS.md

key-decisions:
  - "TLS issue/trust calls land between the mkdir/chmod block and Postgres initdb in start_native_services, each with its own die message, so a hosted failure names which of the two steps died."
  - "The https health probe builds its SSLContext from <server_root>/tls/ca.pem and has no plaintext URL and no broad try/except — a TLS misconfiguration surfaces through the existing 'exited before health' / 'health deadline exceeded' exit paths rather than hanging."
  - "cleanup_native_services calls mac-ci-tls.sh untrust before tearing down anything else (Phoenix, Postgres, the native root), because untrust reads the CA out of the directory that removal would otherwise delete first."
  - "required_exact became a dict of lists (not a rename) so the existing UI and live-server single-identifier checks kept working unmodified; only the live-server list gained the ceremony identifier."
  - "PairingCeremonyTests was NOT added to LiveServer.xctestplan's selectedTests in this plan (it already has a 5th entry per 04.5-04) and four-layer-topology-test.sh's stale expected_live set was left untouched per the plan's scope note — that failure is pre-existing and unrelated to --required-test wiring."

patterns-established:
  - "A CI cleanup function's teardown order follows dependency direction: remove things that read from a resource before removing the resource itself."

requirements-completed: [PROT-01]

coverage:
  - id: D1
    description: "The hosted runner issues, trusts, health-probes over https, and untrusts its own per-run TLS anchor, with no plaintext fallback."
    requirement: PROT-01
    verification:
      - kind: other
        ref: "bash -n playstead-mac/scripts/ci/run-mac-verification.sh"
        status: pass
      - kind: other
        ref: "grep -v '^[[:space:]]*#' run-mac-verification.sh | grep -c urlopen(\"http://127.0.0.1:4010 -> 0"
        status: pass
    human_judgment: true
    rationale: "This executor has no passwordless sudo and no TTY, so mac-ci-tls.sh trust/untrust and the full runner sequence could only be statically verified here, not executed. A hosted run (or a local run with sudo) is required to prove the live TLS handshake through this code path; local-live-server.sh already proves the underlying mac-ci-tls.sh script works, but not this specific runner wiring."
  - id: D2
    description: "PairingCeremonyTests is a --required-test of the live-server layer; a failed, skipped, or absent ceremony node fails --verify-layer-result."
    requirement: PROT-01
    verification:
      - kind: other
        ref: "playstead-mac/scripts/ci/tests/pairing-ceremony-gate-test.sh"
        status: pass
      - kind: other
        ref: "run-mac-verification.sh --self-test-contracts (contract: pairing-ceremony-gate-test.sh line)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The ceremony identifier is anchored against silent removal via verify_required_uat_allowlist, and the complete-evidence validator's required_exact also requires it for the live-server layer."
    requirement: PROT-01
    verification:
      - kind: other
        ref: "run-mac-verification.sh --self-test-contracts (contract: cross-layer UAT allowlist anchors; contract: complete hosted-run validator fixtures)"
        status: pass
    human_judgment: false
  - id: D4
    description: "PROT-01 marked complete in REQUIREMENTS.md, citing the recorded green ceremony run rather than a registration."
    requirement: PROT-01
    verification:
      - kind: other
        ref: "grep -aE \"Test Case .*PairingCeremonyTests.* passed\" .planning/phases/04.5-mac-pairing-ceremony/04.5-04-SUMMARY.md"
        status: pass
    human_judgment: false

duration: 35min
completed: 2026-09-09
status: complete
---

# Phase 04.5 Plan 05: Fail-closed CI wiring for the pairing ceremony, and PROT-01 completion Summary

**Wired mac-ci-tls.sh into the hosted runner's TLS lifecycle, promoted PairingCeremonyTests to a --required-test of the live-server layer with a gate that is proven (not just asserted) to fail closed, and flipped PROT-01 citing the recorded green run.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-09-09T00:00:00Z (approx.)
- **Completed:** 2026-09-09T00:35:00Z (approx.)
- **Tasks:** 3
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- `start_native_services` now issues and trusts a per-run TLS anchor before Phoenix bootstraps, and the readiness probe validates over https against that anchor's own CA — no plaintext probe URL survives anywhere in the runner outside comments.
- `cleanup_native_services` calls `mac-ci-tls.sh untrust` first, through the already-armed EXIT trap, before any other teardown step, and fails the cleanup (and therefore the run) if that removal does not succeed.
- `PairingCeremonyTests/testAHumanCanPairAFreshMacEntirelyFromInsideTheAppAgainstTheRealServer` is now one of five `--required-test` entries on the live-server layer, anchored in three places in the runner source (the layer invocation, `verify_required_uat_allowlist`, and the restructured `required_exact` per-layer allowlist).
- `pairing-ceremony-gate-test.sh` builds synthetic Xcode result JSON and proves — by actually invoking `--verify-layer-result`, not by grepping source — that a passing ceremony node exits 0 and a failed, skipped, or entirely absent ceremony node exits non-zero, plus that the identifier is anchored at least twice in the runner source.
- PROT-01 is `[x]` in REQUIREMENTS.md, its annotation naming the TLS-terminating live-server proof and citing `04.5-04-SUMMARY.md`, and its status-table row reads `Complete`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Provision, trust, and tear down the hosted runner's TLS material, and probe health over https** - `899e728` (feat)
2. **Task 2: Promote PairingCeremonyTests to --required-test and prove the promoted gate fails closed** - `3ec7895` (feat)
3. **Task 3: Flip PROT-01 to complete, citing the recorded run** - `3e8d460` (docs)

**Plan metadata:** (this commit)

## Files Created/Modified
- `playstead-mac/scripts/ci/run-mac-verification.sh` - TLS issue/trust/untrust wired into `start_native_services`/`cleanup_native_services`, https health probe, ceremony test added to `--required-test`, `verify_required_uat_allowlist`, and `required_exact` (restructured to a list per layer), plus the `run_complete_hosted_self_tests` fixture updated to keep matching `required_exact`'s new shape
- `playstead-mac/scripts/ci/tests/pairing-ceremony-gate-test.sh` - new self-test proving the promoted gate fails closed (pass/failed/skipped/absent + anchor count)
- `.planning/REQUIREMENTS.md` - PROT-01 checkbox (line 19) and status-table row (line 122) flipped to complete, citing `04.5-04-SUMMARY.md`

## Decisions Made
- Placed the TLS issue/trust calls between the native root's mkdir/chmod block and Postgres `initdb`, rather than immediately before `mix phx.server`, so both steps land clearly "before the Phoenix bootstrap" as the plan specifies and each gets an independently-named `die` failure.
- Restructured `required_exact` to a dict of lists instead of introducing a second parallel structure, so the existing UI/live-server single-identifier checks needed no behavioral change beyond widening to "all of" instead of "the one."
- Left `four-layer-topology-test.sh` and `LiveServer.xctestplan` untouched: this plan's changes are entirely in `run-mac-verification.sh`'s `--required-test`/allowlist wiring and do not require or depend on the xctestplan's `selectedTests` set, so the plan's "only if my changes require it" condition does not apply here.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

**Pre-existing failure, not caused by this plan:** `playstead-mac/scripts/ci/tests/four-layer-topology-test.sh` fails both before and after this plan's changes, for the reason already logged in `.planning/phases/04.5-mac-pairing-ceremony/deferred-items.md` — `LiveServer.xctestplan`'s `selectedTests` already contains a 5th entry (the pairing ceremony) that the test's hardcoded `expected_live` set does not account for. Confirmed via `git log -p` that this test's file and the xctestplan predate 04.5-04's dispatch and are outside this plan's `files_modified` list. `--self-test-contracts` therefore still exits non-zero overall (the same single failing contract as before this plan), while every contract this plan touches or added — `pairing-ceremony-gate-test.sh`, `four-layer-verifier-test.sh`, `cross-layer UAT allowlist anchors`, `complete hosted-run validator fixtures` — passes.

**Sudo/TTY constraint honored as instructed:** This executor has no passwordless sudo and no TTY. `mac-ci-tls.sh trust`/`untrust`, and therefore the full hosted-runner TLS sequence and `local-live-server.sh`, could not be executed here. All new code was statically verified (`bash -n`, grep-based acceptance criteria, and the gate self-test against synthetic result JSON) rather than run end-to-end. The hosted CI run has not yet executed against this plan's commits (they are unpushed as of this SUMMARY), so the hosted-layer evidence for Task 1's TLS wiring specifically is still pending a push and a real hosted run.

## User Setup Required

None - no external service configuration required.

## `--self-test-contracts` tail (this session)

```
contract: pairing-ceremony-gate-test.sh
pairing-ceremony-gate: pass/failed/skipped/absent/anchor-count checks all passed
...
contract: complete hosted-run validator fixtures
complete hosted validator rejected 17 identity/status/schema/job fixtures
contract: cross-layer UAT allowlist anchors
verified 12 cross-layer UAT allowlist anchors
contract: validator unit tests
----------------------------------------------------------------------
Ran 42 tests in 1.916s

OK
contract: phase-3 UAT/roadmap evidence boundaries
phase-3 UAT and Roadmap evidence boundaries verified
mac verification: one or more static contract guards failed
```

The only failing contract in that run is the pre-existing, unrelated `four-layer-topology-test.sh` documented above under Issues Encountered — every contract this plan added or touched passed, including `contract: pairing-ceremony-gate-test.sh`.

**Not yet run:** the hosted GitHub Actions `mac-verification` job. This plan's three commits (and 04.5-04's TLS/ceremony work before them) are not yet pushed, so there is no hosted-layer evidence yet that exercises the real `mac-ci-tls.sh trust`/`untrust` calls, the https health probe against a real Phoenix, or the promoted `--required-test` gate under a real xcresult. That hosted proof is the next required step before this criterion can be considered closed in the fully durable sense the plan's success criteria describe.

## Assumptions carried forward (flagged, not resolved)

- **A-01 (concurrency, carried from 04.5-04):** `start_native_services` dies if `NATIVE_ROOT` already exists and `mac-ci-tls.sh issue` dies if `tls/` already exists, so two concurrent runs sharing a root cannot clobber each other's TLS material — but this plan still does not prove trust removal under a real SIGKILL of the hosted job. The EXIT-trap composition (`restore_live_server_xctestrun; cleanup_live_server_runtime_config; cleanup_native_services; restore_keyboard_mode`) reaches `untrust` on every bash-level exit path, but a SIGKILL that bypasses bash's trap machinery entirely is still unproven.
- **A-04 (passwordless sudo, carried from 04.5-04):** The hosted runner is assumed to have passwordless `sudo`; `mac-ci-tls.sh trust` dies with the distinct token `mac-ci-tls: passwordless sudo unavailable` if not, surfacing as a runner failure rather than a silently untrusted run. This executor's own lack of passwordless sudo is exactly the condition that token exists to catch, which is why Task 1's live TLS trust step could not be executed here.

## Next Phase Readiness

- ROADMAP criterion 6's CI wiring is complete and statically proven fail-closed; PROT-01 is marked complete citing the 04.5-04 recorded run.
- Outstanding before this can be considered fully durable: push these commits and observe one hosted `mac-verification` run exercise the new TLS/trust/untrust sequence and the promoted `--required-test` gate for real.
- `SaveEndToEndTests` remains a known-red, deliberately deferred item (`.planning/todos/pending/save-e2e-duplicate-revision-409.md`), unrelated to this plan's scope, unchanged here.
- `four-layer-topology-test.sh`'s stale `expected_live` set remains open per `deferred-items.md`; a follow-up plan should update it to include `PairingCeremonyTests` and re-verify the `mac_ci_fixture` occurrence count.

---
*Phase: 04.5-mac-pairing-ceremony*
*Completed: 2026-09-09*
