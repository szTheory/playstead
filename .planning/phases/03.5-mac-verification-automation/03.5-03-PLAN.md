---
phase: 03.5-mac-verification-automation
plan: "03"
type: execute
wave: 3
depends_on: [03.5-02]
files_modified:
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/Playstead/UITesting/UITestBootstrap.swift
  - playstead-mac/Playstead/UITesting/DeterministicProfile.swift
  - playstead-mac/Playstead/Design/AccessibilityIdentifiers.swift
  - playstead-mac/Playstead/Design/FocusRing.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/DeterministicProfileTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift
autonomous: true
requirements: [QUAL-01, LIBR-03, LIBR-04]
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Finite compile-gated profiles seed production stores under unique roots, assert exact counts/states, and fail closed rather than using fallback (D-08, D-09)."
    - "A non-testing Release product contains no bootstrap/profile type, key, symbol, or test-only composition path (D-08)."
    - "Stable identifiers never derive from user/content/security data, and one shared #38BDF8 focus ring implements the locked focus seam (D-18, D-22)."
  artifacts:
    - path: "playstead-mac/Playstead/UITesting/DeterministicProfile.swift"
      provides: "Finite production-store profile enum"
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/DeterministicProfileTests.swift"
      provides: "Exact profile non-vacuity and isolation tests"
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift"
      provides: "Release binary/symbol/key absence gate"
    - path: "playstead-mac/Playstead/Design/AccessibilityIdentifiers.swift"
      provides: "Namespaced machine identity vocabulary"
    - path: "playstead-mac/Playstead/Design/FocusRing.swift"
      provides: "Shared production focus contract"
  key_links:
    - from: "playstead-mac/Playstead/App/PlaysteadApp.swift"
      to: "playstead-mac/Playstead/UITesting/DeterministicProfile.swift"
      via: "compile-gated fail-closed AppEnvironment composition"
      pattern: "UI_TESTING"
  prohibitions:
    - statement: "MUST NOT execute arbitrary profile data/SQL, use proprietary or user-derived content, or ship test hooks in Release."
      status: unverified
      flagged: true
---

<objective>
Provide release-safe deterministic composition and the shared production identity/focus seams used by rendering and XCUITest.

Purpose: Later tests must exercise real stores and real focus/identity while eliminating fallback and shipped-hook false greens.
Output: Finite profiles, fail-closed bootstrap, release absence proof, identifiers, and focus ring.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
@.planning/phases/03.5-mac-verification-automation/03.5-02-SUMMARY.md
@playstead-mac/Playstead/App/PlaysteadApp.swift
@playstead-mac/Playstead/App/AppPaths.swift
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Launch one exact production-store profile and prove it fails closed</name>
  <files>playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/UITesting/UITestBootstrap.swift, playstead-mac/Playstead/UITesting/DeterministicProfile.swift, playstead-mac/PlaysteadTests/SnapshotTests/DeterministicProfileTests.swift</files>
  <behavior>
    - One populated profile creates a unique AppPaths root, seeds fixed synthetic catalogue/curation/queue/pin/quota/CAS state through production APIs, and asserts exact counts/state set before launch.
    - Missing/unknown profile or root/store failure aborts UI-testing composition; production fallback behavior remains unchanged outside the compilation condition.
  </behavior>
  <action>Under the dedicated UI-testing condition, add the finite D-09 enum for empty library, populated library/curation/reorder, paused/active queue, quota block/reclaim, and storage. Use fixed synthetic IDs, bytes, digests, positions, UTC timestamps, and exact expected counts/state sets through production stores under a unique `AppPaths(root:)`. Extend the single AppEnvironment composition root; in UI-test mode fail closed on root/database/profile/bootstrap failure rather than using in-memory fallback. Keep the live-server profile empty so Plan 08 receives catalogue state only from snapshot. Add tests for zero/one/three boundaries, invalid input, unique cleanup, and every exact state set.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/DeterministicProfileTests</automated></verify>
  <done>Deterministic state is finite, isolated, non-vacuous, production-store-backed, and fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Release absence and establish stable identity/focus primitives</name>
  <files>playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/UITesting/UITestBootstrap.swift, playstead-mac/Playstead/UITesting/DeterministicProfile.swift, playstead-mac/Playstead/Design/AccessibilityIdentifiers.swift, playstead-mac/Playstead/Design/FocusRing.swift, playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift</files>
  <behavior>
    - A build without the test condition lacks every selected bootstrap/profile symbol and environment key and cannot reference test composition.
    - Identifiers are namespaced and independent of labels, titles, filenames, paths, hashes, credentials, and coordinates.
    - Focused controls render the shared #38BDF8 overlay and unfocused controls do not, without equating selection to focus.
  </behavior>
  <action>Add D-08 binary/string/symbol/compilation-boundary guards, including a seeded positive-control failure, against a non-testing Release build. Create the D-18 namespaced identifier vocabulary and D-22 SwiftUI `.focused()` plus custom `#38BDF8` overlay for reuse by production views. Keep both primitives production-safe while keeping bootstrap/profile code entirely compilation-gated. Do not add a pairing credential override or test network endpoint.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/ReleaseHookAbsenceTests</automated></verify>
  <done>Release artifacts are test-hook-free and later plans share one stable identity and visible-focus contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Profile selector → production stores | Finite test input controls real local persistence APIs. |
| UI-testing build → Release product | Test-only composition must not cross into shipping code. |

## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-08 | Elevation | bootstrap/profile Release leakage | high | mitigate | Compilation condition plus binary/string/symbol absence and positive-control tests. |
| T-03.5-09 | Tampering | profile selector/root | high | mitigate | Finite enum, unique validated roots, exact production-store counts/states, fail-closed composition. |
| T-03.5-10 | Information disclosure | identifiers/fixtures | medium | mitigate | Fixed synthetic identity; prohibit user/content/security-derived identifiers. |
</threat_model>

<verification>
- Run profile and Release absence tests through the Rendering layer.
- Deliberately seed a forbidden hook symbol and confirm the guard fails.
- Confirm each profile removes its unique root.
</verification>

<success_criteria>
- All required states are exact, finite, and production-store-backed.
- Release cannot execute or reveal the test bootstrap/profile path.
- Production views can adopt shared stable IDs and visible focus without data-derived identity.
</success_criteria>

## Artifacts this phase produces
- `PlaysteadApp.swift`: compile-gated fail-closed UI-test composition.
- `UITestBootstrap.swift`: single test bootstrap extended later for one-use credentials.
- `DeterministicProfile.swift` and `DeterministicProfileTests.swift`: finite exact profile contract.
- `AccessibilityIdentifiers.swift`: stable namespaced identity vocabulary.
- `FocusRing.swift`: shared focus overlay.
- `ReleaseHookAbsenceTests.swift`: Release binary/symbol/key guard.

<output>Create `.planning/phases/03.5-mac-verification-automation/03.5-03-SUMMARY.md` when done.</output>
