---
phase: 03.5-mac-verification-automation
plan: "06"
subsystem: mac-curation-verification
tags: [swiftui, xcuitest, curation, drag-reorder, keyboard-focus, durable-outbox]

requires:
  - phase: 03.5-mac-verification-automation
    provides: Deterministic compile-gated UI profiles, production library routes, stable accessibility surfaces, and prompt-safe hosted Mac verification
provides:
  - Stable collection, queue, and shelf roots plus distinct collection/queue row and reorder-action identities
  - One shared production settlement path for drag and keyboard reorder, with boundary guards and retained focus
  - UUID-scoped deterministic profile reuse across process relaunch and bounded order/outbox/catalogue evidence
  - Exact named XCUITest for five shelf states, drag, keyboard parity, one-effect accounting, content identity, and relaunch durability
affects: [03.5-08, 03.5-09, mac-verification, curation, accessibility]

actuals:
  tokens: 11755
  tasks: 2
  commits: 6

tech-stack:
  added: []
  patterns: [shared drag-keyboard settlement, stable row-scoped action identity, UUID-only persisted test profile, predicate-based durable evidence]

key-files:
  created:
    - playstead-mac/PlaysteadUITests/CurationInteractionTests.swift
  modified:
    - playstead-mac/Playstead/Curation/CollectionDetailView.swift
    - playstead-mac/Playstead/Curation/QueueShelfView.swift
    - playstead-mac/Playstead/Library/ShelfView.swift
    - playstead-mac/Playstead/App/PlaysteadApp.swift
    - playstead-mac/Playstead/UITesting/DeterministicProfile.swift
    - playstead-mac/Playstead/UITesting/UITestBootstrap.swift
    - playstead-mac/PlaysteadUITests/Support/UITestHarness.swift
    - playstead-mac/scripts/ci/run-mac-verification.sh
    - playstead-mac/scripts/ci/tests/four-layer-topology-test.sh
    - playstead-mac/scripts/ci/tests/keyboard-mode-cleanup-test.sh

key-decisions:
  - "Drag and keyboard reorder call one private settleMove function per production surface, so neither interaction can bypass the begin/preview/commit/refresh discipline."
  - "Relaunch verification accepts only a UUID session token resolved beneath a fixed temporary parent; no arbitrary path, SQL, JSON, credential, or fixture bytes enter through the process environment."
  - "Compile-gated evidence exposes only synthetic row IDs, an outbox count, and sorted catalogue digests; it excludes filenames, paths, payload JSON, credentials, and user content."

requirements-completed: [QUAL-01, LIBR-03]

coverage:
  - id: D1
    description: "Five curation routes expose exact populated or honest-empty fixture states, and collection detail exposes exactly three distinct production rows."
    requirement: LIBR-03
    verification:
      - kind: static_contract
        ref: "Plan 06 static contract: 20 checks passed; full Swift source/UI-test parser passed"
        status: pass
      - kind: automated_ui
        ref: "PlaysteadUITests/CurationInteractionTests/testFiveShelvesAndDurableDragReorder"
        status: pending_hosted
    human_judgment: false
  - id: D2
    description: "Real drag and ordinary keyboard reorder each settle through one production mutation path, increment the outbox exactly once, preserve catalogue content identity, and survive process relaunch."
    requirement: LIBR-03
    verification:
      - kind: static_contract
        ref: "Shared-path/boundary/focus/session/evidence source contracts passed"
        status: pass
      - kind: automated_ui
        ref: "Centrally orchestrated hosted UI-layer run required; local app launch prohibited"
        status: pending_hosted
    human_judgment: false
  - id: D3
    description: "Plan 05 no-password safety remains intact while persisted UI sessions stay compile-gated and path-contained."
    requirement: QUAL-01
    verification:
      - kind: security_contract
        ref: "keychain-prompt-safety-test.sh and four-layer-topology-test.sh passed"
        status: pass
    human_judgment: false

duration: 26m20s
completed: 2026-09-01
status: complete
---

# Phase 03.5 Plan 06: Durable Curation Reorder Summary

**Production collection and queue rows now support stable drag plus boundary-guarded keyboard reorder through one durable mutation path, with an exact hosted XCUITest contract for five shelf states, one-effect accounting, retained focus, unchanged content identity, and process-relaunch persistence.**

## Performance

- **Duration:** 26m20s
- **Started:** 2026-09-01T06:17:37Z
- **Completed:** 2026-09-01T06:43:57Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments

- Added stable roots and row/action identifiers to the real collection, queue, and shared shelf views while retaining SwiftUI `.onMove`.
- Added visible Move Up/Move Down actions with first/last boundary disabling and explicit focus retention on the moved row action.
- Unified drag and keyboard interactions behind the same `settleMove` begin/preview/commit/refresh path so one user action produces one durable outbox intent.
- Added UUID-only compile-gated profile persistence and bounded synthetic evidence so the named XCUITest can prove exact order/outbox/catalogue state after two process relaunches without accepting arbitrary filesystem input.
- Added `testFiveShelvesAndDurableDragReorder`, covering exact populated/honest-empty shelf fixtures, three distinct rows, real element-to-element drag, keyboard activation, boundary states, focus retention, exact effect counts, catalogue digest stability, and durability.

## Task Commits

1. **Task 1 RED: failing five-shelf and durable drag contract** — `f92e02d` (test)
2. **Task 1 GREEN: stable rows, bounded evidence, and persisted relaunch profile** — `b0e488f` (feat)
3. **Task 2 RED: failing keyboard parity contract** — `67a27f5` (test)
4. **Task 2 GREEN: boundary-guarded keyboard controls and retained focus** — `9f7e281` (feat)
5. **Rule 1 correction: wait for durable settlement before reading frames** — `c80e466` (fix)
6. **Hosted blocker repair: preserve the underlying early failure across cleanup** — `a4ef343` (fix)

## Files Created/Modified

- `PlaysteadUITests/CurationInteractionTests.swift` — exact five-route, drag, keyboard, effect, focus, content-identity, and relaunch proof.
- `CollectionDetailView.swift` and `QueueShelfView.swift` — stable rows/actions, shared drag-keyboard settlement, boundary guards, and focus ownership.
- `Library/ShelfView.swift` — explicit production shelf root identity at the file's real location.
- `UITestHarness.swift` — opt-in UUID session token and process relaunch helper.
- `DeterministicProfile.swift` and `UITestBootstrap.swift` — fixed-parent persisted profile reopening with invariant validation and no arbitrary path input.
- `PlaysteadApp.swift` — compile-gated bounded curation evidence derived from the real shared store/outbox.
- `run-mac-verification.sh` and keyboard/topology shell contracts — globally initialized trap state, precise restoration, and a dynamic early-failure regression.

## Decisions Made

- Kept every reorder action on the production view-model path; the test does not inject callbacks or duplicate reorder logic.
- Used row database IDs for stable action identity, never game titles, filenames, hashes, coordinates, or labels.
- Kept persisted-session selection finite and path-contained by accepting a UUID only and resolving it under a fixed temporary parent.
- Preserved Plan 07's concurrent quota fixture additions while extending the adjacent deterministic-profile infrastructure.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected the nonexistent ShelfView plan path**
- **Found during:** Task 1 project inspection
- **Issue:** The plan named `Playstead/Curation/ShelfView.swift`, but the production type lives at `Playstead/Library/ShelfView.swift`.
- **Fix:** Modified the real Library file and recorded the correction in the broken-windows ledger.
- **Files modified:** `playstead-mac/Playstead/Library/ShelfView.swift`
- **Committed in:** `b0e488f`

**2. [Rule 3 - Blocking] Added narrowly scoped relaunch/evidence infrastructure**
- **Found during:** Task 1 RED analysis
- **Issue:** Existing deterministic profiles always created and deleted a fresh temporary store per process, so the four originally owned files could not prove process-relaunch durability, exact outbox effects, or unchanged content identity.
- **Fix:** Added compile-gated UUID-only session reopening, invariant validation, harness relaunch support, and bounded synthetic evidence. No Plan 07-owned storage production file was changed.
- **Files modified:** `DeterministicProfile.swift`, `UITestBootstrap.swift`, `UITestHarness.swift`, `PlaysteadApp.swift`
- **Committed in:** `b0e488f`

**3. [Rule 1 - Bug] Waited for durable settlement before frame-order assertions**
- **Found during:** Plan-level static verification
- **Issue:** The first test draft could read row frames immediately after a gesture, before durable evidence proved SwiftUI and SQLite had settled; it also expected unowned route-root identifiers where honest-empty text already provided an exact route postcondition.
- **Fix:** Ordered predicate-based evidence waits before frame reads, asserted exactly three collection rows, and used exact empty-state text for the three empty routes.
- **Files modified:** `playstead-mac/PlaysteadUITests/CurationInteractionTests.swift`
- **Committed in:** `c80e466`

**4. [Rule 1 - Bug] Preserved the underlying hosted build failure across keyboard cleanup**
- **Found during:** Combined Wave 6 hosted run 33478091423
- **Issue:** The aggregate runner returned early after the build stage, then its EXIT trap referenced function-local `previous_keyboard_mode` after local scope had unwound. Under `set -u`, cleanup replaced the real build result with `unbound variable` and hid the actionable failure.
- **Fix:** Moved trap-owned keyboard state to globally initialized variables, guarded cleanup with a captured-state flag, disarmed before fallible restoration, reused the same helpers for aggregate and targeted UI paths, and added a dynamic regression that fails with exit 97 before capture while proving no `defaults` command runs.
- **Files modified:** `run-mac-verification.sh`, `keyboard-mode-cleanup-test.sh`, `four-layer-topology-test.sh`
- **Verification:** Bash syntax, early-cleanup regression, prompt safety, four-layer topology, ShellCheck, source contract, and diff checks passed without launching Xcode or the app.
- **Committed in:** `a4ef343`

---

**Total deviations:** 4 auto-fixed (2 blocking plan/infrastructure gaps, 2 bugs). **Impact:** The additions are the minimum needed for the locked D-21 proof and preserve the no-password/credential-isolation boundary; hosted cleanup now exposes rather than masks the underlying build/layer result.

## Verification

Local app-launching verification was deliberately not run. The parent explicitly prohibited local Xcode, XCUITest, Playstead app, LiveServer, and Keychain execution after Plan 05 established a password-prompt hazard.

Passed locally without launching the app:

- Full Swift parser over `Playstead` and `PlaysteadUITests`.
- Plan 06 static source contract: 20 checks passed.
- Task 2 keyboard source contract: 13 checks passed.
- Curation settlement test contract: 5 checks passed.
- `keychain-prompt-safety-test.sh` passed.
- `four-layer-topology-test.sh` passed.
- `keyboard-mode-cleanup-test.sh` preserved the synthetic pre-capture failure status 97, invoked no `defaults` operation, and emitted no unbound-variable error.
- ShellCheck passed for the runner and both changed shell contracts.
- `git diff --check` passed.
- TDD history contains RED `test(...)` commits before both GREEN `feat(...)` commits.

Required hosted verification:

1. `scripts/ci/run-mac-verification.sh --layers ui --only-testing PlaysteadUITests/CurationInteractionTests/testFiveShelvesAndDurableDragReorder`
2. `scripts/ci/run-mac-verification.sh --layers ui --only-testing PlaysteadUITests/CurationInteractionTests`

Both are recorded as open `unrun-verify` entries in `.planning/WINDOWS.md` for the central hosted-verification orchestrator.

Hosted run 33478091423 is diagnostic only: it failed before layer execution and staged two sanitized files, but the cleanup trap masked the underlying early build result. Commit `a4ef343` repairs that masking; the combined wave must be rerun to reveal or clear the original build failure and then execute the Plan 06 UI contract.

## Authentication Gates

- No credentials, passwords, Keychain access, push, or external authentication were requested or used.
- Hosted verification is an execution-environment requirement, not an authentication request for this executor.

## Known Stubs

None. The scan found only legitimate optional/default empty values and comments rejecting placeholders; no UI-flowing mock, TODO, FIXME, skipped test, or unfinished Plan 06 behavior was added.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: test-only-file-access | `playstead-mac/Playstead/UITesting/DeterministicProfile.swift` | Compile-gated persisted UI sessions reopen SQLite beneath a fixed temporary parent after strict UUID validation; no arbitrary path is accepted. |

## User Setup Required

None. Do not enable local UI/LiveServer execution. The central orchestrator should run the two exact UI commands on the pinned hosted macOS/Xcode environment.

## Next Phase Readiness

- Plan 08/09 can include the exact named curation test in the authoritative hosted inventory and requirement map.
- Once hosted evidence passes, the two Plan 06 `unrun-verify` ledger entries can be resolved with the immutable run ID and exact SHA.

## Self-Check: PASSED

- The summary and all eight Plan 06 implementation/test files exist.
- All six Plan 06 task/deviation commits exist in repository history.
- Full Swift parsing, 20 plan-contract checks, 13 keyboard-contract checks, 5 settlement-contract checks, prompt-safety, early keyboard cleanup, four-layer topology, ShellCheck, and diff checks passed after the final code change.
- The two intentionally unrun app-launching checks are explicitly marked `pending_hosted` and recorded in `.planning/WINDOWS.md`; no local password-prompt risk was taken.

---
*Phase: 03.5-mac-verification-automation*
*Completed: 2026-09-01*
