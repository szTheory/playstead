---
phase: 03.5-mac-verification-automation
plan: "06"
subsystem: mac-curation-verification
tags: [swiftui, xcuitest, curation, drag-reorder, keyboard-focus, durable-outbox]

requires:
  - phase: 03.5-mac-verification-automation
    provides: Deterministic compile-gated UI profiles, production library routes, stable accessibility surfaces, and prompt-safe hosted Mac verification
provides:
  - Stable collection, queue, and shelf roots plus distinct collection/queue row and reorder-action identities
  - One shared production settlement path for drag and keyboard reorder, with boundary guards and retained focus
  - UUID-scoped deterministic profile reuse across process relaunch and bounded order/outbox/catalogue evidence
  - Exact hosted XCUITest stages for five shelf states, drag durability, and full drag-keyboard-relaunch parity
affects: [03.5-08, 03.5-09, mac-verification, curation, accessibility]

actuals:
  tokens: 20585
  tasks: 2
  commits: 17

tech-stack:
  added: []
  patterns: [shared drag-keyboard settlement, stable row-scoped action identity, UUID-only persisted test profile, predicate-based durable evidence]

key-files:
  created:
    - playstead-mac/PlaysteadUITests/CurationInteractionTests.swift
  modified:
    - playstead-mac/Playstead/Curation/CollectionDetailView.swift
    - playstead-mac/Playstead/Curation/CollectionsViewModel.swift
    - playstead-mac/Playstead/Curation/QueueShelfView.swift
    - playstead-mac/Playstead/Library/ShelfView.swift
    - playstead-mac/Playstead/App/PlaysteadApp.swift
    - playstead-mac/Playstead/UITesting/DeterministicProfile.swift
    - playstead-mac/Playstead/UITesting/UITestBootstrap.swift
    - playstead-mac/PlaysteadUITests/Support/UITestHarness.swift
    - playstead-mac/PlaysteadTests/CurationTests/OrderingTests.swift
    - playstead-mac/scripts/ci/run-mac-verification.sh
    - playstead-mac/scripts/ci/tests/four-layer-topology-test.sh
    - playstead-mac/scripts/ci/tests/keyboard-mode-cleanup-test.sh

key-decisions:
  - "Drag and keyboard reorder call one private settleMove function per production surface, so neither interaction can bypass the begin/preview/commit/refresh discipline."
  - "Relaunch verification accepts only a UUID session token resolved beneath a fixed temporary parent; no arbitrary path, SQL, JSON, credential, or fixture bytes enter through the process environment."
  - "Compile-gated evidence exposes only synthetic row IDs, an outbox count, and sorted catalogue digests; it excludes filenames, paths, payload JSON, credentials, and user content."

requirements-completed: [QUAL-01, LIBR-03]

coverage:
  - id: D1
    description: "Five curation routes expose exact populated or honest-empty fixture states, and collection detail exposes exactly three distinct production rows."
    requirement: LIBR-03
    verification:
      - kind: static_contract
        ref: "Plan 06 static contract: 20 checks passed; full Swift source/UI-test parser passed"
        status: pass
      - kind: automated_ui
        ref: "Exact profile, sidebar, and five per-shelf CurationInteractionTests identifiers"
        status: pending_hosted
    human_judgment: false
  - id: D2
    description: "Real drag and ordinary keyboard reorder each settle through one production mutation path, increment the outbox exactly once, preserve catalogue content identity, and survive process relaunch."
    requirement: LIBR-03
    verification:
      - kind: static_contract
        ref: "Shared-path/boundary/focus/session/evidence source contracts passed"
        status: pass
      - kind: automated_ui
        ref: "Centrally orchestrated hosted UI-layer run required; local app launch prohibited"
        status: pending_hosted
    human_judgment: false
  - id: D3
    description: "Plan 05 no-password safety remains intact while persisted UI sessions stay compile-gated and path-contained."
    requirement: QUAL-01
    verification:
      - kind: security_contract
        ref: "keychain-prompt-safety-test.sh and four-layer-topology-test.sh passed"
        status: pass
    human_judgment: false

duration: 26m20s
completed: 2026-09-01
status: complete
---

# Phase 03.5 Plan 06: Durable Curation Reorder Summary

**Production collection and queue rows now support stable drag plus boundary-guarded keyboard reorder through one durable mutation path, with an exact hosted XCUITest contract for five shelf states, one-effect accounting, retained focus, unchanged content identity, and process-relaunch persistence.**

## Performance

- **Duration:** 26m20s
- **Started:** 2026-09-01T06:17:37Z
- **Completed:** 2026-09-01T06:43:57Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- Added stable roots and row/action identifiers to the real collection, queue, and shared shelf views while retaining SwiftUI `.onMove`.
- Added visible Move Up/Move Down actions with first/last boundary disabling and explicit focus retention on the moved row action.
- Unified drag and keyboard interactions behind the same `settleMove` begin/preview/commit/refresh path so one user action produces one durable outbox intent.
- Added UUID-only compile-gated profile persistence and bounded synthetic evidence so the named XCUITest can prove exact order/outbox/catalogue state after two process relaunches without accepting arbitrary filesystem input.
- Added three exact hosted stages for shelf rendering, drag durability, and full drag-keyboard-relaunch behavior. The keyboard stage retains the complete end-to-end sequence, including exact effect counts, focus, boundary states, catalogue digest stability, and two relaunches.

## Task Commits

1. **Task 1 RED: failing five-shelf and durable drag contract** — `f92e02d` (test)
2. **Task 1 GREEN: stable rows, bounded evidence, and persisted relaunch profile** — `b0e488f` (feat)
3. **Task 2 RED: failing keyboard parity contract** — `67a27f5` (test)
4. **Task 2 GREEN: boundary-guarded keyboard controls and retained focus** — `9f7e281` (feat)
5. **Rule 1 correction: wait for durable settlement before reading frames** — `c80e466` (fix)
6. **Hosted blocker repair: preserve the underlying early failure across cleanup** — `a4ef343` (fix)
7. **Hosted diagnostic repair: expose exact curation failure stages** — `b8d8a2a` (fix)
8. **Hosted diagnostic repair: isolate profile, navigation, and shelf failures** — `2b32bd9` (fix)
9. **Hosted selector repair: route through owned accessibility nodes** — `47ef91a` (fix)
10. **Hosted selector repair: target final card and drag-owner nodes** — `824b44b` (fix)
11. **Hosted diagnostic split: isolate curation reorder boundaries** — `4130571` (test)
12. **Hosted action repair: dispatch reorder input once on macOS** — `d322df6` (fix)
13. **Hosted action repair: activate owned reorder commands** — `5412d46` (fix)

The corresponding shared runner/topology registration landed concurrently in Plan 07 commit `21fec46`; it preserved all Plan 07 stages and is not counted as a Plan 06 commit.
The later macOS action-dispatch topology guard landed in concurrent Plan 07 commit `5a963b1` and likewise is not counted as a Plan 06 commit.

## Files Created/Modified

- `PlaysteadUITests/CurationInteractionTests.swift` — exact five-route, drag, keyboard, effect, focus, content-identity, and relaunch proof.
- `CollectionDetailView.swift` and `QueueShelfView.swift` — stable rows/actions, shared drag-keyboard settlement, boundary guards, and focus ownership.
- `Library/ShelfView.swift` — explicit production shelf root identity at the file's real location.
- `UITestHarness.swift` — opt-in UUID session token and process relaunch helper.
- `DeterministicProfile.swift` and `UITestBootstrap.swift` — fixed-parent persisted profile reopening with invariant validation and no arbitrary path input.
- `PlaysteadApp.swift` — compile-gated bounded curation evidence derived from the real shared store/outbox.
- `run-mac-verification.sh` and keyboard/topology shell contracts — globally initialized trap state, precise restoration, and a dynamic early-failure regression.

## Decisions Made

- Kept every reorder action on the production view-model path; the test does not inject callbacks or duplicate reorder logic.
- Used row database IDs for stable action identity, never game titles, filenames, hashes, coordinates, or labels.
- Kept persisted-session selection finite and path-contained by accepting a UUID only and resolving it under a fixed temporary parent.
- Preserved Plan 07's concurrent quota fixture additions while extending the adjacent deterministic-profile infrastructure.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected the nonexistent ShelfView plan path**
- **Found during:** Task 1 project inspection
- **Issue:** The plan named `Playstead/Curation/ShelfView.swift`, but the production type lives at `Playstead/Library/ShelfView.swift`.
- **Fix:** Modified the real Library file and recorded the correction in the broken-windows ledger.
- **Files modified:** `playstead-mac/Playstead/Library/ShelfView.swift`
- **Committed in:** `b0e488f`

**2. [Rule 3 - Blocking] Added narrowly scoped relaunch/evidence infrastructure**
- **Found during:** Task 1 RED analysis
- **Issue:** Existing deterministic profiles always created and deleted a fresh temporary store per process, so the four originally owned files could not prove process-relaunch durability, exact outbox effects, or unchanged content identity.
- **Fix:** Added compile-gated UUID-only session reopening, invariant validation, harness relaunch support, and bounded synthetic evidence. No Plan 07-owned storage production file was changed.
- **Files modified:** `DeterministicProfile.swift`, `UITestBootstrap.swift`, `UITestHarness.swift`, `PlaysteadApp.swift`
- **Committed in:** `b0e488f`

**3. [Rule 1 - Bug] Waited for durable settlement before frame-order assertions**
- **Found during:** Plan-level static verification
- **Issue:** The first test draft could read row frames immediately after a gesture, before durable evidence proved SwiftUI and SQLite had settled; it also expected unowned route-root identifiers where honest-empty text already provided an exact route postcondition.
- **Fix:** Ordered predicate-based evidence waits before frame reads, asserted exactly three collection rows, and used exact empty-state text for the three empty routes.
- **Files modified:** `playstead-mac/PlaysteadUITests/CurationInteractionTests.swift`
- **Committed in:** `c80e466`

**4. [Rule 1 - Bug] Preserved the underlying hosted build failure across keyboard cleanup**
- **Found during:** Combined Wave 6 hosted run 33478091423
- **Issue:** The aggregate runner returned early after the build stage, then its EXIT trap referenced function-local `previous_keyboard_mode` after local scope had unwound. Under `set -u`, cleanup replaced the real build result with `unbound variable` and hid the actionable failure.
- **Fix:** Moved trap-owned keyboard state to globally initialized variables, guarded cleanup with a captured-state flag, disarmed before fallible restoration, reused the same helpers for aggregate and targeted UI paths, and added a dynamic regression that fails with exit 97 before capture while proving no `defaults` command runs.
- **Files modified:** `run-mac-verification.sh`, `keyboard-mode-cleanup-test.sh`, `four-layer-topology-test.sh`
- **Verification:** Bash syntax, early-cleanup regression, prompt safety, four-layer topology, ShellCheck, source contract, and diff checks passed without launching Xcode or the app.
- **Committed in:** `a4ef343`

**5. [Rule 1 - Bug] Removed ambiguous drag geometry and split hosted failure identity**
- **Found during:** Combined Wave 6 hosted run 33481640835
- **Issue:** The only failing UI identifier covered shelves, drag, keyboard, focus, and two relaunches, so the evidence could not locate the failing stage. Its first-row-to-last-row-center gesture also had a geometrically ambiguous insertion side.
- **Fix:** Split the proof into exact shelf, drag-durability, and keyboard-durability test identifiers; retained the full drag → relaunch → keyboard → relaunch sequence in the keyboard stage; and changed the real element drag to last-row-to-first-boundary with exact `[3,1,2]` and `[3,2,1]` order/effect assertions.
- **Files modified:** `CurationInteractionTests.swift`, `run-mac-verification.sh`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, exact-stage source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `b8d8a2a`

**6. [Rule 1 - Bug] Fixed stale shelf identity and persisted-session validation**
- **Found during:** Exact Plan 06 hosted run 33483731474
- **Issue:** All three curation stages failed. Static audit found the Favorites assertion queried nonexistent `library.card`, Continue/Collections/Recent declared stable roots but did not attach them to their production views, and `UITestBootstrap.makeSession()` redundantly required fresh seed positions after `makeFixture()` had already accepted a valid persisted reorder inventory. The latter made every post-reorder relaunch fail closed.
- **Fix:** Asserted the exact synthetic favorite title count, attached the declared production roots, preserved `makeFixture()`'s fresh-versus-reopen validation distinction, and split hosted identity into profile bootstrap, sidebar inventory, five individual shelf stages, drag durability, and keyboard durability. The full drag and keyboard end-to-end assertions remain unchanged.
- **Files modified:** `ContinueShelfView.swift`, `CollectionsView.swift`, `RecentShelfView.swift`, `UITestBootstrap.swift`, `CurationInteractionTests.swift`, `run-mac-verification.sh`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, exact-stage/profile/root source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology including all eight Plan 07 storage stages, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `2b32bd9`

**7. [Rule 1 - Bug] Routed Favorites and Collections through owned accessibility nodes**
- **Found during:** Exact Plan 06 hosted run 33486052488
- **Issue:** Profile, sidebar, Continue, Queue, and Recent passed; Favorites, Collections, drag, and keyboard failed. `GameCardView` intentionally collapses title children into its own `library.card` node, so a `staticTexts` title query could not observe Favorites content, while the shell incorrectly assigned a game-card identifier to the entire Favorites scroll container. Collections exposed a selectable `List` row only as an inner `Text`, so the test clicked a child that did not own the route action.
- **Fix:** Removed the false container identity, asserted the exact card node/count/label, added a stable collection-row button that explicitly owns selection and routing, changed collection opening to that button, and split both Favorites and Collections into root-versus-child hosted stages.
- **Files modified:** `CollectionsView.swift`, `LibraryShellView.swift`, `CurationInteractionTests.swift`, `run-mac-verification.sh`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, exact-node/route source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology including Plan 07 stages, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `47ef91a`

**8. [Rule 1 - Bug] Attached identity and drag gestures to their final owner nodes**
- **Found during:** Exact Plan 06 hosted run 33488360822
- **Issue:** Favorites' root and Collections' root/route passed, leaving the exact card plus drag/keyboard stages failing. `GameCardView` applied its identifier before creating the final collapsed accessibility element, which could strand identity on a discarded child. The reorder gesture targeted the semantic row `HStack`, while SwiftUI's `.onMove` is owned by the enclosing `List` cell.
- **Fix:** Applied card identity after accessibility collapse/label composition, targeted the enclosing cells containing the stable row IDs for the real element-to-element drag, and split drag mutation from relaunch durability while retaining the full keyboard end-to-end sequence.
- **Files modified:** `GameCardView.swift`, `CurationInteractionTests.swift`, `run-mac-verification.sh`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, final-card/drag-cell source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `824b44b`

**9. [Rule 3 - Blocking] Split opaque reorder failures at every hosted-observable boundary**
- **Found during:** Exact Plan 06 hosted run 33490616418
- **Issue:** Favorites passed, but the hosted artifact exposed only three aggregate reorder IDs and no assertion/element details, leaving route state, cell ownership, gesture mutation, SQLite/outbox evidence, focus, and relaunch durability indistinguishable.
- **Fix:** Added exact stages for collection-detail seeded state, three distinct drag-owner cells, drag mutation/effect, drag relaunch, direct keyboard mutation/focus, direct keyboard relaunch, and the retained full drag→keyboard flow. The element-to-element drag now uses slow velocity plus a bounded hold so macOS receives a stable drop.
- **Files modified:** `CurationInteractionTests.swift`; shared registration/contracts in `run-mac-verification.sh` and `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, boundary inventory/slow-drag source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `4130571`; shared registration in concurrent `21fec46`

**10. [Rule 1 - Bug] Dispatched each reorder input exactly once through the macOS-owned API**
- **Found during:** Atomic hosted run 33495336534
- **Issue:** Exact collection state and distinct List-cell ownership passed, but neither drag nor direct keyboard mutation fired. The drag used the touch-style `press(...thenDragTo:)` call in a macOS UI test instead of the mouse click-drag API. Static review initially misread `focusContainedAction` as activating after focus.
- **Fix:** Switched the real element-to-element gesture to `click(forDuration:thenDragTo:)`. The keyboard assumption was corrected after the next hosted run and deeper helper audit in deviation 11.
- **Files modified:** `CurationInteractionTests.swift`; topology guard in `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, macOS-drag/single-activation source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `d322df6`; shared topology guard in concurrent `5a963b1`

**11. [Rule 1 - Bug] Restored the missing keyboard activation and isolated production button dispatch**
- **Found during:** Atomic hosted run 33498048537
- **Issue:** The boundary was unchanged. Deeper source tracing proved `focusContainedAction` only tabs until the requested button owns focus; it never sends Space. Removing the caller's Space therefore produced zero keyboard activations. Drag also needed the documented velocity-and-hold form to distinguish a completed pointer sweep from a settled drop.
- **Fix:** Sent exactly one Space after the focus-only helper returns, added independent stages for Move Up availability/ownership and mouse-click effect, and used the macOS click-drag overload with slow velocity plus a one-second destination hold.
- **Files modified:** `CurationInteractionTests.swift`, `run-mac-verification.sh`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, owned-action/single-activation/held-drag source contract, prompt safety, keyboard cleanup, Wave 6 semantic contract, four-layer topology, ShellCheck, and diff checks passed without launching Xcode or the app.
- **Committed in:** `5412d46`

**12. [Rule 1 - Bug] Published optimistic member moves through the observable view-model boundary**
- **Found during:** Atomic hosted run 33500660524
- **Issue:** The exact production Move Up button existed, was enabled, and owned focus, but its click appeared to produce no effect. Static tracing showed the action did settle one outbox intent and update the member row in SQLite; however, `CollectionDetailView` fetched members through an unobserved computed method while `refresh()` changed only collection metadata. Neither the visible order nor bounded effect evidence could invalidate after a successful move.
- **Fix:** Added an observable member revision advanced by `refresh()` and read by every member query. Added a unit regression that observes the query, settles an optimistic move, requires invalidation, and verifies the resulting `[b, a]` order.
- **Files modified:** `CollectionsViewModel.swift`, `OrderingTests.swift`
- **Verification:** Both Swift files parse, the observation/order regression is present, and `git diff --check` passed without launching Xcode or the app.
- **Committed in:** `fa789df`

**13. [Rule 1 - Bug] Put keyboard focus and identity on the final reorder button node**
- **Found during:** Atomic hosted run 33503640089
- **Issue:** Direct click, drag mutation, and drag relaunch durability all passed, leaving only keyboard stages. Collection and queue reorder buttons bypassed the production `playsteadFocusable` contract: an enum-valued focus modifier and the accessibility identifier were composed separately, so the identifier queried by XCUI did not own the proven final-node focus behavior.
- **Fix:** Applied `playsteadFocusable(identifier:)` directly to each Move Up/Down button after its label and minimum target frame, removed the competing focus binding, and retained exactly one app-level Space after the helper confirms the exact identified button owns focus. Stable `ForEach` member identity preserves that final button's focus across optimistic refresh.
- **Files modified:** `CollectionDetailView.swift`, `QueueShelfView.swift`
- **Verification:** Both Swift files parse, the final-node modifier/identifier and single-Space source contract passes, four-layer topology passes, and `git diff --check` passes without launching Xcode or the app.
- **Committed in:** `a9cc202`

**14. [Rule 1 - Bug] Dispatched keyboard activation through the proven focused element path**
- **Found during:** Atomic hosted run 33506605120
- **Issue:** Final-node focus alignment did not clear the three keyboard stages. The passing hosted quota/readiness controls send Space through the exact element after `focusContainedAction`; curation instead sent Space through the app. The combined drag-relaunch-keyboard test had a second concrete omission: it focused the Move Up button but asserted the moved state without sending any activation.
- **Fix:** Both curation keyboard paths now prove focus with the same helper and send exactly one Space through that exact button. No click is used, so the tests still prove ordinary keyboard activation, retained focus, one effect, and durability.
- **Files modified:** `CurationInteractionTests.swift`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, updated exact-element/two-activation topology guard, four-layer topology, and `git diff --check` pass without launching Xcode or the app.
- **Committed in:** `d474490`

**15. [Rule 1 - Bug] Required retained focus only on an enabled post-move action**
- **Found during:** Hosted run 33510034845
- **Issue:** Element-scoped Space still left all three keyboard tests failing while direct click and drag mutation/durability passed. Static sequence tracing found the direct keyboard helper moved member 2 to the first boundary and then required its newly disabled Move Up button to retain focus. AppKit transfers focus away from disabled controls, making the asserted state contradictory even after a successful keyboard move; the durability stage aborted inside the same helper before relaunch.
- **Fix:** The fresh-order keyboard proof now moves member 3 from last to middle. Its exact Move Up action remains enabled and can retain focus, while the test still requires one outbox effect, exact `[1, 3, 2]` order, first/last boundary guards, unchanged catalogue evidence, and relaunch durability. The combined post-drag path already moves its last member into the middle and remains unchanged.
- **Files modified:** `CurationInteractionTests.swift`, `four-layer-topology-test.sh`
- **Verification:** Swift parse, Bash syntax, non-boundary retained-focus source guard, combined four-layer topology (including concurrent Plan 07 focus contract), and `git diff --check` pass without launching Xcode or the app.
- **Committed in:** `e8326eb`

---

**Total deviations:** 15 auto-fixed (3 blocking plan/infrastructure/evidence gaps, 12 bugs). **Impact:** The additions are the minimum needed for the locked D-21 proof and preserve the no-password/credential-isolation boundary; hosted evidence now distinguishes production button ownership/click from keyboard focus/activation and held drag settlement, while collection member changes invalidate the real detail surface and retained-focus assertions target controls that remain enabled after settlement.

## Verification

Local app-launching verification was deliberately not run. The parent explicitly prohibited local Xcode, XCUITest, Playstead app, LiveServer, and Keychain execution after Plan 05 established a password-prompt hazard.

Passed locally without launching the app:

- Full Swift parser over `Playstead` and `PlaysteadUITests`.
- Plan 06 static source contract: 20 checks passed.
- Task 2 keyboard source contract: 13 checks passed.
- Curation settlement test contract: 5 checks passed.
- `keychain-prompt-safety-test.sh` passed.
- `four-layer-topology-test.sh` passed.
- `keyboard-mode-cleanup-test.sh` preserved the synthetic pre-capture failure status 97, invoked no `defaults` operation, and emitted no unbound-variable error.
- ShellCheck passed for the runner and both changed shell contracts.
- `git diff --check` passed.
- TDD history contains RED `test(...)` commits before both GREEN `feat(...)` commits.

Required hosted verification:

1. Profile: `testCurationProfileBootstrapsLibrarySurface`
2. Navigation: `testSidebarExposesAllFiveCurationDestinations`
3. Shelves: `testContinueShelfRendersHonestEmptyFixture`, `testFavoritesShelfRootExists`, `testFavoritesShelfRendersExactSeededCard`, `testCollectionsShelfRootExists`, `testCollectionsShelfRendersExactSeededRoute`, `testQueueShelfRendersHonestEmptyFixture`, and `testRecentShelfRendersHonestEmptyFixture`
4. Reorder setup: `testCollectionDetailOpensExactSeededState`, `testCollectionDragTargetsOwnDistinctListCells`, `testCollectionMoveUpActionIsEnabledAndOwned`, and `testCollectionMoveUpClickProducesOneEffect`
5. Drag: `testDragReorderProducesOneEffect` and `testDragReorderSurvivesRelaunch`
6. Keyboard: `testKeyboardReorderProducesOneEffectAndRetainsFocus`, `testKeyboardReorderSurvivesRelaunch`, and `testKeyboardReorderRetainsFocusAndSurvivesRelaunch`
7. Full class: `scripts/ci/run-mac-verification.sh --layers ui --only-testing PlaysteadUITests/CurationInteractionTests`

The drag-stage and full-class requirements are recorded as open `unrun-verify` entries in `.planning/WINDOWS.md` for the central hosted-verification orchestrator; the eighteen exact outcomes are the diagnostic form of that same pending coverage.

Hosted run 33478091423 is diagnostic only: it failed before layer execution and staged two sanitized files, but the cleanup trap masked the underlying early build result. Commit `a4ef343` repairs that masking; the combined wave must be rerun to reveal or clear the original build failure and then execute the Plan 06 UI contract.

Hosted run 33481640835 proved routing and all unrelated layers: Unit passed 269, LiveServer passed, Linux/Compose passed, and UI passed 42 except the former broad curation identifier. Rendering's only failure was the separately owned missing storage baseline. Commit `b8d8a2a` converts the curation rerun into three exact stage outcomes and removes the ambiguous drag destination; a hosted rerun remains required because local UI execution is prohibited.

Hosted run 33483731474 reported failures for shelf fixtures, drag durability, and keyboard durability. Commit `2b32bd9` fixes the shared static mismatches and expands the next hosted artifact to nine exact curation outcomes, so bootstrap, sidebar, every shelf, drag, and keyboard are independently attributable.

Hosted run 33486052488 passed profile bootstrap, sidebar inventory, Continue, Queue, and Recent, isolating failures to the two populated surfaces and the Collection-dependent reorder flows. Commit `47ef91a` fixes their concrete accessibility ownership mismatches and expands the next artifact to eleven exact outcomes.

Hosted run 33488360822 further passed Favorites root plus Collections root/route, leaving only the exact Favorite card and the two drag-dependent flows. Commit `824b44b` attaches identity and gestures to their final owner nodes and expands the next artifact to twelve exact outcomes by separating drag mutation from relaunch durability.

Hosted run 33490616418 passed the exact Favorite card, leaving only drag mutation, drag durability, and the drag-dependent keyboard E2E flow. Commit `4130571` plus shared registration `21fec46` expands the next artifact to sixteen exact curation outcomes, including direct keyboard-only focus/effect and durability stages.

Atomic run 33495336534 passed exact collection-detail state and distinct List-cell ownership but failed both mutation inputs and their consequential durability/full-flow stages. Commit `d322df6` corrected the macOS drag modality but, based on an incorrect helper interpretation, removed keyboard activation; the same sixteen exact outcomes remained pending.

Atomic run 33498048537 left the boundary unchanged, proving the first keyboard-helper interpretation wrong. Commit `5412d46` restores one Space after focus, isolates production Move Up availability/click effect, and expands the required rerun to eighteen exact outcomes while holding the macOS drag at its destination.

Atomic run 33500660524 passed the exact production Move Up availability/ownership stage but failed its direct click effect, proving input targeting was no longer the boundary. Commit `fa789df` repairs the unobserved SQLite-member query: a successful optimistic move now invalidates the detail surface and its bounded effect evidence. Hosted UI rerun remains required; the new observation/order unit regression will execute in the same hosted wave.

Atomic run 33503640089 passed direct click, drag mutation, and drag relaunch durability after the observation repair. Only keyboard mutation/focus and its consequential durability/full-flow stages failed. Commit `a9cc202` aligns Move Up/Down with the hosted-proven `playsteadFocusable` final-node ownership contract while preserving the test's single Space activation. Hosted rerun remains required for the three keyboard outcomes.

Atomic run 33506605120 left the same three keyboard outcomes failing despite final-node focus alignment. Commit `d474490` matches the passing quota/readiness activation path by typing Space through the exact focused button and repairs the combined E2E stage's missing Space. Hosted rerun remains required for the three keyboard outcomes.

Hosted run 33510034845 passed 271 Unit tests, 20 Rendering tests, LiveServer, all curation fixture/direct-click stages, and drag mutation plus relaunch durability. The same three keyboard identifiers failed. Commit `e8326eb` removes the impossible requirement that a Move Up button retain focus after becoming disabled at the first boundary, without weakening one-effect, exact-order, boundary, focus, or durability assertions. Hosted rerun remains required for those three keyboard outcomes.

## Authentication Gates

- No credentials, passwords, Keychain access, push, or external authentication were requested or used.
- Hosted verification is an execution-environment requirement, not an authentication request for this executor.

## Known Stubs

None. The scan found only legitimate optional/default empty values and comments rejecting placeholders; no UI-flowing mock, TODO, FIXME, skipped test, or unfinished Plan 06 behavior was added.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: test-only-file-access | `playstead-mac/Playstead/UITesting/DeterministicProfile.swift` | Compile-gated persisted UI sessions reopen SQLite beneath a fixed temporary parent after strict UUID validation; no arbitrary path is accepted. |

## User Setup Required

None. Do not enable local UI/LiveServer execution. The central orchestrator should run the two exact UI commands on the pinned hosted macOS/Xcode environment.

## Next Phase Readiness

- Plan 08/09 can include the eighteen exact named curation stages in the authoritative hosted inventory and requirement map.
- Once hosted evidence passes, the two Plan 06 `unrun-verify` ledger entries can be resolved with the immutable run ID and exact SHA.

## Self-Check: PASSED

- The summary and all eight Plan 06 implementation/test files exist.
- All seventeen listed Plan 06 task/deviation commits exist in repository history; concurrent shared commits `21fec46`, `5a963b1`, and `6fb5e46` also exist.
- Affected Swift parsing and diff checks passed after the final code change; the prior full Swift parse, 20 plan-contract checks, 13 keyboard-contract checks, 5 settlement-contract checks, prompt-safety, early keyboard cleanup, four-layer topology, and ShellCheck remain the latest permitted static evidence for their unchanged surfaces.
- The two intentionally unrun app-launching checks are explicitly marked `pending_hosted` and recorded in `.planning/WINDOWS.md`; no local password-prompt risk was taken.

---
*Phase: 03.5-mac-verification-automation*
*Completed: 2026-09-01*
