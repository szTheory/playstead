---
phase: 03.5-mac-verification-automation
plan: "09"
type: execute
wave: 7
depends_on: [03.5-08]
files_modified:
  - .github/workflows/ci.yml
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - playstead-mac/scripts/ci/validate-phase-3-uat-evidence.py
  - playstead-mac/scripts/ci/tests/test_validate_phase_3_uat_evidence.py
  - .planning/phases/03.5-mac-verification-automation/evidence/final-hosted-run.json
  - .planning/ROADMAP.md
  - .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
autonomous: false
requirements: [QUAL-01, LIBR-01, LIBR-03, LIBR-04]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The complete allowlist and evidence schema are finalized only after Plans 04-08 have produced every exact UAT-mapped test (D-02-D-04, D-24)."
    - "An authorized push/PR makes the exact final phase commit remotely reachable; one exact persisted run ID is watched, viewed, downloaded, and validated for four Mac layers plus Linux test and compose-smoke (D-01-D-04)."
    - "A structured section-aware validator proves checkpoint 2 live-server evidence, checkpoint 3 card/semantic evidence, checkpoint 5/10 accessibility evidence, exact source boundaries, every checkpoint 5/6 mapping, D-25 blockers, overall partial status, and the Linux-Compose/native-Mac roadmap wording (D-23-D-25)."
  artifacts:
    - path: "playstead-mac/scripts/ci/run-mac-verification.sh"
      provides: "Final four-layer/six-job allowlist, complete evidence schema, and exact-run validator"
    - path: "playstead-mac/scripts/ci/validate-phase-3-uat-evidence.py"
      provides: "Section-aware fail-closed UAT/ROADMAP contract validator"
    - path: ".planning/phases/03.5-mac-verification-automation/evidence/final-hosted-run.json"
      provides: "Exact validated final hosted run identity and sanitized outcomes"
    - path: ".planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md"
      provides: "Exact named automated evidence and retained partial/blockers"
  key_links:
    - from: ".planning/phases/03.5-mac-verification-automation/evidence/final-hosted-run.json"
      to: ".planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md"
      via: "Task 3 validates the exact run before any documentation mutation"
      pattern: "source: automated"
    - from: "playstead-mac/scripts/ci/validate-phase-3-uat-evidence.py"
      to: ".planning/ROADMAP.md"
      via: "section-aware Linux Compose deployment/native Mac behavior criterion check"
      pattern: "compose-smoke"
  prohibitions:
    - statement: "MUST NOT push without user authorization, select an unrelated/latest run, mutate docs before exact evidence passes, infer neighboring coverage, promote residual hardware/experiential/signing/emulator claims, or mark Phase 3 complete."
      status: unverified
      flagged: true
---

<objective>Finalize the complete evidence contract, validate one exact authorized hosted run, and then record only its proven Phase 3 UAT boundaries.
Purpose: Documentation is a trust boundary; neither local results nor an unrelated GitHub run may advance canonical UAT state.
Output: Final allowlist/schema, structured documentation validator with fixture tests, exact run record, roadmap clarification, and exact UAT mappings.</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>

<context>@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-VALIDATION.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/phases/03.5-mac-verification-automation/03.5-08-SUMMARY.md
@playstead-mac/scripts/ci/run-mac-verification.sh</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Finalize the six-job evidence path and section-aware documentation contract</name>
  <files>.github/workflows/ci.yml, playstead-mac/scripts/ci/run-mac-verification.sh, playstead-mac/scripts/ci/validate-phase-3-uat-evidence.py, playstead-mac/scripts/ci/tests/test_validate_phase_3_uat_evidence.py</files>
  <behavior>
    - Complete evidence requires Unit, Rendering, UI, and LiveServer layer results plus Linux `test` and `compose-smoke` jobs for one run/head SHA; every named identifier is discovered, executed, non-skipped, passed, and non-vacuous.
    - Hosted-run fixtures reject wrong workflow/event/ref/SHA/run ID, queued/in-progress/failed conclusion, absent or skipped layer/canary/test, missing Linux job, and malformed or mismatched downloaded manifest.
    - Documentation fixtures reject a mapping placed in the wrong checkpoint, missing checkpoint 2 live-server proof, missing checkpoint 3 card/semantic proof, missing checkpoint 5/10 accessibility proof, missing or overbroad `source: automated`, any absent D-25 blocker, non-partial status, or incorrect roadmap topology wording.
  </behavior>
  <action>After Plans 06, 07, and 08 are complete, finalize the exact complete allowlist in `run-mac-verification.sh` and the CI artifact `complete-verification-evidence.json`. Include the mappings below, the four Mac layer outcomes, environment fingerprint, exact run identity fields supplied by GitHub, and both Linux job conclusions per D-02-D-04/D-24. Extend hosted-run self-tests so every identity/status/schema/skip/job rejection is exercised. Create a parser-based UAT/ROADMAP validator that scopes assertions to frontmatter, the exact numbered checkpoint section, and Phase 3.5 success criterion 4 rather than using unscoped text matches. Its fixture suite must cover each required positive mapping and a negative mutation for every boundary: checkpoints 2, 3, 5, 6, automated split 10, residual split 10, exact `source: automated` placement, blockers 4/7/8/9/14/15, `status: partial`, and Linux Compose deployment topology versus native PostgreSQL/Phoenix Mac behavior. Do not edit ROADMAP/UAT in this task.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-hosted-run-validator --verify-required-uat-allowlist --verify-complete-evidence-schema &amp;&amp; python3 -m unittest scripts/ci/tests/test_validate_phase_3_uat_evidence.py</automated></verify>
  <done>The final workflow can emit and validate exact six-job evidence, and the documentation validator rejects every incomplete, misplaced, or overclaimed handoff.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Authorize publication of the exact final phase commit</name>
  <action>Obtain explicit user authorization and confirmation for the exact final remote publication described below before any push or PR action.</action>
  <instructions>Stop before any push or PR mutation. Report the post-Task-1 `git rev-parse HEAD` and verify it contains the complete Phase 3.5 workflow, all test producers, the final allowlist/schema, and documentation validator. Ask the user to perform or explicitly authorize their normal push/PR action. The remote branch or pull-request head must resolve to this exact SHA and trigger `ci.yml` through `push` or `pull_request`; do not rely on `workflow_dispatch` existing on the default branch. Request confirmation containing the remote branch, expected event, and PR number/URL when applicable.</instructions>
  <verification>Confirm the remote branch/PR head SHA exactly equals the reported final phase SHA and that GitHub created a CI run for that push or pull request.</verification>
  <resume-signal>Reply with "published", the remote branch, expected event (`push` or `pull_request`), and the PR URL/number when applicable.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate the exact final run before mutating Roadmap and UAT</name>
  <precondition>The user confirmed an authorized remote branch/PR whose head SHA equals the post-Task-1 commit.</precondition>
  <files>.planning/phases/03.5-mac-verification-automation/evidence/final-hosted-run.json, .planning/ROADMAP.md, .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md</files>
  <action>First query `gh run list` for explicit `ci.yml`, confirmed branch, and confirmed `push` or `pull_request` event; filter by exact `headSha` and require exactly one match. Persist its run ID and identity, execute `gh run watch RUN_ID --exit-status`, retrieve exact fields/jobs with `gh run view RUN_ID`, and download only that run's `complete-verification-evidence` artifact using `gh run download RUN_ID`. Run the hosted validator against the exact view JSON and downloaded manifest; stop without a documentation diff on any wrong identity, non-completion/non-success, malformed evidence, missing/skipped/failed layer/test/canary, missing Linux job, or non-vacuity failure. Only after that gate passes, clarify Roadmap criterion 4: Linux `compose-smoke` proves deployment topology while native PostgreSQL 17 plus Phoenix beside XCUITest proves Mac client/server behavior per D-04. Update D-24 checkpoints using the exact table below. Give each promoted checkpoint its own `source: automated` line and evidence block; split checkpoint 10 into an automated keyboard/live-tree/audit record and a blocked physical-controller/experiential-VoiceOver record so source attribution cannot bleed across the boundary. Preserve `status: partial`, checkpoint 4's deliberate scope blocker, checkpoint 7's emulator/game-bytes blocker, checkpoints 8/9 physical hardware blockers, checkpoints 14/15 signing/notarization blockers, and all other existing blocked records per D-23-D-25. Finally run the structured validator on the changed canonical files.</action>
  <verify><automated>RUN_ID=$(jq -er '.run_id' .planning/phases/03.5-mac-verification-automation/evidence/final-hosted-run.json) &amp;&amp; gh run watch "$RUN_ID" --exit-status &amp;&amp; gh run view "$RUN_ID" --json databaseId,workflowName,event,headBranch,headSha,status,conclusion,url,jobs &gt; /tmp/playstead-final-run-view.json &amp;&amp; cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --verify-hosted-run complete --run-record ../.planning/phases/03.5-mac-verification-automation/evidence/final-hosted-run.json --run-view /tmp/playstead-final-run-view.json &amp;&amp; python3 scripts/ci/validate-phase-3-uat-evidence.py --uat ../.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md --roadmap ../.planning/ROADMAP.md</automated></verify>
  <done>The exact authorized final run proves all four Mac layers and both Linux jobs before the section-aware validator accepts the partial, precisely attributed Roadmap/UAT handoff.</done>
</task>

</tasks>

## Exact UAT checkpoint/test-name mapping

| Checkpoint | Exact covering tests | Evidence boundary |
|------------|----------------------|-------------------|
| 2 | `LiveServerSnapshotTests/testPairedFreshMirrorRendersSnapshotBeforeAnyBlobDownloadAndPersistsKeychainAcrossRelaunch` | Public pairing/snapshot, fresh mirror, Keychain relaunch, zero blob routes; no real game bytes. |
| 3 | `LibraryContractSnapshotTests/testCardAndStatusVisualContract`; `testSemanticContractOracles`; `testFiveCurationShelfVisualContract` | Locked card/status/navigation/empty/curation visual and semantic contract. |
| 5 | `StorageContractSnapshotTests/testDownloadsQuotaReclaimAndStorageVisualContract`; `testStorageMotionAndReducedMotionContract`; `StorageInteractionTests/testDownloadsQuotaReclaimAndStorageFlows`; `SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit` | Visual, motion/reduced-motion, interaction, focus/live semantics/audits; experiential VoiceOver remains blocked. |
| 6 | `LibraryContractSnapshotTests/testFiveCurationShelfVisualContract`; `CurationInteractionTests/testFiveShelvesAndDurableDragReorder`; `PlaySessionTests/test_launchSucceedsIndependentlyOfPlaySessionRecording`; `test_offlineSession_isDeliveredAfterReachabilityReturns`; `test_sameSessionIdentifierPostedTwice_resultsInOneServerSideEffect`; `test_userDeletion_enqueuesDeleteIntentAndRemovesFromRecent` | Shelf visuals, drag/keyboard reorder, launch independence, delivery/idempotency, individual deletion. |
| 10 automated split | `SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit` | Keyboard and machine-checkable live-tree labels, roles, state, hierarchy, focus, and audits only. |
| 10 blocked split | No automated test | Physical controller d-pad/shoulder behavior and experiential VoiceOver pronunciation, rotor behavior, sentence quality, and comprehension remain blocked/unclaimed. |

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local repository → GitHub remote | Final publication is an external mutation requiring explicit user authority. |
| Hosted evidence → canonical planning records | Remote run conclusions determine which UAT claims advance. |

## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-20 | Spoofing/Repudiation | evidence handoff | high | mitigate | Authorized exact SHA/ref/event, unique run ID, exit-status watch, exact view, same-run artifact, four layers, two Linux jobs, and fail-closed validation. |
| T-03.5-21 | Tampering | UAT status | high | mitigate | Parser-scoped claim-to-test/source mapping, negative fixture mutations, retained partial status, and exact residual blocker assertions. |
| T-03.5-24 | Elevation | remote publication | high | mitigate | Blocking human-action checkpoint before push/PR and no dispatch/default-branch assumption. |
</threat_model>

<verification>- Validate final allowlist/evidence and documentation validators with positive/negative fixtures before publication.
- Validate the exact persisted run ID/SHA/event/ref and same-run downloaded artifact before any documentation diff.
- Run the section-aware validator after mutation and inspect only the Roadmap/UAT diff.</verification>

<success_criteria>- Documentation mutation is gated on one exact authorized run with four Mac layers and two Linux jobs.
- Checkpoints 2, 3, 5, 6, and the automated split of 10 have exact section-scoped source/test evidence.
- Phase 3 remains partial with every D-25 blocker and the residual checkpoint 10 boundary explicit.
- Roadmap criterion 4 states Linux Compose topology proof and native Mac client/server proof separately.</success_criteria>

## Artifacts this phase produces
- `.github/workflows/ci.yml` and `run-mac-verification.sh`: final allowlist, six-job manifest, and exact-run validation.
- `validate-phase-3-uat-evidence.py` plus fixture tests: structured documentation gate.
- `final-hosted-run.json`: exact validated final run identity/outcomes.
- `.planning/ROADMAP.md`: Linux Compose/native Mac criterion split.
- `03-UAT.md`: exact automated evidence for 2/3/5/6 and split 10 with blockers retained.

## Multi-Source Coverage Audit
| SOURCE | ID | Feature / Requirement | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | — | Human-clicked Mac behavior proven on every push | 01-10 | COVERED |
| REQ | QUAL-01 | Keyboard/focus/reduced motion/live semantics | 01-07,09-10 | COVERED |
| REQ | LIBR-01 | Fresh paired catalogue before bytes | 01,08-10 | COVERED |
| REQ | LIBR-03 | Five shelves and durable reorder | 03-06,09 | COVERED |
| REQ | LIBR-04 | Honest hidden/contextual/storage states | 03-05,07,09 | COVERED |
| RESEARCH | — | Five hosted adoption gates, four layers, exact evidence | 01-02,08-10 | COVERED |
| CONTEXT | D-01-D-25 | All locked decisions | 01-10 | COVERED |

<output>Create `.planning/phases/03.5-mac-verification-automation/03.5-09-SUMMARY.md` when done.</output>
