---
phase: 03.5-mac-verification-automation
plan: "08"
type: execute
wave: 7
depends_on: [03.5-06, 03.5-07]
files_modified:
  - playstead-server/config/mac_ci.exs
  - playstead-server/lib/mix/tasks/playstead.mac_ci_fixture.ex
  - playstead-server/test/mix/tasks/playstead_mac_ci_fixture_test.exs
  - playstead-mac/Playstead/Net/KeychainStore.swift
  - playstead-mac/Playstead/UITesting/UITestBootstrap.swift
  - playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift
  - playstead-mac/scripts/ci/live-server.sh
autonomous: true
requirements: [QUAL-01, LIBR-01]
estimate: {tokens: 57000, raw_tokens: 57000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Production setup/import/approval plus public request/redeem pair a fresh Mac; scoped Keychain relaunch renders two exact sentinels before any blob route (D-06, D-07, D-10)."
    - "Native PostgreSQL/Phoenix remains the Mac client-behavior proof while Linux compose-smoke remains the deployment-topology proof (D-04)."
  artifacts:
    - path: "playstead-server/lib/mix/tasks/playstead.mac_ci_fixture.ex"
      provides: "Production-context owner/sentinel/exact approval fixture"
    - path: "playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift"
      provides: "Fresh-mirror public-pairing Keychain relaunch acceptance proof"
  key_links:
    - from: "LiveServerSnapshotTests.swift"
      to: "KeychainStore.swift"
      via: "one-use handoff then normal loadCredential relaunch"
      pattern: "testPairedFreshMirrorRendersSnapshotBeforeAnyBlobDownloadAndPersistsKeychainAcrossRelaunch"
  prohibitions:
    - statement: "MUST NOT bypass public pairing/snapshot, use raw SQL/private endpoints/credential overrides, or attach secrets/databases/content bytes."
      status: unverified
      flagged: true
---
<objective>Complete the native Phoenix pairing/snapshot spine after the curation and storage layers exist.
Purpose: LIBR-01 is proven only by real public pairing, fresh mirror, normal Keychain auth, exact snapshot state, and zero bytes.
Output: Production fixture and secure bootstrap/relaunch XCUITest; Plan 09 alone finalizes the complete allowlist and hosted manifest.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-06-SUMMARY.md
@.planning/phases/03.5-mac-verification-automation/03.5-07-SUMMARY.md
@playstead-server/lib/playstead/pairing.ex
@playstead-mac/Playstead/Net/KeychainStore.swift</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Pair one fresh launched Mac to one production-context sentinel</name>
  <files>playstead-server/config/mac_ci.exs, playstead-server/lib/mix/tasks/playstead.mac_ci_fixture.ex, playstead-server/test/mix/tasks/playstead_mac_ci_fixture_test.exs, playstead-mac/scripts/ci/live-server.sh, playstead-mac/Playstead/Net/KeychainStore.swift, playstead-mac/Playstead/UITesting/UITestBootstrap.swift, playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift</files>
  <behavior>- Owner/sentinel use Setup/Blobs/Import; request/redeem use public HTTP; approval asserts exact ID/code/unique label/sole pending request.
- Owned 0600 handoff is deleted before real scoped storeCredential; app then uses normal loadCredential and fresh empty mirror.</behavior>
  <action>Expand the adopted `mac_ci` environment/helper into the bounded production-context fixture with fixed public synthetic non-ROM bytes and sanitized control files. Implement exact approval and public request/redeem per D-06. Extend the single compile-gated bootstrap to validate/delete the 0600 handoff before real Keychain persistence and fail closed per D-07/D-11. Add the named live test's first launch: exact sentinel/title/asset-set, rendered row, nonempty cursor, successful snapshot counter, zero blob requests, empty objects/partials.</action>
  <verify><automated>cd playstead-server &amp;&amp; MIX_ENV=test mix test test/mix/tasks/playstead_mac_ci_fixture_test.exs &amp;&amp; cd ../playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers live-server --only-testing PlaysteadUITests/LiveServerSnapshotTests/testPairedFreshMirrorRendersSnapshotBeforeAnyBlobDownloadAndPersistsKeychainAcrossRelaunch</automated></verify>
  <done>A fresh Mac reaches real authoritative catalogue state before bytes through production security paths.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove Keychain-only relaunch through the focused LiveServer layer</name>
  <files>playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift, playstead-mac/scripts/ci/live-server.sh</files>
  <behavior>- After mirror clear and second server sentinel, relaunch without handoff renders both using scoped Keychain only.
- Cleanup always removes the native services, one-use handoff, scoped Keychain, mirror, and synthetic fixture roots without attaching them.</behavior>
  <action>Complete D-10 second-sentinel/mirror-reset/relaunch and D-11 fail-safe cleanup. Keep this plan scoped to the LiveServer implementation and its exact focused test. Do not finalize the cross-plan allowlist or complete hosted evidence here: Plans 06 and 07 now precede this plan, and downstream Plan 09 owns the authoritative aggregation after every producer has finished.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers live-server --only-testing PlaysteadUITests/LiveServerSnapshotTests</automated></verify>
  <done>The focused live-server layer proves fresh-mirror pairing and Keychain-only relaunch with complete cleanup, ready for Plan 09 aggregation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| XCUITest → public HTTP | Pairing/snapshot crosses real auth and authorization. |
| Handoff → Keychain → API client | Credential is transient then normally loaded. |
## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-17 | Spoofing/Elevation | pairing fixture | high | mitigate | Public request/redeem, exact sole approval, normal authenticated snapshot. |
| T-03.5-18 | Information disclosure | handoff/Keychain/logs | high | mitigate | Owned 0600 one-use file, scoped Keychain, trap cleanup, sanitizer. |
| T-03.5-19 | Repudiation | snapshot-before-bytes | high | mitigate | Fresh/reset mirror, two sentinels, cursor/route counters, zero blob, empty CAS. |
</threat_model>
<verification>- Run fixture ExUnit and the named LiveServer test.
- Confirm the test starts from an empty mirror and relaunches without the handoff file.
- Confirm no cross-plan allowlist or hosted manifest is finalized before Plan 09.</verification>
<success_criteria>- Fresh paired catalogue renders before bytes and repeats from Keychain after mirror reset.
- No bypass or sensitive artifact enters evidence.</success_criteria>
## Artifacts this phase produces
- `mac_ci.exs`, fixture Mix task/test, and `live-server.sh`: real isolated native fixture lifecycle.
- `KeychainStore.swift` and `UITestBootstrap.swift`: scoped one-use credential path.
- `LiveServerSnapshotTests.swift`: exact two-sentinel relaunch proof.
<output>Create `.planning/phases/03.5-mac-verification-automation/03.5-08-SUMMARY.md` when done.</output>
