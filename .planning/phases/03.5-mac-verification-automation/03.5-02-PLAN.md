---
phase: 03.5-mac-verification-automation
plan: "02"
type: execute
wave: 1
depends_on:
  - 03.5-01
files_modified:
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/Playstead/UITesting/UITestBootstrap.swift
  - playstead-mac/Playstead/UITesting/DeterministicProfile.swift
  - playstead-mac/Playstead/Design/FocusRing.swift
  - playstead-mac/Playstead/Design/AccessibilityIdentifiers.swift
  - playstead-mac/Playstead/Library/GameCardView.swift
  - playstead-mac/Playstead/Library/StatusSlotView.swift
  - playstead-mac/Playstead/Library/SearchField.swift
  - playstead-mac/Playstead/Library/FilterChipRow.swift
  - playstead-mac/Playstead/Library/ShowAllSystemsControl.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/PlaysteadSnapshot.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/LibraryContractSnapshotTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/__Snapshots__/LibraryContractSnapshotTests/card-and-status-contract.png
  - playstead-mac/PlaysteadTests/SnapshotTests/__Snapshots__/LibraryContractSnapshotTests/library-and-curation-states.png
  - playstead-mac/PlaysteadTests/SnapshotTests/__Snapshots__/LibraryContractSnapshotTests/storage-surfaces.png
autonomous: true
requirements:
  - QUAL-01
  - LIBR-03
  - LIBR-04
estimate:
  tokens: 61000
  raw_tokens: 61000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Finite compile-gated profiles seed only production stores under unique roots, assert exact counts/state sets, and abort rather than falling back when deterministic setup fails (D-08, D-09)."
    - "Focused snapshots and exact semantic assertions jointly prove the locked card, navigation, status, empty-state, curation, download/quota/reclaim/storage, focus, long-title, and appearance contracts (D-13, D-14, D-16)."
    - "Every render proves exact pixel dimensions and nonempty/nonuniform content; a meaningful mutation fails and centralized calibrated noise passes without per-test tolerance loosening (D-15)."
    - "The 280×158 card has two title lines, three zones, all seven status treatments, no cover-art surface, and a title-independent visual seam (D-13, D-14)."
    - "A non-testing Release build contains no deterministic-profile/bootstrap types, environment keys, or symbols and cannot reach test composition (D-08)."
  artifacts:
    - path: "playstead-mac/Playstead/UITesting/DeterministicProfile.swift"
      provides: "Finite exact profile enum and production-store seeders"
      exports: ["DeterministicProfile"]
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/PlaysteadSnapshot.swift"
      provides: "Pinned rendering environment, centralized tolerance, pixel and content guards"
      exports: ["PlaysteadSnapshot"]
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/LibraryContractSnapshotTests.swift"
      provides: "Pixel and semantic assertions for the complete locked UI contract"
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift"
      provides: "Non-testing binary/symbol/environment-key absence guard"
  key_links:
    - from: "playstead-mac/Playstead/App/PlaysteadApp.swift"
      to: "playstead-mac/Playstead/UITesting/DeterministicProfile.swift"
      via: "compile-gated fail-closed AppEnvironment composition using production stores"
      pattern: "UI_TESTING"
    - from: "playstead-mac/PlaysteadTests/SnapshotTests/LibraryContractSnapshotTests.swift"
      to: "playstead-mac/Playstead/Library/GameCardView.swift"
      via: "real production views rendered by PlaysteadSnapshot under fixed environment"
      pattern: "GameCardView"
    - from: "playstead-mac/Playstead/Design/FocusRing.swift"
      to: "playstead-mac/Playstead/Library/*.swift"
      via: "shared focused-state overlay with UI-SPEC token #38BDF8"
      pattern: "playsteadFocusRing"
  prohibitions:
    - statement: "MUST NOT seed copyrighted ROMs, proprietary BIOS material, arbitrary JSON, arbitrary SQL, copied production databases, or user-derived titles/paths/hashes into deterministic profiles."
      status: unverified
      flagged: true
    - statement: "MUST NOT ship the UI-test bootstrap/profile hooks or any production pairing bypass in a non-testing Release product."
      status: unverified
      flagged: true
    - statement: "MUST NOT use pixel similarity to substitute for exact navigation, status, ordering, empty-state, geometry, or vocabulary assertions."
      status: unverified
      flagged: true
---

<objective>
Turn the Phase 3 UI design contract into deterministic, non-vacuous rendering and semantic tests while keeping all test state finite, production-store-backed, and absent from Release builds.

Purpose: LIBR-03/LIBR-04 and the visual portion of QUAL-01 currently rely on human observation even though their expected states and geometry are locked.
Output: Release-safe profiles, the `PlaysteadSnapshot` wrapper, three reviewed contact-sheet baselines, exact semantic oracles, and production focus/identifier seams.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UI-SPEC.md
@.planning/phases/03.5-mac-verification-automation/03.5-01-SUMMARY.md
@playstead-mac/Playstead/App/PlaysteadApp.swift
@playstead-mac/Playstead/App/AppPaths.swift
@playstead-mac/PlaysteadTests/AccessibilityTests/AccessibilityAuditTests.swift
</context>

<flagged_assumptions>
- LIBR-03 / adjacency: D-21 settles UI adjacency at the list boundaries: Move Up/Down is disabled at the respective first/last row, focus stays on the moved row, and the three-row drag has exact before/after neighbors. Treat those as explicit acceptance; no merge/collision behavior is invented.
- LIBR-03 / empty: 03-UI-SPEC settles zero/one/many presentation. Home hides empty shelves while sidebar entries retain exact explainers; fixtures must include exact zero, one, and three-item states and must reject a missing profile identifier rather than treating it as empty.
- LIBR-03 / ordering: Exact fixture order and durable post-relaunch order are explicit. Equal underlying fractional-position semantics are not newly specified by this phase; keep the probe flagged and do not manufacture a tie-breaker in view tests.
- LIBR-04 / unclassified: 03-UI-SPEC explicitly settles hidden empty systems, hidden irrelevant advanced settings, count-labeled expansion, and contextual setup actions; those become semantic assertions but remain flagged until the rendering/UI layers pass.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose finite fail-closed UI profiles through production stores and prove Release absence</name>
  <read_first>
    - playstead-mac/Playstead/App/PlaysteadApp.swift
    - playstead-mac/Playstead/App/AppPaths.swift
    - playstead-mac/PlaysteadTests/SyncTests/SyncEngineTests.swift
    - .planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
  </read_first>
  <reversibility rating="costly">D-08 makes leaked test composition a release-security defect whose removal would require auditing all app composition and shipped binaries.</reversibility>
  <files>playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/UITesting/UITestBootstrap.swift, playstead-mac/Playstead/UITesting/DeterministicProfile.swift, playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift</files>
  <behavior>
    - Each exact profile creates a unique AppPaths root and seeds expected nonzero counts/state sets through LocalStore, CatalogueStore, CurationStore, DownloadQueue, PinStore, QuotaManager, and CASManager.
    - Unknown/missing profile names and any root/database/store failure abort UI-testing composition; the production in-memory fallback is unchanged outside the compile condition.
    - A non-testing Release binary contains none of the selected bootstrap/profile symbols or environment-key names.
  </behavior>
  <action>Under a dedicated UI-testing compilation condition, add the finite D-09 profile enum for empty library, populated library/curation/reorder, paused/active download queue, quota block/reclaim, and storage. Use fixed synthetic IDs, bytes, digests, positions, UTC timestamps, and exact expected counts/state sets, flowing every row and file through the production store/CAS/queue APIs and a unique `AppPaths(root:)`, following the SyncEngineTests isolation analog. Extend the single `AppEnvironment` composition root rather than introducing a fake app architecture. In test mode only, fail closed on root/database/profile/bootstrap errors rather than using `inMemoryFallback`; leave production fallback unchanged. Add D-08 guards that build without the test condition and inspect binary strings/symbols plus compilation boundaries for every selected type and environment key. The bootstrap in this task handles finite profiles only; Plan 04 extends the same compile-gated type for the one-use credential handoff without creating a second bootstrap.</action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/ReleaseHookAbsenceTests --only-testing PlaysteadTests/DeterministicProfileTests</automated>
  </verify>
  <acceptance_criteria>
    - Every finite profile asserts its exact expected item count and state set before the app launches, including empty/one/three-item boundaries.
    - Arbitrary profile input cannot execute and a missing/invalid profile fails the test launch.
    - Each run owns and removes a unique application-support root; no profile uses user-derived or proprietary content.
    - The Release absence guard fails on a seeded hook symbol/key and passes on the non-testing build.
  </acceptance_criteria>
  <done>Deterministic test state is finite, non-vacuous, production-store-backed, isolated, fail-closed, and absent from Release products.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the complete visual contract through one calibrated snapshot wrapper</name>
  <read_first>
    - .planning/phases/03-mac-offline-play-vertical-slice/03-UI-SPEC.md
    - playstead-mac/Playstead/Library/GameCardView.swift
    - playstead-mac/Playstead/Library/StatusSlotView.swift
    - playstead-mac/Playstead/Library/DownloadsView.swift
    - playstead-mac/Playstead/Curation/CollectionDetailView.swift
  </read_first>
  <files>playstead-mac/PlaysteadTests/SnapshotTests/PlaysteadSnapshot.swift, playstead-mac/PlaysteadTests/SnapshotTests/LibraryContractSnapshotTests.swift, playstead-mac/PlaysteadTests/SnapshotTests/__Snapshots__/LibraryContractSnapshotTests/card-and-status-contract.png, playstead-mac/PlaysteadTests/SnapshotTests/__Snapshots__/LibraryContractSnapshotTests/library-and-curation-states.png, playstead-mac/PlaysteadTests/SnapshotTests/__Snapshots__/LibraryContractSnapshotTests/storage-surfaces.png</files>
  <behavior>
    - Card/status contact sheet covers 280×158 geometry, two-line long Unicode title, three zones, seven status glyph/shape/color treatments, focused/unfocused states, no cover surface, and title-independent presentation in light/dark at 2×.
    - Library/curation contact sheet covers populated and exact honest empty states, standard and Large text, with animations disabled and fixed synthetic data/clock/locale.
    - Storage contact sheet covers Downloads, Quota, Reclaim, and Storage in representative active/paused/blocked/reclaimable states.
  </behavior>
  <action>Create `PlaysteadSnapshot` as the only project entry point to SnapshotTesting. Fix UTC, `en_US_POSIX`, synthetic clock/data, active state, animations off, standard Dynamic Type plus targeted Large text, explicit light/dark appearance, 2× scale, and SDR 8-bit sRGB per D-16. Store one centrally documented tolerance inherited by every image assertion. Before comparison, assert exact pixel dimensions and a nonempty/nonuniform content bounding box; reuse Plan 01's mutation/noise canaries. Build three focused reviewed contact sheets per D-13: card/status contract, library/curation representative shells, and downloads/quota/reclaim/storage. Include no ambient appearance or broad locale/scale matrix. Treat the real production views as subjects; do not introduce ViewInspector or custom PNG comparison.</action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/LibraryContractSnapshotTests</automated>
  </verify>
  <acceptance_criteria>
    - All three named reference images exist and CI recording remains disabled.
    - Each render checks exact pixel size plus nonempty/nonuniform bounds before similarity.
    - Light/dark, long Unicode/two-line title, Large text, focused/unfocused, all seven status treatments, populated/empty, and storage states are visible in the named contact sheets.
    - The card contract contains no cover-art surface or title-derived palette change.
  </acceptance_criteria>
  <done>Every genuinely visual 03-UI-SPEC claim has a deterministic focused snapshot whose failure produces a named reviewable diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add independent semantic oracles, stable identity, and the shared focus contract</name>
  <read_first>
    - .planning/phases/03-mac-offline-play-vertical-slice/03-UI-SPEC.md
    - playstead-mac/PlaysteadTests/AccessibilityTests/AccessibilityAuditTests.swift
    - playstead-mac/Playstead/Library/GameCardView.swift
    - playstead-mac/Playstead/Library/StatusSlotView.swift
    - playstead-mac/Playstead/Library/SearchField.swift
  </read_first>
  <files>playstead-mac/Playstead/Design/AccessibilityIdentifiers.swift, playstead-mac/Playstead/Design/FocusRing.swift, playstead-mac/Playstead/Library/GameCardView.swift, playstead-mac/Playstead/Library/StatusSlotView.swift, playstead-mac/Playstead/Library/SearchField.swift, playstead-mac/Playstead/Library/FilterChipRow.swift, playstead-mac/Playstead/Library/ShowAllSystemsControl.swift, playstead-mac/PlaysteadTests/SnapshotTests/LibraryContractSnapshotTests.swift</files>
  <behavior>
    - Independently authored expected sidebar labels equal Home, Continue, Favorites, Collections, Queue, Recent, Systems, Unidentified with registry-filtered nonempty systems and conditional Unidentified last.
    - Expected status set equals all seven cases; every priority combination selects exactly one slot; system/status token literals are disjoint and title changes do not change presentation tokens.
    - Empty shelves hide/show exactly as specified; locked copy, 280×158 constants, two-line title, and search/show-all-system labels match the UI contract.
    - Shared `#38BDF8` focus ring is visible for focused controls and absent while unfocused without conflating focus with selection.
  </behavior>
  <action>Author the exact expected navigation, status, priority-combination, geometry, vocabulary, empty-state, copy, token-disjointness, and title-independence values inside the test contract rather than reading them from the production arrays per D-14. Add a namespaced `AccessibilityIdentifiers` vocabulary that never derives identity from labels, titles, filenames, paths, hashes, credentials, or coordinates; apply it to the reusable card, status, search, filter, and show-all-system components. Add the shared SwiftUI `.focused()` plus custom `#38BDF8` overlay required by 03-UI-SPEC and snapshot both states. Retain useful headless semantics but do not treat the current same-source manifest `tabOrder` as order evidence; Plan 03 supplies the independent live-tree order.</action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/LibraryContractSnapshotTests/testSemanticContractOracles</automated>
  </verify>
  <acceptance_criteria>
    - Expected order/status/copy/geometry tables are test-owned literals and cannot pass by reusing production arrays.
    - Every priority combination yields exactly one selected status, including pinned versus verified at the shared rank.
    - Core identifiers are namespaced, stable, unique, and independent from user/content/display data.
    - Focused and unfocused snapshots prove the shared focus overlay without relying on color alone for state semantics.
  </acceptance_criteria>
  <done>The visual snapshots are backed by exact independent semantic assertions and reusable production identity/focus seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compile-gated test input → production AppEnvironment | Profile selection and roots influence the real app composition and stores. |
| Synthetic fixture → storage/CAS/queue APIs | Test bytes, digests, IDs, positions, and timestamps cross production invariants. |
| Production view → snapshot artifact | Rendered text/identity can enter reviewable artifacts. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-05 | Elevation of privilege | UITestBootstrap / Release product | high | mitigate | Compile-gated types, fail-closed composition, and binary/string/symbol absence tests against a non-testing Release build. |
| T-03.5-06 | Tampering | DeterministicProfile selector/root | high | mitigate | Finite enum only, unique validated roots, production store APIs, exact counts/state sets, and no arbitrary data/SQL execution. |
| T-03.5-07 | Information disclosure | snapshot references/diffs | medium | mitigate | Fixed public synthetic content only; identifier vocabulary excludes user/content/security data; Plan 01 sanitizer controls upload. |
| T-03.5-08 | Repudiation | pixel-only assertions | medium | mitigate | Independent semantic oracles, exact dimensions/content bounds, deliberate mutation proof, and named baselines. |
</threat_model>

<verification>
- Run the Release absence/profile tests first, then the focused Rendering test plan on fresh DerivedData.
- Confirm every fixture proves its exact counts/state set and every render proves its dimensions/content bounds.
- Review named light/dark contact-sheet diffs on the pinned environment; do not record inside CI.
- Run exact semantic assertions separately from the pixel comparisons.
</verification>

<success_criteria>
- The locked navigation/status/empty/card/storage contract fails for either a semantic or visual regression.
- Profile/bootstrap behavior cannot execute in a non-testing Release build.
- Rendering is fixed to the documented environment and tolerance, with non-vacuity guards.
- Core identifiers and focus ring are production seams ready for live XCUITest.
</success_criteria>

## Artifacts this phase produces

- `playstead-mac/Playstead/UITesting/UITestBootstrap.swift`: compile-gated, fail-closed test composition entry point.
- `playstead-mac/Playstead/UITesting/DeterministicProfile.swift`: `DeterministicProfile` finite enum and exact production-store seeders.
- `playstead-mac/Playstead/Design/AccessibilityIdentifiers.swift`: namespaced production/test identifier vocabulary.
- `playstead-mac/Playstead/Design/FocusRing.swift`: `playsteadFocusRing` shared `.focused()` + `#38BDF8` overlay.
- `playstead-mac/PlaysteadTests/SnapshotTests/PlaysteadSnapshot.swift`: fixed environment, centralized tolerance, pixel/content guards, reference/actual/diff wrapper.
- `playstead-mac/PlaysteadTests/SnapshotTests/LibraryContractSnapshotTests.swift`: `testLockedLibraryShellContract`, `testSemanticContractOracles`, fixture non-vacuity assertions.
- `playstead-mac/PlaysteadTests/SnapshotTests/ReleaseHookAbsenceTests.swift`: `testUITestHooksAreAbsentFromReleaseProduct` and seeded guard self-test.
- Three exact reference images: `card-and-status-contract.png`, `library-and-curation-states.png`, and `storage-surfaces.png` under the named snapshot directory.

<output>
Create `.planning/phases/03.5-mac-verification-automation/03.5-02-SUMMARY.md` when done.
</output>
