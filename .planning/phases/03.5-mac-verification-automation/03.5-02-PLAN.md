---
phase: 03.5-mac-verification-automation
plan: "02"
type: execute
wave: 2
depends_on: [03.5-10]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/mac-snapshot-refresh.yml
  - playstead-mac/TestPlans/Unit.xctestplan
  - playstead-mac/TestPlans/Rendering.xctestplan
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/TestPlans/LiveServer.xctestplan
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - playstead-mac/scripts/ci/sanitize-evidence.sh
autonomous: true
requirements: [QUAL-01]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One fresh build feeds four independent serial XCTest layers; later layers and sanitization run after an earlier failure, while aggregate status still fails and no automatic retry occurs (D-02, D-03)."
    - "Only allowlisted sanitized failure evidence is uploaded for seven days, snapshot recording is off in CI, and manual refresh emits candidates without committing or accepting them (D-05, D-17)."
  artifacts:
    - path: "playstead-mac/TestPlans/*.xctestplan"
      provides: "Unit, Rendering, UI, and LiveServer ownership contracts"
    - path: "playstead-mac/scripts/ci/run-mac-verification.sh"
      provides: "One-build/four-result aggregation and fail-closed xcresult verification"
    - path: "playstead-mac/scripts/ci/sanitize-evidence.sh"
      provides: "Bounded allowlist staging, redaction, and secret/content scan"
    - path: ".github/workflows/mac-snapshot-refresh.yml"
      provides: "Candidate-only manual baseline workflow"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "playstead-mac/TestPlans/*.xctestplan"
      via: "run-mac-verification one build then four test-without-building invocations"
      pattern: "test-without-building"
  prohibitions:
    - statement: "MUST NOT cache DerivedData, retry failing layers, mass-record snapshots, or upload raw credentials, databases, Keychains, content bytes, paths, names, or hashes."
      status: unverified
      flagged: true
---

<objective>
Expand the approved hosted tracer into the reusable four-layer verification and evidence platform.

Purpose: Every later test needs independent discovery/outcome evidence and privacy-safe diagnostics without rebuilding the app.
Output: Four test plans, fail-closed orchestration/sanitization, final CI layer topology, and candidate-only refresh.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-10-SUMMARY.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run one real test through each independent layer from one build</name>
  <files>playstead-mac/TestPlans/Unit.xctestplan, playstead-mac/TestPlans/Rendering.xctestplan, playstead-mac/TestPlans/UI.xctestplan, playstead-mac/TestPlans/LiveServer.xctestplan, playstead-mac/scripts/ci/run-mac-verification.sh, .github/workflows/ci.yml</files>
  <action>Create explicit disjoint Unit, Rendering, UI, and LiveServer plans. Extend the approved wrapper to create fresh DerivedData, run one `build-for-testing`, then four serial `test-without-building` commands with distinct xcresults and bounded deadlines. Parse Xcode 26.6 JSON for exact identifier, discovery, executed count, skip status, and outcome; fail closed on missing bundle, zero tests, parse/schema error, skipped allowlist entry, or non-pass. Capture all statuses without short-circuiting, run later layers after failure, and return one nonzero aggregate with no retries per D-02/D-03. Keep the Wave 0 canaries as the first allowlisted entries and preserve both Linux jobs per D-04.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-result-verifier</automated></verify>
  <done>One hosted build produces four authoritative independent result layers and any discovery/outcome defect fails the aggregate.</done>
</task>

<task type="auto">
  <name>Task 2: Sanitize failure evidence and enforce candidate-only snapshot refresh</name>
  <files>playstead-mac/scripts/ci/sanitize-evidence.sh, playstead-mac/scripts/ci/run-mac-verification.sh, .github/workflows/ci.yml, .github/workflows/mac-snapshot-refresh.yml</files>
  <action>Stage only environment fingerprints, redacted test JSON, snapshot reference/actual/diff images, sanitized screenshots, redacted tree/focus traces, and sanitized app/server logs. Bound size/count, reject authorization/pairing/Keychain/database/object/handoff/environment material and non-synthetic content identifiers, keep raw xcresults/DerivedData outside upload, and upload only after failure for seven days per D-05/D-11. Keep recording disabled and missing references fatal in CI. Add a manual workflow pinned to the identical runner/Xcode fingerprint that emits named candidate diffs only and lacks write/commit/auto-accept behavior per D-17.</action>
  <verify><automated>bash -n playstead-mac/scripts/ci/run-mac-verification.sh playstead-mac/scripts/ci/sanitize-evidence.sh &amp;&amp; playstead-mac/scripts/ci/run-mac-verification.sh --self-test-sanitizer --verify-four-layer-topology</automated></verify>
  <done>Failures yield bounded reviewable evidence without leaking trust-critical data, and no CI path can silently rewrite baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| XCTest/servers → evidence staging | Raw output may contain secrets, paths, labels, or content metadata. |
| Manual refresh → committed baselines | Candidate rendering must not become repository truth without review. |

## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-05 | Information disclosure | failure artifacts | high | mitigate | Allowlist staging, structured redaction, bounded scan, failure-only seven-day upload. |
| T-03.5-06 | Repudiation | result aggregation | medium | mitigate | Per-layer JSON discovery/non-skip/outcome checks and aggregate status. |
| T-03.5-07 | Tampering | baseline refresh | high | mitigate | Candidate-only artifact, read-only permissions, no commit/record/accept path. |
</threat_model>

<verification>
- Run verifier and sanitizer positive/negative fixtures.
- Confirm one build and exactly four independent result bundles.
- Confirm CI recording is off and refresh has no repository-write permission.
</verification>

<success_criteria>
- All four layers are independently invocable and fail from authoritative result metadata.
- Later layers/evidence run after failure without retrying.
- Only sanitized short-retention failure evidence can leave the runner.
</success_criteria>

## Artifacts this phase produces
- `.github/workflows/ci.yml`: final one-build/four-layer invocation and failure-only upload.
- `.github/workflows/mac-snapshot-refresh.yml`: manual candidate-only refresh.
- `playstead-mac/TestPlans/Unit.xctestplan`: existing-unit layer.
- `playstead-mac/TestPlans/Rendering.xctestplan`: rendering/semantic layer.
- `playstead-mac/TestPlans/UI.xctestplan`: interaction/accessibility layer.
- `playstead-mac/TestPlans/LiveServer.xctestplan`: native integration layer.
- `playstead-mac/scripts/ci/run-mac-verification.sh`: independent layer/result aggregation.
- `playstead-mac/scripts/ci/sanitize-evidence.sh`: bounded privacy-safe evidence staging.

<output>Create `.planning/phases/03.5-mac-verification-automation/03.5-02-SUMMARY.md` when done.</output>
