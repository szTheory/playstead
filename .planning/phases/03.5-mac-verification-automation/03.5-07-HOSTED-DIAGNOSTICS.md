# Plan 03.5-07 Hosted Diagnostics

## Accepted rendering evidence

- Hosted run `33490616418` passed the Rendering layer with the reviewed storage baseline.
- The accepted 5760×3040 candidate digest is `9610323ab380010c8cf55fca7f997bb859a8c13b3d8ba115c119e6291188bc93`.
- The exact reviewed baseline was adopted in commit `17ed5ab`.

## UI evidence before the atomic split

Run `33490616418` continued to report four broad failures:

- reclaim candidate evidence
- reclaim selection
- reclaim confirmation
- storage reclaim confirmation/postconditions

Downloads, quota, storage presentation, storage selection, and pinned-copy protection passed. The sanitized artifact exposed only test identifiers and outcomes, not assertion locations or raw XCTest output.

## Static diagnosis and correction

No additional production mutation defect was proven by the available evidence. The remaining tests were therefore split into single-observation hosted stages for:

- production root presentation
- exact initial state
- eligible row identity
- eligible row accessibility value
- toggle ownership and keyboard reachability
- exact selection text
- enabled confirmation action
- live action audit
- confirmation mutation/effect
- post-mutation canonical-row preservation

The canonical-row assertion had one provable selector mismatch: `GameRowView` combines its title/system text into one accessibility node, so an exact `staticTexts[title]` query was not a valid proof that the catalogue row disappeared. The diagnostic tests now locate a source-controlled combined accessibility label beginning with the exact title. Exact candidate counts, eligibility, byte values, post-mutation storage state, and both canonical titles remain required.

Commit `21fec46` registers every atomic stage as a hosted-required UI test and rejects reintroduction of the broad identities. Because the runner and topology contract are shared Wave 6 files, that commit also captured Plan 06's concurrent curation registration hunks already present in those files; the Plan 06 executor owns and documents those registrations, and no curation source file was included in the Plan 07 commit.

## Remaining gate

Plan 03.5-07 remains incomplete until a hosted UI rerun identifies and resolves any failing atomic stage. Local Xcode, XCUITest, application, Keychain, rendering, and LiveServer execution remained prohibited; only Swift parser and static shell contracts were run locally.

## Hosted UI deadline adjustment

Hosted run `33492852331` did not produce UI test outcomes: the runner's finite 900-second UI watchdog terminated `xcodebuild` with status 143 while the expanded 38-stage UI plan was still executing. Unit (269), Rendering (19), and LiveServer (1) all passed in that run.

The UI-only deadline is now 1800 seconds. This is a proportional finite allowance for the diagnostic stage count, not an unbounded wait or retry. Unit remains 900 seconds, Rendering 600 seconds, and LiveServer 900 seconds. The topology contract parses all four invocations, requires those exact values, and rejects zero, negative, duplicate, missing, or greater-than-1800-second deadlines.

## Reclaim presentation trigger

Hosted run `33495336534` completed the expanded plan and isolated the remaining issue before `ReclaimPromptView`: all ten Reclaim stages failed beginning with production-root presentation, while all ten Storage Inventory stages passed, including exact selection, mutation, and canonical-row preservation.

The deterministic quota profile contains two catalogue rows. `GameRowView` initializes both rows as `.needsDownload` and asynchronously refreshes each status in `.task`. The previous test queried the first generic `Download` button as soon as the library sentinel appeared, so it could keyboard-activate the already-cached reclaim candidate during that transient state. Its pending byte count is zero, correctly producing no quota prompt. The uncached target is the only Download action after both rows settle.

The route now waits, with a finite five-second expectation, until the production list exposes exactly one `Download` action before moving keyboard focus to and activating it. A separately required hosted stage proves that settled trigger independently from prompt presentation. No production quota math or reclaim behavior changed: the fixture still requires 32 additional bytes with 32 bytes already used under a 16-byte quota, for the exact 48-byte shortfall.

Hosted run `33498048537` passed the unique-trigger stage but still failed prompt presentation, isolating the problem between settlement and keyboard dispatch. The activation helper had used `target.typeKey(.space, ...)` after detecting focus. The shared macOS harness contract dispatches keys through `XCUIApplication` because key events belong to the currently focused control; an element query does not own keyboard dispatch. The helper now returns the uniquely focused Download control, asserts that it owns focus, and sends one Space event through the application. Separate required stages prove focus ownership and the resulting prompt effect before the prompt-content stages run.

Hosted run `33500660524` passed unique-trigger settlement but failed the next focus-ownership stage. Static inspection found the production difference: storage controls and command-bar controls use `playsteadFocusable`, while `GameRowView`'s Download action was a plain button with neither the shared `@FocusState` ownership nor a stable identifier. The Download action now uses the same focus modifier with `playstead.game.<asset-set-id>.download`. After the generic list settles to one Download label, the UI test resolves the exact deterministic target's final button identifier, confirms its visible label remains Download, and tabs to that focus-owning node before sending one application Space event.

Hosted run `33503640089` again passed trigger settlement but failed exact focus ownership. `playsteadFocusable` binds `@FocusState`; it does not opt a view into focus interactions. Quota controls inherit focusability in a plain stack, but the Download action is embedded in a macOS `List` row. The button now applies `.focusable()` before the shared focus-owner modifier, ensuring the `@FocusState`, accessibility identifier, visible ring, and Space action belong to the same final button. A separately required direct-click stage proves the Download action's quota effect independently; keyboard focus plus one application Space remains the parity acceptance route.

Hosted run `33506605120` passed the unique trigger but failed both keyboard focus and the new direct-click quota-effect stage. The latter proved focus was no longer causal. End-to-end static tracing found that `UITestBootstrap` intentionally constructs an unpaired, Keychain-free client and then blocks external I/O. `AppEnvironment.attemptDownload` checked that unavailable credential before consulting the local quota, so the exact persisted profile state could only return `.notPaired`; its 48-byte quota verdict was unreachable.

Local capacity admission now precedes credential/network admission. A blocked local verdict still returns before any connection; an allowed verdict still requires a paired credential before transfer. The deterministic fixture validation and a separately required hosted unit test now prove the exact production decision: external I/O blocked, used bytes 32, pending bytes 32, quota 16, quota shortfall 48, and one 32-byte reclaim candidate. This closes the profile/bootstrap/composition mismatch without introducing a synthetic credential or permitting UI-profile network access.

Hosted run `33510034845` passed all non-UI layers, the exact quota decision contract, and direct Download activation. The only root failures were keyboard focus ownership and the consequent keyboard activation. Static comparison with the passing command-bar and quota controls isolated the production difference: the Download action's `@FocusState` lived inside a reusable modifier hosted by a dynamic `List` row, followed by an explicit `.focusable()` wrapper, rather than on the long-lived view that owns the Button. That composition can expose a focus participant distinct from the exact actionable AX button queried by XCUITest.

`GameRowView` now owns the Download focus state directly and binds focus, the asset-scoped accessibility identifier, and the visible ring to the same Button, matching the proven command-bar composition. The List-row action no longer adds an explicit or modifier-owned focus participant. Direct click behavior and the quota decision are unchanged; hosted verification must confirm Tab focus and one application Space event now reach the same production action.

Hosted run `33513649409` produced the same two keyboard root failures after the row-owned focus change; direct activation and every non-UI layer still passed. This falsifies modifier lifetime as the sufficient cause. The sanitized result carries no focused-node or tab-count payload, so it cannot honestly distinguish an AX identifier wrapper from a target beyond the prior 24-stop bound. It does prove the activation stage never reached its Space dispatch because it first calls the same failing focus helper.

The UI contract now separates stable identity from final focus ownership without weakening either: the asset-scoped button must exist with label `Download` and share the exact frame of the fixture's sole actionable Download button. Focus is observed on that unique final action through a bounded 64-stop whole-window search rather than the 24-stop sheet bound. Once observed, Space is sent to that already-focused element, matching the hosted-passing Downloads and Quota paths. Static topology rejects application-wide Space dispatch and unbounded traversal; hosted verification will reveal whether the remaining issue was wrapper observation or the undersized traversal bound.

Hosted run `33518726537` failed the same two keyboard root stages after both the final-node query and the 64-stop traversal, while direct activation still passed. This rules out an undersized bound and makes further button-level focus modifiers unjustified. The production mismatch is the macOS `List` model itself: the List owns keyboard focus and row selection; embedded row buttons are not guaranteed Tab stops.

The production route now preserves List semantics explicitly. Activating List establishes a selected row and transfers focus into the List; arrow keys change the exact asset selection; one visible, selection-scoped `Download selected` command owns ⌘D and emits an asset-id command handled by exactly one `GameRowView`. That row calls the same existing async download/quota method as its direct button, so there is no duplicate policy path and no duplicate per-row shortcut. The hosted tests retain the proven direct button route separately, then require List focus, exact selected asset id, enabled command state, ⌘D activation, and the same reclaim prompt effect.

Hosted run `33523210057` exited during the build before producing result bundles, isolating a compile/integration regression in the new route. With raw compiler output unavailable, the SDK-shape audit retained the already-proven selection overload while removing the new inference-heavy forms: the List now uses explicit `List(selection:) { ForEach(...) }`, focus is assigned from the concrete List `onAppear` lifecycle rather than `Task.yield()`, and the shortcut uses scalar `.command` modifiers. Static contracts reject regression to the generic task deferral and pin these compiler-obvious forms.

Hosted run `33524428133` narrowed the remaining build failure to `StorageInteractionTests.swift` lines 451 and 454: the optional `XCUIElement.value as? String` cast was embedded directly in a `for where` predicate and an XCTest autoclosure. The test now materializes typed `String?` locals before control flow and assertion, removing both ambiguous expressions while preserving the exact two-step bound and selected asset equality.

Hosted run `33525573881` moved the bounded errors to the comparison lines, exposing the actual missing symbol: the test declared only the full `quotaDownloadAction`, but the new selection assertions referenced an undeclared `quotaDownloadAssetID`. The fixture UUID is now declared once and the action identifier is derived from it, so direct-button identity and List-selection identity cannot drift.

Hosted run `33526574205` compiled and reached UI, but all reclaim-route stages failed beginning with the previously proven unique Download settlement. Static comparison isolated the new selectable-row accessibility identifier: it was applied outside `GameRowView`, where SwiftUI can promote the container identity over descendant identifiers. The exact selection is already published by `playstead.library.list-selection`, so the redundant row identifier was removed. The selectable `tag`, direct Download identifier, List focus, and exact selected-asset evidence remain independent and intact.

Hosted run `33530222970` passed every layer and every required UI stage except `testReclaimPromptPostMutationPreservesCanonicalRows`, whose broad `label BEGINSWITH` lookup failed at the shared helper. The canonical title/system content is intentionally a combined accessibility subtree, so label-only descendant discovery is not a stable identity contract. That summary subtree now has an asset-scoped identifier separate from all sibling action controls; the post-mutation assertion requires both exact catalogue asset IDs and their expected title prefixes.
