---
phase: 03.5-mac-verification-automation
verified: 2026-09-03T02:42:19Z
status: human_needed
score: 6/6 truths present and wired; 1 human-verification item outstanding (unpushed CI-wiring commits not yet run on hosted infra)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Push commits 04f38cc, 665a62f, d7268f8 (local HEAD is 5 commits ahead of origin/main) and watch the next `mac-verification` job on the real GitHub-hosted macos-26 runner."
    expected: "The job succeeds end to end: `Verify static contract guards` (the new `--self-test-contracts` step) passes before the ~20-30 minute build, followed by a green `--run-four-layer-verification`, matching the local run recorded below."
    why_human: "The last real hosted CI run (33702909968, 29m40s, all green) predates these three commits. Those commits fix a genuine regression the code-review/regression gates found (CI previously invoked zero of the ten static contract guards) and realign two guards that had silently drifted. I reran `scripts/ci/run-mac-verification.sh --self-test-contracts` locally on this machine, which is macOS 26.6 / Xcode 26.6 (matching the pinned runner fingerprint) and confirmed all 10 guards, the validator's 12 unittest cases, and the validator itself pass. That is strong evidence but it is not the same infrastructure as the actual `macos-26` GitHub-hosted runner (different Homebrew cache/tap state was already visible as a warning annotation in the prior real run) and the harness's own topology guard exists specifically because static-guard wiring drifted silently before — the phase goal ('proven ... on every push') is not fully closed until this exact HEAD state has itself been proven green by a real hosted run, not by local reproduction plus code inspection."
---

# Phase 03.5: Mac Verification Automation Verification Report

**Phase Goal:** Mac client behavior that today can only be confirmed by a human clicking through the app is proven by an automated test run on every push.
**Verified:** 2026-09-03T02:42:19Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A `macos-*` GitHub-hosted runner job builds the app and runs `PlaysteadTests` on every push/PR, failing on any test failure | ✓ VERIFIED (with residual risk noted) | `.github/workflows/ci.yml`'s `mac-verification` job: `runs-on: macos-26`, `on: push`/`pull_request` with no path filter, pinned `DEVELOPER_DIR: /Applications/Xcode_26.6.app`. Real hosted run `33702909968` (https://github.com/szTheory/playstead/actions/runs/33702909968) succeeded in 29m40s on 2026-09-03, all three jobs green. The exact HEAD verified here differs from that run by 3 local, unpushed commits (04f38cc, 665a62f, d7268f8) that fix a real CI-wiring gap the code review and regression gate found (`--self-test-contracts` was never invoked). Verified locally: `bash scripts/ci/run-mac-verification.sh --self-test-contracts` passes all 10 static guards + 12 validator unittests + the phase-3 UAT validator, on this machine's matching macOS 26.6/Xcode 26.6. See human-verification item — not yet proven on the actual hosted runner. |
| 2 | An XCUITest target drives the real app: library shell, curation shelves (incl. `.onMove` drag reorder), downloads/quota/reclaim/storage | ✓ VERIFIED | `PlaysteadUITests/CurationInteractionTests.swift` (429 lines) drives a real `XCUIApplication`: `testDragReorderProducesOneEffect`/`testDragReorderSurvivesRelaunch` use a genuine mouse `click(forDuration:thenDragTo:withVelocity:thenHoldForDuration:)` gesture against the actual `List`+`.onMove` in `CollectionDetailView.swift`/`QueueShelfView.swift`, and separately test the D-21 keyboard Move Up/Down alternative (`testKeyboardCommandProducesOneEffect`, focus-retention assertions). `PlaysteadUITests/StorageInteractionTests.swift` (518 lines) drives Downloads pause/resume, Quota edit, Reclaim, and Storage surfaces with exact-byte and focus-restoration assertions. Both targets are wired into `TestPlans/UI.xctestplan` and `TestPlans/LiveServer.xctestplan` with non-empty `selectedTests` lists, and every test is separately named in `run-mac-verification.sh`'s `--required-test` allowlist (discovered/execution_count==1/non-skipped/passed enforced by `verify_layer_result`'s Python — confirmed by reading its strict equality checks). |
| 3 | A view-rendering/snapshot harness asserts the locked 03-UI-SPEC.md contract (nav order, status vocabulary/ladder, honest empty states, card geometry never using cover art/title color) so a regression fails CI | ✓ VERIFIED | `PlaysteadTests/SnapshotTests/PlaysteadSnapshot.swift` wraps the pinned Point-Free `SnapshotTesting` package (present in `project.pbxproj` and `Package.resolved`) with fixed locale/timezone/appearance/scale/Dynamic-Type/animation-disabled rendering (D-16), 0.99 precision, and a non-vacuous content-bounding-box guard. Committed reference PNGs exist at `PlaysteadTests/SnapshotTests/__Snapshots__/{LibraryContractSnapshotTests,StorageContractSnapshotTests}/*.png`. `LibraryContractSnapshotTests.testSemanticContractOracles` asserts the complete 7-status set, exact glyphs/labels/sentences, `DesignTokens.CardGeometry` == 280×158, and that `SystemAccent`/`StatusToken` vocabularies are disjoint (title/cover-art independence, D-14). A dedicated snapshot-refresh workflow (`mac-snapshot-refresh.yml`) is `workflow_dispatch`-only, `contents: read`, and asserts `git diff --exit-code` after rendering candidates — it cannot silently rewrite or auto-accept baselines (D-17). `SnapshotHarnessCanaryTests` (a deliberate-mutation and an intentional-mismatch canary) are in the required-test allowlist, closing the "harness never actually detects a regression" failure mode. |
| 4 | Linux `compose-smoke` proves deployment topology; native PostgreSQL 17 + Phoenix beside XCUITest proves Mac client/server behavior | ✓ VERIFIED (one dead-code gap noted) | `compose-smoke` job unchanged and still gated by `dorny/paths-filter`. `start_native_services()` in `run-mac-verification.sh` installs/starts real `postgresql@17` via Homebrew (`brew list --versions postgresql@17`, `initdb`, `pg_ctl start`), boots the real `mix phx.server` (`MIX_ENV=mac_ci`), and polls `/healthz` with a real HTTP request — every step `die`s (fails closed) on error, so evidence of health is never fabricated. `LiveServerSnapshotTests.testPairedFreshMirrorRendersSnapshotBeforeAnyBlobDownloadAndPersistsKeychainAcrossRelaunch` genuinely pairs against this native server via the public API, asserts a nonempty stored cursor, zero blob-download evidence, and Keychain survival across two relaunches with a second server-added sentinel (D-10). **Gap noted (not blocking):** `run-mac-verification.sh`'s `verify_complete_hosted_run()` — the function that would strictly enforce `linux_jobs == {"test":"success","compose_smoke":"success"}` plus per-layer non-vacuity on the actual uploaded `complete-verification-evidence.json` — is reachable only via `--verify-hosted-run complete` / `--self-test-hosted-run-validator --verify-complete-evidence-schema`, and grep confirms neither flag combination is invoked anywhere in `ci.yml`, `run_contract_self_tests` (the new `--self-test-contracts` gate), or any `tests/*.sh` file. This is the same "guard exists but nothing calls it" pattern as code-review finding WR-03 (the Phase 3 UAT validator), applied to a second, more central validator. It does not invalidate criterion 4 today (the native services really run and really fail closed independently), but it means a future regression in the Linux-job-conclusion or cross-job wiring would not be caught by this otherwise-real validator. |
| 5 | A keyboard-only navigation pass over every Mac surface is automated, and the accessibility audit runs against a live accessibility tree, not the old declarative-manifest tree-walk | ✓ VERIFIED | `PlaysteadUITests/SurfaceAccessibilityTests.swift::testKeyboardOnlySurfaceInventoryAndLiveAudit` traverses a hand-authored 20-surface D-18 inventory (`XCTAssertEqual(expectedSurfaces.count, 20, ...)`), uses `harness.traverseExactFocusSequence` (real keypresses, exact independently-authored expected order, `hasKeyboardFocus` polling) and calls `UITestHarness.audit(_:rootIdentifier:)`, which invokes Apple's real `app.performAccessibilityAudit(for: category.xcuiType)` (`XCUIAccessibilityAuditType.contrast/.elementDetection/.hitRegion/.sufficientElementDescription/.action/.parentChild`) against the live app — not the old `AccessibilityAuditTests.swift` same-source-array manifest walk described in D-22/03.5-CONTEXT. Exclusions require exact category+identifier+rationale (fail-closed, no blanket handler), matching D-20. |
| 6 | Each Phase 3 UAT checkpoint this phase closes is re-recorded in `03-UAT.md` as `source: automated` with its covering test named | ✓ VERIFIED | Read `03-mac-offline-play-vertical-slice/03-UAT.md` directly. Checkpoints 2, 3, 5, 6 are `result: pass` / `source: automated`, each naming exact test identifiers and an evidence boundary, and each cites the real hosted run `33702909968`. Checkpoint 10 is split exactly as D-23 specified: an automated keyboard/live-tree sub-record (`SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit`) plus a still-`blocked` physical-controller/experiential sub-record. Out-of-scope items remain `blocked` as required: checkpoint 4 (availability-filter scope decision), 7 (real emulator+ROM), 8/9 (physical controller), 13 (BIOS reference digest), 14/15 (Developer ID). Checkpoint 16 is `skipped` (owner decision, not a test). Top-level phase status remains `partial`, matching D-25 ("Phase 3 must not be marked complete merely because it has no pending checkpoints"). |

**Score:** 6/6 truths present, wired, and — for truths 2, 3, 5 — behaviorally proven by tests I read line-by-line. Truths 1 and 4 have one residual item each (see human-verification and the WR-03-pattern gap) that are evidence-quality/process gaps, not missing or stubbed functionality.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.github/workflows/ci.yml` (`mac-verification` job) | Required macOS job, every push/PR, pinned image/Xcode | ✓ VERIFIED | `runs-on: macos-26`, `DEVELOPER_DIR` pinned, no `if:`-skip other than `always()` to still run after Linux failures, uploads both a required evidence artifact (`if-no-files-found: error`) and failure-only sanitized evidence. |
| `.github/workflows/mac-snapshot-refresh.yml` | Manual, read-only baseline-candidate workflow | ✓ VERIFIED | `workflow_dispatch` only, `contents: read`, asserts `git diff --exit-code`. |
| `playstead-mac/TestPlans/{Unit,Rendering,UI,LiveServer}.xctestplan` | Four non-empty, real test-plan allowlists | ✓ VERIFIED | All four plans read; `selectedTests`/`skippedTests` are populated, none is an empty allowlist. |
| `playstead-mac/PlaysteadUITests/*.swift` (5 files, 2015 lines) | Real XCUITest target driving the app | ✓ VERIFIED | Read `CurationInteractionTests.swift`, `StorageInteractionTests.swift`, `SurfaceAccessibilityTests.swift`, `LiveServerSnapshotTests.swift`, `HostedRunnerCanaryTests.swift` — genuine `XCUIApplication` launches, clicks, drags, and keypresses; no stub bodies found. |
| `playstead-mac/PlaysteadTests/SnapshotTests/*.swift` + `__Snapshots__/*.png` | Real pixel-snapshot harness with committed baselines | ✓ VERIFIED | `PlaysteadSnapshot.swift` wraps `SnapshotTesting`; baseline PNGs exist and are committed. |
| `playstead-mac/scripts/ci/run-mac-verification.sh` | Non-vacuous discovery/execution/skip/pass verifier | ✓ VERIFIED | `verify_layer_result`'s Python enforces `discovered==True`, `execution_count==1`, `skipped==False`, `outcome=="passed"` per named required test; malformed/duplicate identifiers rejected. |
| `playstead-mac/scripts/ci/validate-phase-3-uat-evidence.py` | Fail-closed Phase 3 UAT/roadmap contract validator | ✓ VERIFIED, now WIRED | Confirmed newly invoked from `run_contract_self_tests` (`--self-test-contracts`), which is now a CI step (`665a62f`) — closes code-review WR-03 for this validator specifically. |
| `playstead-mac/scripts/ci/run-mac-verification.sh::verify_complete_hosted_run` | Strict "complete evidence" schema/linux-job validator | ⚠️ ORPHANED | Function exists and is well-written (confirmed reading its assertions) but is not called from `ci.yml`, `--self-test-contracts`, or any `tests/*.sh` file — same unwired-guard pattern as WR-03, on a different validator. See truth 4 gap note. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `ci.yml` `mac-verification` step "Verify static contract guards" | `run-mac-verification.sh --self-test-contracts` | direct invocation, before build | ✓ WIRED | Confirmed both in `ci.yml` text and by `four-layer-topology-test.sh`'s `gate < build` ordering assertion, which I also ran locally and it passed. |
| `run_contract_self_tests` | `tests/*.sh` (10 files) + validator unittest + `validate-phase-3-uat-evidence.py` | glob enumeration + direct calls | ✓ WIRED | Read the loop (`for test_script in "${SCRIPT_DIR}"/tests/*.sh`) and ran it; all 10 files executed, all passed. |
| `CurationInteractionTests`/`StorageInteractionTests`/`SurfaceAccessibilityTests` | `TestPlans/UI.xctestplan` | `selectedTests` array | ✓ WIRED | Test class names appear verbatim in the plan's `selectedTests`. |
| `LiveServerSnapshotTests` | native `mix phx.server` (MIX_ENV=mac_ci) | `PLAYSTEAD_MAC_CI_ROOT`/`MAC_CI_DATABASE_URL` env vars, `xctestplan` variable expansion, real HTTP pairing calls | ✓ WIRED | Traced env var flow from `start_native_services()` exports through `LiveServer.xctestplan`'s `environmentVariableEntries` to `resolvedFixtureEnvironment()` in the test file. |
| `SurfaceAccessibilityTests.audit()` | `XCUIApplication.performAccessibilityAudit(for:)` | direct Apple API call | ✓ WIRED | Not the old manifest tree-walk; confirmed by reading `UITestHarness.swift`'s `audit()` implementation. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `complete-verification-evidence.json` `native_health` block | `postgresql_major`/`phoenix_healthy`/`cleanup_complete` | Hardcoded literals in `write_complete_verification_evidence()` | Yes — reachable only after `start_native_services()`'s real `die`-gated health checks succeed | ✓ FLOWING (fail-closed precondition, not a fabricated static value) |
| `complete-verification-evidence.json` `linux_jobs` block | `LINUX_TEST_CONCLUSION`/`LINUX_COMPOSE_CONCLUSION` | `needs.test.result`/`needs.compose-smoke.result` in `ci.yml` | Yes, but unvalidated downstream (see `verify_complete_hosted_run` gap) | ⚠️ STATIC-DOWNSTREAM — value flows from real job results, but nothing currently asserts it equals `"success"` |
| Snapshot reference/actual/diff PNGs | rendered view bitmap | `PlaysteadSnapshot.render()` from real SwiftUI view hierarchy | Yes | ✓ FLOWING |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| QUAL-01 | All 10 plans | Controller/keyboard/pointer/screen-reader/focus/reduced-motion support without stranding users | ✓ SATISFIED (Mac verification scope) | Truths 2, 5 above; `SurfaceAccessibilityTests` and `CurationInteractionTests`' keyboard-parity tests. |
| LIBR-01 | 03.5-01, 03.5-08, 03.5-09, 03.5-10 | Browse complete server catalogue before downloading bytes | ✓ SATISFIED | Truth 4/`LiveServerSnapshotTests`; UAT checkpoint 2 re-recorded automated. |
| LIBR-03 | 03.5-03, 03.5-04, 03.5-06, 03.5-09 | Curate via favorites/collections/Continue/Recent/queue without altering canonical bytes | ✓ SATISFIED | Truth 2; UAT checkpoint 6 re-recorded automated. |
| LIBR-04 | 03.5-03, 03.5-04, 03.5-05, 03.5-07 | Empty/unconfigured systems hidden by default with contextual actions | ✓ SATISFIED | `testContinueShelfRendersHonestEmptyFixture`/`testQueueShelfRendersHonestEmptyFixture`/`testRecentShelfRendersHonestEmptyFixture` assert honest empty-shelf copy. |

No orphaned requirements found — `REQUIREMENTS.md`'s Phase-3-mapped rows for these four IDs are consistent with this phase's stated purpose ("unblocks re-verification," not reassignment of ownership).

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in the phase's key Swift/shell/Python files read during this verification. No stub return patterns (`return null`, empty-body closures, hardcoded-empty state feeding a render) were found in the UI test or snapshot harness files inspected. One pre-existing, already-documented finding carried forward from `03.5-REVIEW.md` (not re-litigated here): WR-01 (tracked `.pyc`) — confirmed fixed (`.gitignore` updated, file untracked in `d7268f8`). WR-02, WR-04, WR-05, IN-01–03 remain open as documented in `03.5-REVIEW.md`; none blocks the phase goal.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 10 static contract guards + validator unittests + phase-3 UAT validator pass on this machine (macOS 26.6/Xcode 26.6, matching pinned runner) | `bash scripts/ci/run-mac-verification.sh --self-test-contracts` | "all static contract guards passed"; 12 unittests OK; UAT/roadmap validator passed | ✓ PASS |
| Real hosted CI run exists and is green for the pre-fix HEAD | `gh run view 33702909968` | 3/3 jobs succeeded, mac job 29m40s | ✓ PASS |
| The 3 guard-fix commits are not yet on the branch GitHub Actions runs from | `git status` / `git log origin/main -1` | local HEAD is 5 commits ahead of `origin/main` | ✗ FLAGGED — routed to human verification |

### Probe Execution

No `scripts/*/tests/probe-*.sh`-style probes were declared by this phase; `scripts/ci/tests/*.sh` are the phase's own static contract guards, already covered above as part of the artifact/link verification (not a separate probe convention).

### Human Verification Required

### 1. Push the guard-fix commits and confirm a green hosted run

**Test:** Push `04f38cc`, `665a62f`, `d7268f8` to `main` (or open the PR that will carry them) and watch the resulting `mac-verification` GitHub Actions job.
**Expected:** The job succeeds: "Verify static contract guards" step passes in seconds, then "Run one build and four serial test layers" succeeds in roughly the same ~29-30 minutes as run 33702909968, with the `complete-verification-evidence.json` artifact uploaded.
**Why human:** This exact commit set has only been exercised locally on this sandboxed macOS 26.6/Xcode 26.6 machine, not on the actual `macos-26` GitHub-hosted runner referenced by the phase's own success criteria ("proven ... on every push"). The prior real hosted run (33702909968) is genuine and green, but it predates these three commits, which exist specifically to close a real regression (CI previously ran zero of the ten static guards). Until this exact HEAD is proven green on GitHub's infrastructure, the phase's central promise is not fully closed for the current tree.

### Gaps Summary

No must-have truth failed outright — every mechanism required by the six success criteria exists, is genuinely wired end-to-end, and (where feasible to run locally) executes correctly. Two items keep this from a clean `passed`:

1. **(human_needed)** The three commits that fix the CI-wiring regression the code-review/regression gates found have not yet run on the real hosted `macos-26` runner — only locally. Given the phase goal is specifically about proof "on every push," this needs one confirmed green push before the phase can be called fully closed.
2. **(non-blocking, informational)** `verify_complete_hosted_run()` — a real, well-written strict validator for the complete evidence bundle's schema and Linux-job conclusions — is dead code today; nothing in CI or the new `--self-test-contracts` gate invokes it. This mirrors the exact "guard exists, nothing calls it" failure mode that WR-03 already documented for a different validator, just not yet caught for this one. Recommend wiring it into `run-mac-verification.sh`'s post-build step (verifying its own freshly-written `complete-verification-evidence.json`) in a follow-up, the same way `--self-test-contracts` was just added for the other ten guards.

---

_Verified: 2026-09-03T02:42:19Z_
_Verifier: Claude (gsd-verifier)_
