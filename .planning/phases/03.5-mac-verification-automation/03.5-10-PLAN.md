---
phase: 03.5-mac-verification-automation
plan: "10"
type: execute
wave: 1
depends_on: [03.5-01]
files_modified:
  - .planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json
autonomous: false
requirements: [QUAL-01, LIBR-01]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "A user-authorized push or pull request makes the exact Plan 01 commit remotely reachable with the new push/PR workflow before GitHub evidence is queried (D-01)."
    - "The persisted adoption record names one exact workflow run ID, event, branch/ref, head SHA, URL, completed/success conclusion, five non-skipped canaries, adoption layer, and both green Linux jobs (D-02-D-04)."
    - "Downstream plans remain blocked until the exact hosted evidence passes machine validation and human observation."
  artifacts:
    - path: ".planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json"
      provides: "Validated exact-run identity and sanitized Wave 0 adoption evidence"
  key_links:
    - from: ".planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json"
      to: "playstead-mac/scripts/ci/run-mac-verification.sh"
      via: "--verify-hosted-run wave-0 validation of exact run metadata and downloaded artifact"
      pattern: "run_id"
  prohibitions:
    - statement: "MUST NOT push without user authorization, depend on workflow_dispatch being present on the default branch, or select a run by recency alone."
      status: unverified
      flagged: true
---

<objective>
Publish and approve the Plan 01 adoption tracer through one exact authorized push/PR-triggered GitHub Actions run.

Purpose: GitHub cannot supply authoritative evidence for a local-only commit, and the executor has no implied authority to push it.
Output: User-confirmed remote publication, exact run capture/watch/download/validation, persisted evidence, and blocking human approval.
</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-01-SUMMARY.md
@.github/workflows/ci.yml
@playstead-mac/scripts/ci/run-mac-verification.sh
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authorize publication of the exact adoption commit</name>
  <action>Obtain explicit user authorization and confirmation for the exact remote publication described below before any push or PR action.</action>
  <instructions>Stop before any push or PR mutation. Report the local `git rev-parse HEAD`, verify that this commit contains `.github/workflows/ci.yml` with `push` and `pull_request` triggers plus the complete Plan 01 harness, and ask the user to perform or explicitly authorize their normal push/PR action. The remote branch or pull-request head must resolve to that exact SHA; the default branch need not already contain `workflow_dispatch`. Request confirmation containing the remote branch name and whether the expected event is `push` or `pull_request`; for a PR also request its number or URL.</instructions>
  <verification>After the authorized action, confirm in GitHub that the reported remote branch/PR head SHA exactly equals the executor-reported local SHA and that the CI workflow was triggered by that push or pull request.</verification>
  <resume-signal>Reply with "published", the remote branch, expected event (`push` or `pull_request`), and the PR URL/number when applicable.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Capture, watch, download, and validate the exact adoption run</name>
  <precondition>The user confirmed an authorized remote branch/PR whose head SHA equals the committed Plan 01 SHA.</precondition>
  <files>.planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json</files>
  <action>Query `gh run list` with the explicit `ci.yml` workflow, confirmed branch, and confirmed `push` or `pull_request` event, then filter the returned JSON by exact `headSha`; require exactly one matching run instead of using the latest result. Persist its database ID, workflow name, event, head branch/ref, head SHA, URL, status, and conclusion. Run `gh run watch RUN_ID --exit-status`, then `gh run view RUN_ID --json databaseId,workflowName,event,headBranch,headSha,status,conclusion,url,jobs` and download only that run's `wave-0-adoption` artifact with `gh run download RUN_ID --name wave-0-adoption`. Pass the exact view JSON and downloaded manifest to `run-mac-verification.sh --verify-hosted-run wave-0`; fail on wrong run/SHA/event/ref, incomplete or non-success conclusion, malformed schema, absent/skipped/failed canary or layer, missing/failed Linux `test` or `compose-smoke`, or missing fingerprint/native/snapshot evidence. Write the bounded sanitized validated record to the declared evidence path; do not persist raw artifacts or secrets per D-05/D-11.</action>
  <verify><automated>RUN_ID=$(jq -er '.run_id' .planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json) &amp;&amp; gh run watch "$RUN_ID" --exit-status &amp;&amp; gh run view "$RUN_ID" --json databaseId,workflowName,event,headBranch,headSha,status,conclusion,url,jobs &gt; /tmp/playstead-wave-0-run-view.json &amp;&amp; cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --verify-hosted-run wave-0 --run-record ../.planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json --run-view /tmp/playstead-wave-0-run-view.json</automated></verify>
  <done>The exact authorized run is completed/successful and its persisted record proves all five canaries, the adoption layer, and both Linux jobs without ambiguity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Observe and approve the exact hosted Wave 0 run</name>
  <what-built>The executor persisted and machine-validated one exact push/PR-triggered run ID and SHA in `wave-0-hosted-run.json`.</what-built>
  <how-to-verify>Open the recorded run URL. Confirm the displayed event, branch/PR, and head SHA match the publication confirmed in Task 1. Confirm green Linux `test`, Linux `compose-smoke`, and macos-26 adoption jobs. In the macOS summary confirm ad-hoc launch, Full Keyboard Access, scoped file-Keychain twice, native PostgreSQL 17/Phoenix health, and SnapshotTesting mismatch/mutation/noise are all discovered, non-skipped, and passed. Reject a local log, different SHA, skipped canary, or different run ID.</how-to-verify>
  <resume-signal>Type "approved" with the exact hosted run URL, or describe the failed/missing canary or job.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local repository → GitHub remote | Publication is an external mutation requiring explicit user authority. |
| GitHub run/artifact → adoption record | Remote metadata and artifacts may be stale, unrelated, incomplete, or maliciously malformed. |

## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-22 | Elevation | remote publication | high | mitigate | Blocking human-action checkpoint names the exact SHA/ref/event and forbids implicit push authority. |
| T-03.5-23 | Spoofing/Repudiation | hosted run selection | high | mitigate | Exact workflow+branch+event+headSha filtering, unique run ID, exit-status watch, exact view, artifact download, fail-closed validator, and human observation. |
</threat_model>

<verification>
- The remote head equals the committed Plan 01 SHA before run lookup.
- The recorded run ID is watched with `--exit-status`, viewed explicitly, and is the source of the downloaded artifact.
- The hosted validator proves all five canaries/layers and both Linux jobs for that SHA.
</verification>

<success_criteria>
- Plan 10 never pushes without explicit authority and never depends on manual-dispatch availability.
- One exact remotely reachable commit has authoritative hosted adoption evidence.
- Plan 02 cannot start before machine and human approval of that exact run.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/03.5-mac-verification-automation/evidence/wave-0-hosted-run.json`: exact validated adoption run identity and sanitized outcomes.

<output>Create `.planning/phases/03.5-mac-verification-automation/03.5-10-SUMMARY.md` when done.</output>
