---
status: passed
phase: 03.5-mac-verification-automation
source: [03.5-VERIFICATION.md]
started: 2026-09-03T02:45:40Z
updated: 2026-09-03T03:56:00Z
---

## Current Test

number: 1
name: Push the guard-fix commits and confirm a green hosted run
expected: |
  The `mac-verification` job on the real GitHub-hosted `macos-26` runner succeeds
  end to end at this exact HEAD: the new "Verify static contract guards" step
  (`run-mac-verification.sh --self-test-contracts`, 13 contracts) passes in
  seconds, then "Run one build and four serial test layers" goes green in roughly
  the ~29-30 minutes that run 33702909968 took, uploading
  `complete-verification-evidence.json`.
result: pass

## Tests

### 1. Push the guard-fix commits and confirm a green hosted run

expected: The `mac-verification` job succeeds on the real hosted `macos-26` runner at this HEAD, with the new contract-guard step passing before the build and the four-layer verification going green.
result: pass
evidence: |
  Run 33710832760 (https://github.com/szTheory/playstead/actions/runs/33710832760)
  on SHA 541520b, all three jobs green:
    mix precommit ................................ success
    docker compose cold start .................... success
    macOS 26 unit + rendering + UI + live server . success (31m49s)
  Mac job steps: "Verify static contract guards" success (22 contract lines),
  then "Run one build and four serial test layers" success with unit: PASSED,
  rendering: PASSED, ui: PASSED, live-server: PASSED, and
  complete-verification-evidence.json uploaded.

  Two real defects were found and fixed before this run went green:
    bb9f52c  mix format rejected the WR-05 pipeline parens
    541520b  wave6-swift-semantic-test.sh depended on Homebrew ripgrep, absent
             from the macos-26 image. It had never run on CI, and would have
             passed VACUOUSLY there -- `if rg -q ...` reads a 127 exit as
             "pattern absent". Its negative control is what made it fail loudly.
             Reimplemented in python3 and verified by injecting the regression.

notes: |
  The last real hosted run (33702909968, 29m40s, all green) predates the four
  commits that close the CI-wiring regression found during this phase's gates:

    04f38cc  realign stale contract guards with shipped code
    665a62f  run the static contract guards in CI
    d7268f8  stop tracking compiled Python artifacts
    66947c2  exercise the hosted-run validator in the contract gate

  All 13 contracts pass locally on macOS 26.6 / Xcode 26.6, matching the pinned
  runner fingerprint. That is strong corroboration but not the same
  infrastructure, and the phase goal is specifically "proven on every push" — so
  this stays human until one real hosted run at this HEAD is green.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
