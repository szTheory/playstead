---
phase: 03.5-mac-verification-automation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/mac-snapshot-refresh.yml
  - playstead-mac/Playstead.xcodeproj/project.pbxproj
  - playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme
  - playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - playstead-mac/TestPlans/Unit.xctestplan
  - playstead-mac/TestPlans/Rendering.xctestplan
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/TestPlans/LiveServer.xctestplan
  - playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - playstead-mac/scripts/ci/sanitize-evidence.sh
autonomous: true
requirements:
  - QUAL-01
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "On every push and pull request, a required macos-26 ARM64 job selects Xcode 26.6 build 17F113, builds Playstead once, and fails when any selected XCTest fails (D-01, D-02)."
    - "The existing Linux test and compose-smoke jobs remain enabled and unchanged in purpose; the Mac job proves behavior without claiming Docker Compose ran on a standard Mac runner (D-04)."
    - "Every named layer is discovered, executed, non-skipped, and passed from machine-readable xcresult metadata; later layers still run after an earlier failure and there are no automatic retries (D-02, D-03)."
    - "Snapshot recording is disabled in CI, missing references fail, deliberate mismatch/mutation failures are ordinary assertions with reviewable output, and calibrated noise passes (D-12, D-15, D-17)."
    - "Failure artifacts are allowlisted, sanitized, secret-scanned, uploaded only on failure, and retained briefly; fresh DerivedData is never cached (D-05)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Required pinned hosted-Mac verification job alongside preserved Linux jobs"
    - path: "playstead-mac/Playstead.xcodeproj/project.pbxproj"
      provides: "PlaysteadUITests target and exact test-only SnapshotTesting package reference"
    - path: "playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme"
      provides: "Shared app, unit-test, and UI-test build/test scheme"
    - path: "playstead-mac/scripts/ci/run-mac-verification.sh"
      provides: "One-build, independent-layer, xcresult-verified orchestration command"
    - path: "playstead-mac/scripts/ci/sanitize-evidence.sh"
      provides: "Allowlisted evidence staging and secret/content scan"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "playstead-mac/scripts/ci/run-mac-verification.sh"
      via: "required macos-26 job invokes the phase wrapper after asserting DEVELOPER_DIR and architecture"
      pattern: "run-mac-verification"
    - from: "playstead-mac/scripts/ci/run-mac-verification.sh"
      to: "playstead-mac/TestPlans/*.xctestplan"
      via: "one build-for-testing followed by four serial test-without-building result bundles"
      pattern: "test-without-building"
    - from: "playstead-mac/Playstead.xcodeproj/project.pbxproj"
      to: "playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift"
      via: "SnapshotTesting 1.19.4 is linked to the test target only"
      pattern: "SnapshotTesting"
  prohibitions:
    - statement: "MUST NOT report physical-controller, real-emulator/game-byte, Developer ID/notarization, or experiential VoiceOver evidence as automated by this hosted runner."
      status: unverified
      flagged: true
    - statement: "MUST NOT upload credentials, Keychain data, pairing responses, user-content names or hashes, databases, object bytes, or raw unsanitized logs as CI evidence."
      status: unverified
      flagged: true
---

<objective>
Establish the production-quality hosted Mac verification tracer and the reusable Xcode/CI harness every later Phase 3.5 proof runs through.

Purpose: QUAL-01 cannot be re-verified on every change until the repository has a pinned, fail-loud Mac job with authoritative test discovery and privacy-safe evidence.
Output: A shared UI-test target/scheme, four test plans, an exact snapshot dependency gate, one-build orchestration, and required macOS CI without weakening Linux or Compose coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
@.planning/phases/03.5-mac-verification-automation/03.5-VALIDATION.md
@.github/workflows/ci.yml
@playstead-mac/Playstead.xcodeproj/project.pbxproj
</context>

<assumption_delta_decision>
Primary noun: verification layer. Decision: no-change. The new native-Mac behavior layer complements the existing Linux Compose topology layer; it does not pluralize application identity, content identity, or any persisted model.
</assumption_delta_decision>

<flagged_assumptions>
- QUAL-01 / unclassified: The automated claim is deliberately bounded to keyboard, pointer, visible focus, reduced-motion checks, and machine-checkable live accessibility semantics. Controller hardware and experiential VoiceOver remain unverified per D-23 and D-25; this assumption stays flagged until the named hosted tests pass.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one hosted Mac path from push trigger to authoritative XCTest result</name>
  <read_first>
    - .github/workflows/ci.yml
    - playstead-mac/Playstead.xcodeproj/project.pbxproj
    - .planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
    - .planning/phases/03.5-mac-verification-automation/03.5-VALIDATION.md
  </read_first>
  <files>.github/workflows/ci.yml, playstead-mac/Playstead.xcodeproj/project.pbxproj, playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme, playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <action>Following the exact Linux workflow/target analogs in 03.5-PATTERNS.md, add a synchronized `PlaysteadUITests` target with application dependency and a shared `Playstead` scheme, then add one required unfiltered `macos-26` job for push and pull_request. Set `DEVELOPER_DIR=/Applications/Xcode_26.6.app`, fail unless architecture is arm64 and `xcodebuild -version` is build 17F113, and record the mutable macOS/runner image fingerprint without hard-failing on the patch number per D-01. Implement the first production tracer in `run-mac-verification.sh`: fresh DerivedData, one `build-for-testing`, existing `PlaysteadTests`, and a launched-process two-control XCUITest canary that proves the actual Full Keyboard Access mechanism by exact forward/reverse focus and an activation postcondition. Parse xcresult test metadata to prove the named canary and one existing unit test were discovered, executed, non-skipped, and passed; aggregate layer exits, continue gathering results after failure, and return nonzero once evidence is collected, with no retry or fixed sleep per D-02/D-03. Preserve the existing Linux `test` and `compose-smoke` jobs exactly in role and gating per D-04. The file-system-synchronized target and workflow patterns are direct analogs; the shared scheme, UI target, and hosted canary intentionally depart because no repository analog exists.</action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers unit,hosted-canary --derived-data "$(mktemp -d)/DerivedData"</automated>
  </verify>
  <acceptance_criteria>
    - The wrapper asserts ARM64/Xcode 26.6 build 17F113, builds once, and produces separate Unit and HostedCanary xcresults.
    - Machine-readable result parsing names both allowlisted tests and rejects missing, skipped, failed, or schema-unreadable results.
    - The canary tabs forward and backward through two real controls, proves exactly one focused element at each step, activates one, and has no fixed sleeps.
    - `.github/workflows/ci.yml` retains both Linux jobs and adds an unfiltered required Mac job on push and pull request.
  </acceptance_criteria>
  <done>The smallest hosted-Mac verification path is real end to end: a push-triggered pinned runner builds the app and fails from an authoritative unit/UI test result.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the tracer into four isolated, one-build verification layers and sanitized evidence</name>
  <read_first>
    - playstead-mac/scripts/ci/run-mac-verification.sh
    - .github/workflows/ci.yml
    - .planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
    - .planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
  </read_first>
  <files>playstead-mac/TestPlans/Unit.xctestplan, playstead-mac/TestPlans/Rendering.xctestplan, playstead-mac/TestPlans/UI.xctestplan, playstead-mac/TestPlans/LiveServer.xctestplan, playstead-mac/scripts/ci/run-mac-verification.sh, playstead-mac/scripts/ci/sanitize-evidence.sh</files>
  <action>Expand the tracer per D-02/D-03 into explicit Unit, Rendering, UI, and LiveServer shared test plans. `run-mac-verification.sh` must build once then run serial `test-without-building` layers with distinct result bundles and a fixed allowlist of UAT-mapped identifiers, collecting all exit statuses without `set -e` short-circuiting or retries. Make its xcresult parser fail closed on missing bundles, parse/schema errors, zero discovered tests, skips, missing allowlisted identifiers, or non-pass outcomes. Add bounded per-layer deadlines. Stage only environment fingerprints, redacted test-result JSON, snapshot reference/actual/diff files, sanitized screenshots, redacted tree dumps/focus traces, and sanitized app/server logs; scan the staging tree before upload and fail if it contains authorization material, pairing responses, Keychain/database/object files, source paths outside the run root, or non-synthetic content identifiers per D-05/D-11. Keep DerivedData and raw result bundles outside the upload tree. Use the existing failure-only `upload-artifact@v4` shape as the analog while deliberately narrowing its input to the sanitizer's allowlist.</action>
  <verify>
    <automated>bash -n playstead-mac/scripts/ci/run-mac-verification.sh playstead-mac/scripts/ci/sanitize-evidence.sh &amp;&amp; playstead-mac/scripts/ci/run-mac-verification.sh --self-test-result-verifier --self-test-sanitizer</automated>
  </verify>
  <acceptance_criteria>
    - Four test plans have disjoint, named ownership and share the Playstead build products.
    - A synthetic bundle missing an allowlisted test, containing a skip, or failing schema parsing makes the verifier fail; a complete all-pass bundle succeeds.
    - A seeded forbidden artifact/secret makes sanitization fail and a clean allowlisted fixture yields a bounded staging tree.
    - Layer failure does not prevent later layers or evidence sanitization from running, but the aggregate command exits nonzero.
  </acceptance_criteria>
  <done>All four Mac verification layers share one build while retaining independent results, authoritative discovery, bounded execution, and privacy-safe failure evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Gate the exact snapshot dependency and candidate-only baseline workflow</name>
  <read_first>
    - playstead-mac/Playstead.xcodeproj/project.pbxproj
    - playstead-mac/scripts/ci/run-mac-verification.sh
    - .planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
    - .planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
  </read_first>
  <reversibility rating="costly">D-04 fixes the public CI topology and D-12 adds a shared test dependency; replacing either later requires coordinated workflow, baseline, and package-resolution changes.</reversibility>
  <files>playstead-mac/Playstead.xcodeproj/project.pbxproj, playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved, playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift, .github/workflows/mac-snapshot-refresh.yml, .github/workflows/ci.yml</files>
  <action>Resolve Point-Free `SnapshotTesting` exactly at 1.19.4 / revision `59a99c458de4d2dee580529b61b4f78dca7b7fa6`, link it only to the unit/render test target, and commit the package resolution per D-12. Add canaries that intentionally create a meaningful mismatch/mutation and prove it becomes an ordinary failed assertion with reference/actual/diff evidence, while centrally calibrated antialiasing noise passes; do not accept the package if the mismatch crashes or loses reviewable output per D-15. Configure CI with recording disabled and missing references fatal. Add a manual snapshot-refresh workflow pinned to the same runner/Xcode fingerprint that emits candidate artifacts only, never writes/commits/auto-accepts references, and requires named reviewed diffs per D-17. Wire the sanitized failure staging to `upload-artifact@v4` with a short seven-day retention and failure-only condition per D-05. This dependency has no repository analog; follow the audited official source and exact pin in 03.5-RESEARCH.md rather than introducing another renderer/comparator.</action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --snapshot-canary-gate --derived-data "$(mktemp -d)/DerivedData"</automated>
  </verify>
  <acceptance_criteria>
    - `Package.resolved` matches exactly version 1.19.4 and revision `59a99c458de4d2dee580529b61b4f78dca7b7fa6`; the app/Release target does not link SnapshotTesting.
    - The known mutation yields a normal mismatch plus reference/actual/diff files, and calibrated noise passes without a per-test tolerance override.
    - CI has recording disabled and fails on missing references.
    - Manual refresh produces candidate artifacts only, while CI uploads only sanitizer-approved failure evidence for seven days.
  </acceptance_criteria>
  <done>The snapshot toolchain is accepted only after its failure semantics are proven on the pinned environment, and no workflow can silently record or commit baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow → hosted runner | Repository-controlled commands run on a mutable third-party VM image and download locked dependencies. |
| XCTest/UI process → xcresult/evidence staging | Test output can contain paths, environment data, UI labels, attachments, and secrets before sanitization. |
| Test target → Release application | Test-only packages/targets must not become runtime dependencies or a shipped bypass. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-01 | Elevation of privilege | Xcode target graph / Release product | high | mitigate | Link SnapshotTesting and UI hooks only to test configurations; later Plan 02 adds binary/symbol absence guards before Release evidence can pass. |
| T-03.5-02 | Information disclosure | xcresult and artifact upload | high | mitigate | Allowlist export, structured redaction, secret/content scan, failure-only upload, seven-day retention, and fail closed before upload. |
| T-03.5-03 | Repudiation | test discovery/result aggregation | medium | mitigate | Parse Xcode 26.6 xcresult metadata and require named discovered/executed/non-skipped/pass outcomes, plus aggregate exit status. |
| T-03.5-04 | Denial of service | serial render/UI layers | medium | mitigate | Per-layer deadlines, fresh one-time build, bounded artifacts, and guaranteed cleanup; no retries. |
| T-03.5-SC | Tampering | SwiftPM SnapshotTesting install | high | mitigate | Official Point-Free repository, exact 1.19.4 tag/revision lock, test-target-only link, and hosted intentional-mismatch adoption gate. |
</threat_model>

<verification>
- Run the tracer/unit/canary path locally on Xcode 26.6 and through the unfiltered hosted Mac job.
- Run result-verifier and sanitizer negative self-tests before trusting any green layer.
- Confirm existing Linux server tests and compose-smoke still execute under their original jobs.
- Confirm the snapshot mismatch produces reviewable artifacts and manual refresh cannot commit or accept them.
</verification>

<success_criteria>
- A push/PR reaches a pinned `macos-26`/Xcode 26.6 job and any failed, skipped, missing, or undiscovered selected test fails the job.
- The app builds once and all four independent layers are runnable through committed shared test plans.
- Snapshot dependency/version/failure semantics are proven before any contract baselines rely on it.
- Only sanitized, failure-only, short-retention evidence can leave the runner.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ci.yml`: `mac-verification` job; command `playstead-mac/scripts/ci/run-mac-verification.sh`; failure-only sanitized upload; preserved Linux `test` and `compose-smoke` jobs.
- `.github/workflows/mac-snapshot-refresh.yml`: manual candidate-only refresh workflow; no commit/accept permission.
- `playstead-mac/Playstead.xcodeproj/project.pbxproj`: `PlaysteadUITests` target, app dependency, UI-testing configuration, exact test-only `SnapshotTesting` package product.
- `playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme`: shared `Playstead` scheme covering app, `PlaysteadTests`, and `PlaysteadUITests`.
- `playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`: SnapshotTesting 1.19.4 at revision `59a99c458de4d2dee580529b61b4f78dca7b7fa6`.
- `playstead-mac/TestPlans/Unit.xctestplan`, `Rendering.xctestplan`, `UI.xctestplan`, `LiveServer.xctestplan`: the four independent test layers.
- `playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift`: `HostedRunnerCanaryTests/testFullKeyboardAccessCanaryFocusesAndActivatesTwoControls`.
- `playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift`: deliberate mismatch, mutation-sensitivity, and noise-calibration canaries.
- `playstead-mac/scripts/ci/run-mac-verification.sh`: fingerprint/build/layer/result aggregation command and UAT allowlist verifier.
- `playstead-mac/scripts/ci/sanitize-evidence.sh`: bounded allowlist staging, redaction, and secret/content scan.

<output>
Create `.planning/phases/03.5-mac-verification-automation/03.5-01-SUMMARY.md` when done.
</output>
