---
phase: 03.5-mac-verification-automation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - playstead-mac/Playstead.xcodeproj/project.pbxproj
  - playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme
  - playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - playstead-mac/Playstead/Net/KeychainStore.swift
  - playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - playstead-server/config/mac_ci.exs
autonomous: true
requirements: [QUAL-01, LIBR-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The repository contains one production-quality push/pull-request path capable of proving ad-hoc launch, Full Keyboard Access, disposable scoped Keychain behavior, native PostgreSQL 17/Phoenix startup, and SnapshotTesting mismatch semantics on macos-26/ARM64/Xcode 26.6 build 17F113 (D-01, D-04, D-07, D-12, D-19)."
    - "The local verifier fails closed on wrong SHA/event/ref, incomplete/failed runs, missing or skipped canaries/layers, missing Linux jobs, malformed evidence, or local-only evidence (D-02, D-03)."
    - "The existing Linux test and compose-smoke jobs remain enabled and unchanged in purpose (D-04)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Unfiltered push/pull-request macOS adoption job and preserved Linux jobs"
    - path: "playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift"
      provides: "Ad-hoc launch, exact keyboard traversal, and disposable Keychain canaries"
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift"
      provides: "Intentional mismatch, mutation, and calibrated-noise adoption canaries"
    - path: "playstead-mac/scripts/ci/run-mac-verification.sh"
      provides: "Native-service lifecycle, canary result verifier, hosted-run validator, and evidence schema"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "playstead-mac/scripts/ci/run-mac-verification.sh"
      via: "required macos-26 adoption invocation on push and pull_request"
      pattern: "wave-0-adoption"
    - from: "playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift"
      to: "playstead-mac/Playstead/Net/KeychainStore.swift"
      via: "kSecUseKeychain/kSecMatchSearchList scoped twice-run canary"
      pattern: "testScopedFileKeychainStoresLoadsAndDeletesTwice"
  prohibitions:
    - statement: "MUST NOT accept local execution, command exit zero, an unrelated/latest run, or a skipped test as hosted adoption evidence."
      status: unverified
      flagged: true
    - statement: "MUST NOT mutate the login Keychain search list, grant blanket ACL, use Developer ID credentials, or upload secret-bearing raw evidence."
      status: unverified
      flagged: true
---

<objective>
Build the nine-file hosted adoption tracer and its fail-closed exact-run verifier before any remote publication is requested.

Purpose: The selected mechanisms are plan-time decisions, but Plan 10 may adopt them only after this committed harness runs through an authorized push or pull request on GitHub.
Output: A push/PR-triggered macos-26 tracer, all five canaries, machine-readable evidence schema, and exact-run validation commands.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
@.planning/phases/03.5-mac-verification-automation/03.5-VALIDATION.md
@.github/workflows/ci.yml
@playstead-mac/Playstead.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one signed-launch canary from the shared scheme to push/PR evidence</name>
  <files>.github/workflows/ci.yml, playstead-mac/Playstead.xcodeproj/project.pbxproj, playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme, playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - A shared Playstead scheme discovers an ad-hoc-signed UI test target and the required macos-26 push/pull-request job runs its exact identifier.
    - The result verifier rejects a missing bundle, absent identifier, zero execution count, skip, non-pass, schema error, local-only record, or mismatched run metadata.
  </behavior>
  <action>Add the synchronized PlaysteadUITests target and shared scheme, then add one unfiltered `macos-26` job on `push` and `pull_request`. Assert ARM64, `/Applications/Xcode_26.6.app`, build 17F113, and record the mutable image/macOS fingerprint per D-01. Use ad-hoc signing (`CODE_SIGN_IDENTITY=-`, no team/Developer ID) and prove launched-app success in `testAdHocSignedAppLaunchesOnHostedRunner`. Implement result and hosted-run validation modes in `run-mac-verification.sh`: they consume explicitly supplied GitHub run metadata plus downloaded evidence, bind workflow/event/ref/head SHA/run ID, require completed/success, and fail closed rather than selecting the latest run. Preserve both Linux jobs exactly in purpose per D-04.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-wave-0-verifier --self-test-hosted-run-validator</automated></verify>
  <done>One production-quality signed-launch path is wired from push/PR trigger through authoritative result metadata and exact-run validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove keyboard navigation and scoped Keychain isolation</name>
  <files>playstead-mac/Playstead/Net/KeychainStore.swift, playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - Full Keyboard Access proves exact forward/reverse order, wrap, exactly one focus, and activation; defaults-command success alone is insufficient.
    - Scoped file-Keychain performs two complete store/load/delete cycles without search-list mutation, blanket ACL, prompts, or surviving files.
  </behavior>
  <action>Select Full Keyboard Access with `defaults write NSGlobalDomain AppleKeyboardUIMode -int 3`, but accept it only from the named focus/activation canary per D-19. Extend `KeychainStore` with optional scope keys and prove two cycles without shared-state mutation per D-07. Extend the result schema with exact discovery/execution/skip/outcome records for both canaries and negative fixtures for missing, skipped, or failed results; never include Keychains or credential material per D-05/D-11.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-wave-0-verifier --only-canaries keyboard,keychain</automated></verify>
  <done>The keyboard and Keychain adoption mechanisms have exact non-skipped result contracts without trusting command success or shared host state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove native services and snapshot mismatch semantics in hardened evidence</name>
  <files>playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved, playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift, playstead-server/config/mac_ci.exs, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - Native PostgreSQL 17/Phoenix binds loopback under a non-sandbox mac_ci environment, reaches real health, and always cleans up.
    - Intentional snapshot mismatch produces an ordinary assertion and reference/actual/diff triplet; meaningful mutation fails and calibrated noise passes.
    - Hosted validation rejects missing/skipped/failed canaries, adoption layer, Linux test job, Linux compose-smoke job, malformed artifacts, and mismatched SHA/event/ref/run identity.
  </behavior>
  <action>Resolve Point-Free SnapshotTesting exactly at 1.19.4/revision 59a99c458de4d2dee580529b61b4f78dca7b7fa6 to the test target only. Add the dedicated non-sandbox `mac_ci` config; have the bounded main CI wrapper install/start isolated PostgreSQL 17, migrate, start real loopback Phoenix, observe health, and trap cleanup per D-04. Add the SnapshotTesting mismatch/mutation/noise canaries per D-12/D-15. Emit sanitized `wave-0-adoption.json` containing run identity, fingerprint, exact results for all five canaries, native health, snapshot triplet inventory, and Linux job conclusions. Add positive and negative fixtures for every hosted identity/status/schema/layer/job rejection branch; never include databases, raw logs, or pairing material per D-05/D-11.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-wave-0-verifier --self-test-hosted-run-validator --verify-wave-0-topology</automated></verify>
  <done>All five adoption risks and both preserved Linux jobs have an exact, sanitized, fail-closed hosted evidence contract ready for Plan 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow → hosted runner | Repository code executes on a mutable third-party image. |
| UI test → scoped Keychain | Test credentials cross Security.framework without entering shared login state. |
| Hosted results → adoption decision | Untrusted remote metadata/artifacts must bind to one authorized run and exact commit. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-01 | Spoofing | hosted evidence | high | mitigate | Bind workflow, event, branch/ref, run ID, head SHA, completion, conclusion, named discovery/non-skip outcomes, and both Linux jobs in one fail-closed validator. |
| T-03.5-02 | Information disclosure | Keychain/xcresult/logs | high | mitigate | Disposable scoped Keychain, synthetic data, bounded manifest, sanitized artifacts, no raw credential/database upload. |
| T-03.5-03 | Tampering | SnapshotTesting package | high | mitigate | Exact official tag/revision, test-target-only link, intentional hosted mismatch and triplet gate. |
| T-03.5-04 | Denial of service | native services/UI canaries | medium | mitigate | Bounded waits, finite fixtures, process traps, no retries. |
</threat_model>

<verification>
- Run all result/evidence validator positive and negative fixtures locally.
- Confirm the workflow has push/pull_request triggers and no dispatch-only dependency.
- Confirm both Linux jobs remain present and enabled.
</verification>

<success_criteria>
- The first end-to-end tracer and all remaining canaries are committed before remote publication.
- An exact push/PR run can be identified without ambiguous latest-run selection.
- No secret, Keychain, database, content identifier, or raw unsanitized log can enter hosted evidence.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ci.yml`: required `macos-26` push/PR adoption job plus preserved Linux jobs.
- `playstead-mac/Playstead.xcodeproj/project.pbxproj`: UI-test target and exact test-only package link.
- `playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme`: shared hosted scheme.
- `playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`: exact SnapshotTesting resolution.
- `playstead-mac/Playstead/Net/KeychainStore.swift`: optional scoped query support preserving production behavior.
- `playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift`: signing, keyboard, and Keychain canaries.
- `playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift`: mismatch/mutation/noise canaries.
- `playstead-mac/scripts/ci/run-mac-verification.sh`: native lifecycle, xcresult verifier, hosted-run validator, and evidence schemas.
- `playstead-server/config/mac_ci.exs`: dedicated real-Repo native environment.

<output>
Create `.planning/phases/03.5-mac-verification-automation/03.5-01-SUMMARY.md` when done.
</output>
