---
phase: 03.5-mac-verification-automation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - playstead-mac/Playstead.xcodeproj/project.pbxproj
  - playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme
  - playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
  - playstead-mac/Playstead/Net/KeychainStore.swift
  - playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - playstead-server/config/mac_ci.exs
  - playstead-mac/scripts/ci/live-server.sh
autonomous: false
requirements: [QUAL-01, LIBR-01]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An actual required macos-26/ARM64/Xcode 26.6 build 17F113 run proves ad-hoc signed UI launch, Full Keyboard Access, disposable scoped file-Keychain behavior, native PostgreSQL 17/Phoenix startup, and SnapshotTesting mismatch artifacts before any downstream plan starts (D-01, D-04, D-07, D-12, D-19)."
    - "Every canary is discovered, executed, non-skipped, and passed from xcresult/native-service JSON; command success and local-only execution are insufficient (D-02, D-03)."
    - "The existing Linux test and compose-smoke jobs remain enabled and unchanged in purpose (D-04)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Unfiltered hosted adoption-gate job and preserved Linux jobs"
    - path: "playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift"
      provides: "Ad-hoc launch, exact keyboard traversal, and disposable Keychain canaries"
    - path: "playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift"
      provides: "Intentional mismatch, mutation, and calibrated-noise adoption canaries"
    - path: "playstead-mac/scripts/ci/live-server.sh"
      provides: "Native PostgreSQL 17/Phoenix startup and cleanup canary"
    - path: "playstead-mac/scripts/ci/run-mac-verification.sh"
      provides: "Machine-readable Wave 0 canary verifier and hosted evidence manifest"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "playstead-mac/scripts/ci/run-mac-verification.sh"
      via: "required macos-26 adoption-gate invocation"
      pattern: "wave-0-adoption"
    - from: "playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift"
      to: "playstead-mac/Playstead/Net/KeychainStore.swift"
      via: "kSecUseKeychain/kSecMatchSearchList scoped twice-run canary"
      pattern: "testScopedFileKeychainStoresLoadsAndDeletesTwice"
  prohibitions:
    - statement: "MUST NOT accept local execution, a defaults command exit code, target exit zero, or a skipped test as hosted adoption evidence."
      status: unverified
      flagged: true
    - statement: "MUST NOT mutate the login Keychain search list, grant blanket ACL, use Developer ID credentials, or upload secret-bearing raw evidence."
      status: unverified
      flagged: true
---

<objective>
Empirically settle every hosted-runner adoption risk before broader Phase 3.5 work begins.

Purpose: The selected mechanisms are plan-time decisions, but their adoption is safe only after an actual hosted run proves all five runner-specific assumptions.
Output: A 10-file Wave 0 tracer, machine-readable hosted evidence, and a blocking human-observable run checkpoint.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/03.5-mac-verification-automation/03.5-RESEARCH.md
@.planning/phases/03.5-mac-verification-automation/03.5-PATTERNS.md
@.planning/phases/03.5-mac-verification-automation/03.5-VALIDATION.md
@.github/workflows/ci.yml
@playstead-mac/Playstead.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire all five hosted adoption canaries through one real macos-26 path</name>
  <files>.github/workflows/ci.yml, playstead-mac/Playstead.xcodeproj/project.pbxproj, playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme, playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved, playstead-mac/Playstead/Net/KeychainStore.swift, playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift, playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh, playstead-server/config/mac_ci.exs, playstead-mac/scripts/ci/live-server.sh</files>
  <action>Add the synchronized PlaysteadUITests target and shared scheme, resolve Point-Free SnapshotTesting exactly at 1.19.4/revision 59a99c458de4d2dee580529b61b4f78dca7b7fa6 to the test target only, and add one unfiltered `macos-26` workflow-dispatch/push/pull-request adoption job. Assert ARM64, `/Applications/Xcode_26.6.app`, build 17F113, and record the mutable image/macOS fingerprint per D-01. Use ad-hoc signing (`CODE_SIGN_IDENTITY=-`, no team/Developer ID) and prove launched-app success in `testAdHocSignedAppLaunchesOnHostedRunner`. Select Full Keyboard Access with `defaults write NSGlobalDomain AppleKeyboardUIMode -int 3`, but accept it only when `testFullKeyboardAccessCanaryFocusesAndActivatesTwoControls` proves exact forward/reverse focus, wrap, one-focused-element, and activation per D-19. Extend `KeychainStore` with optional scope keys and prove two store/load/delete cycles in `testScopedFileKeychainStoresLoadsAndDeletesTwice`, without search-list mutation or blanket ACL per D-07. Add a dedicated non-sandbox `mac_ci` config and a bounded helper that installs/starts isolated PostgreSQL 17, migrates, starts real loopback Phoenix, observes health, and traps cleanup per D-04. Add SnapshotTesting mismatch/mutation/noise canaries that require an ordinary assertion plus reference/actual/diff evidence per D-12/D-15. Preserve both Linux jobs exactly in purpose per D-04.</action>
  <verify><automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-wave-0-verifier</automated></verify>
  <acceptance_criteria>
    - The verifier names all five gates and rejects missing, skipped, failed, schema-unreadable, or local-only evidence.
    - The snapshot pin and revision are exact and absent from the application/Release link graph.
    - Native startup uses normal Repo ownership, PostgreSQL major 17, real loopback HTTP, and guaranteed cleanup.
    - The existing Linux test and compose-smoke jobs remain present and enabled.
  </acceptance_criteria>
  <done>The repository can dispatch one production-quality hosted tracer that tests every empirical adoption risk end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch the hosted workflow and persist authoritative adoption evidence</name>
  <files>.github/workflows/ci.yml, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <action>Attempt `gh workflow run ci.yml --ref` for the executor's committed branch and poll the resulting run through the GitHub CLI. Export a bounded `wave-0-adoption.json` artifact containing run URL, commit SHA, runner/Xcode fingerprint, exact canary identifiers, discovery count, skip count, outcome, native PostgreSQL/Phoenix health evidence, and snapshot triplet inventory. Fail closed unless all named canaries are discovered, non-skipped, and passed on `macos-26`; do not substitute a local invocation if dispatch/auth/remote availability fails. Upload only sanitized synthetic evidence, never the temporary Keychain, credentials, database, raw logs, or raw pairing material per D-05/D-11.</action>
  <verify><automated>gh run list --workflow ci.yml --limit 1 --json databaseId,url,headSha,status,conclusion</automated></verify>
  <acceptance_criteria>
    - The selected run targets the committed SHA and exposes a browser-visible URL.
    - `wave-0-adoption.json` records macos-26, ARM64, Xcode 26.6/17F113 and green non-skipped outcomes for all five gates.
    - Any inability to obtain authoritative hosted evidence stops the plan before downstream work.
  </acceptance_criteria>
  <done>Machine-readable evidence ties every adopted mechanism to an actual hosted run and exact commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Observe and approve the hosted Wave 0 adoption run</name>
  <what-built>The executor dispatched the committed `ci.yml` workflow and produced `wave-0-adoption.json` for its macos-26 job.</what-built>
  <how-to-verify>Open the GitHub Actions run URL reported by Task 2. Confirm the job runner is `macos-26`, the run targets the expected commit, and the job summary lists all five gates—ad-hoc signed launch, Full Keyboard Access, scoped file-Keychain twice, native PostgreSQL 17/Phoenix health, and SnapshotTesting mismatch/mutation/noise—as discovered, non-skipped, and passed. Confirm Linux `test` and `compose-smoke` also remain present. Do not approve a local log, skipped canary, or command-only proof.</how-to-verify>
  <resume-signal>Type "approved" with the hosted run URL, or describe the failed/missing canary.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow → hosted runner | Repository code executes on a mutable third-party image. |
| UI test → scoped Keychain | Test credentials cross Security.framework without entering shared login state. |
| Hosted results → adoption decision | Machine output establishes whether downstream architecture is safe to use. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-01 | Spoofing | hosted evidence | high | mitigate | Bind evidence to run URL, commit SHA, exact runner fingerprint, named discovery and non-skip outcomes, plus blocking human observation. |
| T-03.5-02 | Information disclosure | Keychain/xcresult/logs | high | mitigate | Disposable scoped Keychain, synthetic data, bounded manifest, sanitized artifacts, no raw credential/database upload. |
| T-03.5-03 | Tampering | SnapshotTesting package | high | mitigate | Exact official tag/revision, test-target-only link, intentional hosted mismatch and triplet gate. |
| T-03.5-04 | Denial of service | native services/UI canaries | medium | mitigate | Bounded waits, finite fixtures, process traps, no retries. |
</threat_model>

<verification>
- Verify the self-test rejects every missing/skipped/failed canary permutation.
- Inspect the actual hosted run and its machine-readable adoption manifest.
- Confirm both Linux jobs remain present and enabled.
</verification>

<success_criteria>
- All five empirical mechanisms are selected in research and adopted only after authoritative hosted evidence.
- Downstream plans remain blocked until the blocking-human checkpoint approves the exact run.
- No secret, Keychain, database, content identifier, or raw unsanitized log leaves the runner.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ci.yml`: required `macos-26` Wave 0 adoption job plus preserved Linux jobs.
- `playstead-mac/Playstead.xcodeproj/project.pbxproj`: UI-test target and exact test-only package link.
- `playstead-mac/Playstead.xcodeproj/xcshareddata/xcschemes/Playstead.xcscheme`: shared hosted scheme.
- `playstead-mac/Playstead.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`: exact SnapshotTesting resolution.
- `playstead-mac/Playstead/Net/KeychainStore.swift`: optional scoped query support preserving production behavior.
- `playstead-mac/PlaysteadUITests/HostedRunnerCanaryTests.swift`: signing, keyboard, and Keychain canaries.
- `playstead-mac/PlaysteadTests/SnapshotTests/SnapshotHarnessCanaryTests.swift`: mismatch/mutation/noise canaries.
- `playstead-mac/scripts/ci/run-mac-verification.sh`: hosted Wave 0 verifier and evidence manifest.
- `playstead-server/config/mac_ci.exs`: dedicated real-Repo native environment.
- `playstead-mac/scripts/ci/live-server.sh`: native PostgreSQL/Phoenix lifecycle canary.

<output>
Create `.planning/phases/03.5-mac-verification-automation/03.5-01-SUMMARY.md` when done.
</output>
