---
phase: 04-persistent-save-continuity
plan: 22
type: execute
wave: 2
gap_closure: true
depends_on: ["04-21"]
files_modified:
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/Playstead/Readiness/ReadinessSheetView.swift
  - playstead-mac/Playstead/Library/GameRowView.swift
  - playstead-mac/Playstead/Library/LibraryShellView.swift
  - playstead-mac/Playstead/Persistence/SaveStore.swift
  - playstead-mac/Playstead/Persistence/Migrations.swift
  - playstead-mac/Playstead/Saves/SaveOriginNames.swift
  - playstead-mac/Playstead/Saves/SaveHistorySheet.swift
  - playstead-mac/PlaysteadTests/SavesTests/SaveHistorySessionBuilderTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/SaveHistoryContractSnapshotTests.swift
  - playstead-mac/scripts/ci/reachability-allowlist.txt
autonomous: true
requirements: [SAVE-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening 'Review versions…' for a game that has at least one recorded save revision renders that revision as a row in `SaveHistorySheet`; the 'No saves yet.' empty state appears if and only if `SaveStore` holds zero revisions for that game's save line (ROADMAP criterion 2). [edge: SAVE-02/empty]"
    - "Each rendered row reports the revision's real durability (local-only / waiting to copy / on your server), whether it is current on this Mac, whether it was restored here, and whether it is one of two-or-more standing heads — the six-state per-revision vocabulary, not a coarse per-game reading."
    - "`SaveRollup.rollup(for:)` has at least one production call site rendering real `SaveStore` data, closing WINDOWS #47, and the reachability allowlist shrinks by exactly that symbol."
    - "Building the sessions list twice from an unchanged `SaveStore` returns an equal array — the builder reads and never writes. [edge: SAVE-02/idempotency]"
    - statement: "Session grouping and row identity compare full revision-id and session-id strings byte for byte, never a truncated digest prefix and never a case-folded or normalized form, so two revisions can never collapse into one row."
      verification: backstop
    - statement: "The sessions builder runs on the main actor during sheet presentation and reads a committed SQLite snapshot; a capture that lands mid-presentation changes the next presentation, never partially rewrites the one on screen."
      verification: backstop
  artifacts:
    - playstead-mac/Playstead/Saves/SaveOriginNames.swift
    - playstead-mac/PlaysteadTests/SavesTests/SaveHistorySessionBuilderTests.swift
  key_links:
    - "`ReadinessSheetView.saveHistorySessions` (ReadinessSheetView.swift:27) is a closure defaulting to `{ [] }` and is overridden at neither of its two call sites (`GameRowView.swift:130`, `LibraryShellView.swift:150`), so `SaveHistorySheet` always renders its empty state for real users. WINDOWS #43."
    - "`SaveHistorySheet` already renders all six states correctly — `SaveHistoryRowView.label` and `.glyph` (lines ~148-180) branch on `isCurrent`, `isSeparateVersion`, `isRestoredHere` and `durability` today. Nothing about the view needs redesign; it has never been given data."
    - "`SaveRevisionRow` (SaveStore.swift:29-64) carries `durability`, `sessionID`, `originDeviceID`, `recordedAt`, `tier` and `origin`, but has NO restored-here provenance column. That is the one datum the six-state vocabulary needs and the schema does not have."
    - "`AppEnvironment.conflictSides(forAssetSetID:)` (PlaysteadApp.swift:1026) is the established shape for a real-data view-model builder on `AppEnvironment` — same entry/line resolution via `catalogueEntry(assetSetID:)` and `saveLine(for:)`. Follow it rather than inventing a second shape."
    - "`SaveStateModel.isConflicted(headRevisionIDs:)` is documented as the ONLY place conflictedness is computed anywhere in this codebase. `isSeparateVersion` must come from it."
  prohibitions:
    - statement: "Do not collapse durability, position and provenance into a single save-status enum. D-35 states plainly that collapsing these axes is a bug, not a simplification; the row already carries them as independent booleans."
      status: flagged-unverified
    - statement: "Do not author new user-facing save copy. `SaveVocabulary` and `shared/save-vocabulary.json` are the one authoritative source (D-67) and its golden fixture asserts exhaustiveness; reuse existing strings or the existing `{Origin name}` slot."
      status: flagged-unverified
    - statement: "Do not widen `Playstead.Attention.Reason`. It stays a frozen, nine-member, import-recognition-scoped vocabulary (D-66)."
      status: flagged-unverified
    - statement: "Do not add a green colour token or a glyph outside `SaveHistorySheet.Glyph.all`; the save glyph set is asserted disjoint from the card's status ladder (D-39)."
      status: flagged-unverified
    - statement: "Do not compute a save's durability by hashing at render time. Durability is read from the committed SQLite column; the sheet's own doc comment forbids hashing during render."
      status: flagged-unverified
---

<objective>
Give the per-revision save history real data, and give D-36's rollup a production caller.

Purpose: 04-VERIFICATION failed ROADMAP criterion 2 outright. The only surface built to show per-revision state renders its empty state for every real user, because its data source is an unwired default closure. The coarser sibling the user could otherwise have read, `SaveRollup`, has zero production callers. WINDOWS #43 and #47.

Output: a real sessions builder on `AppEnvironment` reading committed `SaveStore` rows, wired at both `ReadinessSheetView` call sites; a persisted restored-here provenance marker so the `restored` state can be told truthfully rather than guessed; and a rollup summary line that gives `SaveRollup` its caller.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-persistent-save-continuity/04-CONTEXT.md
@.planning/phases/04-persistent-save-continuity/04-VERIFICATION.md
@.planning/phases/04-persistent-save-continuity/04-16-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files this plan CREATES (excluded from drift verification — they do not exist yet):

| Symbol / path | Kind |
|---|---|
| `playstead-mac/Playstead/Saves/SaveOriginNames.swift` | new file |
| `SaveOriginNames` | new enum |
| `SaveOriginNames.thisDevice` | new constant |
| `AppEnvironment.saveHistorySessions(forAssetSetID:)` | new method |
| `AppEnvironment.saveRollupSummary(forAssetSetID:)` | new method |
| `SaveHistorySessionBuilder` | new type |
| `SaveHistorySessionBuilder.build(revisions:headIDs:thisDeviceName:)` | new function |
| `SaveStore.markRestoredHere(revisionID:at:)` | new method |
| `SaveRevisionRow.restoredHereAt` | new field |
| `SaveHistorySheet.summary` | new optional property |
| `playstead-mac/PlaysteadTests/SavesTests/SaveHistorySessionBuilderTests.swift` | new test file |

<tasks>

<task id="1" type="auto" tdd="true" title="Persist restored-here provenance, so the sixth state can be told truthfully">
  <name>Task 1: Persist restored-here provenance, so the sixth state can be told truthfully</name>
  <files>playstead-mac/Playstead/Persistence/Migrations.swift, playstead-mac/Playstead/Persistence/SaveStore.swift, playstead-mac/Playstead/Library/GameRowView.swift</files>
  <read_first>
    - `playstead-mac/Playstead/Persistence/Migrations.swift` lines 350-405 — the `save_revision` table and the additive `try? connection.execute("ALTER TABLE save_revision ADD COLUMN …")` pattern that later columns follow. Match it exactly; do not write a table rebuild.
    - `playstead-mac/Playstead/Persistence/SaveStore.swift` — `SaveRevisionRow` (lines 29-64), `insertRevision(_:)` (line 207), `updateDurability(id:durability:)` (line 250) as the shape for a single-column update, and `row(from:)` (line 353) which decodes every column.
    - `playstead-mac/Playstead/Library/GameRowView.swift` lines 441-476 — `buildSaveLaunchPlan`, whose returned `executeSavePlan` closure is the exact and only production place a restore actually happens.
    - `playstead-mac/Playstead/Saves/LaunchSavePlanner.swift` lines 74-79 — the `SavePlan` enum; `.restore(revisionDigest:notice:)` and `.fastForward(revisionDigest:)` are the two cases that place another revision's bytes at the live artifact path.
    - `playstead-mac/Playstead/Saves/SaveStateModel.swift` — `isValidRestoredTransition(from:to:)`, which already states provenance is immutable once set.
  </read_first>
  <behavior>
    - A revision that has never been restored on this Mac reads `restoredHereAt == nil`.
    - After the launch path executes a `.restore` plan for revision digest D, the revision row on that line whose `blobSHA256 == D` reads a non-nil `restoredHereAt`.
    - A `.fastForward` plan marks its revision the same way — both cases place another revision's bytes at the live path.
    - A `.fresh` or `.keep` plan marks nothing.
    - Marking a revision restored twice keeps the FIRST timestamp; provenance is immutable once set.
    - An existing install whose `save_revision` table predates the column still opens, and every existing row reads `restoredHereAt == nil`.
  </behavior>
  <action>
Add a `restored_here_at TEXT` column to `save_revision` using the same additive `try? connection.execute("ALTER TABLE save_revision ADD COLUMN …")` form the `tier` / `origin` / `manifest_digest` / `session_id` / `artifact_set_json` columns already use, placed alongside them so an existing install upgrades in place.

Add `restoredHereAt: String?` to `SaveRevisionRow` as the last stored property with a default of `nil` in its memberwise initialization sites, decode it in `SaveStore.row(from:)`, and persist it in `insertRevision`.

Add `SaveStore.markRestoredHere(revisionID:at:)` following `updateDurability(id:durability:)`'s single-statement shape, with the `SET restored_here_at = ?` guarded by `WHERE id = ? AND restored_here_at IS NULL` so a second call cannot overwrite the first timestamp. That guard is what makes the immutability `SaveStateModel.isValidRestoredTransition` describes true in storage rather than only in a helper.

In `GameRowView.buildSaveLaunchPlan`, inside the returned `executeSavePlan` closure and only AFTER `executor.execute(plan, targetURL:)` returns without throwing, switch on the plan: for `.restore(let digest, _)` and `.fastForward(let digest)`, resolve the revision on this line whose `blobSHA256` equals `digest` via the `SaveStore` instance already constructed there and call `markRestoredHere`. `.fresh` and `.keep` mark nothing. A marking error must be swallowed rather than thrown — provenance bookkeeping must never turn a successful restore into a failed launch.
  </action>
  <acceptance_criteria>
    - `grep -n "restored_here_at" playstead-mac/Playstead/Persistence/Migrations.swift playstead-mac/Playstead/Persistence/SaveStore.swift` returns the ALTER statement, the SELECT decode, the INSERT, and the guarded UPDATE.
    - `grep -n "markRestoredHere" playstead-mac/Playstead/Library/GameRowView.swift` returns at least one non-test call site inside `buildSaveLaunchPlan`.
    - A test opens a store, inserts a revision, calls `markRestoredHere` twice with two different timestamps, and asserts the stored value equals the FIRST timestamp.
    - A test drives `buildSaveLaunchPlan`'s returned `executeSavePlan` for a `.restore` plan and asserts the matching revision reads a non-nil `restoredHereAt`; the same test with a `.keep` plan asserts it stays nil.
    - A test opens a store created without the column and asserts opening succeeds and existing rows read `restoredHereAt == nil`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; xcodebuild build-for-testing -project playstead-mac/Playstead.xcodeproj -scheme Playstead -destination 'platform=macOS,arch=arm64' -derivedDataPath playstead-mac/.build/gsd-gate/DerivedData -quiet CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER=</automated>
    <fails_when>non-zero exit, or any line containing "error:" in the xcodebuild output</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; xcodebuild test-without-building -project playstead-mac/Playstead.xcodeproj -scheme Playstead -testPlan Unit -destination 'platform=macOS,arch=arm64' -derivedDataPath playstead-mac/.build/gsd-gate/DerivedData -quiet CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER= -only-testing:PlaysteadTests/SaveStoreTests -only-testing:PlaysteadTests/PlayPathSaveWiringTests</automated>
    <fails_when>non-zero exit, or the xcodebuild summary reports "Executed 0 tests", or any line containing "failed" against a test identifier</fails_when>
  </verify>
  <done>A revision restored by the real launch path carries a durable, immutable restored-here timestamp, and an existing install upgrades without losing a row.</done>
</task>

<task id="2" type="auto" tdd="true" title="A real sessions builder, wired at both call sites">
  <name>Task 2: A real sessions builder, wired at both call sites</name>
  <files>playstead-mac/Playstead/Saves/SaveOriginNames.swift, playstead-mac/Playstead/Saves/SaveHistorySheet.swift, playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/Readiness/ReadinessSheetView.swift, playstead-mac/Playstead/Library/GameRowView.swift, playstead-mac/Playstead/Library/LibraryShellView.swift, playstead-mac/PlaysteadTests/SavesTests/SaveHistorySessionBuilderTests.swift</files>
  <read_first>
    - `playstead-mac/Playstead/Saves/SaveHistorySheet.swift` lines 1-40 — `SaveHistoryRevisionRow` and `SaveHistorySession`, the exact structs to produce, and lines 145-185 (`SaveHistoryRowView.label` / `.glyph`), which prove the six states are already rendered and only need data.
    - `playstead-mac/Playstead/Readiness/ReadinessSheetView.swift` lines 12-40 and 190-200 — the `saveHistorySessions` default closure (line 27) and the private `thisDeviceOrigin = "This Mac"` constant (line 197).
    - `playstead-mac/Playstead/Library/GameRowView.swift` lines 129-147 — call site 1.
    - `playstead-mac/Playstead/Library/LibraryShellView.swift` lines 149-160 — call site 2.
    - `playstead-mac/Playstead/App/PlaysteadApp.swift` lines 1026-1050 — `conflictSides(forAssetSetID:)` and `relativeSaveDescription(for:)`, the established real-data builder shape and the existing relative-time formatter to reuse verbatim.
    - `playstead-mac/Playstead/Persistence/SaveStore.swift` — `fetchRevisions(saveLineID:)` (line 324) and `fetchHeads(saveLineID:)` (line 298), the two reads this builder needs.
    - `playstead-mac/Playstead/Saves/SaveStateModel.swift` — `isConflicted(headRevisionIDs:)`, the only sanctioned source of conflictedness.
  </read_first>
  <behavior>
    - A line with zero revisions produces an empty array, so the sheet's empty state means what it says.
    - A line with one promoted revision produces one session containing one row.
    - Two revisions sharing a `sessionID` produce ONE session with two rows, ordered oldest first by `recordedAt`.
    - A revision with a nil `sessionID` gets its own single-row session keyed on its revision id, never merged with other nil-session revisions.
    - A row's `durability` equals the revision's stored `durability` column value, for each of local-only, queued and uploaded.
    - When the line has two standing heads, both head rows report `isSeparateVersion == true` and non-head rows report false.
    - When the line has exactly one head, no row reports `isSeparateVersion`.
    - A revision with a non-nil `restoredHereAt` reports `isRestoredHere == true`.
    - Exactly one row reports `isCurrent == true`: the newest by `recordedAt` among revisions this Mac either promoted locally or restored here. A line whose only revisions came from another device and were never restored here has no current row.
    - Calling the builder twice over the same rows returns an equal array.
  </behavior>
  <action>
Create `playstead-mac/Playstead/Saves/SaveOriginNames.swift` with `enum SaveOriginNames { static let thisDevice = "This Mac" }`, and change `ReadinessSheetView`'s `private static let thisDeviceOrigin` to reference it, so the origin name for this device has exactly one definition. This is an origin-name slot fill, not a new copy string — do not add a key to `SaveVocabulary` or `shared/save-vocabulary.json`.

Add a pure `SaveHistorySessionBuilder` type (a `enum` with static functions, no stored state, no `SaveStore` dependency) exposing `build(revisions:headIDs:thisDeviceName:) -> [SaveHistorySession]`. Being pure and store-free is what lets every behavior above be tested without SQLite:
- Group by `sessionID`; a nil `sessionID` forms a singleton group keyed on the revision's own `id`.
- Order rows within a group by `recordedAt` ascending, and order groups by their earliest `recordedAt` descending, so the most recent session is first.
- `deviceName` for a group is the first row's `originDeviceID`, or `thisDeviceName` when it is nil.
- `durability` maps the stored string through `SaveDurability(rawValue:)`, defaulting to `.localOnly` for an unrecognized value.
- `isSeparateVersion` is `SaveStateModel.isConflicted(headRevisionIDs: headIDs) && headIDs.contains(revision.id)`.
- `isRestoredHere` is `revision.restoredHereAt != nil`.
- `isCurrent` is true for exactly the single revision with the greatest `recordedAt` among those where `originDeviceID == nil` (captured on this Mac) or `restoredHereAt != nil` — the last revision whose bytes this Mac actually placed at, or read from, the live save path. Zero such revisions means no current row, which is the truthful reading for a line whose history all came from elsewhere.
- `relativeTime` reuses the same `ISO8601DateFormatter` + `RelativeDateTimeFormatter` pair `AppEnvironment.relativeSaveDescription(for:)` already uses; extract that private helper to a shared location rather than writing a second formatter.

Add `AppEnvironment.saveHistorySessions(forAssetSetID:) -> [SaveHistorySession]` that resolves the entry and line exactly as `conflictSides(forAssetSetID:)` does (`catalogueEntry(assetSetID:)` then `saveLine(for:)`), returns `[]` when either is nil, reads `saveStore.fetchRevisions(saveLineID:)` and `saveStore.fetchHeads(saveLineID:)`, and returns `SaveHistorySessionBuilder.build(...)` with `thisDeviceName: SaveOriginNames.thisDevice`.

Override the closure at BOTH `ReadinessSheetView` call sites — `GameRowView.swift:130` and `LibraryShellView.swift:150` — passing `saveHistorySessions: { environment.saveHistorySessions(forAssetSetID: entry.id) }`. Leaving either one unwired reproduces exactly the defect this plan exists to close.
  </action>
  <acceptance_criteria>
    - `grep -rn "saveHistorySessions:" playstead-mac/Playstead/Library/GameRowView.swift playstead-mac/Playstead/Library/LibraryShellView.swift` returns one match in each file.
    - `grep -rn "SaveHistorySessionBuilder" playstead-mac/Playstead | grep -v Tests` returns at least the type definition and its `AppEnvironment` call site.
    - `PlaysteadTests/SavesTests/SaveHistorySessionBuilderTests.swift` covers every bullet in this task's `<behavior>` block, one test per bullet, with at least 10 tests.
    - A test asserts an `AppEnvironment` whose `SaveStore` holds one revision for a game returns exactly one session with one row from `saveHistorySessions(forAssetSetID:)`, and that a game with no line returns an empty array.
    - `grep -rn "SaveOriginNames.thisDevice" playstead-mac/Playstead/Readiness/ReadinessSheetView.swift` returns a match, and `grep -c "SaveOriginNames" playstead-mac/Playstead/Saves/SaveOriginNames.swift` is at least 1 — the device origin name has exactly one definition and `ReadinessSheetView` reads it.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; xcodebuild build-for-testing -project playstead-mac/Playstead.xcodeproj -scheme Playstead -destination 'platform=macOS,arch=arm64' -derivedDataPath playstead-mac/.build/gsd-gate/DerivedData -quiet CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER=</automated>
    <fails_when>non-zero exit, or any line containing "error:" in the xcodebuild output</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; xcodebuild test-without-building -project playstead-mac/Playstead.xcodeproj -scheme Playstead -testPlan Unit -destination 'platform=macOS,arch=arm64' -derivedDataPath playstead-mac/.build/gsd-gate/DerivedData -quiet CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER= -only-testing:PlaysteadTests/SaveHistorySessionBuilderTests</automated>
    <fails_when>non-zero exit, or the xcodebuild summary reports "Executed 0 tests" or fewer than 10 tests executed, or any line containing "failed" against a test identifier</fails_when>
  </verify>
  <done>A user opening "Review versions…" for a game with real history sees that history, with each row carrying its own durability, position, provenance and lineage reading.</done>
</task>

<task id="3" type="auto" title="Give SaveRollup a production caller and shrink the allowlist">
  <name>Task 3: Give SaveRollup a production caller and shrink the allowlist</name>
  <files>playstead-mac/Playstead/Saves/SaveHistorySheet.swift, playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/Readiness/ReadinessSheetView.swift, playstead-mac/scripts/ci/reachability-allowlist.txt, playstead-mac/PlaysteadTests/SnapshotTests/SaveHistoryContractSnapshotTests.swift</files>
  <read_first>
    - `playstead-mac/Playstead/Saves/SaveRollup.swift` in full — `SaveRollupInput`'s four fields and `rollup(for:showFootnote:)`'s first-match-wins ordering, all of which already exist and must not be reimplemented.
    - `playstead-mac/Playstead/Saves/SaveHistorySheet.swift` lines 76-125 — the sheet body and its `emptyState`, where a game-level summary line belongs.
    - `playstead-mac/PlaysteadTests/SnapshotTests/SaveHistoryContractSnapshotTests.swift` — the existing fixtures that must keep rendering unchanged.
    - `playstead-mac/scripts/ci/reachability-allowlist.txt` — the `SaveRollup` entry and the header's instruction that a future plan should shrink this file by fixing the wiring, not by deleting lines.
  </read_first>
  <action>
The verification report offered two dispositions for WINDOWS #47: wire `SaveRollup.rollup(for:)` or delete it. Wire it. Its strings are the decided D-36 game-level vocabulary, the history sheet has no game-level line at all today, and the readiness Save row's `SaveReadinessCase` answers a different question (is this game ready to play) than the rollup does (where does this game's newest progress live).

Add an OPTIONAL `var summary: SaveRollupResult? = nil` to `SaveHistorySheet`, rendered above the session list as header, muted second line and footnote using the sheet's existing `psLabelEmphasized` / `psLabel` fonts and `DesignTokens.textPrimary` / `.textMuted` tokens — no new colour literal and no new glyph. Because it defaults to nil, every existing `SaveHistoryContractSnapshotTests` fixture renders byte-identically and no snapshot rebaseline is forced; add ONE new fixture that supplies a summary.

Add `AppEnvironment.saveRollupSummary(forAssetSetID:) -> SaveRollupResult?`, resolving entry and line the same way `saveHistorySessions` does, returning nil when there is no line, and otherwise calling `SaveRollup.rollup(for:)` with a `SaveRollupInput` built from committed rows: `newestDurability` from the revision with the greatest `recordedAt`; `hasDivergentHeads` from `SaveStateModel.isConflicted(headRevisionIDs:)` over `fetchHeads`; `earlierLocalOnlyCount` as the count of revisions strictly older than the newest whose `durability` is the local-only raw value; and `title` from the catalogue entry's display title.

Pass it through `ReadinessSheetView` to `SaveHistorySheet` as a second closure alongside `saveHistorySessions`, overridden at the same two call sites.

Then remove ONLY the `SaveRollup` line from `reachability-allowlist.txt`. Every other entry stays — they are real, still out of scope, and still disclosed.
  </action>
  <acceptance_criteria>
    - `grep -rn "SaveRollup.rollup" playstead-mac/Playstead | grep -v Tests` returns at least one non-test production call site.
    - `git diff --numstat playstead-mac/scripts/ci/reachability-allowlist.txt` reports exactly `0` added and `1` deleted.
    - `cd playstead-mac && scripts/ci/reachability-sweep.sh --strict` exits 0 with the shortened allowlist, which is only possible if the symbol is genuinely reached from production.
    - A test asserts `saveRollupSummary(forAssetSetID:)` returns the two-versions header for a diverged line and the on-server header for a line whose newest revision is uploaded.
    - Every pre-existing `SaveHistoryContractSnapshotTests` fixture still passes with no recorded-snapshot change; the one new fixture supplying a summary is the only addition.
  </acceptance_criteria>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/reachability-sweep.sh --strict</automated>
    <fails_when>non-zero exit, or any finding line printed by the sweep</fails_when>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/SaveHistoryContractSnapshotTests</automated>
    <fails_when>non-zero exit, or the run reports 0 executed tests, or a snapshot mismatch is reported for a pre-existing fixture</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; xcodebuild test-without-building -project playstead-mac/Playstead.xcodeproj -scheme Playstead -testPlan Unit -destination 'platform=macOS,arch=arm64' -derivedDataPath playstead-mac/.build/gsd-gate/DerivedData -quiet CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER=</automated>
    <fails_when>non-zero exit, or the summary reports fewer than 545 tests executed, or any line containing "failed" against a test identifier other than the two known-flaky ones named in this plan's verification block</fails_when>
  </verify>
  <done>D-36's rollup is reachable from the shipped app, the allowlist is one line shorter, and the strict sweep proves it rather than an allowlist entry excusing it.</done>
</task>

</tasks>

<threat_model>
Builds on `.planning/phases/04-persistent-save-continuity/04-SECURITY.md`; only the NEW surface (real save history rendered from committed local rows, and a persisted restore-provenance column) is modelled here. ASVS L1, block on high. No package-manager install task exists in this plan, so no package-legitimacy checkpoint applies.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `SaveStore` (SQLite) -> SwiftUI render | Server-supplied `origin_device_id` strings, written by a journal apply, become rendered on-screen text |
| launch path -> `save_revision` row | A restore writes durable provenance that later drives what the user is told about where their progress came from |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22-01 | Spoofing | `deviceName` rendered from `originDeviceID` | medium | mitigate | The session header renders `originDeviceID` verbatim, so a server-supplied device name could impersonate this Mac. `SaveOriginNames.thisDevice` is used ONLY when `originDeviceID` is nil (a genuinely local capture), never matched by string comparison against a remote name — so a remote row claiming the name cannot change any `isCurrent` or local-capture determination, only its own displayed label. |
| T-04-22-02 | Tampering | `markRestoredHere` overwriting provenance | medium | mitigate | The UPDATE carries `AND restored_here_at IS NULL`, making the first write the only write. A test asserts the second call is a no-op. |
| T-04-22-03 | Information disclosure | history sheet showing device names and save times | low | accept | The data is the owner's own, rendered locally on the owner's Mac. Showing it is the criterion. |
| T-04-22-04 | Denial of service | `fetchRevisions` over an unbounded history at render | low | accept | Bounded by the per-user revision backstop already surfaced as attention (D-28); grouping is a single linear pass with no per-row query. |
| T-04-22-05 | Repudiation | no audit of the restored-here marking | low | accept | The revision row itself is the record, and restore provenance is immutable once set, which is stronger evidence than a mutable audit line. |
</threat_model>

<verification>
- Swift Unit suite passes at or above 545 tests with 0 failures.
- Rendering suite passes at or above 46 executed / 2 skipped.
- `cd playstead-mac && scripts/ci/reachability-sweep.sh --strict` exits 0 with the allowlist one line shorter and nothing else changed.
- Falsification check: revert the `saveHistorySessions:` override at `LibraryShellView.swift` only, and confirm a test goes red. If only the `GameRowView` call site is covered, the test is not asserting reachability from both surfaces — this exact half-wiring is what the verification report found.
- Known-flaky, do not chase: `AppPathsBackupExclusionTests.testExistingInstallWithStaleRootFlagIsRepairedAtInit` and `SetupWizardJourneyTest` fail only under full-suite load and pass in isolation.
</verification>

<success_criteria>
ROADMAP criterion 2 is met: a real user can see, per revision, whether it is local-only, queued, uploaded, current, restored, or a separate version — not a coarse per-game reading and not an empty state.
</success_criteria>

<output>
Create `.planning/phases/04-persistent-save-continuity/04-22-SUMMARY.md` when done.
</output>
