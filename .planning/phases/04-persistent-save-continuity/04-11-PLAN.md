---
phase: 04-persistent-save-continuity
plan: 11
type: execute
wave: 6
depends_on: ["04-09", "04-10"]
files_modified:
  - playstead-mac/Playstead/Saves/SaveAttentionSource.swift
  - playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift
  - playstead-mac/Playstead/Saves/SaveConflictResolver.swift
  - playstead-mac/Playstead/Saves/SaveIntent.swift
  - playstead-mac/Playstead/Library/StatusSlotView.swift
  - playstead-mac/Playstead/Sync/Outbox.swift
  - playstead-mac/PlaysteadTests/SavesTests/SaveConflictResolverTests.swift
  - playstead-mac/PlaysteadTests/SnapshotTests/ConflictComparisonContractSnapshotTests.swift
  - playstead-mac/PlaysteadUITests/ConflictResolutionInteractionTests.swift
autonomous: true
requirements: [SAVE-02, SAVE-04]

estimate:
  tokens: 92000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Divergence reaches the library card only through the existing rank-1 `needsAttention` rung, fed by a union of sources that does not include `Playstead.Attention.Reason` (D-38, D-66)."
    - "The comparison sheet is reachable from three entry points — the attention inbox, game detail, and (server-side) the console — and is never a modal, a notification, or a launch prompt (D-53)."
    - "Choosing a side performs a local mutation plus exactly one idempotent outbox entry, and the whole flow works with no server reachable (D-53)."
    - "Both sides' 32 KB artifacts are eagerly fetched at divergence detection, so comparison and choosing work offline (D-53)."
    - "Choosing appends a resolution rather than moving a head; the non-chosen version stays on screen with a live \"Continue from this one\" control, permanently (D-48, D-49)."
    - "There is no Undo affordance, no confirmation dialog before choosing, no recommended badge, no pre-selected side, and no bulk always-use-this-Mac preference — each absence asserted by test (D-49, D-51, D-55)."
    - "The only bulk action is \"Keep both in all {N}\" (D-55)."
    - "\"Keep both\" is a peer of the choice buttons, leaves both heads standing, and is never re-raised for that fork (D-52)."
    - "Exactly four facts are shown per side — origin, last saved, play time since the split, saves since the split — and file size, byte-diff, similarity score, revision ordinal, and parent pointer are never shown (D-50)."
    - "Every affordance works without gesture, hover, or pointer; each side is one accessibility element carrying one complete comparison sentence; no colour distinguishes the sides (D-55)."
    - "Resolving the same fork twice from the Mac enqueues no second outbox entry and produces no second resolution revision."
    - "A fork with three or more heads renders the N-variant copy and resolves in one act."
    - "A fork whose game is not downloaded renders the locked not-downloaded string and still permits choosing and exporting."
    - statement: "Choosing on this Mac while another device resolves the same fork offline leaves both resolutions retained and the local state converged once both arrive."
      verification: backstop
    - statement: "A divergence sheet opened while a background sync is applying a new revision for the same line renders one consistent head set rather than a mix."
      verification: backstop
  artifacts:
    - playstead-mac/Playstead/Saves/SaveAttentionSource.swift
    - playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift
    - playstead-mac/Playstead/Saves/SaveConflictResolver.swift
    - playstead-mac/PlaysteadTests/SavesTests/SaveConflictResolverTests.swift
    - playstead-mac/PlaysteadUITests/ConflictResolutionInteractionTests.swift
  key_links:
    - "The resolver calls the same server context functions the console calls (plan 04-10), through the outbox, so the two clients cannot diverge in behaviour."
    - "The card's rank-1 rung is a presentation rank fed by a union; the frozen `Attention.Reason` is untouched, and the union is the same shape the server-side inbox uses."
    - "The eager 32 KB fetch at detection is what makes the whole flow work with no server — the same reasoning as the launch-path prefetch in plan 04-04."
  prohibitions:
    - statement: "No confirmation dialog may precede \"Continue from this one\" (D-51)."
      status: flagged-unverified
      verification: backstop
    - statement: "No Undo toast or control may be rendered after a resolution; a permanent, self-evident history replaces it (D-49)."
      status: flagged-unverified
      verification: backstop
    - statement: "No side may be recommended, highlighted, pre-selected, or distinguished by colour (D-51, D-55)."
      status: flagged-unverified
      verification: backstop
    - statement: "No bulk \"always use this Mac\" preference may exist; it is a rejected feature, not a deferred one (D-55)."
      status: flagged-unverified
      verification: backstop
    - statement: "The word \"conflict\" must never reach player-facing copy on any of these surfaces (D-54)."
      status: flagged-unverified
      verification: backstop
    - statement: "Divergence must never be surfaced as a modal, a notification, or a launch prompt (D-53)."
      status: flagged-unverified
      verification: backstop
---

<objective>
Ship "Two versions of your progress" on the Mac: the attention item, the comparison sheet, and append-only resolution that works with no server.

Purpose: SAVE-04's promise is that when two devices save from the same base, both remain available with device, time, and play context, and the user can inspect, choose, export, and resolve without silent last-write-wins. Playing on two Macs is the product working — the copy must never imply the user erred, and no ranking heuristic may stand in for the user's judgement, because any such heuristic is last-write-wins wearing a UI costume.

Output: A Mac-side saves attention source unioned into the card's rank-1 rung, the comparison sheet with its four facts per side, and a resolver that mutates locally and enqueues exactly one idempotent outbox entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistent-save-continuity/04-CONTEXT.md
@.planning/phases/04-persistent-save-continuity/04-PATTERNS.md
@.planning/phases/04-persistent-save-continuity/discussion-research/G-conflict-resolution-ux.md
@.planning/phases/04-persistent-save-continuity/04-09-SUMMARY.md
@.planning/phases/04-persistent-save-continuity/04-10-SUMMARY.md
@shared/save-vocabulary.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A Mac saves attention source feeding the existing rank-1 rung</name>
  <files>playstead-mac/Playstead/Saves/SaveAttentionSource.swift, playstead-mac/Playstead/Library/StatusSlotView.swift, playstead-mac/PlaysteadTests/SavesTests/SaveConflictResolverTests.swift</files>
  <read_first>
    - `playstead-mac/Playstead/Library/StatusSlotView.swift` — the single status slot, its rank ordering, and the shipped rank-1 `needsAttention` rung
    - `playstead-mac/Playstead/Library/GameCardView.swift` — how the status slot is fed and the card's accessible name is composed
    - `playstead-mac/Playstead/Saves/SaveStateModel.swift` — the set-level `conflicted` derivation from plan 04-09
    - `playstead-mac/Playstead/Persistence/SaveStore.swift` — the local head set and revision metadata
    - `shared/save-vocabulary.json` — the divergence attention-item strings
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-38, D-53, D-66, and the "Divergence — the attention item" copy table
  </read_first>
  <behavior>
    - A line whose local head set has more than one member and whose fork is unacknowledged produces one attention item.
    - The item's title, body, and primary action are the locked strings, with the N-variant used when the head count exceeds two.
    - The item raises the card's existing rank-1 `needsAttention` rung with no new case, glyph, colour token, or copy.
    - A blocked capture also raises through the same source.
    - Two games with divergence produce the grouped header "Two versions of your progress in {N} games" and the grouped secondary action "Keep both in all {N}".
    - Acknowledging a fork clears its item and it is not re-raised.
    - The library card's shipped snapshot references still match — no rebaseline.
    - No divergence item is presented as a modal, a notification, or a launch prompt.
  </behavior>
  <action>
Add `SaveAttentionSource.swift` producing the Mac's saves attention items from local state: divergence (a line with more than one unacknowledged head) and blocked capture (from plan 04-06). Its output is unioned into the attention inbox and into the card's rank-1 `needsAttention` rung. The card's attention rung is a presentation rank fed by a union of sources — this source is not, and must not become, a member of the server's frozen `Playstead.Attention.Reason`.

Use the locked attention-item copy verbatim from the vocabulary: item title "Two versions of your progress in {title}" and its N-variant "{N} versions of your progress in {title}"; item body "You played {title} on {Origin A} and {Origin B} without them syncing in between. Both versions are saved. Nothing has been overwritten." and its N-variant; primary action "Compare versions"; grouped header "Two versions of your progress in {N} games"; grouped secondary action "Keep both in all {N}". The card's own status-slot string stays the shipped "Needs attention" and its accessible name stays "{title} needs your attention." — unchanged.

Add no new status case, glyph, colour token, or copy to the card, so no snapshot baseline needs refreshing. Never present divergence as a modal, a notification, or a launch prompt — the rank-1 rung makes it un-missable without interrupting.
  </action>
  <verify>
    <automated>cd playstead-mac && xcodebuild test -project Playstead.xcodeproj -scheme Playstead -testPlan Unit -destination 'platform=macOS' CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER= -only-testing:PlaysteadTests/SaveConflictResolverTests</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, or "Executed 0 tests" in the test summary</fails_when>
    <automated>cd playstead-mac && scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/LibraryContractSnapshotTests</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, or "Executed 0 tests" — a failure here means the card changed and a rebaseline was required, which D-38 forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - The unit run reports at least 8 tests executed and 0 failed for the attention-source behaviours.
    - `LibraryContractSnapshotTests` passes and `git status --porcelain` shows no modified library-card reference image.
    - `git diff playstead-mac/Playstead/Design/StatusToken.swift` is empty.
    - A test asserts the grouped header and grouped secondary action strings for two divergent games.
    - `grep -rniE 'NSAlert|\.alert\(|UNNotification|sheet\(isPresented.*conflict' playstead-mac/Playstead/Saves/SaveAttentionSource.swift` returns nothing.
  </acceptance_criteria>
  <done>Divergence is un-missable and never interrupting, and the card is byte-identical to what shipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The comparison sheet — four facts, no ranking, full keyboard parity</name>
  <files>playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift, playstead-mac/PlaysteadTests/SnapshotTests/ConflictComparisonContractSnapshotTests.swift, playstead-mac/PlaysteadUITests/ConflictResolutionInteractionTests.swift</files>
  <read_first>
    - `playstead-mac/Playstead/Saves/SaveHistorySheet.swift` — the sheet presentation, focus containment, and dismissal conventions established in plan 04-09
    - `playstead-mac/Playstead/Design/DesignTokens.swift`, `FocusRing.swift`, `MotionPreference.swift`, `AccessibilityIdentifiers.swift` — the shipped tokens, focus ring, reduced-motion rules, and identifier conventions
    - `playstead-mac/PlaysteadUITests/CurationInteractionTests.swift` — the shipped keyboard-parity test shape, and the lesson that a gesture-only affordance fails the parity contract
    - `playstead-mac/docs/ACCESSIBILITY.md` — the project's accessibility contract
    - `shared/save-vocabulary.json` — the comparison-sheet and result-state strings
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-50, D-51, D-52, D-53, D-54, D-55, and the "Divergence — the comparison sheet" and "result states" copy tables
  </read_first>
  <behavior>
    - The sheet renders exactly four facts per side: origin, last saved, play time since the split, saves since the split.
    - File size, byte-diff, similarity score, revision ordinal, and parent pointer are absent from the rendered output; the digest appears only under "Details".
    - Neither side is highlighted, recommended, pre-selected, or colour-distinguished.
    - "Keep both" renders as a peer of the choice buttons, not as a secondary or dismissal action.
    - The N-variant subtitle and the "Keep them all" label render for three or more heads.
    - The "no recorded play here since these split" and the singular "in 1 save" variants render for their cases.
    - The clock caveat renders for a side whose reported time contradicts arrival order.
    - The not-downloaded string renders and choosing and exporting remain available.
    - After choosing, the result string renders and the non-chosen side remains on screen with a live "Continue from this one" control.
    - No Undo control renders after any action.
    - Every action is reachable and operable by keyboard alone, with visible focus.
    - Each side is exposed as one accessibility element carrying one complete comparison sentence.
    - Reduced-motion preference substitutes the shipped non-animated presentation.
  </behavior>
  <action>
Add `ConflictComparisonSheet.swift` using the locked comparison-sheet copy verbatim from the vocabulary. Reach it from the attention inbox and from game detail; it is never a modal over the launch path, never a notification, and never a launch prompt.

Show exactly the four facts per side. Play-time-since-split is the only available fact that correlates with value rather than ordering — it is the reason this sheet is worth building rather than showing two timestamps. Never show file size (it is the constant 32,768 for every side and is a documented mistake elsewhere), byte-diff, similarity score, revision ordinal, or parent pointer; the digest lives under "Details" for support only.

Recommend nothing. No side is highlighted, pre-selected, or colour-distinguished, and there is no confirmation dialog before choosing — a confirmation before a non-destructive, fully reversible act is exactly what trains click-through. There is no Undo control: the non-chosen version stays on screen with a live "Continue from this one" button, permanently, because a permanent self-evident history beats a transient toast and an Undo control would imply harm was done.

"Keep both" is a peer of the choice buttons and is never re-raised for that fork. Unresolved-forever is a supported, first-class outcome.

Zero gesture-only, hover-only, or pointer-only affordances, and zero colour distinguishing the sides. Each side is one accessibility element carrying one complete comparison sentence. The only bulk action anywhere is "Keep both in all {N}".

Add `ConflictComparisonContractSnapshotTests.swift` to the Rendering plan covering the two-side, N-way, not-downloaded, post-choice, and reduced-motion renderings, and `ConflictResolutionInteractionTests.swift` to the UI plan covering keyboard-only traversal and activation of every action, plus the absence assertions.
  </action>
  <verify>
    <automated>cd playstead-mac && scripts/ci/run-mac-verification.sh --layers rendering --only-testing PlaysteadTests/ConflictComparisonContractSnapshotTests</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, "Executed 0 tests" in the test summary, or a missing-reference-image error from the snapshot harness</fails_when>
    <automated>cd playstead-mac && scripts/ci/run-mac-verification.sh --layers ui --only-testing PlaysteadUITests/ConflictResolutionInteractionTests</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, or "Executed 0 tests" in the test summary</fails_when>
  </verify>
  <acceptance_criteria>
    - The rendering run reports at least 5 tests executed and 0 failed; the UI run reports at least 6 tests executed and 0 failed.
    - A UI test drives choosing entirely by keyboard and asserts the mutation occurred with no intervening confirmation.
    - A test asserts the rendered output contains no Undo control, no recommended badge, and no pre-selected side.
    - `grep -rniE 'undo|recommended|suggested' playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift` returns nothing.
    - `grep -rniE '#[0-9a-f]{6}|Color\(red:|\.green' playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift` returns nothing.
    - A test asserts each side is a single accessibility element with a complete sentence label.
  </acceptance_criteria>
  <done>A user can tell the two versions apart by what they played, and nothing on the screen decides for them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append-only resolution that works with no server</name>
  <files>playstead-mac/Playstead/Saves/SaveConflictResolver.swift, playstead-mac/Playstead/Saves/SaveIntent.swift, playstead-mac/Playstead/Sync/Outbox.swift, playstead-mac/PlaysteadTests/SavesTests/SaveConflictResolverTests.swift</files>
  <read_first>
    - `playstead-mac/Playstead/Sync/Outbox.swift` lines 40-70 — `enqueue`'s optimistic-local-write-plus-durable-row-in-one-transaction moduledoc and implementation
    - `playstead-mac/Playstead/Sync/CurationIntent.swift` — the shipped intent type shape to mirror for a save intent
    - `playstead-mac/Playstead/Sync/OutboxWorker.swift` — in-order drain, backoff, and attempt cap
    - `playstead-mac/Playstead/Persistence/SaveStore.swift` — the local revision and head storage
    - `playstead-mac/Playstead/Saves/SaveUploadLane.swift` — the save lane's ordering guarantee from plan 04-04
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-48, D-49, D-52, D-53
  </read_first>
  <behavior>
    - Choosing a side writes the local resolution and enqueues exactly one outbox entry, in one transaction.
    - The whole flow completes with the server unreachable, and the entry drains later.
    - Both sides' artifacts are fetched eagerly at divergence detection, so the sheet renders and choosing works offline.
    - Choosing appends a resolution locally; both original heads remain present afterwards.
    - Resolving the same fork twice enqueues no second entry and produces no second resolution.
    - Switching back to the other side later appends another resolution and renders the "After switching back" string.
    - "Keep both" enqueues one acknowledgement entry and leaves both heads standing.
    - An acknowledged fork is never re-raised as an attention item.
    - A crash between the local write and the outbox row cannot lose the entry — the two are one transaction.
    - Nothing in the resolver deletes a revision or moves a head pointer.
  </behavior>
  <action>
Add `SaveIntent.swift` following `CurationIntent.swift`'s shape, with cases for choosing a side and for acknowledging a fork, and add `SaveConflictResolver.swift` performing the local mutation and the enqueue in one transaction, mirroring `Outbox.enqueue`'s documented discipline so a crash between the local write and the network send cannot lose the entry.

The whole flow works with no server: local metadata plus an eager 32 KB fetch of both sides at divergence detection means comparing and choosing are local operations, and the outbox entry is the only thing that needs the network. Make the entry idempotent so a retry, a replay, or a second tap produces one server-side resolution.

Choosing appends a resolution locally exactly as the server does — both original heads remain, nothing is deleted, and no head pointer moves. Switching back later appends another resolution and renders the "Continuing from {Origin} again. The {Other origin} version is still here." string. Keep-both appends an acknowledgement, leaves both heads standing, and marks the fork never-re-raise.

Render the locked result strings after each action: "Continuing from {Origin}. The {Other origin} version is still here — you can switch to it anytime." and its N-variant, and "Keeping both. This Mac plays the {Origin} version. Neither version will be removed."

Cover the ten behaviours in `SaveConflictResolverTests.swift`.
  </action>
  <verify>
    <automated>cd playstead-mac && xcodebuild test -project Playstead.xcodeproj -scheme Playstead -testPlan Unit -destination 'platform=macOS' CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER= -only-testing:PlaysteadTests/SaveConflictResolverTests</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, or "Executed 0 tests" in the test summary</fails_when>
  </verify>
  <acceptance_criteria>
    - The `-only-testing:PlaysteadTests/SaveConflictResolverTests` run reports at least 18 tests executed in total across this plan's three tasks and 0 failed.
    - A test completes a full choose flow with the network transport failing every request and asserts the local state is correct and one entry is queued.
    - A test asserts resolving the same fork twice yields one outbox entry.
    - A test asserts both original heads are still present after a resolution.
    - `grep -rniE 'func delete|removeRevision|setHead' playstead-mac/Playstead/Saves/SaveConflictResolver.swift` returns nothing.
  </acceptance_criteria>
  <done>A user can decide on a plane, and the decision is durable, idempotent, and reversible forever.</done>
  <reversibility rating="reversible">The local resolver and intent shapes are client-internal; the durable representation is the server's append-only resolution from plan 04-05.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user decision → durable lineage | Choosing mutates what a device will continue from |
| local mutation ↔ outbox entry | A crash between them could lose the decision |
| divergence detection → eager fetch | Both sides' bytes are downloaded before the user asks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11-01 | Repudiation | a decision lost between the local write and the send | high | mitigate | The local mutation and the durable outbox row are written in one transaction, mirroring the shipped `Outbox.enqueue` discipline; a crash test covers the window |
| T-04-11-02 | Tampering | a resolution silently discarding the other version | critical | mitigate | Resolution appends and never deletes or moves a head; both original heads are asserted present after every resolution, and the non-chosen side stays on screen with a live switch-back control |
| T-04-11-03 | Repudiation | duplicate resolutions from retries or double taps | medium | mitigate | The outbox entry is idempotent; a test asserts a second resolution of the same fork enqueues nothing new |
| T-04-11-04 | Spoofing | a ranked or pre-selected side standing in for the user's judgement | high | mitigate | No recommendation, highlight, pre-selection, or colour distinction exists; their absence is asserted by source scan and by rendering test |
| T-04-11-05 | Information Disclosure | eager fetch of both sides on a metered connection | low | accept | Accepted: the artifacts are 32 KB, and fetching them at detection is what makes the whole flow work offline |
| T-04-11-06 | Denial of Service | divergence interrupting play | medium | mitigate | Divergence is never a modal, notification, or launch prompt; the rank-1 rung surfaces it without interrupting, and playing a diverged slot is never blocked |
| T-04-11-07 | Information Disclosure | internal nouns in divergence copy | medium | mitigate | The copy-contract test from plan 04-09 scans divergence strings for the banned words, including "conflict", which survives only as an internal state name |
| T-04-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; RESEARCH.md's Package Legitimacy Audit records that this phase installs no external packages |
</threat_model>

<verification>
- The unit, rendering, and UI runs above all pass with non-zero executed test counts.
- `LibraryContractSnapshotTests` passes with no reference-image changes.
- `grep -rniE 'always use this mac|prefer this mac' playstead-mac/Playstead/` returns nothing.
</verification>

<success_criteria>
- Two versions of a save both survive, are comparable by what the user actually played, and are resolvable offline.
- No affordance on any divergence surface decides for the user or implies they erred.
- The library card is unchanged from what shipped.
</success_criteria>

## Artifacts this phase produces

New symbols and paths created by this plan:

- Swift types `SaveAttentionSource`, `ConflictComparisonSheet`, `SaveConflictResolver`, `SaveIntent` (choose-side and acknowledge-fork cases)
- The union feed from `SaveAttentionSource` into the existing rank-1 `needsAttention` rung
- Files `playstead-mac/PlaysteadTests/SavesTests/SaveConflictResolverTests.swift`, `playstead-mac/PlaysteadTests/SnapshotTests/ConflictComparisonContractSnapshotTests.swift`, `playstead-mac/PlaysteadUITests/ConflictResolutionInteractionTests.swift`

<output>
Create `.planning/phases/04-persistent-save-continuity/04-11-SUMMARY.md` when done
</output>
