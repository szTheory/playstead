---
phase: 04-persistent-save-continuity
plan: 13
type: execute
wave: 7
depends_on: ["04-07", "04-08", "04-11", "04-12"]
files_modified:
  - playstead-mac/PlaysteadUITests/SaveRestoreProofTests.swift
  - playstead-mac/PlaysteadUITests/ZeroNetworkPlayFlowTests.swift
  - playstead-mac/TestPlans/LiveServer.xctestplan
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - .planning/phases/04-persistent-save-continuity/04-VALIDATION.md
  - .planning/phases/04-persistent-save-continuity/04-UAT.md
autonomous: false
requirements: [SAVE-01, SAVE-02, SAVE-03, SAVE-04, PORT-01]

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir` exists by that exact name, is assigned to a named test plan, is discovered and executed (not skipped), and proves the file half of SAVE-03: a captured revision restores to a byte-identical artifact in the save directory (D-68)."
    - "The zero-network Play flow test asserts zero recorded HTTP requests across the whole Play flow, including fire-and-forget tasks — stricter than the shipped blob-only precedent (D-68)."
    - "CP7-SAVE-C is a named, explicitly human checkpoint carrying the continuation proof, and is never marked passed by an automated test (D-68)."
    - "The automated proxy and CP7-SAVE-C are recorded as two distinct verification items, so a green suite cannot be read as a passed continuation proof (D-68)."
    - "`04-VALIDATION.md`'s Per-Task Verification Map lists every task in plans 04-01 through 04-12 with its plan, wave, requirement, threat reference, test type, and automated command."
    - "No three consecutive tasks in the phase lack an automated verify command."
    - "Every requirement ID — SAVE-01, SAVE-02, SAVE-03, SAVE-04, PORT-01 — appears in the verification map at least once."
    - "The full server suite and all four Mac test plans pass before the phase is verified."
    - "Running the phase gate twice on an unchanged tree produces the same pass result — the gate is deterministic and has no recording or watch mode."
    - statement: "A gate command that is missing or unresolvable exits non-zero rather than being read as clean, so a rotted gate cannot pass silently."
      verification: backstop
  artifacts:
    - playstead-mac/PlaysteadUITests/SaveRestoreProofTests.swift
    - playstead-mac/PlaysteadUITests/ZeroNetworkPlayFlowTests.swift
    - .planning/phases/04-persistent-save-continuity/04-VALIDATION.md
    - .planning/phases/04-persistent-save-continuity/04-UAT.md
  key_links:
    - "The automated proxy proves the file half only; the human half is what remains genuinely blocked on a real emulator and a real commercial title, which is why the two are recorded separately."
    - "The four-layer Mac evidence topology and the named/discovered/non-skipped test-evidence rule from Phase 3.5 are the machinery this plan registers into rather than a new gate."
  prohibitions:
    - statement: "CP7-SAVE-C must never be marked passed by an automated test, and no automated result may be substituted for it (D-68)."
      status: flagged-unverified
      verification: backstop
    - statement: "Nothing anywhere may tell the user \"saved\" after play; Play returns silently (D-47)."
      status: flagged-unverified
      verification: backstop
    - statement: "No gate may be added that passes when its command is missing; a missing command exits non-zero and fails the gate."
      status: flagged-unverified
      verification: backstop
---

<objective>
Prove what can be proven automatically, and name honestly what cannot.

Purpose: The continuation proof — that a real commercial title flushes on a bounded cadence, that captured bytes are accepted back by the emulator with correct in-game progress, and how post-exit writeback actually behaves — cannot be automated. The honest automated proxy proves the file half only. Recording both as one green result would be the single most consequential lie available in this phase.

Output: The named restore-proof test, the strict zero-network Play flow assertion, a filled validation map, and CP7-SAVE-C as an explicitly human checkpoint.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistent-save-continuity/04-CONTEXT.md
@.planning/phases/04-persistent-save-continuity/04-VALIDATION.md
@.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md
@.planning/phases/04-persistent-save-continuity/04-07-SUMMARY.md
@.planning/phases/04-persistent-save-continuity/04-11-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The named restore proof and the strict zero-network Play flow assertion</name>
  <files>playstead-mac/PlaysteadUITests/SaveRestoreProofTests.swift, playstead-mac/PlaysteadUITests/ZeroNetworkPlayFlowTests.swift, playstead-mac/TestPlans/LiveServer.xctestplan, playstead-mac/TestPlans/UI.xctestplan, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <read_first>
    - `playstead-mac/scripts/ci/run-mac-verification.sh` — the four-layer topology, `run_test_layer`, the required-tests manifest, and the named/discovered/non-skipped evidence validator
    - `playstead-mac/TestPlans/LiveServer.xctestplan` and `UI.xctestplan` — the shipped registration format
    - `playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift` — the shipped live-server test shape, pairing fixture, and teardown
    - `playstead-mac/Playstead/Saves/SavePlanExecutor.swift` — the restore path and the recording-transport hook from plan 04-07
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-68 (the named test, the split, and the strictness of the zero-network assertion), D-47
    - `.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md` — D-03 (named/discovered/non-skipped test evidence), D-08 (release guard), D-09 (deterministic profiles)
  </read_first>
  <behavior>
    - A test named exactly `testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir` exists, is discovered, and executes without being skipped.
    - It captures a revision from a written artifact, restores it onto a clean save directory, and asserts the resulting on-disk bytes are byte-identical to the captured bytes.
    - It asserts the restored artifact's sha256 equals the revision's recorded digest.
    - The zero-network test records every HTTP request attempted across plan, execute, spawn, and any task started during the flow, and asserts the count is zero.
    - The zero-network test fails if a single request is recorded, including one issued from a detached or fire-and-forget task.
    - Both tests are registered in a named test plan and appear in the CI required-tests manifest, so a skipped or absent test fails the evidence validator rather than passing silently.
    - The phase gate exits non-zero when a required test is absent, skipped, or fails.
  </behavior>
  <action>
Add `SaveRestoreProofTests.swift` containing the test named exactly `testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir` — the name is fixed by decision and is what the phase's evidence refers to. It writes a 32,768-byte artifact into a game's save directory, drives capture, clears the save directory to simulate a clean Mac, restores the captured revision through the launch path, and asserts the resulting bytes are byte-identical to what was captured and that the sha256 matches the recorded digest. This proves the file half of SAVE-03 and nothing more; it must not be described, named, or reported as proving that a game continues.

Add `ZeroNetworkPlayFlowTests.swift` installing a recording transport for the entire Play flow — planning, execution, spawn, and anything started along the way — and asserting zero recorded HTTP requests. This is deliberately stricter than the shipped blob-only precedent: a post-spawn fetch could not help anyway, because mutating a mmap'd `.sav` under a running emulator is itself corruption.

Register both in their test plans and in the CI required-tests manifest so the shipped evidence validator enforces named, discovered, and non-skipped execution. Follow the existing registration and manifest conventions exactly; do not add a new gate mechanism. Ensure the gate exits non-zero when a required test is absent, skipped, or failing — a missing command must never read as clean.
  </action>
  <verify>
    <automated>cd playstead-mac && xcodebuild test -project Playstead.xcodeproj -scheme Playstead -testPlan LiveServer -destination 'platform=macOS' CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- DEVELOPMENT_TEAM= PROVISIONING_PROFILE_SPECIFIER= -only-testing:PlaysteadUITests/SaveRestoreProofTests/testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, or "Executed 0 tests" in the test summary</fails_when>
    <automated>cd playstead-mac && scripts/ci/run-mac-verification.sh --layers ui --only-testing PlaysteadUITests/ZeroNetworkPlayFlowTests</automated>
    <fails_when>non-zero exit, "TEST FAILED" in the output, or "Executed 0 tests" in the test summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir' playstead-mac/PlaysteadUITests/SaveRestoreProofTests.swift` is at least `1`, spelled exactly.
    - Both runs report a non-zero executed test count and 0 failures.
    - `grep -c 'SaveRestoreProofTests\|ZeroNetworkPlayFlowTests' playstead-mac/scripts/ci/run-mac-verification.sh` is at least `2` — both are in the required-tests manifest.
    - A negative check proves the gate fails when a required test is removed from the manifest's expected set.
    - `grep -rn '"saved"' playstead-mac/Playstead/Saves/ playstead-mac/Playstead/Library/ | grep -vi test` returns nothing that is a user-facing success message.
  </acceptance_criteria>
  <done>The half of SAVE-03 that can be proven by machine is proven by a named, non-skippable test, and the Play flow is provably offline.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the validation map and record the two verification items separately</name>
  <files>.planning/phases/04-persistent-save-continuity/04-VALIDATION.md, .planning/phases/04-persistent-save-continuity/04-UAT.md</files>
  <read_first>
    - `.planning/phases/04-persistent-save-continuity/04-VALIDATION.md` — the template's Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, and sign-off sections
    - Every `04-NN-PLAN.md` in the phase directory — the task list, requirement IDs, threat IDs, and `<automated>` commands to transcribe
    - `.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md` — the shipped UAT document shape and how a blocked checkpoint is recorded
    - `.planning/phases/03.5-mac-verification-automation/03.5-CONTEXT.md` — D-24/D-25 (UAT evidence boundaries)
  </read_first>
  <action>
Fill `04-VALIDATION.md` completely.

Test Infrastructure: ExUnit for the server (`cd playstead-server && MIX_ENV=test mix test`) and XCTest across the four named Mac test plans (Unit, Rendering, UI, LiveServer), with the quick commands being the per-file `mix test <file>` and the per-test `xcodebuild test … -only-testing:` forms this phase's plans use.

Per-Task Verification Map: one row per task across plans 04-01 through 04-12, carrying the task id, plan, wave, requirement id, threat reference, the secure behaviour it establishes, test type, the exact automated command from the plan, whether the test file exists yet, and status. Every one of SAVE-01, SAVE-02, SAVE-03, SAVE-04, and PORT-01 must appear at least once. Check the map for sampling continuity: no three consecutive tasks may lack an automated verify.

Wave 0 Requirements: list the test files that do not yet exist and must be created by the plan that first needs them, following the gap list already identified for this phase.

Manual-Only Verifications: exactly one row — CP7-SAVE-C — stating the behaviour (a real commercial title flushes on a bounded cadence; captured bytes are accepted back by the emulator with correct in-game progress; post-exit writeback behaviour), the requirement (SAVE-03), why it is manual (it requires a real emulator and real game bytes and cannot be automated), and the test instructions.

Create `04-UAT.md` recording exactly two verification items for SAVE-03 and making their separation explicit: the automated proxy `testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir`, which proves the file half, and CP7-SAVE-C, which carries the human continuation proof and must never be marked passed by an automated result. Record CP7-SAVE-C as blocked until a human performs it, and state in the document that a green suite is not a passed continuation proof.

Set `nyquist_compliant: true` in the validation front matter only if the sampling-continuity check passes; otherwise leave it false and record the gap.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/playstead && python3 -c "
import re,glob,sys
v=open('.planning/phases/04-persistent-save-continuity/04-VALIDATION.md').read()
for r in ['SAVE-01','SAVE-02','SAVE-03','SAVE-04','PORT-01','CP7-SAVE-C']:
    assert r in v, r
rows=[l for l in v.splitlines() if l.startswith('| 4-')]
assert len(rows)>=25, len(rows)
u=open('.planning/phases/04-persistent-save-continuity/04-UAT.md').read()
assert 'testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir' in u
assert 'CP7-SAVE-C' in u
print('validation map ok', len(rows))
"</automated>
    <fails_when>non-zero exit, an AssertionError traceback, a FileNotFoundError, or the line beginning "validation map ok" missing from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - `04-VALIDATION.md` contains at least 25 task rows, one per task across plans 04-01 through 04-12.
    - All five requirement IDs appear in the map.
    - The Manual-Only Verifications table contains exactly one row, for CP7-SAVE-C.
    - `04-UAT.md` names both verification items and states that the automated proxy does not satisfy CP7-SAVE-C.
    - No row in the map has an empty automated-command cell without a corresponding Wave 0 entry.
  </acceptance_criteria>
  <done>Every task's proof is written down, and the one thing a machine cannot prove is written down as unproven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: CP7-SAVE-C — the human continuation proof</name>
  <what-built>
    The full Phase 4 save path is implemented and its automated half is green: capture with quiescence and crash recovery, the two-endpoint upload, the server revision DAG, the local compatibility gate, the launch planner and executor, the surfacing, divergence resolution, and export. `testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir` proves a captured revision restores to byte-identical bytes on disk, and `ZeroNetworkPlayFlowTests` proves the Play flow makes zero network requests. Neither proves that a real game continues from a restored save — that is what this checkpoint is for, and it is human-only by decision (D-68). No automated result may set its status.

    Before pausing, confirm the pinned mGBA adapter is installed, the server is running, and this Mac is paired, so the developer only has to play the game.
  </what-built>
  <how-to-verify>
    Perform these five steps against a real commercial GBA title through the pinned mGBA adapter, and record each observation in `.planning/phases/04-persistent-save-continuity/04-UAT.md` along with the title used and the adapter version.

    1. Launch the title through Playstead. Play far enough to trigger an in-game save. Note roughly how long after the in-game save the `.sav` on disk changes, and confirm it changes on a bounded cadence rather than never.
    2. Quit the emulator normally. Confirm Playstead captured a revision for the session, and note whether the post-exit settle pass found bytes the pre-exit reads had not seen.
    3. Clear the game's save directory to simulate a clean Mac. Relaunch through Playstead and let the automatic restore run — expect no prompt.
    4. Continue the game. Confirm the in-game progress is the progress that was saved: not a fresh file, not an older state, and with no in-game prompt offering to erase the save.
    5. Repeat step 2 once with a force-quit rather than a normal exit, and confirm the capture still occurred.

    If any step fails, do not mark the checkpoint passed. Record the failure as a phase-blocking finding and stop.
  </how-to-verify>
  <resume-signal>Reply "CP7-SAVE-C passed" with the recorded observations, or describe which step failed and what was observed instead.</resume-signal>
  <verify>
    <human-check>The developer performed all five steps against a real commercial title, recorded the observations in `04-UAT.md`, and set CP7-SAVE-C's status themselves — never from a test result.</human-check>
  </verify>
  <acceptance_criteria>
    - `04-UAT.md` contains a CP7-SAVE-C section with one recorded observation per step, the title used, and the adapter version.
    - CP7-SAVE-C's status is set by the developer, and the document states explicitly that no automated result may set it.
    - If any step failed, the phase is not marked verified and the failure is recorded as a blocking finding.
  </acceptance_criteria>
  <done>A human has confirmed that a real game continues from a restored save, and that confirmation is recorded as a human confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → phase verification claim | A green suite could be read as proving more than it proves |
| CI gate → correctness claim | A gate whose command is missing could exit clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13-01 | Repudiation | an automated pass claiming the continuation proof | high | mitigate | The automated proxy and CP7-SAVE-C are two distinct recorded verification items; the UAT document states explicitly that a green suite is not a passed continuation proof, and the checkpoint is a blocking human gate |
| T-04-13-02 | Repudiation | a required test silently absent or skipped | high | mitigate | Both new tests are registered in the CI required-tests manifest, which the shipped evidence validator enforces as named, discovered, and non-skipped; a negative check proves removal fails the gate |
| T-04-13-03 | Repudiation | a gate passing because its command is missing | high | mitigate | Gate commands exit non-zero when unresolvable; the acceptance criteria include a negative check rather than only a positive run |
| T-04-13-04 | Information Disclosure | UAT evidence containing ROM names or paths | medium | mitigate | The recorded observations name the title and adapter version, which the developer chooses to record; no paths, digests of user content, or save bytes are captured, consistent with the shipped UAT evidence boundaries |
| T-04-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; RESEARCH.md's Package Legitimacy Audit records that this phase installs no external packages |
</threat_model>

<verification>
- `cd playstead-server && MIX_ENV=test mix test` passes.
- All four Mac test plans pass through the shipped four-layer verification run.
- `testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir` and `ZeroNetworkPlayFlowTests` are present in the required-tests manifest and execute non-skipped.
- CP7-SAVE-C is recorded with a developer-set status.
</verification>

<success_criteria>
- The file half of SAVE-03 is proven by a named, non-skippable automated test.
- The Play flow is proven to make zero network requests.
- The continuation proof is recorded as human-confirmed, and no automated result claims it.
</success_criteria>

## Artifacts this phase produces

New symbols and paths created by this plan:

- Test `testCapturedRevisionRestoresToByteIdenticalArtifactInLaunchDir` in `playstead-mac/PlaysteadUITests/SaveRestoreProofTests.swift`
- File `playstead-mac/PlaysteadUITests/ZeroNetworkPlayFlowTests.swift`
- Required-tests manifest entries for both new suites in `playstead-mac/scripts/ci/run-mac-verification.sh`
- File `.planning/phases/04-persistent-save-continuity/04-UAT.md` with the two separated SAVE-03 verification items
- Checkpoint identifier `CP7-SAVE-C`

<output>
Create `.planning/phases/04-persistent-save-continuity/04-13-SUMMARY.md` when done
</output>
