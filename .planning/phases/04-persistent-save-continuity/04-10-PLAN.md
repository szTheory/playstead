---
phase: 04-persistent-save-continuity
plan: 10
type: execute
wave: 5
depends_on: ["04-05", "04-08"]
files_modified:
  - playstead-server/priv/repo/migrations/20260904000004_create_save_attention_items.exs
  - playstead-server/lib/playstead/saves/attention_source.ex
  - playstead-server/lib/playstead/saves/attention_item.ex
  - playstead-server/lib/playstead/saves.ex
  - playstead-server/lib/playstead_web/live/saves_live.ex
  - playstead-server/lib/playstead_web/live/saves_live/comparison_panel.ex
  - playstead-server/lib/playstead_web/live/attention_live.ex
  - playstead-server/lib/playstead_web/live/exports_live.ex
  - playstead-server/lib/playstead_web/router.ex
  - playstead-server/test/playstead/saves_attention_source_test.exs
  - playstead-server/test/playstead_web/live/saves_live_test.exs
autonomous: true
requirements: [SAVE-02, SAVE-04, PORT-01]

estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`Playstead.Attention.Reason` is unchanged — its nine members and their import-recognition scope are identical before and after this plan (D-66)."
    - "Divergence, blocked capture, and the per-user backstops raise items through a saves-owned attention source with its own table and its own reason vocabulary, unioned into the inbox view at read time (D-66)."
    - "Raising the same saves attention item twice for the same grouping key upserts and increments rather than creating a duplicate row."
    - "Resolving or acknowledging a fork clears its divergence attention item, and an acknowledged fork is never re-raised (D-52)."
    - "The console can inspect, choose, keep both, and export a save version — full vocabulary parity with partial verbs (D-67)."
    - "The console never renders a global playhead: `current` is device-scoped and no console surface claims a single system-wide current version (D-67)."
    - "The console's post-action copy uses the two console-only result strings, not the Mac's device-scoped ones (D-54)."
    - "The console presents no confirmation dialog before choosing, no Undo, no recommended or pre-selected side, and no bulk always-use-this-Mac control; each absence is asserted by test (D-49, D-51, D-55)."
    - "The console exposes exactly one saves-scope control on the export surface, and there is no Mac-side export action in this phase (D-62)."
    - "\"Export this version…\" from the comparison panel deep-links into the same server-side export machinery as an ordinary export, with the saves scope narrowed to that version (D-62)."
    - "A user with no divergence and no blocked captures sees no saves attention items and no empty-state error."
    - statement: "Two console sessions acting on the same fork concurrently both succeed, and the resulting retained-revision set is the same regardless of order."
      verification: backstop
  artifacts:
    - playstead-server/lib/playstead/saves/attention_source.ex
    - playstead-server/lib/playstead/saves/attention_item.ex
    - playstead-server/lib/playstead_web/live/saves_live.ex
    - playstead-server/test/playstead/saves_attention_source_test.exs
    - playstead-server/test/playstead_web/live/saves_live_test.exs
  key_links:
    - "The saves attention source is unioned into the existing inbox view and into the Mac card's rank-1 rung (plan 04-11) — one source, two consumers, and no widening of a bounded context that was scoped to import recognition."
    - "Choosing and keeping-both call `Saves.resolve_divergence/4` and `Saves.acknowledge_divergence/3` from plan 04-05 — the same context functions the Mac calls, so the two clients cannot diverge in behaviour."
    - "The export deep link reuses `Export.SavesPlan` and the persisted `saves_scope` from plan 04-08 rather than a console-specific export path."
  prohibitions:
    - statement: "`Playstead.Attention.Reason` must not be widened by any member, alias, or catch-all (D-66)."
      status: flagged-unverified
      verification: backstop
    - statement: "The console must never render a global or system-wide current-version playhead; `current` is device-scoped (D-67)."
      status: flagged-unverified
      verification: backstop
    - statement: "No confirmation dialog, Undo affordance, recommended badge, pre-selected side, or bulk always-use-this-Mac preference may exist on any divergence surface (D-49, D-51, D-55)."
      status: flagged-unverified
      verification: backstop
    - statement: "No Mac-side export action may be added in this phase; the console holds the one export control (D-62)."
      status: flagged-unverified
      verification: backstop
---

<objective>
Give saves their own attention source without widening a frozen bounded context, and give the console full save vocabulary with partial verbs.

Purpose: Divergence and blocked capture need to reach a user's inbox, but `Playstead.Attention.Reason` is a frozen, nine-member, import-recognition-scoped vocabulary — widening it to carry save-domain meaning is exactly the infrastructure-leaks-into-domain error the project's context-boundary rule exists to prevent. A saves-owned source preserves the user-visible outcome exactly while keeping the boundary intact.

Output: A saves-owned attention table and source unioned into the inbox, a console saves surface that can inspect, choose, keep both, and export, and one saves-scope export control.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistent-save-continuity/04-CONTEXT.md
@.planning/phases/04-persistent-save-continuity/04-PATTERNS.md
@.planning/phases/04-persistent-save-continuity/discussion-research/G-conflict-resolution-ux.md
@.planning/phases/04-persistent-save-continuity/04-05-SUMMARY.md
@.planning/phases/04-persistent-save-continuity/04-08-SUMMARY.md
@shared/save-vocabulary.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A saves-owned attention source that leaves Attention.Reason frozen</name>
  <files>playstead-server/priv/repo/migrations/20260904000004_create_save_attention_items.exs, playstead-server/lib/playstead/saves/attention_item.ex, playstead-server/lib/playstead/saves/attention_source.ex, playstead-server/lib/playstead/saves.ex, playstead-server/lib/playstead_web/live/attention_live.ex, playstead-server/test/playstead/saves_attention_source_test.exs</files>
  <read_first>
    - `playstead-server/lib/playstead/attention.ex` lines 40-70 — the `Repo.insert(changeset, on_conflict: [inc: [count: 1]], conflict_target: [:user_id, :grouping_key, :reason])` upsert shape to copy structurally
    - `playstead-server/lib/playstead/attention/reason.ex` — the nine frozen members and their import-recognition scope; studied here and deliberately not extended
    - `playstead-server/lib/playstead_web/live/attention_live.ex` — how the inbox renders items and how a second source can be unioned in at read time
    - `playstead-server/lib/playstead/saves/branches.ex` — `diverged?/2` and `heads/2` from plan 04-05
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-66 (the arbitration and its reasoning), D-31 (blocked capture), D-28 (backstops surface as attention, never deletion), and the divergence attention-item copy table
  </read_first>
  <behavior>
    - `Playstead.Attention.Reason`'s member list is byte-identical before and after this plan.
    - Raising a divergence item for a fork creates one saves attention item with the fork's grouping key.
    - Raising the same item again upserts and increments rather than inserting a duplicate.
    - Resolving the fork clears its item.
    - Acknowledging the fork (keep both) clears its item and prevents it being re-raised for that fork.
    - A blocked capture raises a saves attention item, and clearing the blockage clears it.
    - Crossing a per-user backstop raises an attention item and refuses no commit.
    - The inbox view returns the union of `Playstead.Attention` items and saves items for a user, ordered consistently.
    - A user with neither kind of item gets an empty inbox, not an error.
  </behavior>
  <action>
Create a `save_attention_items` table (forward-only migration) with `user_id`, a `grouping_key`, its own `reason` column with a saves-specific vocabulary (`divergence`, `capture_blocked`, `retention_backstop`), a `count`, timestamps, and a unique index on `(user_id, grouping_key, reason)`.

Add `Playstead.Saves.AttentionItem` (the schema) and `Playstead.Saves.AttentionSource` (raise, clear, and list functions). Copy `Playstead.Attention`'s upsert-by-grouping-key pattern structurally — `on_conflict: [inc: [count: 1]]` with the unique index as the conflict target — but keep the reason vocabulary entirely within the saves context. Do not add a member, alias, or catch-all to `Playstead.Attention.Reason`; assert its member list is unchanged with a test that pins the exact list.

Wire the three raisers: divergence when a line's head set becomes larger than one and the fork is not already acknowledged; blocked capture reported by the client; and the per-user backstop crossings computed in plan 04-05. A backstop item surfaces pressure as attention and never refuses a commit or deletes anything.

Wire the clearers: resolving a fork clears its divergence item, acknowledging a fork clears it and marks it never-re-raise, and a cleared blockage clears its item.

Union the saves items into the existing inbox at read time in `attention_live.ex`. The union happens in the view layer; neither context learns about the other's rows.
  </action>
  <verify>
    <automated>cd playstead-server && MIX_ENV=test mix test test/playstead/saves_attention_source_test.exs</automated>
    <fails_when>non-zero exit, "0 tests" in the summary line, or any "test ... ** (" failure block in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `mix test test/playstead/saves_attention_source_test.exs` reports at least 9 tests, 0 failures.
    - A test pins `Playstead.Attention.Reason`'s exact member list and fails if a member is added.
    - `git diff playstead-server/lib/playstead/attention/reason.ex` is empty.
    - A test asserts an acknowledged fork is never re-raised.
    - A test asserts crossing a backstop raises an item and the next commit still succeeds.
  </acceptance_criteria>
  <done>Saves can demand attention without borrowing another context's vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The console saves surface — inspect, choose, keep both</name>
  <files>playstead-server/lib/playstead_web/live/saves_live.ex, playstead-server/lib/playstead_web/live/saves_live/comparison_panel.ex, playstead-server/lib/playstead_web/router.ex, playstead-server/test/playstead_web/live/saves_live_test.exs</files>
  <read_first>
    - `playstead-server/lib/playstead_web/live/attention_live.ex` and `attention_live/evidence_card.ex` — the shipped inbox LiveView and its component split, the closest in-repo analog for this surface
    - `playstead-server/lib/playstead_web/live/attention_live/bulk_bar.ex` — the shipped bulk-action bar shape, for the grouped "Keep both in all {N}" action
    - `playstead-server/lib/playstead_web/router.ex` — the `:require_authenticated_user` live session and where console routes are declared
    - `playstead-server/lib/playstead/saves.ex` — `resolve_divergence/4` and `acknowledge_divergence/3` from plan 04-05
    - `shared/save-vocabulary.json` — the authoritative strings from plan 04-09
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-50 (the four facts per side, and what is never shown), D-51, D-52, D-53, D-54, D-55, D-67, and the full divergence copy tables
  </read_first>
  <behavior>
    - The comparison panel shows exactly four facts per side: origin, last saved, play time since the split, and saves since the split.
    - File size, byte-diff, similarity score, revision ordinal, and parent pointer are never rendered; the digest appears only behind the "Details" disclosure.
    - Neither side is highlighted, recommended, pre-selected, or distinguished by colour.
    - Choosing a side calls `Saves.resolve_divergence/4` with no confirmation dialog, and renders the console-only post-choice string.
    - "Keep both" is a peer of the choice buttons, calls `Saves.acknowledge_divergence/3`, and renders the console-only keep-both string.
    - No Undo control is rendered after either action.
    - The only bulk action available is "Keep both in all {N}"; no bulk always-use-this-Mac control exists.
    - N-way divergence renders the N-variant subtitle and item title strings.
    - A clock caveat is rendered for a side whose device-reported time contradicts arrival order.
    - The not-downloaded case renders its locked string and still allows choosing and exporting.
    - No console surface renders a global current-version playhead.
    - Every action is reachable by keyboard alone, and each side is one accessibility element carrying one complete comparison sentence.
  </behavior>
  <action>
Add `saves_live.ex` and a `saves_live/comparison_panel.ex` component under `:require_authenticated_user`, following `attention_live.ex`'s LiveView and component split. This is the third of the three entry points to the comparison surface, alongside the Mac's attention inbox and game detail.

Render exactly the four facts per side from D-50: origin, last saved, play time since the split, and saves since the split. Play-time-since-split is the only available fact that correlates with value rather than ordering, and it is the reason this surface is worth building. Never render file size, byte-diff, similarity score, revision ordinal, or parent pointer; the digest lives behind the "Details" disclosure for support purposes only. File size in particular is a documented mistake elsewhere — here it is the constant 32,768 for every side.

Use the locked divergence copy verbatim from `shared/save-vocabulary.json`: the sheet title, subtitle and its N-variant, side headings and the three side lines with their singular and no-session variants, "Continue from this one", "Currently continuing from this one", "Export this version…", the clock caveat, the not-downloaded string, "Keep both"/"Keep them all" with its explainer, "Details", and "Done". After an action, render the two console-only result strings — "Continuing from {Origin}. Your Macs will use this version the next time they sync." and "Keeping both. Each Mac keeps playing the version it already has." — rather than the Mac's device-scoped variants, because the console has no emulator and no playhead.

Assert the absences by test: no confirmation dialog before choosing, no Undo control, no recommended badge or pre-selected side, no bulk always-use-this-Mac preference, and no global playhead. These are rejected features, not deferred ones.

Use no gesture-only, hover-only, or pointer-only affordance and no colour distinguishing the sides. Each side is one accessibility element carrying one complete comparison sentence. Add a keyboard-only traversal test covering every action.
  </action>
  <verify>
    <automated>cd playstead-server && MIX_ENV=test mix test test/playstead_web/live/saves_live_test.exs</automated>
    <fails_when>non-zero exit, "0 tests" in the summary line, or any "test ... ** (" failure block in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `mix test test/playstead_web/live/saves_live_test.exs` reports at least 12 tests, 0 failures.
    - A test asserts the rendered panel contains no file size, byte-diff, similarity score, revision ordinal, or parent pointer.
    - A test asserts choosing performs the mutation with no intervening confirmation.
    - A test asserts the rendered output contains no Undo control and no recommended badge.
    - `grep -rniE 'always use|prefer this mac|recommended' playstead-server/lib/playstead_web/live/saves_live.ex playstead-server/lib/playstead_web/live/saves_live/` returns nothing.
    - A keyboard-only traversal test reaches and activates every action in the panel.
  </acceptance_criteria>
  <done>A user can compare and decide from the console with the same words the Mac uses, and the console never pretends to know which version is being played.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One console export control and the export-this-version deep link</name>
  <files>playstead-server/lib/playstead_web/live/exports_live.ex, playstead-server/lib/playstead_web/live/saves_live.ex, playstead-server/test/playstead_web/live/saves_live_test.exs</files>
  <read_first>
    - `playstead-server/lib/playstead_web/live/exports_live.ex` — the shipped export console surface, its option controls, and how it enqueues an export
    - `playstead-server/lib/playstead/export/export_record.ex` — the `saves_scope` field added in plan 04-08
    - `playstead-server/lib/playstead/export/saves_plan.ex` — the plan the deep link narrows
    - `.planning/phases/04-persistent-save-continuity/04-CONTEXT.md` — D-57 (default `saves_scope: :all`, persisted), D-62 (one console control, no Mac-side export action, the deep link), and the "Export result" locked string
  </read_first>
  <behavior>
    - The exports console renders exactly one saves-scope control, defaulting to the all-revisions scope.
    - Choosing a scope persists it on the enqueued `ExportRecord`.
    - Re-enqueuing that export reproduces the same scope without the user reselecting it.
    - "Export this version…" from the comparison panel enqueues an export through the same machinery with the scope narrowed to that version.
    - The post-export message is the locked "Export result" string.
    - No Mac-side export action exists anywhere in the client.
    - An export of a diverged slot succeeds and produces no drop-in copy, consistent with plan 04-08.
  </behavior>
  <action>
Add one saves-scope control to the exports console surface — one control, not a group — defaulting to the all-revisions scope, and persist the selection onto the enqueued `ExportRecord.saves_scope` so a re-enqueued job reproduces the same plan.

Wire "Export this version…" in the comparison panel to deep-link into that same server-side machinery with the scope narrowed to the chosen version. It is a deep link, not a second export path: the plan, the layout, the sidecar, and the writer are all the ones from plan 04-08.

Render the locked "Export result" string after a completed export: "Exported {title} — {Origin}, {date}. The folder has the exact save file and a manifest listing what's inside."

Add a test asserting no Mac-side export action exists — a source-level assertion over the Swift tree is sufficient, since the decision is that the console holds the one export control in this phase.
  </action>
  <verify>
    <automated>cd playstead-server && MIX_ENV=test mix test test/playstead_web/live/saves_live_test.exs</automated>
    <fails_when>non-zero exit, "0 tests" in the summary line, or any "test ... ** (" failure block in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - The saves-scope control appears exactly once in the rendered exports surface — a test asserts a count of one.
    - A test asserts the enqueued record carries the selected `saves_scope` and a re-enqueue reproduces it.
    - A test asserts the deep link narrows the scope to the chosen version and enqueues through the same worker.
    - `grep -rniE 'export.*save|save.*export' playstead-mac/Playstead/Library/ playstead-mac/Playstead/Saves/ | grep -vi 'test'` returns no user-facing export action.
  </acceptance_criteria>
  <done>Exporting a version is one control in one place, and a re-run cannot silently change what it includes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated console user → save mutations | Choosing and acknowledging mutate durable lineage |
| saves context ↔ attention context | Two bounded contexts share an inbox surface |
| console export request → filesystem | An export writes a tree derived from database content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10-01 | Elevation of Privilege | cross-user access to save lines or attention items | high | mitigate | Every query is `user_id`-scoped under the `:require_authenticated_user` live session, matching the shipped console convention; out-of-scope reads return not-found |
| T-04-10-02 | Tampering | a console action silently discarding a version | high | mitigate | Choosing and keeping-both call the append-only context functions from plan 04-05; nothing is deleted, and the non-chosen side stays on screen with a live switch-back control |
| T-04-10-03 | Repudiation | context-boundary erosion via a widened reason enum | medium | mitigate | `Playstead.Attention.Reason` is left frozen and its member list is pinned by a test; the saves source owns its own table and vocabulary |
| T-04-10-04 | Spoofing | a console-rendered current-version claim the Macs do not honour | medium | mitigate | The console never renders a global playhead and uses the console-only result strings, which say what will happen at the next sync rather than what is being played |
| T-04-10-05 | Information Disclosure | digests and internal nouns in console copy | low | mitigate | The digest is behind the "Details" disclosure only; the copy-contract test from plan 04-09 scans console strings for banned words |
| T-04-10-06 | Denial of Service | unbounded export enqueued from the console | low | accept | Accepted: exports already run through the shipped Oban worker with its existing concurrency limits, and the saves scope narrows rather than widens the work |
| T-04-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; RESEARCH.md's Package Legitimacy Audit records that this phase installs no external packages |
</threat_model>

<verification>
- `cd playstead-server && MIX_ENV=test mix test test/playstead/saves_attention_source_test.exs test/playstead_web/live/saves_live_test.exs` passes with a non-zero test count.
- `git diff playstead-server/lib/playstead/attention/reason.ex` is empty.
- `cd playstead-server && MIX_ENV=test mix test` (full suite) passes, proving the inbox union did not break the shipped attention tests.
</verification>

<success_criteria>
- Divergence and blocked capture reach the inbox without widening a frozen bounded context.
- The console can inspect, choose, keep both, and export — and cannot claim a playhead it does not have.
- Every rejected divergence affordance is absent, and its absence is asserted.
</success_criteria>

## Artifacts this phase produces

New symbols and paths created by this plan:

- Table `save_attention_items` with reason vocabulary `divergence`, `capture_blocked`, `retention_backstop`; migration `20260904000004_create_save_attention_items.exs`
- Schema `Playstead.Saves.AttentionItem`
- Module `Playstead.Saves.AttentionSource` with raise, clear, and list functions
- LiveView `PlaysteadWeb.SavesLive` and component `PlaysteadWeb.SavesLive.ComparisonPanel`
- Console route for the saves surface under `:require_authenticated_user`
- A single saves-scope control on the exports console surface and the "Export this version…" deep link
- Files `playstead-server/test/playstead/saves_attention_source_test.exs`, `playstead-server/test/playstead_web/live/saves_live_test.exs`

<output>
Create `.planning/phases/04-persistent-save-continuity/04-10-SUMMARY.md` when done
</output>
