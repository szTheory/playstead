---
phase: 03-mac-offline-play-vertical-slice
plan: 18
type: execute
gap_closure: true
gap_ids: [G-03-8]
wave: 2
depends_on: ["03-17"]
files_modified:
  - playstead-mac/Playstead/Library/LibraryShellView.swift
  - playstead-mac/Playstead/Controller/ControllerRecoveryBanner.swift
  - playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/scripts/ci/run-mac-verification.sh
autonomous: true
requirements: [PLAY-04, QUAL-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Disconnecting the assigned controller presents a non-modal recovery notice on a reachable production surface while keyboard and pointer still activate controls behind it per D-14."
    - "Reconnect clears the notice without app relaunch, and disconnecting an unassigned device does not raise assigned-controller recovery."
    - "The recovery identifiers are selected and required by recurring CI; zero discovery, skip, or removal is rejected."
  artifacts:
    - path: "playstead-mac/Playstead/Library/LibraryShellView.swift"
      provides: "Reachable non-modal recovery composition"
    - path: "playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift"
      provides: "Packaged-app detach/reconnect and fallback-input evidence"
    - path: "playstead-mac/scripts/ci/run-mac-verification.sh"
      provides: "Fail-closed recovery evidence registration"
  key_links:
    - "ControllerHost.showRecoveryBanner -> LibraryShellView -> ControllerRecoveryBanner while underlying controls remain enabled"
    - "VirtualGamepad detach/reconnect -> production GameController notifications -> visible recovery state"
    - "UI.xctestplan selection -> required-test manifest -> contract mutation checks"
---

<objective>
Close G-03-8's observable recovery gap over the qualified 03-17 framework seam.

The recovery banner exists but has no production call site. Compose it once into the real shell, prove detach and reconnect without stranding keyboard or pointer input, and make the exact tests fail-closed in recurring CI. Production input forwarding, live test, and mapping persistence are isolated in 03-22 and 03-23 so this plan stays within its execution budget.

Purpose: make controller loss visible and recoverable through the packaged app while preserving every D-14 fallback path.
Output: production recovery composition, two focused virtual-HID journeys, and recurring evidence guards.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/debug/g-03-8-controller-automation.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-17-SUMMARY.md

<interfaces>
ControllerHost exposes showRecoveryBanner, lastDisconnectedControllerName, dismissRecoveryBanner(), connectedControllers, assignedControllerID, and connectionState. LibraryShellView is the durable shell root and must compose the existing ControllerRecoveryBanner without creating a second controller state owner.

Plan 03-17 owns virtual-HID feasibility and stable identity. This plan consumes that proven seam but does not add production element forwarding or mapping callbacks; those belong to 03-22 and 03-23 after their packaged-app oracle is red.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. No dependency is installed.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Recover non-modally from detach and reconnect without stranding fallback input</name>
  <files>playstead-mac/Playstead/Library/LibraryShellView.swift, playstead-mac/Playstead/Controller/ControllerRecoveryBanner.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift</files>
  <behavior>
    - RED: detaching the assigned virtual controller does not render a recovery notice because ControllerRecoveryBanner has no production call site.
    - GREEN: detach renders one notice naming the disconnected controller, and reconnect removes it without relaunch.
    - GREEN: while the notice is visible, a keyboard action changes the current surface and a pointer click activates a separate control behind the notice per D-14.
    - Negative control: disconnecting an unassigned second controller does not claim that the assigned controller was lost.
    - Negative control: dismissing the notice does not alter controller assignment or disable keyboard/pointer input.
  </behavior>
  <action>
Compose ControllerRecoveryBanner once at the LibraryShellView root, driven directly by AppEnvironment.controllerHost. Keep it in normal layout or an overlay that does not intercept unrelated hit testing; do not use alert, sheet, disabled, or focus-trapping presentation. Its dismiss action calls dismissRecoveryBanner only.

Write the failing virtual-HID test first. Start with one controller assigned, detach it, require the production banner, use keyboard to move to another sidebar destination, use pointer to activate a command-bar control while the banner remains, then reconnect and require the banner to disappear. Add the two-controller negative case. This is the end-to-end G-03-8 path per D-14: framework notification, host state, visible recovery, and fallback behavior in one test.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing ControllerHardwareIntegrationTests/testDetachRecoveryIsNonModalAndReconnectRestoresInput ControllerHardwareIntegrationTests/testUnassignedDetachDoesNotRaiseAssignedRecovery</automated>
  </verify>
  <done>Detach/reconnect recovery is visible, non-modal, and proves keyboard/pointer survival through the real packaged app.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require the recovery journeys in the recurring evidence contract</name>
  <files>playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift, playstead-mac/TestPlans/UI.xctestplan, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - Both exact recovery identifiers are selected by UI.xctestplan and the UI required-test list.
    - Removing either identifier, reporting it skipped, or reporting zero discovery makes the contract self-test fail.
    - Input-bridge, mapping, and persistence identifiers from 03-22/03-23 are not credited to this plan's recovery evidence.
  </behavior>
  <action>
Keep ControllerHardwareIntegrationTests selected in UI.xctestplan and register both exact recovery identifiers in run-mac-verification.sh's UI required-test list and cross-layer UAT allowlist. Extend the existing result-verifier mutation fixtures so deletion, skip, or zero discovery for either recovery journey is rejected. Keep the evidence category scoped to G-03-8; do not let the enumeration tracer or later lifecycle journey substitute for the visible non-modal fallback assertions.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-contracts</automated>
  </verify>
  <done>Recurring evidence fails closed unless both recovery journeys execute once, non-skipped, and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller notifications -> SwiftUI state | Untrusted device lifecycle events control a visible recovery affordance. |
| Recovery overlay -> fallback controls | Presentation can accidentally intercept keyboard or pointer input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-18-01 | Denial of Service | Recovery banner | medium | mitigate | Non-modal composition, no disabled ancestor, and real keyboard/pointer actions while visible. |
| T-03-18-02 | Spoofing | Disconnect attribution | low | mitigate | Only loss of the assigned runtime id raises recovery; an unassigned-device negative control proves the boundary. |
| T-03-18-03 | Repudiation | Recurring recovery evidence | medium | mitigate | Exact required identifiers plus skip/zero-discovery/removal mutation tests. |
</threat_model>

<verification>
- Focused packaged-app recovery tests execute non-skipped through virtual HID.
- Keyboard and pointer actions succeed while the notice remains visible.
- Contract self-tests reject absent or non-executed recovery evidence.
</verification>

<success_criteria>
- G-03-8 is executable without physical hardware.
- Controller loss never disables keyboard or pointer interaction per D-14.
- The plan contains only recovery composition and its evidence contract; lifecycle input and persistence remain in dependent plans.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-18-SUMMARY.md` when done.
</output>
