---
phase: 03-mac-offline-play-vertical-slice
plan: 18
type: execute
gap_closure: true
gap_ids: [G-03-8, G-03-9]
wave: 2
depends_on: ["03-17"]
files_modified:
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/Playstead/Library/LibraryShellView.swift
  - playstead-mac/Playstead/Controller/ControllerRecoveryBanner.swift
  - playstead-mac/Playstead/Readiness/ReadinessSheetView.swift
  - playstead-mac/Playstead/Controller/ControllerSettingsView.swift
  - playstead-mac/Playstead/Controller/ControllerTestView.swift
  - playstead-mac/Playstead/Controller/ControllerHost.swift
  - playstead-mac/PlaysteadTests/ControllerTests/ControllerHostTests.swift
  - playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
autonomous: true
requirements: [PLAY-04, QUAL-01]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Disconnecting the assigned controller presents a non-modal recovery notice on a reachable production surface while keyboard and pointer still activate controls behind it."
    - "Reconnect clears the notice and resumes input without app relaunch; assigning a second enumerated controller switches the active device without disabling any fallback path."
    - "The production ControllerSettingsView can open live test, assign, remap, and reset; none of those callbacks is a default no-op at its shipped call site."
    - "A remap is persisted under the stable controller model key, reaches AdapterHost through refreshActiveControllerMapping, survives relaunch, and reset restores and persists defaults."
    - "The virtual-HID lifecycle journey is distinct from leaf unit tests and is required by CI with non-vacuity and negative controls."
  artifacts:
    - path: "playstead-mac/Playstead/Library/LibraryShellView.swift"
      provides: "Reachable non-modal recovery composition"
    - path: "playstead-mac/Playstead/Readiness/ReadinessSheetView.swift"
      provides: "Fully wired controller settings and live-test presentation"
    - path: "playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift"
      provides: "Attach/detach/reconnect and full lifecycle E2E evidence"
  key_links:
    - "ControllerHost.showRecoveryBanner -> LibraryShellView -> ControllerRecoveryBanner while underlying controls remain enabled"
    - "ControllerSettingsView actions -> ControllerMappingStore -> AppEnvironment.refreshActiveControllerMapping -> AdapterHost"
    - "VirtualGamepad reports -> ControllerHost.liveInputs -> ControllerTestView accessible pressed/not-pressed state"
---

<objective>
Close the observable controller recovery and full lifecycle gaps over the shared 03-17 framework seam.

The component types already exist, but the recovery banner has no production call site, ControllerTestView is unreachable, and the shipped ControllerSettingsView call site supplies assignment only. Compose the existing types into one real lifecycle and prove attach, live input, assign, remap, reset, disconnect, reconnect, fallback input, and relaunch persistence through the packaged app.

Purpose: make PLAY-04 behavior real and repeatably testable without a physical controller.
Output: production recovery composition, complete settings callbacks, and two fail-closed virtual-HID journeys.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/debug/g-03-8-controller-automation.md
@.planning/debug/g-03-9-controller-lifecycle.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-17-SUMMARY.md

<interfaces>
From ControllerHost: `showRecoveryBanner`, `lastDisconnectedControllerName`, `dismissRecoveryBanner()`, `connectedControllers`, `assignedControllerID`, `connectionState`, `liveInputs`, `assign(controllerID:)`, and `onAssignmentChanged`.

From ControllerMappingStore: `mapping(forControllerProductID:)`, `save(_:)`, and `reset(controllerProductID:)`. AppEnvironment.refreshActiveControllerMapping() is the existing connection from the selected mapping to AdapterHost; every remap/reset must call it after persistence.

ControllerSettingsView already accepts onAssign, onRemap, onReset, and onOpenTestView. ReadinessSheetView currently passes only onAssign at the sole production call site. ControllerTestView accepts controllerName, availableInputs, and liveInputs.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. No dependency is installed.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Recover non-modally from detach and reconnect without stranding fallback input</name>
  <files>playstead-mac/Playstead/Library/LibraryShellView.swift, playstead-mac/Playstead/Controller/ControllerRecoveryBanner.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift</files>
  <behavior>
    - RED: detaching the assigned virtual controller does not render a recovery notice because ControllerRecoveryBanner has no production call site.
    - GREEN: detach renders one notice naming the disconnected controller, and reconnect removes it without relaunch.
    - GREEN: while the notice is visible, a keyboard action changes the current surface and a pointer click activates a separate control behind the notice.
    - Negative control: disconnecting an unassigned second controller does not claim that the assigned controller was lost.
    - Negative control: dismissing the notice does not alter controller assignment or disable keyboard/pointer input.
  </behavior>
  <action>
Compose ControllerRecoveryBanner once at the LibraryShellView root, driven directly by AppEnvironment.controllerHost. Keep it in normal layout or an overlay that does not intercept unrelated hit testing; do not use alert, sheet, disabled, or focus-trapping presentation. Its dismiss action must call dismissRecoveryBanner only.

Write the failing virtual-HID test first. Start with one controller assigned, detach it, require the production banner, use keyboard to move to another sidebar destination, use pointer to activate a command-bar control while the banner remains, then reconnect and require the banner to disappear. Add the two-controller negative case. This is the end-to-end G-03-8 path per D-14: framework notification, host state, visible recovery, and fallback behavior in one test.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing ControllerHardwareIntegrationTests/testDetachRecoveryIsNonModalAndReconnectRestoresInput ControllerHardwareIntegrationTests/testUnassignedDetachDoesNotRaiseAssignedRecovery</automated>
  </verify>
  <done>Detach/reconnect recovery is visible, non-modal, and proves keyboard/pointer survival through the real packaged app.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove then implement production input, live test, mapping actions, and persistence</name>
  <files>playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/Readiness/ReadinessSheetView.swift, playstead-mac/Playstead/Controller/ControllerSettingsView.swift, playstead-mac/Playstead/Controller/ControllerTestView.swift, playstead-mac/Playstead/Controller/ControllerHost.swift, playstead-mac/PlaysteadTests/ControllerTests/ControllerHostTests.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - RED: before any production edit in this task, the packaged-app lifecycle journey enumerates the qualified virtual controller but press/release never reaches liveInputs and the shipped Test controller, remap, and reset controls cannot complete the asserted state changes.
    - GREEN: production GCControllerInputSource forwards named press/release and thresholded d-pad transitions into ControllerHost.liveInputs for startup and later-connected devices, de-duplicates repeats, and clears device-owned inputs on disconnect.
    - GREEN: Test controller presents ControllerTestView for the assigned descriptor and renders press/release changes from ControllerHost.liveInputs.
    - GREEN: assigning a second controller updates ControllerHost, loads its stable-key mapping, and refreshes AdapterHost.
    - GREEN: remapping one adapter input persists the changed ControllerMapping, refreshes AdapterHost, and leaves all other mappings unchanged.
    - GREEN: reset persists ControllerMapping.defaultMapping for the active model key and refreshes AdapterHost.
    - Negative control: no assigned controller keeps remap/reset/test unavailable while keyboard and pointer remain usable.
  </behavior>
  <action>
First expand ControllerHardwareIntegrationTests into the complete packaged-app lifecycle journey described below and run it against the untouched production source. Preserve the result-bundle failure showing that the virtual controller enumerates but a press cannot change ControllerTestView/liveInputs and the settings callbacks cannot satisfy the journey. This failing production-path oracle must exist before modifying ControllerHost, ReadinessSheetView, ControllerSettingsView, ControllerTestView, or AppEnvironment; unit tests or FakeControllerInputSource do not substitute for this RED gate.

Then extend ControllerInputSource with one typed input-event callback carrying controller runtime id, the frozen input name vocabulary, and active state. In GCControllerInputSource, install handlers for the initial inventory and later connects; normalize buttons, shoulders, Menu/Options, and d-pad axes with one threshold, de-duplicate repeated state, tear handlers down on disconnect, and clear device-owned active inputs. Route those events through ControllerHost.reportInput and make fake-source unit tests exercise the same callback contract.

Make ReadinessSheetView own the presentation and current-mapping state required by the already-defined ControllerSettingsView interface. At the production call site, pass all four callbacks explicitly: assignment delegates to ControllerHost.assign; remap loads the active stable-key mapping, calls remapping(adapterInput:to:), saves it, updates presented state, and invokes refreshActiveControllerMapping; reset calls ControllerMappingStore.reset, updates state, and refreshes; live test presents ControllerTestView bound to the assigned descriptor and liveInputs.

Keep AppEnvironment as the composition owner and add narrow methods only where direct store access from the view would duplicate transaction/error handling. Surface persistence errors as calm actionable text; do not silently leave UI and AdapterHost disagreeing. Per D-14, every control remains operable by keyboard and pointer and has stateful accessibility labels/values. Do not add a second mapping store or a controller-only navigation state.

Complete the already-red lifecycle journey using two devices to prove assignment, then open live test, inject press/release, remap a binding, observe adapter-facing evidence generated by the real refreshActiveControllerMapping route, relaunch over the preserved deterministic root, and finally reset plus relaunch. The UI test target must not import app internals or mutate SQLite directly. Add every new identifier to run-mac-verification.sh's UI required-test list and cross-layer UAT allowlist, and add a result-verifier meta-test showing that deleting one required identifier or marking the suite skipped is rejected.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; xcodebuild test -scheme Playstead -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -testPlan Unit -only-testing:PlaysteadTests/ControllerHostTests &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing ControllerHardwareIntegrationTests/testProductionControllerLifecycleAssignsTestsRemapsResetsAndPersists ControllerHardwareIntegrationTests/testDetachRecoveryIsNonModalAndReconnectRestoresInput &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-contracts</automated>
  </verify>
  <done>The pre-implementation packaged-app oracle is recorded red, then G-03-8 and G-03-9 pass as named, non-skipped recurring E2E evidence through production GameController input and every lifecycle call site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller events -> SwiftUI state | Untrusted device input changes visible state and mappings. |
| Settings UI -> SQLite mapping | User actions write persistent emulator input configuration. |
| Mapping store -> AdapterHost | Persistent configuration affects an external emulator process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-18-01 | Tampering | Remap/reset callbacks | medium | mitigate | Validate adapter/input names against frozen vocabularies, persist atomically, and refresh AdapterHost only from the committed mapping. |
| T-03-18-02 | Denial of Service | Recovery banner | medium | mitigate | Non-modal presentation, no disabled ancestor, bounded state changes, and E2E keyboard/pointer assertions while visible. |
| T-03-18-03 | Spoofing | Controller assignment | low | mitigate | Assignment accepts only a currently enumerated runtime id and E2E uses two devices to prove selection. |
| T-03-18-04 | Repudiation | Mapping persistence | low | accept | Local single-user configuration has no audit requirement; deterministic relaunch tests and visible current mapping provide sufficient traceability. |
</threat_model>

<verification>
- Focused unit tests remain green for host/mapping behavior.
- Focused UI tests exercise recovery and lifecycle through the packaged app and virtual HID.
- Contract self-tests reject missing required identifiers and skipped/zero-discovery suites.
</verification>

<success_criteria>
- Every behavior named by G-03-8 and G-03-9 is executable without physical hardware.
- Controller loss never disables keyboard or pointer interaction.
- Mapping state is stable and reaches the adapter path, not merely the settings labels.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-18-SUMMARY.md` when done.
</output>
