---
phase: 03-mac-offline-play-vertical-slice
plan: 19
type: execute
gap_closure: true
gap_ids: [G-03-10]
wave: 3
depends_on: ["03-18"]
files_modified:
  - playstead-mac/Playstead/Controller/ControllerNavigationDispatcher.swift
  - playstead-mac/Playstead/Controller/ControllerHost.swift
  - playstead-mac/Playstead/Library/LibraryShellView.swift
  - playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - playstead-mac/PlaysteadUITests/SurfaceAccessibilityTests.swift
  - playstead-mac/docs/ACCESSIBILITY.md
  - playstead-mac/scripts/ci/tests/plan-05-surface-contract-test.sh
autonomous: true
requirements: [PLAY-04, QUAL-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Virtual d-pad reports move focus or selection through real focus sections, LB/RB cycle the canonical sidebar entries, and Menu opens the production context action for the focused item per D-14."
    - "Controller navigation is proven over an independently declared surface/action matrix; the test fails if its matrix and the production surface inventory drift apart."
    - "Keyboard-only activation and the live accessibility-tree property audit remain required and green alongside controller navigation."
    - "Objective accessibility acceptance is limited to falsifiable properties: reachability, activation, roles, names, values, hierarchy, focus, non-color state, motion, text/contrast/hit-region bounds, and public audit categories."
    - "VoiceOver pronunciation, rotor utility, sentence quality, comprehension, and navigation intuition remain explicit non-gating non-claims; no automated result is described as proving them."
  artifacts:
    - path: "playstead-mac/Playstead/Controller/ControllerNavigationDispatcher.swift"
      provides: "Production mapping from normalized controller events to focus/sidebar/context actions"
    - path: "playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift"
      provides: "Virtual-HID controller navigation acceptance matrix"
    - path: "playstead-mac/docs/ACCESSIBILITY.md"
      provides: "Objective acceptance boundary and subjective non-claims"
  key_links:
    - "GCController event -> ControllerHost -> ControllerNavigationDispatcher -> SwiftUI focus/selection/context action"
    - "Independent test matrix -> accessibility identifiers -> actual focus/selection/action changes"
    - "SurfaceAccessibilityTests + controller suite -> run-mac-verification required tests -> UAT evidence contract"
---

<objective>
Complete G-03-10 with real controller navigation and an honest zero-human accessibility acceptance boundary.

The existing live accessibility suite already proves strong keyboard and accessibility-tree properties. It does not send controller input, and no production d-pad/shoulder dispatcher exists. Add that dispatcher and prove it through the shared virtual-HID path. Separately, rewrite the durable accessibility contract so machine evidence is credited only for objective properties and subjective VoiceOver experience remains explicitly unclaimed but does not gate phase completion.

Purpose: make the locked D-14 controller navigation real while preventing an automated accessibility suite from being misrepresented as human comprehension evidence.
Output: controller navigation production code, an E2E matrix, and objective/non-claim documentation plus guards.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UI-SPEC.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/debug/g-03-10-objective-accessibility.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-18-SUMMARY.md

<interfaces>
LibraryShellView owns `selection: SidebarSection?`, `presentedSurface`, and the focused shell controls. SidebarView.entries(nonEmptySystemIDs:hasUnidentified:) is the canonical D-14 order and must be the only source used for shoulder cycling.

ControllerHost receives the normalized names `dpadUp`, `dpadDown`, `dpadLeft`, `dpadRight`, `leftShoulder`, `rightShoulder`, and `buttonMenu` from 03-17. The navigation dispatcher consumes edge transitions; held/repeated values must not create duplicate sheet/context actions.

SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit already audits 20 production surfaces through real Tab/Shift-Tab/Space and public XCUITest accessibility checks. Preserve this evidence and its narrow boundary.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. This plan adds no dependency.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Navigate real production surfaces with d-pad, shoulders, and Menu through virtual HID</name>
  <files>playstead-mac/Playstead/Controller/ControllerNavigationDispatcher.swift, playstead-mac/Playstead/Controller/ControllerHost.swift, playstead-mac/Playstead/Library/LibraryShellView.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift, playstead-mac/TestPlans/UI.xctestplan</files>
  <behavior>
    - RED: d-pad and shoulder reports reach ControllerHost.liveInputs but do not change focus or sidebar selection.
    - GREEN: one d-pad edge moves focus/selection in the asserted direction inside each declared focus section; release permits the next edge.
    - GREEN: right shoulder advances and left shoulder reverses through SidebarView.entries, including system and Unidentified entries when present, with wrap behavior specified by the test.
    - GREEN: Menu opens the focused game's existing context/readiness action once; a held report cannot open duplicates.
    - Negative control: unknown inputs and inputs from an unassigned controller do not navigate.
    - Matrix control: every surface/action row names a pre-state, injected report, and observable post-state; zero rows or a missing production surface is a failure.
  </behavior>
  <action>
Write an independent controller-navigation matrix in ControllerHardwareIntegrationTests before implementation. Cover the canonical library sidebar, at least one shelf/card focus section, list/filter controls, readiness/controller settings, and a modal dismissal path. Each row must assert an actual focused identifier, selected sidebar label, or presented surface before and after the HID report; checking only that the process stayed alive is not evidence.

Create ControllerNavigationDispatcher as a MainActor production type consuming edge transitions from the assigned controller. For d-pad, route directional commands into the key window's focus system and preserve existing `.focusSection()` boundaries. For shoulders, ask LibraryShellView for SidebarView.entries and update its existing `selection`; do not duplicate the sidebar order. For Menu, invoke the existing context/readiness action associated with the focused game and de-bounce until release. Compose the dispatcher through the single AppEnvironment/LibraryShellView controller path established in 03-18, per D-14. Keyboard, pointer, and accessibility actions must continue to use their existing paths.

Keep the class selected in UI.xctestplan. Test both accepted input and negative controls using VirtualGamepad; do not call the dispatcher directly from XCUITest.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing ControllerHardwareIntegrationTests/testControllerNavigationCoversEveryDeclaredSurfaceAndAction ControllerHardwareIntegrationTests/testUnassignedAndUnknownInputsCannotNavigate</automated>
  </verify>
  <done>D-pad, shoulder, and Menu navigation work through the production GameController path on the independently inventoried surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin objective accessibility evidence and subjective VoiceOver non-claims in recurring guards</name>
  <files>playstead-mac/PlaysteadUITests/SurfaceAccessibilityTests.swift, playstead-mac/docs/ACCESSIBILITY.md, playstead-mac/scripts/ci/tests/plan-05-surface-contract-test.sh, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - RED: the current static guard requires the old controller-hardware blocker language and has no positive contract for the new objective/non-claim boundary.
    - GREEN: every inventoried surface has keyboard reachability/activation plus live roles, names, values, hierarchy, finite frames, focus containment/restoration, non-color distinctions, reduced-motion behavior, and public audit checks.
    - GREEN: the controller-navigation identifier and keyboard/live-tree identifier are both required in CI; neither substitutes for the other.
    - Negative control: deleting any objective property category makes the contract self-test fail.
    - Negative control: text that claims automated proof of pronunciation, rotor usefulness, comprehension, sentence quality, or navigation intuition makes the contract self-test fail.
    - Non-claim control: docs explicitly state those subjective qualities are not established by the automated suite and are non-gating product-research questions.
  </behavior>
  <action>
Refactor docs/ACCESSIBILITY.md into two explicit sections: the objective acceptance contract and the experiential non-claims. Credit SurfaceAccessibilityTests only for machine-observable properties and credit ControllerHardwareIntegrationTests only for d-pad/shoulder/Menu mechanics. State plainly that pronunciation, rotor utility, sentence quality, comprehension, and navigation intuition are not established by automation and are not completion gates. Do not relabel those outcomes as automated or infer them from AX labels.

Update plan-05-surface-contract-test.sh to require the objective property inventory, both required test identifiers, and the explicit non-claim language. Add mutation cases that remove one objective category and that insert an overclaim; both must fail. Extend SurfaceAccessibilityTests only where the diagnosis identified an objective property without a live-tree oracle. Register controller-navigation tests in the recurring UI layer because focus routing is a lasting regression risk; documentation-only checks stay in the existing contract-self-test CI step.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit ControllerHardwareIntegrationTests/testControllerNavigationCoversEveryDeclaredSurfaceAndAction &amp;&amp; scripts/ci/tests/plan-05-surface-contract-test.sh &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-contracts</automated>
  </verify>
  <done>Objective accessibility and controller mechanics have recurring automated evidence, while subjective VoiceOver experience remains an explicit non-gating non-claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller input -> app navigation | Untrusted device events can move focus or invoke actions. |
| Accessibility tree -> acceptance record | Machine-readable semantics are used as evidence and can be overstated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19-01 | Elevation of Privilege | Menu/context dispatch | medium | mitigate | Only assigned-controller edge events invoke existing focused-item actions; held/unknown/unassigned inputs are negative-tested. |
| T-03-19-02 | Denial of Service | D-pad/shoulder repeats | medium | mitigate | Edge de-bounce, release re-arm, and bounded canonical selection transitions. |
| T-03-19-03 | Repudiation | Accessibility evidence claims | medium | mitigate | Evidence-boundary guard rejects missing objective categories and subjective overclaims; UAT reconciliation is deferred to 03-21. |
| T-03-19-04 | Information Disclosure | AX labels/values | low | accept | Tests inspect only synthetic profile content and public semantic properties; no filenames, tokens, hashes, or user content enter evidence. |
</threat_model>

<verification>
- Virtual-HID navigation matrix passes non-skipped against the packaged app.
- Existing keyboard/live-tree aggregate remains green.
- Static contract mutation tests reject missing objective coverage and subjective overclaims.
- CI required-test lists include both controller and keyboard/AX evidence.
</verification>

<success_criteria>
- G-03-10's controller mechanics are implemented and objectively verified.
- Zero human accessibility UAT is required for phase completion.
- No output claims subjective VoiceOver comprehension.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-19-SUMMARY.md` when done.
</output>
