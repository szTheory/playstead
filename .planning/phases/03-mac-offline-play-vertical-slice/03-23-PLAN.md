---
phase: 03-mac-offline-play-vertical-slice
plan: 23
type: execute
gap_closure: true
gap_ids: [G-03-9]
wave: 4
depends_on: ["03-22"]
files_modified:
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/Playstead/Readiness/ReadinessSheetView.swift
  - playstead-mac/Playstead/Controller/ControllerSettingsView.swift
  - playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
autonomous: true
requirements: [PLAY-04, QUAL-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The shipped ControllerSettingsView explicitly wires assign, remap, reset, and live test; no shipped callback relies on a default no-op."
    - "Assignment loads the stable model-key mapping; remap and reset persist atomically, refresh AdapterHost from committed state, and survive relaunch."
    - "The complete packaged lifecycle oracle recorded red in 03-22 becomes green and is required fail-closed by recurring CI."
  artifacts:
    - path: "playstead-mac/Playstead/Readiness/ReadinessSheetView.swift"
      provides: "Production lifecycle callback composition and current mapping state"
    - path: "playstead-mac/Playstead/App/PlaysteadApp.swift"
      provides: "Narrow composition-owned mapping transactions and AdapterHost refresh"
    - path: "playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift"
      provides: "Two-device assign/remap/reset/relaunch persistence journey"
  key_links:
    - "ControllerSettingsView callbacks -> ControllerMappingStore commit -> AppEnvironment.refreshActiveControllerMapping -> AdapterHost"
    - "Stable controller model key -> persisted mapping -> relaunch presentation"
---

<objective>
Finish G-03-9 by wiring assignment, remap, reset, persistence, and adapter refresh into the shipped controller settings lifecycle.

Purpose: turn the complete RED oracle from 03-22 green without mixing persistence work into the recovery or input-bridge plans.
Output: explicit production callbacks, atomic mapping state, adapter refresh, relaunch proof, and recurring evidence guards.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/debug/g-03-9-controller-lifecycle.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-22-SUMMARY.md

<interfaces>
ControllerMappingStore exposes mapping(forControllerProductID:), save(_:), and reset(controllerProductID:). AppEnvironment.refreshActiveControllerMapping() is the sole existing connection from committed mapping state to AdapterHost.

ControllerSettingsView accepts onAssign, onRemap, onReset, and onOpenTestView. Plan 03-22 wires live test and records the complete lifecycle test red before production changes; this plan must finish that exact oracle rather than replace it with a narrower unit test.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. No dependency is installed.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete assignment, remap, reset, adapter refresh, and relaunch persistence</name>
  <files>playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/Readiness/ReadinessSheetView.swift, playstead-mac/Playstead/Controller/ControllerSettingsView.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift</files>
  <behavior>
    - GREEN: assigning a second enumerated controller updates ControllerHost, loads its stable-key mapping, and refreshes AdapterHost.
    - GREEN: remapping one adapter input persists the changed mapping, refreshes AdapterHost, and leaves other bindings unchanged.
    - GREEN: reset persists ControllerMapping.defaultMapping for the active stable key and refreshes AdapterHost.
    - GREEN: remap and reset states survive relaunch over the preserved deterministic root.
    - Negative control: no assigned controller keeps remap/reset/test unavailable while keyboard and pointer remain usable per D-14.
    - Failure control: a persistence error leaves UI and AdapterHost on the last committed mapping and renders calm actionable text.
  </behavior>
  <action>
Use the complete packaged-app lifecycle test and preserved RED result from 03-22 as the binding starting oracle. Make ReadinessSheetView own current-mapping and presentation state. At the only production ControllerSettingsView call site, pass every callback explicitly: assignment delegates to ControllerHost.assign, loads the stable-model-key mapping, and refreshes AdapterHost; remap derives a changed mapping, persists it atomically, updates presented state, and refreshes; reset persists defaults, updates state, and refreshes. Keep AppEnvironment as composition owner and add narrow transaction methods only where direct view access would duplicate store/error handling.

Surface persistence failures without allowing UI and AdapterHost to diverge. Do not add another mapping store or controller-only navigation model. Finish the two-device journey through the packaged app: assign, open live test, inject press/release through the 03-22 bridge, remap, observe adapter-facing evidence from the real refresh route, relaunch, reset, and relaunch again. The UI target must not import app internals or mutate SQLite directly. Preserve keyboard/pointer/accessibility operability per D-14.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing ControllerHardwareIntegrationTests/testProductionControllerLifecycleAssignsTestsRemapsResetsAndPersists</automated>
  </verify>
  <done>The exact lifecycle oracle recorded red in 03-22 passes through production callbacks, committed mappings, AdapterHost refresh, and relaunch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require complete lifecycle and persistence evidence</name>
  <files>playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - The complete lifecycle identifier is required once, non-skipped, in the UI evidence layer.
    - Removing assignment, remap, reset, adapter-refresh, or relaunch evidence from its structured result makes contract self-tests fail.
    - Unit mapping tests cannot substitute for the packaged-app lifecycle identifier.
  </behavior>
  <action>
Promote the complete lifecycle identifier already introduced in 03-22 into the required UI list and cross-layer UAT allowlist. Extend structured evidence and mutation fixtures so every lifecycle checkpoint is present and removal, skip, zero discovery, or unit-test substitution is rejected. Keep the focused live-input identifier independently required; it proves a different boundary.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-contracts</automated>
  </verify>
  <done>G-03-9 cannot pass recurring evidence without the complete packaged assignment/remap/reset/persistence journey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI -> SQLite mapping | User actions write persistent emulator input configuration. |
| Mapping store -> AdapterHost | Committed configuration affects an external emulator process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23-01 | Tampering | Remap/reset callbacks | medium | mitigate | Validate frozen names, persist atomically, and refresh only from committed mapping state. |
| T-03-23-02 | Spoofing | Controller assignment | low | mitigate | Accept only currently enumerated runtime ids and prove selection with two virtual devices. |
| T-03-23-03 | Repudiation | Mapping persistence | medium | mitigate | Structured relaunch checkpoints plus exact required packaged-app identifier. |
</threat_model>

<verification>
- The complete lifecycle journey passes through the packaged app and virtual HID.
- Persistence errors are visible and cannot create UI/adapter divergence.
- Contract mutation tests reject incomplete or fake-only lifecycle evidence.
</verification>

<success_criteria>
- G-03-9's live test, assignment, remap, reset, adapter refresh, and relaunch persistence are executable without physical hardware.
- Every shipped ControllerSettingsView callback is explicit.
- Mapping state reaches AdapterHost rather than only changing labels.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-23-SUMMARY.md` when done.
</output>
