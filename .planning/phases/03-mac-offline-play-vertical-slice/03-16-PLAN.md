---
phase: 03-mac-offline-play-vertical-slice
plan: 16
type: execute
gap_closure: true
wave: 2
depends_on: ["03-15"]
files_modified:
  - playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex
  - playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs
  - playstead-mac/Playstead/Sync/Outbox.swift
  - playstead-mac/Playstead/Sync/CurationIntent.swift
  - playstead-mac/PlaysteadTests/CurationTests/OutboxTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LIBR-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A malformed availability report body — a non-list `entries`, or a list holding a non-map element — is refused with 422 in the same problem shape the sibling endpoints use, instead of raising into a generic 500."
    - "A well-formed report is unaffected by the new guard: the existing controller tests, including the absent-`entries` case, keep their current status codes."
    - "Only the newest full-replacement availability report is ever delivered: enqueuing a new one removes any still-pending or backed-off one, so a backed-off earlier report can no longer land after a later one that already succeeded."
    - "Superseding is scoped to the one intent kind for which newest-wins is actually true, and every other outbox kind still drains in creation order with no entry dropped."
    - "An availability report already in flight is never superseded, because a request on the wire cannot be recalled and is ordered ahead of the report enqueued after it."
    - "LIBR-02 is marked complete only after the Mac-client-driven tests from 03-15 were observed passing in this run, and never on the strength of a test that seeds the server read model."
    - statement: "A device that goes offline for a long period accumulates no unbounded backlog of superseded availability rows on disk."
      verification: backstop
  artifacts:
    - playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex
    - playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs
    - playstead-mac/Playstead/Sync/Outbox.swift
    - playstead-mac/PlaysteadTests/CurationTests/OutboxTests.swift
  key_links:
    - "request body -> AvailabilityController.replace/2 shape guard -> FallbackController -> 422 :validation_failed"
    - "CurationIntentKind.availabilityReport -> supersedesPending -> Outbox.enqueue transaction -> at most one pending availability report row"
    - "03-15's observed Mac-client test results -> REQUIREMENTS.md LIBR-02 checkbox and traceability row"
  prohibitions:
    - "Never flip a requirement checkbox in the same task that writes or runs the test intended to prove it."
    - "Never edit this phase's VERIFICATION.md top-level status field or any gap verdict; only an independent re-verification may change those."
    - "Never describe a hardware-blocked, third-party-blocked, or experiential item as automatically verified — this phase has already had overclaims corrected twice."
    - "Never silently widen a validation guard so that a body which used to be accepted starts being refused."
---

<objective>
Close the two robustness findings the re-verification recorded alongside LIBR-02, then — and only then — flip the requirement.

WR-04: `AvailabilityController.replace/2` hands `entries` straight to a context function whose only guard is `is_list`. A non-list, or a list holding a non-map, raises uncaught into a generic 500 where every sibling endpoint returns 422. Task 1 adds the shape guard and matches the sibling problem shape rather than inventing one.

WR-05: `Outbox.listPending`'s backoff skip, combined with a fresh report row enqueued on every `syncNow()` pass, lets an earlier backed-off full-replacement report be delivered after a later one that already succeeded — temporarily reintroducing stale facts. Only the newest full-replacement report is ever meaningful. Task 2 supersedes pending rows of that kind at enqueue time.

Task 3 flips `REQUIREMENTS.md`'s LIBR-02 marking, gated on `03-15`'s Mac-client-driven tests having been observed passing in this same run. It touches nothing else: `03-VERIFICATION.md`'s verdicts and status stay for an independent re-verification, and no hardware- or human-blocked item is reclassified.

Purpose: remove the two ways the availability path can misbehave, and make the requirement record true rather than convenient.
Output: a 422 guard with sibling-matching shape, a newest-wins outbox rule scoped to one intent kind, and one gated documentation flip.

**Scope fence.** Untouched: physical controller connect/disconnect/reconnect (`PLAY-04`, UAT items 8 and 9 and item 10's blocked sub-record), live interactive launch of the pinned mGBA adapter (UAT item 7), the experiential VoiceOver and visual-fidelity review (item 10's blocked sub-record), acceptance of real legally-owned BIOS bytes (UAT item 13, `partial`), and `LIBR-05`'s human-judgment UX review. None is advanced by this plan and none is claimed. `PLAY-04` and `LIBR-05` stay `Pending`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-REVIEW-GAPS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-15-PLAN.md
</context>

<api_coverage_decision>
detected: false, by the same detector run recorded in `03-15-PLAN.md`. The only endpoint in scope is this project's own `PUT /api/v1/devices/me/availability`; no external API, SDK, or third-party service is integrated.
</api_coverage_decision>

<assumption_delta_decision>
decision: no-change. This plan adds a shape guard and a delivery-ordering rule over an already-decided entry shape; it introduces no new modeled noun, no new optionality, and no new cardinality.
</assumption_delta_decision>

<package_legitimacy_gate>
Inert. No Swift Package Manager, mix, npm, pip, or cargo dependency is added and there is no install task to audit.
</package_legitimacy_gate>

<schema_push_gate>
SCHEMA_PUSH_REQUIRED=false. No Payload/Prisma/Drizzle/Supabase/TypeORM path is in scope and no migration is added. Task 2 writes no new column: superseded rows are removed, not restated, so `outbox_entries`' existing shape is sufficient.
</schema_push_gate>

<planner_assumptions>
1. **FLAGGED — an absent `entries` key must keep its current meaning.** `replace/2` today reads `Map.get(params, "entries", [])`, so a body with no `entries` key is a valid empty full replacement that clears the device's rows. WR-04 asks for a guard on malformed shapes, not a new required field. Turning absence into a 422 would be a silent behavior widening that could break a real client mid-flight, so the guard rejects *wrong shapes* and leaves *absence* exactly as it is. If the existing controller test asserts a status for the absent-key case, that assertion must still pass unchanged — and the acceptance criteria require it.
2. **FLAGGED — probe row LIBR-02 is `unclassified`/`unresolved`, carried forward.** The deterministic edge probe ran against LIBR-02 only, because in gap-closure mode the contracted requirement set is the unresolved set from `03-VERIFICATION.md` rather than the phase's fourteen already-closed IDs. That narrowing is recorded rather than silent. The single probe row classified `unclassified`, so it is surfaced here as an explicit flagged assumption — not auto-resolved with a backstop, not dropped. Task 3's clause-by-clause coverage judgement is what stands in for it, and an independent re-verification must re-derive that judgement rather than inherit it.
3. **Supersede-on-enqueue is chosen over a newest-wins filter inside `listPending`; the alternative is recorded, not done.** `listPending` is a generic read used by `OutboxWorker` and by the rejected-intents surface, and a kind-aware filter there would embed one intent's semantics into a shared query while still leaving the stale rows on disk to accumulate for an offline device. Superseding at enqueue time happens once, inside the transaction that already exists, keeps `listPending` generic, and bounds the row count. Only one of the two is implemented, per the verification's own framing.
4. **Superseded rows are deleted, not re-stated.** `.availabilityReport` is the one `CurationIntentKind` documented as carrying no local optimistic write (see its own doc comment in `CurationIntent.swift`), so removing an unsent row leaves nothing to undo and no user-visible mutation orphaned. The executor confirms that `applyOptimistically` is a no-op for this kind before deleting; if it is not, the task halts and reports rather than deleting. Deletion also avoids adding an `OutboxEntryState` case, which `Outbox.rows`' decoder would map to `.pending` on any older build that read the row back.
5. **`.inFlight` rows are never superseded.** A request already on the wire cannot be recalled, and the newer row is created after it, so `listPending`'s creation-order drain already delivers them in the right order. Superseding only `pending` rows is the minimal correct scope.
6. **This plan advances neither `PLAY-04` nor `LIBR-05`.** Physical controller hardware does not exist in this environment and `LIBR-05`'s remaining item is human judgement. Stated here so their absence from Task 3's flip is a recorded decision rather than an omission.
7. **The phase's terminal status is not this plan's to set.** Even with LIBR-02 flipped, `03-VERIFICATION.md`'s own gate rules make a human-needed status correct while environment-blocked items remain, and an independent re-verification — not this plan — is what may set it.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-04 — refuse a malformed availability report with 422 instead of raising into a 500</name>
  <precondition>The server test database is reachable (`cd playstead-server &amp;&amp; mix ecto.create --quiet` succeeds or reports the database already exists).</precondition>
  <files>playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex, playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs</files>
  <read_first>
    playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex (the whole file — `replace/2`, its `Idempotency.execute/4` call shape, the existing `:too_many_entries` 422 branch, and the `action_fallback` declaration);
    playstead-server/lib/playstead_web/controllers/api/v1/imports_controller.ex around lines 110..132 (`precheck/2`'s guard-clause-plus-catch-all shape and its exact `{:error, {:validation_failed, ...}}` return — this is the sibling pattern to match, not to redesign);
    playstead-server/lib/playstead_web/controllers/api/v1/fallback_controller.ex (how `{:error, {:validation_failed, detail}}` becomes a 422 problem response, and the `Ecto.Changeset` branch beside it);
    playstead-server/lib/playstead_web/error_codes.ex (the `validation_failed` code and its status);
    playstead-server/lib/playstead/availability.ex (`replace_for_device/2`'s `is_list` guard, the `@max_entries` cap and its `:too_many_entries` return, and `normalize_entry/1`, which is where a non-map element actually raises);
    playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs (the whole file — the paired-device setup, the `Idempotency-Key` header helper, and every existing status assertion this guard must not disturb, including any assertion about a body with no `entries` key)
  </read_first>
  <behavior>
    - A body whose `entries` is a string returns 422 with the `validation_failed` problem code, not 500.
    - A body whose `entries` is a map rather than a list returns 422.
    - A body whose `entries` is a list containing a string element returns 422.
    - A body whose `entries` is a list containing a number element returns 422.
    - A body with no `entries` key keeps exactly the status it returns today.
    - A body whose `entries` is an empty list keeps exactly the status it returns today.
    - A well-formed report of maps still returns 200 and still writes its rows.
    - The 422 response body carries the same problem shape a sibling endpoint's validation failure carries — same content type, same code field, same field names.
  </behavior>
  <action>
Add the shape guard in `replace/2`, before `entries` reaches `Availability.replace_for_device/2`. Follow `ImportsController.precheck/2`'s shape: a matching clause for the valid case and a catch-all that returns an `{:error, {:validation_failed, detail}}` tuple for the fallback controller to render. Do not hand-build a response — `action_fallback` is already declared on this controller and `FallbackController` already renders that tuple as a 422 with the `validation_failed` code.

The guard accepts `entries` when it is a list and every element is a map. It rejects a non-list and rejects a list with any non-map element. It does not change what an absent `entries` key means: that path currently defaults to an empty list and performs a valid empty full replacement, and the existing controller tests' status for that case must remain unchanged. Read those tests before writing the guard so you know what they assert.

Write the detail string in the sibling's voice — a short sentence naming the field and the expected shape, matching the phrasing style of `ImportsController.precheck/2`'s message rather than echoing an internal function name or an Elixir type name at the client.

Leave the existing `:too_many_entries` branch exactly as it is: the 5000-entry cap is already enforced in the context and already renders a 422, and duplicating it in the controller would create two places to keep in agreement.

Add one test per `behavior` bullet to `availability_controller_test.exs`, reusing that file's existing device-authentication and `Idempotency-Key` setup verbatim. Give each malformed-shape test its own assertion on its own line with a name that states the shape it sends, so a CI failure is identifiable from `file:line` alone — this repository's evidence pipeline keeps line numbers and drops assertion messages, so two shapes must never share an assertion line. Write the tests first and watch the non-list and non-map-element cases fail with a 500 before adding the guard; record that observed pre-fix status in the SUMMARY, because it is the evidence that the guard fixes a real defect rather than a hypothetical one.
  </action>
  <acceptance_criteria>
    - `cd playstead-server &amp;&amp; mix test test/playstead_web/controllers/api/v1/availability_controller_test.exs` exits zero with `0 failures` and a test count strictly greater than the count that file reported before this task.
    - Sending `entries` as a string, as a map, as a list containing a string, and as a list containing a number each produce status 422 — asserted on four separate lines in four separately named tests.
    - The 422 response body's error code field equals the value `validation_failed`, matching `error_codes.ex`.
    - The pre-existing assertions in `availability_controller_test.exs` are unchanged: `git diff -- playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs` shows added lines only, and no removed or modified assertion line in the tests that existed before this task.
    - `grep -c 'action_fallback' playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex` outputs at least `1`, and the new rejection path returns an error tuple rather than calling `send_problem` directly.
    - `grep -c 'too_many_entries' playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex` outputs at least `1` — the existing cap branch survives.
    - `cd playstead-server &amp;&amp; mix test` exits zero with a total at or above 1065 and `0 failures`.
  </acceptance_criteria>
  <reversibility rating="reversible">A guard clause and a catch-all in one controller function, with the sibling's error tuple; removing it restores the prior behavior in one edit.</reversibility>
  <verify>
    <automated>cd playstead-server &amp;&amp; mix test test/playstead_web/controllers/api/v1/availability_controller_test.exs</automated>
    <fails_when>non-zero exit — including 127 if `mix` is absent from PATH, which must be read as failure and never as a clean run — or a summary line that does not contain `0 failures`, or one reporting `0 tests`</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; grep -c 'action_fallback' playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex</automated>
    <fails_when>an output of `0`, meaning the rejection path no longer routes through the shared fallback and the problem shape can silently drift from the sibling endpoints'</fails_when>
    <automated>cd playstead-server &amp;&amp; mix test</automated>
    <fails_when>non-zero exit, or a summary line without `0 failures`, or a reported total below 1065 — the baseline recorded in `03-VERIFICATION.md` at HEAD 33a06a1; a lower total means tests disappeared rather than passed</fails_when>
  </verify>
  <done>A malformed `entries` payload is refused with 422 in the sibling endpoints' problem shape, well-formed and absent-key bodies behave exactly as before, and the pre-fix 500 was observed and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-05 — only the newest full-replacement availability report is ever delivered</name>
  <precondition>A macOS machine with the Xcode toolchain on PATH (`xcodebuild -version` succeeds) and the `Playstead` scheme resolvable. Without it the Mac layer cannot be built or tested and this task halts rather than proceeding on unbuilt code.</precondition>
  <files>playstead-mac/Playstead/Sync/Outbox.swift, playstead-mac/Playstead/Sync/CurationIntent.swift, playstead-mac/PlaysteadTests/CurationTests/OutboxTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <read_first>
    playstead-mac/Playstead/Sync/Outbox.swift (the whole file — `enqueue(_:at:)`'s single `localStore.transaction` block and its `INSERT INTO outbox_entries`, `listPending(at:)`'s `state = 'pending' AND (next_retry_at IS NULL OR next_retry_at <= ?)` clause and its `created_at ASC, rowid ASC` ordering, `markPendingForRetry`'s backoff scheduling, `markInFlight`, `markDone`, the `OutboxEntryState` cases, and `rows(where:orderBy:params:)`' decoder);
    playstead-mac/Playstead/Sync/CurationIntent.swift (the `CurationIntentKind` enum and its raw values, the `.availabilityReport` case's doc comment stating it carries no local optimistic write, and `applyOptimistically` — confirm by reading that it is genuinely a no-op for this kind before deleting any row);
    playstead-mac/Playstead/Sync/OutboxWorker.swift (how `listPending` is drained, how a failure routes to `markPendingForRetry`, and where `onEnqueue` fires);
    playstead-mac/Playstead/Cache/AvailabilityReporter.swift (`reportAll`, the one caller that enqueues this kind, invoked on every `syncNow()` pass);
    playstead-mac/PlaysteadTests/CurationTests/OutboxTests.swift (the whole file — the existing enqueue/drain/backoff/quarantine tests whose behavior must not change, and the fixtures the new tests reuse);
    playstead-mac/PlaysteadTests/CacheTests/AvailabilityReporterTests.swift lines 200..250 (the existing assertions counting `.availabilityReport` rows in `listAll()` — these constrain what superseding may do);
    playstead-mac/scripts/ci/run-mac-verification.sh around lines 1266..1290 (the unit-layer required-test list)
  </read_first>
  <behavior>
    - Enqueuing a second availability report while the first is still `pending` leaves exactly one availability-report row, and it is the second one.
    - Enqueuing a second availability report while the first is `pending` with a future retry time — the backed-off case WR-05 names — leaves exactly one availability-report row, and it is the second one; the backed-off row is not delivered afterwards.
    - Enqueuing a second availability report while the first is `inFlight` leaves both rows; a request already on the wire is never superseded.
    - Enqueuing a second favorite intent while the first is pending leaves both rows; superseding applies to the one kind for which newest-wins is true and to no other.
    - Draining after a supersede sends exactly one availability report, and the body sent is the newer one's.
    - Every existing test in `OutboxTests` and in `AvailabilityReporterTests` still passes unchanged.
  </behavior>
  <action>
Declare the rule where it is visible next to the kind rather than buried in a query. Add a computed boolean property on `CurationIntentKind` — name it `supersedesPending` — that is true only for `.availabilityReport` and false for every other case. Write it as an exhaustive switch, not a default branch, so a future kind cannot inherit newest-wins by accident. Document on that property why this kind alone is newest-wins: the report is a full replacement of the device's entire fact set, so an older one carries no information the newer one lacks.

In `Outbox.enqueue(_:at:)`, inside the transaction that already exists and before the insert, delete every row whose `kind` matches the incoming intent's kind and whose `state` is `pending`, when and only when that kind's `supersedesPending` is true. Scope the delete by kind and by pending state — never by state alone, and never including `in_flight`, `rejected`, or `quarantined`. Doing it inside the existing transaction is what makes the supersede and the insert atomic: a crash between them must not be able to leave zero rows.

Before implementing the delete, confirm by reading `CurationIntent.applyOptimistically` that `.availabilityReport` performs no local write. If it does perform one, stop and report in the SUMMARY rather than deleting a row whose local mutation would be orphaned.

Leave `listPending` untouched. The alternative fix — a newest-wins filter inside that query — is deliberately not taken: it would put one intent kind's semantics into a read shared by the worker and the rejected-intents surface, and would leave superseded rows accumulating on disk for an offline device. Implement one of the two, not both.

Add one test per `behavior` bullet to `OutboxTests`, each with its own name and its own assertion line, so a CI failure is identifiable from `file:line` alone. For the backed-off case, drive the first row through `markPendingForRetry` so its retry time is genuinely in the future, then enqueue the second and assert the row count and the surviving row's identity — do not simulate the backoff by writing a timestamp by hand, since the point is that the real backoff path is what strands the row. For the drain assertion, assert on the body actually sent, not only on the row count, so a supersede that keeps the newer row but sends the older payload cannot pass.

Suggested identifiers, to be registered verbatim in the harness:
  `test_secondAvailabilityReport_supersedesThePendingFirstOne`
  `test_secondAvailabilityReport_supersedesABackedOffFirstOne`
  `test_secondAvailabilityReport_leavesAnInFlightFirstOneAlone`
  `test_secondFavoriteIntent_isNotSupersededBecauseOnlyReportsAreNewestWins`
  `test_drainAfterSupersede_sendsOnlyTheNewerReportBody`

Register every one of them in `playstead-mac/scripts/ci/run-mac-verification.sh`'s unit-layer `--required-test` list, in the same `PlaysteadTests.OutboxTests/testMethodName` form as the entries already there.
  </action>
  <acceptance_criteria>
    - `grep -c 'supersedesPending' playstead-mac/Playstead/Sync/CurationIntent.swift` outputs at least `1` and the same grep against `playstead-mac/Playstead/Sync/Outbox.swift` outputs at least `1` — the rule is declared beside the kind and consumed at enqueue.
    - `grep -v '^[[:space:]]*//' playstead-mac/Playstead/Sync/Outbox.swift | grep -c 'listPending'` outputs the same number it did before this task: the alternative fix was not also applied.
    - `git diff -- playstead-mac/Playstead/Sync/Outbox.swift` shows no change inside the `listPending(at:)` function body.
    - `grep -c 'OutboxTests/test_secondAvailabilityReport_supersedesABackedOffFirstOne' playstead-mac/scripts/ci/run-mac-verification.sh` outputs at least `1`, and the same holds for each of the other four new identifiers.
    - The Mac test log written by this task's verify command reports a non-zero executed-test count for `OutboxTests` and contains no `Executed 0 tests` line.
    - Removing the kind predicate from the delete — making it supersede every pending row regardless of kind — makes `test_secondFavoriteIntent_isNotSupersededBecauseOnlyReportsAreNewestWins` fail. The executor verifies this by making the change, running the suite, observing that specific test fail, and reverting — the observed failure recorded verbatim in the SUMMARY with its test name and line.
    - `AvailabilityReporterTests` still passes unchanged, including its existing assertions that count availability-report rows in `listAll()`.
  </acceptance_criteria>
  <reversibility rating="reversible">One computed property and one scoped delete inside an existing transaction; the alternative `listPending` filter remains available and the choice is recorded in `planner_assumptions` item 3 rather than buried.</reversibility>
  <verify>
    <automated>cd playstead-mac &amp;&amp; mkdir -p build &amp;&amp; xcodebuild test -scheme Playstead -destination 'platform=macOS' -only-testing:PlaysteadTests/OutboxTests -only-testing:PlaysteadTests/AvailabilityReporterTests > build/outbox-supersede-tests.log 2>&amp;1 || { tail -60 build/outbox-supersede-tests.log; exit 1; }</automated>
    <fails_when>non-zero exit — including 127 if `xcodebuild` is absent from PATH, which must be read as failure and never as a clean run — or the tailed log containing `TEST FAILED` or reporting the scheme or either suite as not found</fails_when>
    <automated>cd playstead-mac &amp;&amp; ! grep -q 'Executed 0 tests' build/outbox-supersede-tests.log</automated>
    <fails_when>non-zero exit, meaning the log contains an `Executed 0 tests` line or the log file is missing — the filter matched nothing and the run proved nothing</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; grep -c 'supersedesPending' playstead-mac/Playstead/Sync/Outbox.swift</automated>
    <fails_when>an output of `0`, meaning the property was declared but never consulted at enqueue, leaving the stale-report race exactly as WR-05 described it</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; bash playstead-mac/scripts/ci/run-mac-verification.sh --help</automated>
    <fails_when>non-zero exit, including 127 if the script is missing or not executable — the harness that enforces the required-test list must itself be present and runnable, since an absent gate is the fail-open mode this project has already found four times</fails_when>
  </verify>
  <done>Enqueuing an availability report removes any pending or backed-off predecessor inside the same transaction, in-flight rows and every other intent kind are untouched, `listPending` is unchanged, and five named tests pin the behavior with the harness registered to require them.</done>
</task>

<task type="auto">
  <name>Task 3: Flip LIBR-02, gated on the Mac-client-driven tests and on nothing else</name>
  <precondition>Both tasks of `03-15-PLAN.md` and Tasks 1 and 2 of this plan have been run in this session and their verify commands observed passing. This task reads those observed results; it does not re-derive them and must not run if any of them is unknown or failing.</precondition>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (the `LIBR-02` checkbox line near line 37, currently unchecked, and its traceability table row near line 134, currently `Gaps Found`; the surrounding `LIBR-01`/`LIBR-03` rows show the exact `- [x]` and `| Complete |` formatting to match);
    .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md item 4 in full (its `expected:` text is what coverage is judged against);
    .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md (the LIBR-02 gap entry, its `missing[]` array, the `human_verification` entry about the missing-dependency decision, and the top-level `status:` field — which is NOT to be touched);
    .planning/phases/03-mac-offline-play-vertical-slice/03-15-SUMMARY.md (the observed test results this task cites — written by the plan that actually ran them);
    .planning/phases/03-mac-offline-play-vertical-slice/03-14-SUMMARY.md (the prior round's clause-to-test mapping and its "Known Stubs" disclosure, so this task's mapping can be compared against it rather than written from scratch)
  </read_first>
  <action>
Judge coverage first, write second.

Read `03-UAT.md` item 4's `expected:` text clause by clause. For each clause, name the specific test that proves it — test file and test name, never a narrative. For the availability/readiness-state clause specifically, the naming test must be one that drives the Mac client: a test in `AvailabilityReporterTests` that exercises `buildEntries`, or the end-to-end test `03-15` added that PUTs the shared fixture to the real endpoint. A test that seeds the server read model directly does not count for that clause, and citing one is the exact failure this round exists to correct. Record the full clause-to-test mapping in the SUMMARY.

If any clause is uncovered, stop: `LIBR-02` stays unchecked, the traceability row stays as it is, and the SUMMARY names the uncovered clause. Under-claiming is a correct outcome here; this phase has had two overclaims corrected by independent re-verification already.

If and only if every clause is covered, and only after `03-15`'s Mac-side and server-side commands were observed passing in this run:
  - change `.planning/REQUIREMENTS.md`'s `LIBR-02` checkbox from unchecked to checked;
  - change its traceability row's status cell from `Gaps Found` to `Complete`.

Change nothing else in that file. `LIBR-05` and `PLAY-04` stay `Pending`, because a human-judgement UX review and physical controller hardware are what they are still waiting on and neither was advanced by this plan or by `03-15`.

Touch no other planning document. Do not edit `03-VERIFICATION.md` — not its top-level status, not its gap verdicts, not its `human_verification` array — because the last time an executor edited its own verdict there, an independent re-verification found the claim was not reproducible, and that is the direct reason this round exists. Do not edit `03-UAT.md`'s item 4 result or its Summary footer; `scripts/check-uat-tally.sh` recomputes that footer from the body and an independent verifier is what should move the item. Note in the SUMMARY that item 4 was deliberately left for the re-verifier, so its non-flip reads as a decision rather than an oversight.
  </action>
  <acceptance_criteria>
    - If every clause of UAT item 4 is covered: `grep -c '^- \[x\] \*\*LIBR-02\*\*' .planning/REQUIREMENTS.md` outputs `1`, and `grep -c '| LIBR-02 | Phase 3 | Complete |' .planning/REQUIREMENTS.md` outputs `1`.
    - If any clause is uncovered: both of the above output `0`, and the SUMMARY names the uncovered clause and the reason.
    - `grep -c '| LIBR-05 | Phase 3 | Pending |' .planning/REQUIREMENTS.md` and `grep -c '| PLAY-04 | Phase 3 | Pending |' .planning/REQUIREMENTS.md` each output `1` — neither was advanced, and neither row was touched. If either row's current text differs from that exact form, the executor records the actual current text in the SUMMARY and asserts it is unchanged from before this task rather than rewriting it.
    - `git diff --exit-code -- .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md` exits zero: no verification verdict and no UAT record was edited by the plan whose work they grade.
    - `git diff --stat -- .planning/REQUIREMENTS.md` shows at most two changed lines.
    - The SUMMARY contains a clause-to-test mapping in which the availability/readiness-state clause is proved by a test that drives `AvailabilityReporter.buildEntries` or the HTTP endpoint, named by file and test name.
  </acceptance_criteria>
  <reversibility rating="reversible">Two lines in one markdown file, and the gating evidence is recorded in the SUMMARY so a re-verifier can re-run the named commands and revert the flip if they do not reproduce.</reversibility>
  <verify>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; git diff --exit-code -- .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md</automated>
    <fails_when>non-zero exit, meaning the executor edited a verification verdict or a UAT record grading its own work — the precise self-grading failure that caused the prior round's closure to be rejected</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; bash scripts/check-uat-tally.sh</automated>
    <fails_when>non-zero exit, including 127 if the script is missing or not executable, or output naming any UAT file whose derived tally differs from its footer; a run that prints nothing at all also fails, since the script names each file it checks</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; grep -c '| LIBR-05 | Phase 3 | Pending |' .planning/REQUIREMENTS.md</automated>
    <fails_when>any output other than `1`, meaning a requirement this plan did not advance was reclassified anyway</fails_when>
    <automated>cd /Users/jon/projects/playstead &amp;&amp; grep -c '| PLAY-04 | Phase 3 | Pending |' .planning/REQUIREMENTS.md</automated>
    <fails_when>any output other than `1`, meaning the hardware-blocked controller requirement was marked as something it is not</fails_when>
  </verify>
  <done>`LIBR-02` is checked and marked Complete if and only if every clause of UAT item 4 maps to a named passing test and the availability/readiness clause is proved by a Mac-client-driven or HTTP-driven test; no verification verdict, no UAT record, and no other requirement row was touched.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
**New symbols**
- `CurationIntentKind.supersedesPending` — a computed `Bool` written as an exhaustive switch, true only for `.availabilityReport`.
- A new rejection clause and catch-all in `PlaysteadWeb.Api.V1.AvailabilityController.replace/2` returning `{:error, {:validation_failed, detail}}`.
- New tests in suite `OutboxTests`:
  `test_secondAvailabilityReport_supersedesThePendingFirstOne`,
  `test_secondAvailabilityReport_supersedesABackedOffFirstOne`,
  `test_secondAvailabilityReport_leavesAnInFlightFirstOneAlone`,
  `test_secondFavoriteIntent_isNotSupersededBecauseOnlyReportsAreNewestWins`,
  `test_drainAfterSupersede_sendsOnlyTheNewerReportBody`.
- New malformed-shape tests in `playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs`, one per rejected shape (non-list string, non-list map, list with a string element, list with a number element), each separately named and separately asserted.

**Modified files**
- `playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex` — shape guard on `entries` before the context call; existing `:too_many_entries` branch unchanged.
- `playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs` — added tests only; existing assertions unchanged.
- `playstead-mac/Playstead/Sync/CurationIntent.swift` — `supersedesPending` added to `CurationIntentKind`.
- `playstead-mac/Playstead/Sync/Outbox.swift` — a kind-scoped, pending-only delete inside `enqueue(_:at:)`'s existing transaction; `listPending(at:)` unchanged.
- `playstead-mac/PlaysteadTests/CurationTests/OutboxTests.swift` — five tests added.
- `playstead-mac/scripts/ci/run-mac-verification.sh` — five identifiers added to the unit-layer required-test list.
- `.planning/REQUIREMENTS.md` — `LIBR-02` checkbox and traceability row, conditionally, at most two lines.

**Consumed, not produced**
- `PUT /api/v1/devices/me/availability`, `Playstead.Availability.replace_for_device/2`, `PlaysteadWeb.Api.V1.FallbackController`, `PlaysteadWeb.Problem`, `error_codes.ex`'s `validation_failed`, `ImportsController.precheck/2`'s error-tuple pattern, `Outbox.markPendingForRetry`'s backoff, `scripts/check-uat-tally.sh` (created by 03-14), and `03-15`'s Mac-client-driven tests.
</artifacts_this_plan_produces>

<threat_model>
ASVS enforcement level: 1. Blocking severity: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device HTTP request body -> `AvailabilityController.replace/2` | An attacker-controllable body from a compromised or buggy device client crosses into server code |
| controller -> `Playstead.Availability` -> Ecto | Per-entry fields reach a changeset and a transaction |
| Mac outbox rows -> outbound delivery order | What the server believes a device holds depends on which queued row wins |
| executor -> project requirement records | An agent writes the document that grades its own work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16-01 | Denial of Service | unhandled raise on a malformed `entries` body | high | mitigate | Task 1's shape guard refuses a non-list and a list with a non-map element before the context call, returning 422 through the shared `action_fallback` instead of letting `normalize_entry/1` raise into a 500; four separately-named tests assert the four rejected shapes |
| T-03-16-02 | Denial of Service | unbounded `entries` array length | medium | accept | Already mitigated upstream at T-03-13-04: `Playstead.Availability`'s 5000-entry cap returns `{:error, :too_many_entries}` and the controller already renders it as 422. An acceptance criterion requires that branch to survive this task rather than be replaced by the new guard |
| T-03-16-03 | Tampering | per-entry field-type confusion reaching Ecto | medium | mitigate | Entries that clear the map-shape guard still pass through `build_changeset`, whose casts reject out-of-range `download_percent` and bad types with a changeset error the fallback controller renders as 422; the new guard closes only the shape hole that raised before a changeset was ever built |
| T-03-16-04 | Tampering | a device writing availability facts scoped to another device or user | low | accept | Confirmed already mitigated and tested with a real negative case per `03-VERIFICATION.md`: `replace_for_device/2` filters incoming entries to asset sets owned by the reporting device's own user before any insert, and `facts_for_user/1` reads only `user_id`-scoped rows. Re-confirmed here, not re-implemented |
| T-03-16-05 | Tampering | a stale full-replacement report overwriting newer facts | high | mitigate | Task 2 deletes any pending or backed-off row of the same kind inside the enqueue transaction, so the stranded earlier report can no longer be delivered after a later one that already succeeded; a test drives the real `markPendingForRetry` backoff rather than a hand-written timestamp |
| T-03-16-06 | Tampering | superseding too widely and dropping unrelated intents | high | mitigate | The delete is scoped by kind and by pending state via an exhaustive `supersedesPending` switch; a named test asserts a second favorite intent is not superseded, and a corrupt-and-revert check requires that test to fail when the kind predicate is removed |
| T-03-16-07 | Repudiation | an executor flipping a requirement on its own say-so | high | mitigate | Task 3 is gated by an explicit precondition on `03-15`'s observed runs, requires a clause-to-test mapping in which the readiness clause names a Mac-client-driven or HTTP-driven test, and is blocked by a `git diff --exit-code` verify from touching `03-VERIFICATION.md` or `03-UAT.md` at all |
| T-03-16-08 | Tampering | a CI gate that passes on nothing | high | mitigate | Every verify command fails closed on exit 127 by explicit `<fails_when>` statement, the Mac commands fail on an `Executed 0 tests` line, the server commands require a total at or above the 1065 baseline, and the harness script itself is invoked to prove it is present and runnable |
| T-03-16-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no package-manager install task and no new dependency, so the package-legitimacy gate has nothing to audit |
</threat_model>

<verification>
- `mix test test/playstead_web/controllers/api/v1/availability_controller_test.exs` exits zero with `0 failures` and more tests than before this plan.
- The four malformed `entries` shapes each return 422 with the `validation_failed` code; the absent-key and empty-list cases return exactly what they returned before.
- `cd playstead-server && mix test` exits zero with a total at or above 1065.
- The Mac run executes `OutboxTests` and `AvailabilityReporterTests` with a non-zero test count and no `Executed 0 tests` line.
- `Outbox.listPending(at:)`'s body is unchanged in `git diff`.
- Both corrupt-and-revert checks (removing the kind predicate from the delete) were observed to fail and are recorded verbatim in the SUMMARY.
- `03-VERIFICATION.md` and `03-UAT.md` are untouched (`git diff --exit-code` clean on both).
- `LIBR-05` and `PLAY-04` are still `Pending`; `.planning/REQUIREMENTS.md` shows at most two changed lines.
- `bash scripts/check-uat-tally.sh` exits zero.
</verification>

<success_criteria>
- A malformed availability report is refused the way every sibling endpoint refuses one, and the pre-fix 500 was observed before the fix rather than assumed.
- A device that has been offline and backed off can no longer deliver a stale full-replacement report after a newer one, and no other intent kind changed behavior.
- `LIBR-02` is marked complete only on the strength of tests that drive the Mac client or the real endpoint, with the clause-to-test mapping written down where a re-verifier can re-run it.
- No verification verdict, no UAT record, and no hardware- or human-blocked item was touched by the plan that would benefit from touching it.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-16-SUMMARY.md` when done. Record verbatim: the observed pre-fix status code for the non-list and non-map-element bodies; the final guard clause as implemented and the exact detail string it returns; the observed failure from removing the kind predicate from the supersede delete, with test name and line; the confirmation that `applyOptimistically` is a no-op for the availability-report kind; the clause-by-clause mapping from UAT item 4 to named tests, marking which test drives the Mac client; whether `LIBR-02` was flipped and on what evidence; and an explicit list of what remains open after this plan.
</output>
