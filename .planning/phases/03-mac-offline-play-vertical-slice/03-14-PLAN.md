---
phase: 03-mac-offline-play-vertical-slice
plan: 14
type: execute
gap_closure: true
wave: 2
depends_on: ["03-13"]
files_modified:
  - playstead-mac/Playstead/Cache/AvailabilityReporter.swift
  - playstead-mac/Playstead/Cache/AvailabilityVocabulary.swift
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/PlaysteadTests/CacheTests/AvailabilityReporterTests.swift
  - playstead-mac/PlaysteadTests/CacheTests/AvailabilityVocabularyContractTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
  - scripts/check-uat-tally.sh
  - .github/workflows/ci.yml
  - .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
  - .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LIBR-02, QUAL-01]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Mac client that has cached, pinned, or is downloading content actually sends those facts to the server, so the console's availability chips filter over live facts rather than over an empty table."
    - "The wire shape the Mac sends and the wire shape the server accepts are proven to agree by a fixture both codebases assert against, not by one side's stub of the other."
    - "The availability report never runs on the launch path; a failure to report never blocks, delays, or fails a game launch."
    - "The new Mac tests are registered with the verification harness by name, so a run that discovers none of them fails instead of passing quietly."
    - "Every UAT file's Summary footer and status annotation is recomputed from that file's own per-item results, and a committed command re-derives the counts and exits non-zero when a footer disagrees."
    - "LIBR-02 is flipped in REQUIREMENTS.md only after, and only if, the tests that prove it were observed passing in this plan's own run."
    - "Items that cannot be verified in this environment - physical controller hardware, a live interactive emulator session, an experiential VoiceOver walkthrough, and acceptance of real BIOS bytes - stay recorded as human verification and are never described as automatically verified."
    - statement: "A report attempted while the server is unreachable is retried later rather than lost, and produces no user-visible error."
      verification: backstop
  artifacts:
    - playstead-mac/Playstead/Cache/AvailabilityReporter.swift
    - playstead-mac/Playstead/Cache/AvailabilityVocabulary.swift
    - playstead-mac/PlaysteadTests/CacheTests/AvailabilityReporterTests.swift
    - playstead-mac/PlaysteadTests/CacheTests/AvailabilityVocabularyContractTests.swift
    - scripts/check-uat-tally.sh
  key_links:
    - "AvailabilityInputs facts from DownloadQueue/CAS/PinStore -> AvailabilityReporter -> Outbox -> PUT /api/v1/devices/me/availability"
    - "PlaysteadApp composition root -> AvailabilityReporter (registered, not orphaned)"
    - "shared/availability-vocabulary.json -> AvailabilityVocabulary.swift and Playstead.AvailabilityVocabulary -> contract tests on both sides"
    - "run-mac-verification.sh required-test list -> the new Mac test identifiers"
    - "scripts/check-uat-tally.sh -> every .planning/phases/*/*-UAT.md footer -> ci.yml test job"
  prohibitions:
    - "Never claim a hardware-only, third-party-only, or experiential item as automatically verified - this phase has already had two overclaims corrected and a third would be worse than an open item."
    - "Never flip a requirement checkbox in the same task that writes the test intended to prove it."
    - "Never edit this phase's VERIFICATION.md top-level status field; only an independent re-verification may change it."
    - "Never let a report of what a device holds influence whether a game is allowed to launch."
    - "Never write a UAT tally by hand; it is recomputed from the file body or it is not written."
---

<objective>
Close the loop `03-13-PLAN.md` opened, and then tell the truth about it.

`03-13` built the console's six-value availability filter over a read model fed by device reports. Nothing sends those reports yet, so today every one of the four new chips would match nothing for every real user. That is the "plumbed everywhere, proven nowhere" shape - a field wired through every layer that nobody ever writes. Task 1 makes the Mac client actually report, wires it at the composition root the same way `03-11` wired `BiosReferences.production`, and proves both ends of the wire agree through a shared fixture rather than through one side's stub of the other.

Task 2 fixes the second, smaller gap `03-VERIFICATION.md` recorded: `03-UAT.md`'s own "Current Test" annotation and `## Summary` footer both contradict the file's own body. The fix is not a hand-edit - a hand-written tally is exactly what rotted - it is a committed command that recomputes the tally from the per-item results and fails when a footer disagrees, run over every UAT file in the repository.

Task 3 flips the documents, and only then. `LIBR-02`'s checkbox is not touched until Tasks 1 and 2 have been observed passing in this same run.

Purpose: make the LIBR-02 work real end to end, and make the project's own records match what was actually proven.
Output: a Mac availability reporter, a two-sided wire contract, a self-checking UAT tally, and a gated documentation flip.

**Scope fence - what this plan does NOT claim.** Four items remain genuinely unverifiable in this execution environment and stay routed to human verification exactly as `03-VERIFICATION.md` already records them: physical controller connect, disconnect, reconnect, live input test, remap and reset (`PLAY-04`, UAT items 8 and 9 and item 10's blocked sub-record); a live interactive emulator session with a real downloaded game and an installed emulator (UAT item 7); the experiential VoiceOver pronunciation and sentence-quality walkthrough (item 10's blocked sub-record, distinct from its separately-passing automated keyboard and live-tree record); and acceptance of real, legally-owned BIOS bytes (UAT item 13, already reduced to the single operator command `scripts/verify-bios-reference.sh`). `LIBR-05`'s find-a-game UX review is human judgment and is likewise untouched. **This plan advances neither `PLAY-04` nor `LIBR-05` beyond what is already there, and says so in Task 3's text rather than implying otherwise by silence.** No automated substitute for any of these is to be invented.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-13-SUMMARY.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-11-PLAN.md
@playstead-mac/Playstead/Cache/AvailabilityState.swift
@playstead-mac/Playstead/Curation/PlaySessionRecorder.swift
</context>

<api_coverage_decision>
detected: false, by the same detector run recorded in `03-13-PLAN.md` (the probe ran; it did not skip). The endpoint this plan's Mac client calls is this project's own `/api/v1`; no external API, SDK, or third-party service is integrated.
</api_coverage_decision>

<assumption_delta_decision>
decision: no-change. The identity question (availability is one enumerated dimension, not two) was answered in `03-13-PLAN.md`. This plan adds a producer for an already-decided shape and changes no cardinality or optionality of its own.
</assumption_delta_decision>

<package_legitimacy_gate>
Inert. No Swift Package Manager, mix, npm, pip, or cargo dependency is added by this plan and there is no install task to audit.
</package_legitimacy_gate>

<schema_push_gate>
SCHEMA_PUSH_REQUIRED=false. No ORM path of any listed kind appears in this plan, and no migration is added - the one migration for this work landed in `03-13`.
</schema_push_gate>

<planner_assumptions>
1. **The reporter is an after-the-fact outbox producer, never a launch-path caller.** `PlaySessionRecorder` establishes this pattern in this codebase and its own moduledoc states that nothing on the launch path calls its endpoint. The reporter copies that posture. This is load-bearing for the offline promise: a game must remain launchable with no server reachable, so nothing about reporting may become a precondition of launching.
2. **UAT tally ground truth, counted independently while planning.** Taking the first `result:` line under each of the 44 `### N.` headings in `03-UAT.md`: **38 pass, 4 blocked (items 4, 7, 8, 9), 2 partial (items 10, 13), 0 skipped**, totalling 44. This agrees with the verifier's count. One structural subtlety the executor must reproduce rather than trip over: item 10 carries a second `result:` line for a sub-record, so a naive count of every `result:` line yields 45, not 44. The file's own annotation ("5 blocked ... items 4, 7, 8, 9, 11") and its footer ("passed: 31, blocked: 12, skipped: 1") are both stale. The executor recomputes rather than adopting any of these numbers; if the recomputation disagrees with 38/4/2/0, it must say so explicitly in the SUMMARY rather than quietly writing a different number.
3. **UAT item 4's flip is conditional, not assumed.** Item 4 is the LIBR-02 UAT record, blocked since 2026-08-31. It flips to `pass` only if its own `expected:` text is fully covered by tests that `03-13` and this plan actually ran. If any clause of item 4's expected text is not covered, it stays open and the SUMMARY names the uncovered clause. Do not stretch coverage to close it.
4. **`PLAY-04` and `LIBR-05` are not advanced by this plan.** `PLAY-04` needs physical controller hardware that does not exist here; `LIBR-05`'s remaining item is a human-judgment UX review. Both stay `Pending` in `REQUIREMENTS.md` and stay in `03-VERIFICATION.md`'s `human_verification` array. Stated here so their absence from this plan's requirement advances is a recorded decision rather than an omission.
5. **The phase's terminal status is not this plan's to set.** Even with `LIBR-02` closed, `03-VERIFICATION.md`'s own gate rules make `human_needed` the correct status while any human-verification item remains - and an independent re-verification, not this plan, is what may set it.
6. **Probe and UI-SPEC accounting is inherited, not re-derived.** The 29-row deterministic edge probe and the `## UI Considerations` lift were accounted for in full in `03-13-PLAN.md`'s `planner_assumptions` (2 flagged, 27 recorded as already covered by prior plans; the in-scope UI rows lifted and the out-of-scope rows named). This plan authors no new probe rows and drops none.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The Mac client actually reports its availability, and both ends of the wire are proven to agree</name>
  <precondition>A macOS machine with the Xcode toolchain on PATH (`xcodebuild -version` succeeds) and the `Playstead` scheme resolvable. Without it the Mac layer cannot be built or tested, and this task halts rather than proceeding on unbuilt code.</precondition>
  <files>playstead-mac/Playstead/Cache/AvailabilityReporter.swift, playstead-mac/Playstead/Cache/AvailabilityVocabulary.swift, playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/PlaysteadTests/CacheTests/AvailabilityReporterTests.swift, playstead-mac/PlaysteadTests/CacheTests/AvailabilityVocabularyContractTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <read_first>
    playstead-mac/Playstead/Cache/AvailabilityState.swift (the whole file - `AvailabilityInputs`' facts and `derive`/`deriveForStorageView`. The reporter sends FACTS, and this file defines which facts exist and where they come from. Do not change this file);
    playstead-mac/Playstead/Curation/PlaySessionRecorder.swift (the exact after-the-fact outbox producer shape to copy, including its stance that nothing on the launch path calls it);
    playstead-mac/Playstead/Sync/Outbox.swift and playstead-mac/Playstead/Sync/OutboxWorker.swift (how an intent is enqueued, drained, and retried when the server is unreachable);
    playstead-mac/Playstead/Net/APIClient.swift (request construction, the Idempotency-Key header convention, and error surfacing);
    playstead-mac/Playstead/App/PlaysteadApp.swift (the composition root - specifically the construction region around `BiosStore` that `03-11` wired, which is both the pattern and the grep-countable proof technique this task reuses);
    playstead-mac/Playstead/Saves/SaveVocabulary.swift and playstead-mac/PlaysteadTests/SavesTests/SaveCopyContractTests.swift (the Swift half of this repository's shared-fixture contract pattern);
    shared/availability-vocabulary.json (created by 03-13 - the single fixture both languages assert against);
    playstead-server/lib/playstead_web/controllers/api/v1/availability_controller.ex and playstead-server/test/playstead_web/controllers/api/v1/availability_controller_test.exs (created by 03-13 - the exact accepted request shape this client must produce);
    playstead-mac/scripts/ci/run-mac-verification.sh (the unit-layer required-test list around line 1266 - the mechanism that makes a zero-discovery run fail);
    playstead-mac/Playstead/Cache/PinStore.swift and playstead-mac/Playstead/Cache/DownloadQueue.swift (where the facts are actually read from)
  </read_first>
  <behavior>
    - Given a game whose required members are all in the content-addressed cache and which is pinned, the reporter emits an entry for it with the verified fact and the pinned fact both true.
    - Given a game with an active transfer, the reporter emits an entry with the downloading fact true and a percent between 0 and 100 inclusive.
    - Given a game with no local bytes and no queue row, the reporter emits an entry whose facts are all false, rather than omitting the game.
    - The encoded request body decodes into exactly the field names the server controller accepts; a field renamed on either side fails the contract test.
    - The same report enqueued twice carries the same idempotency key and results in one server-side effect.
    - With the server unreachable, the report is enqueued and drained later; no error surfaces to the user and no launch is affected.
    - The reporter is reachable from the composition root - a check asserts it is constructed there, not merely defined.
  </behavior>
  <action>
Create `Playstead/Cache/AvailabilityVocabulary.swift` mirroring `shared/availability-vocabulary.json`'s six values, labels, and accessible names, following `Saves/SaveVocabulary.swift`'s structure. Create `PlaysteadTests/CacheTests/AvailabilityVocabularyContractTests.swift` proving exhaustively and in both directions that this Swift declaration and the `shared/` fixture agree - a key present in one and absent in the other fails. The Elixir side already asserts the same fixture (`availability_vocabulary_contract_test.exs`, created by 03-13), so the fixture is what makes the two codebases agree; neither side asserts against the other's stub. That is deliberate: a test whose transport is a stub of its counterpart can go green while the real contract is broken.

Create `Playstead/Cache/AvailabilityReporter.swift` with a type `AvailabilityReporter` that:
  - builds one entry per catalogue game from the same facts `AvailabilityInputs` names - required member digests, queue rows, the active transfer and its percent, cached member digests, and the pin flag - reading them from the same stores `AvailabilityState`'s callers read (`DownloadQueue`, the content-addressed cache manager, `PinStore`). It sends facts, not a derived state name: the ladder rank is the server's to compute, per D-21's rule that no computed rank is ever stored or transmitted as durable truth.
  - encodes a full-replacement body - the complete current set for this device, matching the server's replace-per-device semantics - and enqueues it through `Outbox` with an idempotency key, exactly as `PlaySessionRecorder` does. It never calls the network synchronously from a launch path and never surfaces a transport failure to the user.
  - exposes a single entry point the composition root can call and a test can drive with injected stores.

Wire it at `PlaysteadApp.swift`'s composition root alongside the other environment members, in the same construction region `03-11` used for `BiosStore`. Wiring is the point of this task - an unwired reporter is the exact defect `03-11` was written to fix - and the acceptance criteria below make the wiring grep-countable rather than assumed.

Create `PlaysteadTests/CacheTests/AvailabilityReporterTests.swift` covering every case in `behavior`, with injected stores rather than real disk state. Write the tests first and watch them fail before implementing.

Register every new test identifier in `scripts/ci/run-mac-verification.sh`'s unit-layer required-test list, in the same `PlaysteadTests.SuiteName/testMethodName` form as the entries already there. This is what makes a run that discovers none of these tests fail rather than exit green - the failure mode `03-12`'s SUMMARY documented, where filters matched nothing and the run still passed.

Finally, run the server suite unchanged to confirm this task broke nothing on the other side of the wire.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*//' playstead-mac/Playstead/App/PlaysteadApp.swift | grep -c 'AvailabilityReporter'` outputs at least `1`, proving the reporter is constructed at the composition root and not orphaned.
    - `grep -c 'AvailabilityReporterTests\|AvailabilityVocabularyContractTests' playstead-mac/scripts/ci/run-mac-verification.sh` outputs at least `2`, proving both new suites are registered as required tests.
    - The Mac test log written by this task's verify command reports a non-zero executed-test count for each new suite.
    - A contract test fails when a single key is removed from `shared/availability-vocabulary.json`. The executor verifies this by temporarily removing one key, running both the Swift and the Elixir contract test, observing both fail, and restoring the key - both observed failures recorded verbatim in the SUMMARY.
    - `git diff --exit-code -- playstead-mac/Playstead/Cache/AvailabilityState.swift` exits zero: the Mac client's CACH-02 derivation is unmodified by this plan.
    - `AvailabilityReporter` is referenced only from the composition root and its own tests; `grep -rl 'AvailabilityReporter' playstead-mac --include='*.swift'` lists no launch-path source file (nothing under `Playstead/Adapter/` or `Playstead/Launch*`).
    - `cd playstead-server && mix test` exits zero with a test count no lower than the count recorded at the end of `03-13`.
  </acceptance_criteria>
  <verify>
    <automated>cd playstead-mac && mkdir -p build && xcodebuild test -scheme Playstead -destination 'platform=macOS' -only-testing:PlaysteadTests/AvailabilityReporterTests -only-testing:PlaysteadTests/AvailabilityVocabularyContractTests > build/availability-tests.log 2>&1; tail -40 build/availability-tests.log</automated>
    <fails_when>the log contains `Executed 0 tests`, or contains no executed-test line at all, or contains `TEST FAILED`, or the scheme or a named suite is reported as not found - any of which means the filters matched nothing and the run proved nothing</fails_when>
    <automated>cd playstead-mac && grep -c 'Executed 0 tests' build/availability-tests.log</automated>
    <fails_when>any output other than `0`</fails_when>
    <automated>cd /Users/jon/projects/playstead && grep -v '^\s*//' playstead-mac/Playstead/App/PlaysteadApp.swift | grep -c 'AvailabilityReporter'</automated>
    <fails_when>an output of `0`, or a non-zero exit from grep, either of which means the reporter is defined but never constructed at the composition root</fails_when>
    <automated>cd playstead-server && mix test</automated>
    <fails_when>non-zero exit; or a summary line reporting `0 tests`; or a total lower than the count recorded at the end of 03-13</fails_when>
  </verify>
  <done>The Mac client builds a real availability report from its own stores, enqueues it through the outbox, and is constructed at the composition root; both codebases assert the same shared wire fixture; and the new suites are registered by name with the verification harness so a zero-discovery run cannot pass.</done>
</task>
<task type="auto">
  <name>Task 2: Recompute every UAT tally from its own body, and make the recomputation a command</name>
  <files>scripts/check-uat-tally.sh, .github/workflows/ci.yml, .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md</files>
  <read_first>
    .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md (the whole file - the `## Current Test` annotation line, all 44 `### N.` item headings with their `result:` lines, item 10's second `result:` sub-record line, and the `## Summary` footer);
    .planning/phases/01-private-custody-and-durable-protocol/01-UAT.md, .planning/phases/03.5-mac-verification-automation/03.5-UAT.md, and .planning/phases/04-persistent-save-continuity/04-UAT.md (the other three files the guard will run over - confirm their footers agree with their bodies before wiring the guard, and fix any that do not);
    .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md (the "Note on 03-UAT.md's own internal inconsistency" paragraph - the ground-truth figures to compare against);
    playstead-server/scripts/compose-smoke.sh (this repository's shell-script idiom: strict mode, explicit failure tokens, fail-closed on a missing input);
    .github/workflows/ci.yml (the `test` job, where a cheap dependency-free guard step belongs)
  </read_first>
  <action>
Write `scripts/check-uat-tally.sh`. For each file matching `.planning/phases/*/*-UAT.md` it derives the tally directly from the body: one record per `### N.` heading, taking the FIRST `result:` line that follows that heading and ignoring any later `result:` line belonging to a sub-record - item 10 of `03-UAT.md` has exactly such a sub-record and a naive count of every `result:` line yields 45 against 44 headings. It then compares the derived totals against the `## Summary` footer's `total`, `passed`, `blocked`, `skipped` (and any other result keys present) and exits non-zero, naming the file and both numbers, on any disagreement.

Fail closed in every direction, because this project has already found four fail-open CI gates: run under `set -euo pipefail`; exit non-zero if the glob matches no file at all; exit non-zero if a matched file has no `## Summary` section or no `### N.` headings; exit non-zero if any derived count is zero for a file that has headings. A guard that silently finds nothing to check is the failure mode, not a pass.

Then recompute `03-UAT.md` from its own body using that script's own derivation, and rewrite both stale places to the derived numbers: the `## Current Test` annotation line (which currently names item 11 as blocked when its result is pass) and the `## Summary` footer (which currently reports passed 31, blocked 12, skipped 1). Write the derived numbers, not the numbers in this plan and not the numbers in `03-VERIFICATION.md`. If the derivation disagrees with 38 pass, 4 blocked, 2 partial, 0 skipped, stop and record the disagreement with the per-item breakdown in the SUMMARY rather than adopting a different number quietly. The footer must carry every result kind present in the body, so `partial` gets a line - its absence is part of why the old footer could not add up.

Run the guard over the other three UAT files. If any of them also disagrees with its body, recompute and rewrite that footer too, and list every file changed in the SUMMARY.

Wire the script as a step in `ci.yml`'s `test` job. It needs no toolchain beyond a shell, so it belongs early in that job where it costs nothing and catches footer rot in any phase, not just this one. Do not add it to the macOS job.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-uat-tally.sh` exits zero against the repository after the rewrite.
    - Corrupting a single number in any UAT `## Summary` footer makes the script exit non-zero and print the file path plus the derived and stated values. The executor verifies this by making the change, running the script, observing the failure, and reverting - the observed output is recorded verbatim in the SUMMARY.
    - Pointing the script at a directory containing no UAT file makes it exit non-zero rather than reporting success.
    - `grep -c 'check-uat-tally.sh' .github/workflows/ci.yml` outputs at least `1`.
    - `grep -c 'items 4, 7, 8, 9, 11' .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md` outputs `0`.
    - The `## Summary` footer of `03-UAT.md` contains a line for every result kind that appears in its body, including `partial`.
    - The derived total for `03-UAT.md` equals the count of `### N.` headings in that file.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/playstead && bash scripts/check-uat-tally.sh</automated>
    <fails_when>non-zero exit, or output naming any file whose derived tally differs from its footer; a run that prints nothing at all also fails, since the script must name each file it checked</fails_when>
    <automated>cd /Users/jon/projects/playstead && grep -c 'items 4, 7, 8, 9, 11' .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md</automated>
    <fails_when>any output other than `0` - the stale annotation naming a passing item as blocked is still present</fails_when>
    <automated>cd /Users/jon/projects/playstead && grep -c 'check-uat-tally.sh' .github/workflows/ci.yml</automated>
    <fails_when>an output of `0` - the guard exists but no CI run would ever execute it, which is how the four previously-found fail-open gates rotted</fails_when>
  </verify>
  <done>Every UAT file's annotation and footer match its own body, the match is re-derivable by one committed command that fails closed, and CI runs that command.</done>
</task>
<task type="auto">
  <name>Task 3: Flip the records, only for what was actually proven, and only after it was proven</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md, .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md</files>
  <precondition>Tasks 1 and 2 of this plan, and all three tasks of `03-13-PLAN.md`, have been run in this session and their verify commands observed passing. This task reads those observed results; it does not re-derive them and must not run if any of them is unknown or failing.</precondition>
  <read_first>
    .planning/REQUIREMENTS.md (the `LIBR-02` checkbox line near line 37 and its traceability table row near line 134 - both must change together, and the surrounding rows show the exact `- [x]` and `| Complete |` formatting);
    .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md (item 4 in full - its `expected:` text is the thing coverage is judged against, and its `blocked_by: prior-phase` marker; plus the evidence-block idiom used by items that cite automated coverage, such as item 2);
    .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md (the `gaps` entry whose truth begins "LIBR-02", its `missing` array, and the `human_verification` array - and the top-level `status:` field, which is NOT to be touched);
    .planning/phases/03-mac-offline-play-vertical-slice/03-12-SUMMARY.md (how the previous gap-closure plan recorded a genuine flip with verbatim tool evidence rather than narration);
    .planning/phases/03-mac-offline-play-vertical-slice/03-13-SUMMARY.md (the test counts and observed command output this task cites)
  </read_first>
  <action>
First judge coverage, then write. Read `03-UAT.md` item 4's `expected:` text clause by clause and decide, for each clause, which specific test proves it - naming the test file and the test name, not a narrative. Record that clause-to-test mapping in the SUMMARY. If any clause is uncovered, item 4 stays open, `LIBR-02` stays unchecked, and the SUMMARY names the uncovered clause. Under-claiming here is a correct outcome; this phase has already had two overclaims corrected by an independent re-verification.

If and only if every clause is covered:

In `.planning/REQUIREMENTS.md`, change the `LIBR-02` checkbox to checked and its traceability row to `Complete`. Change nothing else - `LIBR-05` and `PLAY-04` stay `Pending`, because a human-judgment UX review and physical controller hardware are what they are still waiting on, and neither was advanced by this plan.

In `03-UAT.md`, update item 4 to `pass` with `source: automated` and an evidence block naming the exact commands that were run and the exact test names that covered it, in the idiom item 2 already uses. Then re-run `scripts/check-uat-tally.sh` and rewrite the footer and annotation from the new derivation - the tally moves with this edit, and it is derived, never hand-adjusted.

In `03-VERIFICATION.md`, update the `gaps` entry for the LIBR-02 truth: set `status: resolved`, `resolved_by: "03-13-PLAN.md, 03-14-PLAN.md"`, empty its `missing` array, and write a `reason` that states what was built, which commands proved it, and - explicitly - that the console's six filter values follow `03-UI-SPEC.md` and D-13 rather than the `CACH-02` wording this file's original gap text used, with the mapping. Also update the corresponding `human_verification` entry (the one asking for the LIBR-02 completion-or-descope decision) to record that the build path was taken.

Three things are forbidden in that file and must be left exactly as they are: the top-level `status:` field (only an independent re-verification may change it), the four remaining `human_verification` entries for physical controller hardware, a live interactive emulator session, the experiential VoiceOver walkthrough, and real BIOS-byte acceptance, and the `behavior_unverified_items` array. None of those were addressed by this plan and none may be described as automatically verified.

Finish by stating plainly in the SUMMARY what remains open after this plan: `LIBR-05` and `PLAY-04` pending, four human-verification items outstanding, and the phase's correct terminal status still to be set by an independent re-verification run, not by this plan.
  </action>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*LIBR-02\*\*' .planning/REQUIREMENTS.md` outputs `1`, and `grep -c '| LIBR-02 | Phase 3 | Complete |' .planning/REQUIREMENTS.md` outputs `1` - both only if the coverage judgement passed.
    - `grep -c '| LIBR-05 | Phase 3 | Pending |' .planning/REQUIREMENTS.md` outputs `1` and `grep -c '| PLAY-04 | Phase 3 | Pending |' .planning/REQUIREMENTS.md` outputs `1` - neither was advanced and neither may be flipped.
    - `grep -c 'blocked_by: prior-phase' .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md` outputs `0`.
    - `grep -c '^status: gaps_found' .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md` outputs `1` - the top-level status is unchanged by this plan.
    - The `human_verification` array in `03-VERIFICATION.md` still contains the four environment-blocked entries; a count of its `- test:` keys is no lower than 4.
    - `bash scripts/check-uat-tally.sh` exits zero after the item 4 edit.
    - The SUMMARY contains a clause-by-clause mapping from `03-UAT.md` item 4's `expected:` text to named test files and test names.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/playstead && bash scripts/check-uat-tally.sh && grep -c '^status: gaps_found' .planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md</automated>
    <fails_when>non-zero exit, or an output other than `1` for the status grep - a changed top-level status means this plan graded its own phase, which is exactly the failure an independent re-verification already caught twice here</fails_when>
    <automated>cd /Users/jon/projects/playstead && grep -c 'blocked_by: prior-phase' .planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md</automated>
    <fails_when>any output other than `0` when the coverage judgement passed - item 4 is still recorded as blocked on a prior phase; and any output other than `1` when the judgement did not pass, which would mean the record was flipped without coverage</fails_when>
    <automated>cd /Users/jon/projects/playstead && grep -c '| LIBR-05 | Phase 3 | Pending |' .planning/REQUIREMENTS.md && grep -c '| PLAY-04 | Phase 3 | Pending |' .planning/REQUIREMENTS.md</automated>
    <fails_when>either output is other than `1`, meaning a requirement this plan did not advance was marked complete anyway</fails_when>
  </verify>
  <done>The project's records say exactly what was proven and no more: LIBR-02 flipped if and only if every clause of its UAT item is covered by a named passing test, LIBR-05 and PLAY-04 still pending, four human-verification items still open, and the phase's terminal status left to an independent re-verification.</done>
</task>

</tasks>
<artifacts_this_plan_produces>
**New files**
- `playstead-mac/Playstead/Cache/AvailabilityVocabulary.swift` - type `AvailabilityVocabulary`; the six filter values, labels, and accessible names mirroring `shared/availability-vocabulary.json`.
- `playstead-mac/Playstead/Cache/AvailabilityReporter.swift` - type `AvailabilityReporter`; builds the per-device fact set from `DownloadQueue`, the content-addressed cache manager, and `PinStore`, and enqueues a full-replacement report through `Outbox`.
- `playstead-mac/PlaysteadTests/CacheTests/AvailabilityReporterTests.swift` - suite `AvailabilityReporterTests`.
- `playstead-mac/PlaysteadTests/CacheTests/AvailabilityVocabularyContractTests.swift` - suite `AvailabilityVocabularyContractTests`.
- `scripts/check-uat-tally.sh` - derives each UAT file's tally from its own per-item results and compares it against the stated footer; fails closed on a missing file, a missing Summary section, or a zero derivation.

**Modified symbols and files**
- `playstead-mac/Playstead/App/PlaysteadApp.swift` - `AvailabilityReporter` constructed at the composition root.
- `playstead-mac/scripts/ci/run-mac-verification.sh` - two new suites added to the unit-layer required-test list, by `PlaysteadTests.SuiteName/testMethodName` identifier.
- `.github/workflows/ci.yml` - a `check-uat-tally.sh` step in the `test` job.
- `.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md` - recomputed `## Current Test` annotation and `## Summary` footer (now including a `partial` line); item 4 conditionally flipped with an evidence block.
- `.planning/phases/03-mac-offline-play-vertical-slice/03-VERIFICATION.md` - the LIBR-02 gap entry resolved with the commands that proved it; top-level `status:` deliberately unchanged.
- `.planning/REQUIREMENTS.md` - `LIBR-02` checkbox and traceability row, conditionally.

**Wire contract consumed (created by 03-13, not by this plan)**
- `PUT /api/v1/devices/me/availability`
- capability `cache` / `availability-report`
- `shared/availability-vocabulary.json`
</artifacts_this_plan_produces>

<threat_model>
ASVS enforcement level: 1. Blocking severity: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mac client local stores -> outbound report | The client asserts, to a server, facts about content it holds; the report crosses the device boundary |
| outbox retry path -> launch path | A background network producer sits in the same process as the offline launch guarantee |
| repository UAT files -> CI guard | A committed guard reads planning documents and decides whether the build passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14-01 | Denial of Service | `AvailabilityReporter` on the launch path | high | mitigate | The reporter is an after-the-fact outbox producer copied from `PlaySessionRecorder`, never called synchronously from launch; an acceptance criterion lists every file referencing it and requires no launch-path source among them, so a report failure can never block a launch |
| T-03-14-02 | Information disclosure | report body contents | medium | mitigate | The report carries asset-set identifiers and boolean facts only - no file paths, no user content, no credentials - and travels over the already-pinned device-authenticated transport `APIClient` establishes |
| T-03-14-03 | Tampering | full-replacement semantics from a stale client | medium | accept | A device that reports late overwrites its own prior row set with older facts. Accepted: `reported_at` records when, the rows are per-device so no other device is affected, and the console is a convenience view the launch path never consults |
| T-03-14-04 | Spoofing | the console presenting reported facts as certainty | high | mitigate | Recorded as a `must_haves` prohibition: an availability claim over absent content is the one lie this product cannot tell. The Mac client's own `AvailabilityState.derive` remains the sole authority for whether a game can actually start, and this plan leaves that file provably unmodified |
| T-03-14-05 | Repudiation | documentation flips without evidence | high | mitigate | Task 3 is gated on Tasks 1 and 2 by an explicit precondition, requires a clause-by-clause mapping from UAT item 4 to named tests, and is forbidden from touching the top-level verification status - the failure mode an independent re-verification already caught twice in this phase |
| T-03-14-06 | Tampering | a CI guard that passes on nothing | high | mitigate | `check-uat-tally.sh` runs under strict mode and exits non-zero when the glob matches nothing, when a file has no Summary section, and when a derivation is zero; a deliberate corrupt-and-revert check proves the guard fires |
| T-03-14-07 | Tampering | a Mac test run that discovers no tests | high | mitigate | Both new suites are registered in the harness's required-test list, and a verify command greps the run log for a zero-executed-test line and requires a count of zero - the exact vacuous-filter failure `03-12`'s SUMMARY documented |
| T-03-14-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no package-manager install task and no new dependency, so the package-legitimacy gate has nothing to audit |
</threat_model>

<verification>
- The Mac unit run executes both new suites with a non-zero test count and no zero-executed-test line in its log.
- `playstead-mac/Playstead/Cache/AvailabilityState.swift` is unmodified (`git diff --exit-code` clean on that path).
- `cd playstead-server && mix test` exits zero at or above the `03-13` baseline count.
- `bash scripts/check-uat-tally.sh` exits zero, and a deliberately corrupted footer makes it exit non-zero.
- `03-VERIFICATION.md`'s top-level `status:` is still `gaps_found`, and its four environment-blocked `human_verification` entries are intact.
- `LIBR-05` and `PLAY-04` are still `Pending` in `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- The availability chips `03-13` built now filter over facts a real device actually sends.
- Both codebases are proven to agree on the wire shape through a shared fixture, not through a stub.
- Every UAT tally in the repository is derivable by one command that fails closed, and CI runs it.
- `LIBR-02` is marked complete only if every clause of its UAT item maps to a named passing test - and if it does not, that is recorded plainly rather than worked around.
- No human-only or hardware-only item is claimed as automatically verified anywhere in this plan's output.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-14-SUMMARY.md` when done. Record verbatim: the derived UAT tally and how it compares to 38 pass / 4 blocked / 2 partial / 0 skipped; the observed output of the corrupt-and-revert check on `check-uat-tally.sh`; the observed failures from removing a key from `shared/availability-vocabulary.json`; the clause-by-clause mapping from UAT item 4 to named tests; and an explicit list of what remains open after this plan.
</output>