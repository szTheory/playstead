---
phase: 03-mac-offline-play-vertical-slice
plan: 20
type: execute
gap_closure: true
gap_ids: [G-03-13]
wave: 4
depends_on: ["03-19"]
files_modified:
  - playstead-mac/Playstead/App/PlaysteadApp.swift
  - playstead-mac/Playstead/UITesting/UITestBootstrap.swift
  - playstead-mac/Playstead/UITesting/UITestBiosCandidate.swift
  - playstead-mac/PlaysteadUITests/BiosAcceptanceSeamTests.swift
  - playstead-mac/PlaysteadTests/AdapterTests/BiosProductionReferenceTests.swift
  - playstead-mac/scripts/ci/tests/bios-acceptance-seam-test.sh
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/scripts/ci/run-mac-verification.sh
autonomous: true
requirements: [PLAY-03]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The normal AppEnvironment production path explicitly selects BiosReferences.production, whose literals remain equal to 03-BIOS-PIN.json."
    - "One finite UI_TESTING profile supplies one compiled synthetic reference and matching synthetic bytes through the shared AppEnvironment -> BiosStore -> BiosDropTarget -> BiosDropTargetView path."
    - "The packaged app renders acceptance, copies exact bytes to managed BIOS storage, writes the bios_files row, preserves the supplied original, and repeats idempotently."
    - "Same-length wrong digest, N-1/N+1 length, missing reference forwarding, and release-hook inclusion each fail closed with no managed file or database residue."
    - "Evidence proves production composition and generic length+SHA-256 behavior only; it does not claim possession, authenticity, provenance, legality, emulator consumption, hardware fidelity, open-replacement equivalence, or acceptance of proprietary BIOS bytes."
    - "Positive and negative BIOS seam tests run in recurring CI because composition-root drift and release leakage are lasting regression risks."
  artifacts:
    - path: "playstead-mac/PlaysteadUITests/BiosAcceptanceSeamTests.swift"
      provides: "Packaged-app lawful positive and negative BIOS acceptance evidence"
    - path: "playstead-mac/Playstead/App/PlaysteadApp.swift"
      provides: "Explicit production reference selection and bounded test reference dependency"
    - path: "playstead-mac/scripts/ci/tests/bios-acceptance-seam-test.sh"
      provides: "Composition forwarding and release-exclusion negative controls"
  key_links:
    - "UITestBootstrap finite profile -> AppEnvironment references parameter -> BiosStore -> BiosDropTarget -> BiosDropTargetView status"
    - "BiosReferences.production -> 03-BIOS-PIN.json parity test -> production digest-comparison path"
    - "Synthetic candidate bytes -> managed file + bios_files row + original-preservation oracle"
---

<objective>
Close G-03-13 with a lawful deterministic positive acceptance seam through the packaged app.

BiosStore already accepts caller-supplied references and has lower-level synthetic coverage, while AppEnvironment hardcodes BiosReferences.production and UI automation can inject only a candidate path. Introduce a narrow reference dependency at the shared composition root, keep production explicit, and allow exactly one compile-gated UI profile to pass one fixed synthetic reference and matching bytes. Prove accept and rejection through the existing user-facing route without acquiring, generating, or distributing proprietary BIOS content.

Purpose: verify production wiring and generic validation/storage behavior while preserving the project's legal and evidentiary boundaries.
Output: a bounded composition seam, packaged-app positive/negative tests, and release-exclusion guards.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-BIOS-PIN.json
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/debug/g-03-13-bios-acceptance-seam.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-11-SUMMARY.md

<interfaces>
BiosStore.init(localStore:managedDirectory:references:) already takes `[BiosStore.Reference]`; validation is size plus SHA-256, followed by managed copy and persistence. Reuse it unchanged.

AppEnvironment's public production and UI-testing convenience initializers funnel through one private designated initializer, which currently constructs BiosStore with BiosReferences.production. Move selection to an explicit initializer argument; production convenience paths pass BiosReferences.production, and only the compile-gated finite test profile passes the synthetic reference.

UITestBiosCandidate already validates that a candidate path is a regular, non-symlink file under the process temporary directory. Preserve this boundary and the existing production readiness -> BIOS surface -> Choose File route.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. No dependency is installed and no BIOS artifact is added.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Accept one fixed synthetic BIOS through the shared packaged-app composition path</name>
  <files>playstead-mac/Playstead/App/PlaysteadApp.swift, playstead-mac/Playstead/UITesting/UITestBootstrap.swift, playstead-mac/Playstead/UITesting/UITestBiosCandidate.swift, playstead-mac/PlaysteadUITests/BiosAcceptanceSeamTests.swift</files>
  <behavior>
    - RED: matching synthetic bytes are rejected by the packaged app because AppEnvironment always supplies BiosReferences.production.
    - GREEN: the finite acceptance profile reaches the existing BIOS surface, chooses the fixed candidate, and renders the exact accepted state.
    - GREEN: accepted bytes appear in managed BIOS storage, bios_files contains the expected synthetic system row, and the original candidate remains byte-identical at its original path.
    - GREEN: repeating acceptance is idempotent and leaves one managed object/row.
    - Negative control: omitting reference forwarding from the designated initializer makes the packaged-app positive test red.
    - Boundary control: neither digest nor expected length is accepted from process environment, command line, fixture JSON, or user input.
  </behavior>
  <action>
Write BiosAcceptanceSeamTests first and observe the real packaged route reject the matching synthetic candidate. Change AppEnvironment's shared designated initializer to require a BIOS reference set. Every production convenience initializer must pass BiosReferences.production explicitly per D-06; do not use a default that can conceal a missing production argument. Under `#if UI_TESTING`, add one named finite profile in UITestBootstrap that constructs one fixed synthetic system/reference from a short compiled byte sequence and routes it through that same designated initializer. Keep digest and size compiled constants derived in test code, never environment-controlled values.

Use UITestBiosCandidate for the candidate path and the existing readiness/BIOS/Choose File controls. The XCUITest writes only the synthetic byte sequence under its owned temporary root, launches the finite profile, and asserts the visible accepted state. It then inspects only its owned deterministic root to prove managed copy, one bios_files row, original preservation, and idempotency. Do not add an alternate validator, alternate BIOS UI, or direct BiosStore call from the UI test.

The synthetic bytes are test data for a made-up system identifier and must not match, purport to replace, or be described as any proprietary BIOS. No acquisition, generation, brute-force preimage, embedded proprietary byte, or external BIOS fixture is permitted.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing BiosAcceptanceSeamTests/testFixedSyntheticReferenceAcceptsThroughPackagedAppAndPersistsExactly</automated>
  </verify>
  <done>The packaged production wiring accepts and persists one lawful fixed synthetic candidate through the same UI/store path production uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add discriminator, forwarding, and release-exclusion negative controls and require them in CI</name>
  <files>playstead-mac/PlaysteadUITests/BiosAcceptanceSeamTests.swift, playstead-mac/PlaysteadTests/AdapterTests/BiosProductionReferenceTests.swift, playstead-mac/scripts/ci/tests/bios-acceptance-seam-test.sh, playstead-mac/TestPlans/UI.xctestplan, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - Same-size wrong-digest synthetic bytes render rejection and leave no managed file or bios_files row.
    - N-1 and N+1 synthetic lengths render rejection and leave no residue.
    - Production AppEnvironment reference selection remains equal to BiosReferences.production and 03-BIOS-PIN.json.
    - A mutant that drops the designated-initializer reference forwarding makes the static contract fail.
    - A Release build contains no UI_TESTING profile selector, synthetic reference constants, candidate hook key, or virtual-HID hook token.
    - Existing malformed/missing production-pin, no-acquisition sweep, and rejection-copy tests remain green.
  </behavior>
  <action>
Add separate packaged-app tests for same-length wrong digest and both length boundaries, each starting from a clean owned root and asserting both the exact rejection category and absence of file/row residue. Keep production trust metadata independent: extend BiosProductionReferenceTests only to prove that every normal AppEnvironment path passes BiosReferences.production and still matches 03-BIOS-PIN.json; do not require a production digest preimage.

Create bios-acceptance-seam-test.sh with mutation/self-tests for missing reference forwarding and Release exclusion. Inspect a Release build or its compiled symbol/string surface to prove UI_TESTING-only BIOS and HID hook tokens are absent; source `#if` presence alone is not release evidence. Preserve the existing bios-pin-provenance and no-acquisition guards. Add the new UI class to UI.xctestplan and add every positive/negative identifier to run-mac-verification.sh's required UI list and cross-layer allowlist because this seam protects recurring composition and distribution boundaries.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; xcodebuild test -scheme Playstead -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -testPlan Unit -only-testing:PlaysteadTests/BiosProductionReferenceTests -only-testing:PlaysteadTests/BiosTests &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing BiosAcceptanceSeamTests &amp;&amp; scripts/ci/tests/bios-acceptance-seam-test.sh &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-contracts</automated>
  </verify>
  <done>The lawful acceptance seam discriminates valid/invalid synthetic inputs, production pin wiring stays explicit, hooks are absent from Release, and CI requires every oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate file -> BiosStore | Untrusted local file metadata and bytes enter validation/storage. |
| UI-testing profile -> production composition | Compile-gated test data changes a dependency used by shared production wiring. |
| Source/test hooks -> Release artifact | Test-only capabilities must not cross the distribution boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20-01 | Tampering | Synthetic reference seam | high | mitigate | One compiled finite profile, no environment-controlled digest/size, explicit production argument, and missing-forwarding mutation test. |
| T-03-20-02 | Information Disclosure | BIOS artifacts/evidence | high | mitigate | Only synthetic bytes in tests; evidence records states/counts/digests of synthetic data and never user BIOS bytes or paths. |
| T-03-20-03 | Elevation of Privilege | Release test hooks | high | mitigate | `#if UI_TESTING`, target separation, and compiled Release exclusion inspection covering BIOS and HID hook tokens. |
| T-03-20-04 | Repudiation | Production BIOS claim | medium | mitigate | Separate pin-parity and synthetic-behavior oracles plus explicit non-claims; no test is labeled as real proprietary acceptance. |
| T-03-20-05 | Denial of Service | Candidate file handling | medium | mitigate | Existing regular-file/symlink checks and exact length gate run before managed copy; N±1 controls prove early rejection. |
</threat_model>

<verification>
- Unit: production pin parity and existing BiosStore acceptance/rejection tests remain green.
- Packaged E2E: fixed synthetic acceptance plus wrong-digest/N±1 rejection execute non-skipped.
- Static/distribution: missing-forwarding mutant is rejected and Release lacks test-only hooks.
- Legal boundary: repository no-acquisition sweep remains green; no proprietary bytes are introduced.
</verification>

<success_criteria>
- G-03-13 has deterministic production-wiring evidence without proprietary BIOS possession.
- Production continues to use the cited real reference set and offers no acquisition path.
- The evidence text makes only the claims the seam actually proves.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-20-SUMMARY.md` when done.
</output>
