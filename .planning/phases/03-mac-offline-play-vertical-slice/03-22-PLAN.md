---
phase: 03-mac-offline-play-vertical-slice
plan: 22
type: execute
gap_closure: true
gap_ids: [G-03-9]
wave: 3
depends_on: ["03-18"]
files_modified:
  - playstead-mac/Playstead/Controller/ControllerHost.swift
  - playstead-mac/PlaysteadTests/ControllerTests/ControllerHostTests.swift
  - playstead-mac/Playstead/Readiness/ReadinessSheetView.swift
  - playstead-mac/Playstead/Controller/ControllerTestView.swift
  - playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
autonomous: true
requirements: [PLAY-04, QUAL-01]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Before production input code changes, a packaged-app oracle records the qualified virtual controller enumerating while the complete live-test/assign/remap/reset/persistence journey remains red."
    - "Production GameController element events reach ControllerHost.liveInputs for startup and later-connected devices, with de-duplication and disconnect cleanup."
    - "The shipped controller settings surface opens ControllerTestView and displays press/release state through the production event path."
  artifacts:
    - path: "playstead-mac/Playstead/Controller/ControllerHost.swift"
      provides: "Typed production GameController input-event bridge"
    - path: "playstead-mac/Playstead/Readiness/ReadinessSheetView.swift"
      provides: "Reachable live-test presentation"
    - path: "playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift"
      provides: "Preserved complete RED oracle plus passing focused live-input journey"
  key_links:
    - "VirtualGamepad report -> GCControllerInputSource -> ControllerHost.reportInput -> liveInputs"
    - "ControllerSettingsView Test action -> ReadinessSheetView presentation -> ControllerTestView state"
---

<objective>
Establish the RED-first G-03-9 lifecycle oracle, then implement only production input delivery and live test.

Purpose: prove the framework input path independently before mapping persistence and adapter refresh are added in 03-23.
Output: a typed GameController event bridge, reachable live test, focused unit coverage, and fail-closed packaged evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/debug/g-03-9-controller-lifecycle.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-17-SUMMARY.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-18-SUMMARY.md

<interfaces>
ControllerInputSource currently exposes inventory and lifecycle callbacks. Extend that same boundary with a typed event callback carrying runtime controller id, the frozen input name vocabulary, and active state. ControllerHost remains the single state owner and receives events through reportInput.

ControllerSettingsView already defines onOpenTestView. ReadinessSheetView owns the presentation and binds ControllerTestView to the assigned descriptor and ControllerHost.liveInputs. Plan 03-23 owns assignment/remap/reset persistence and AdapterHost refresh.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. No dependency is installed.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Record the complete RED oracle, then deliver production input to live test</name>
  <files>playstead-mac/Playstead/Controller/ControllerHost.swift, playstead-mac/PlaysteadTests/ControllerTests/ControllerHostTests.swift, playstead-mac/Playstead/Readiness/ReadinessSheetView.swift, playstead-mac/Playstead/Controller/ControllerTestView.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift</files>
  <behavior>
    - RED: before production edits, the complete packaged lifecycle test enumerates the qualified virtual controller but press/release cannot change live test and remap/reset/persistence cannot complete.
    - GREEN: buttons, shoulders, Menu/Options, and thresholded d-pad transitions reach liveInputs for initial and later-connected devices.
    - GREEN: repeated values are de-duplicated and disconnect clears only that device's active inputs.
    - GREEN: Test controller opens ControllerTestView for the assigned descriptor and exposes press/release state accessibly.
    - Negative control: events from an unassigned controller do not drive the assigned live-test state.
  </behavior>
  <action>
First write the complete packaged-app lifecycle oracle, including live test, second-device assignment, remap, reset, adapter refresh, and relaunch persistence, and run it against untouched production source. Preserve the result-bundle failure showing successful enumeration followed by missing input/UI lifecycle behavior. This RED artifact must exist before editing any production file in this task or 03-23; fake-source unit failures do not substitute.

Then add one typed input-event callback to ControllerInputSource. In GCControllerInputSource, install handlers for initial inventory and later connects; normalize the frozen button, shoulder, Menu/Options, and d-pad vocabulary with one threshold; de-duplicate state; remove handlers on disconnect; and clear device-owned active inputs. Route events through ControllerHost.reportInput and exercise the same callback contract with the fake source.

Wire only the existing Test controller action in ReadinessSheetView: present ControllerTestView for the assigned descriptor and bind its available/live inputs to ControllerHost. Keep assignment/remap/reset persistence out of this plan. Complete a focused packaged test proving live press/release while retaining the broader lifecycle test as the already-red oracle 03-23 must finish. Per D-14, keyboard and pointer activation remain available.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; xcodebuild test -scheme Playstead -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -testPlan Unit -only-testing:PlaysteadTests/ControllerHostTests &amp;&amp; scripts/ci/run-mac-verification.sh --layers ui --only-testing ControllerHardwareIntegrationTests/testProductionControllerInputReachesLiveTest</automated>
  </verify>
  <done>The complete lifecycle oracle is preserved red before production edits, while the focused production input-to-live-test path is green through virtual HID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the live-input journey fail closed in recurring evidence</name>
  <files>playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - The focused production live-input identifier is required once, non-skipped, in the UI evidence layer.
    - Deleting the identifier, substituting a fake-source unit test, or reporting zero discovery fails contract self-tests.
  </behavior>
  <action>
Register the exact focused packaged-app identifier in the UI required-test list and cross-layer allowlist. Extend the evidence mutation fixtures to reject deletion, skip, zero discovery, or substitution with ControllerHostTests. Keep the complete lifecycle identifier present but do not credit it as passing until 03-23 supplies mapping and persistence behavior.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; scripts/ci/run-mac-verification.sh --self-test-contracts</automated>
  </verify>
  <done>Recurring evidence distinguishes production live input from downstream fake-source unit coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameController -> ControllerHost | Untrusted hardware values become application state. |
| ControllerHost -> live-test UI | Device state is exposed as user-visible and accessibility-visible feedback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-22-01 | Tampering | Input normalization | medium | mitigate | Frozen vocabulary, bounded d-pad threshold, runtime-id attribution, and unknown-input rejection. |
| T-03-22-02 | Denial of Service | Repeated/held reports | medium | mitigate | State de-duplication, release re-arm, handler teardown, and disconnect cleanup. |
| T-03-22-03 | Spoofing | Live-test controller source | medium | mitigate | Only the assigned runtime id drives the presented live test; an unassigned-device negative control verifies it. |
</threat_model>

<verification>
- ControllerHost unit tests prove typed event normalization and cleanup.
- The virtual-HID packaged test proves production press/release reaches ControllerTestView.
- Contract self-tests reject fake-only or missing evidence.
</verification>

<success_criteria>
- G-03-9 has a real production input bridge and reachable live test.
- The complete lifecycle was red before production changes and remains the binding oracle for 03-23.
- No mapping persistence claim is made by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-22-SUMMARY.md` when done.
</output>
