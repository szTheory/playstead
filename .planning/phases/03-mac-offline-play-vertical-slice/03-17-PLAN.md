---
phase: 03-mac-offline-play-vertical-slice
plan: 17
type: execute
gap_closure: true
gap_ids: []
wave: 1
depends_on: []
files_modified:
  - playstead-mac/PlaysteadUITests/Support/VirtualGamepad.swift
  - playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift
  - playstead-mac/PlaysteadUITests/PlaysteadUITests.entitlements
  - playstead-mac/TestPlans/UI.xctestplan
  - playstead-mac/Playstead/Controller/ControllerHost.swift
  - playstead-mac/PlaysteadTests/ControllerTests/ControllerHostTests.swift
  - playstead-mac/scripts/ci/run-mac-verification.sh
autonomous: true
requirements: [PLAY-04, QUAL-01]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The entitled IOHIDUserDevice -> GameController seam passes non-skipped on the same macos-26 signed recurring CI job before any downstream controller plan executes; local SDK exposure alone is not qualification."
    - "If the hosted runner cannot create or enumerate the virtual controller, the phase stops at a named feasibility verdict and escalates G-03-8/G-03-9/G-03-10 instead of allowing dependent plans to claim coverage."
    - "Controller persistence uses a stable model key distinct from the run-local connected-instance identity, with the identical-controller behavior documented and tested."
    - "The virtual-HID seam exists only in the UI-test runner; the distributed app has no HID-creation API, descriptor, entitlement, or environment-controlled arbitrary report channel."
    - "The new integration tests are selected by UI.xctestplan and required by run-mac-verification.sh, so zero discovery, skip, or entitlement failure makes CI red."
  artifacts:
    - path: "playstead-mac/PlaysteadUITests/Support/VirtualGamepad.swift"
      provides: "Finite test-only HID descriptor and deterministic report producer"
    - path: "playstead-mac/Playstead/Controller/ControllerHost.swift"
      provides: "Production GameController enumeration, stable identity, and input-event bridge"
    - path: "playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift"
      provides: "Hosted framework-level enumeration feasibility tracer"
  key_links:
    - "VirtualGamepad IOHID descriptor -> macOS HID stack -> GCController enumeration -> production ControllerSettings surface"
    - "PlaysteadUITests.entitlements -> UI-test runner signing -> IOHIDUserDevice creation"
    - "UI.xctestplan selection -> macos-26 mac-verification evidence -> verify-hosted-evidence binding -> downstream-plan gate"
---

<objective>
Qualify the shared automated hardware seam for G-03-8, G-03-9, and G-03-10 on the actual recurring CI environment.

The existing unit tests stop at FakeControllerInputSource, and SDK symbol availability does not establish that an ad-hoc-signed hosted runner may create a virtual HID device that GameController will enumerate. Build one finite virtual gamepad in the non-distributed UI-test runner and qualify it first on the exact macos-26 signed CI job used by recurring verification. This is the thinnest framework-level path every later controller acceptance journey depends on; production input forwarding moves to 03-22 so its packaged-app oracle can be red before implementation.

Purpose: replace physical-controller availability as an acceptance prerequisite with deterministic integration evidence, without weakening the production GameController boundary.
Output: a hosted-runner feasibility verdict, an entitled virtual-HID producer, a stable identity contract, and fail-closed CI selection.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-CONTEXT.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-UAT.md
@.planning/debug/g-03-8-controller-automation.md
@.planning/debug/g-03-9-controller-lifecycle.md
@.planning/debug/g-03-10-objective-accessibility.md
@.planning/phases/03-mac-offline-play-vertical-slice/03-10-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/verify-hosted-evidence.yml

<interfaces>
ControllerInputSource currently exposes currentControllers plus startObserving(onConnect:onDisconnect:). ControllerHost owns connectedControllers, assignedControllerID, liveInputs, showRecoveryBanner, assign(controllerID:), and reportInput(_:active:). Plan 03-22 extends this boundary with production input events after its packaged-app oracle is red rather than adding a second controller host.

ControllerDescriptor.id currently conflates a run-local ObjectIdentifier-derived instance id with ControllerMapping.controllerProductID. Separate those concepts: the instance id distinguishes simultaneous devices; a stable model key persists the mapping. Identical devices may deliberately share a model mapping, but that behavior must be explicit and tested.

The existing UI layer is selected through TestPlans/UI.xctestplan and run_test_layer ui in scripts/ci/run-mac-verification.sh. Required tests use identifiers of the form PlaysteadUITests.ClassName/testMethodName.
</interfaces>
</context>

<package_legitimacy_gate>
Inert. This plan adds no package-manager dependency; it uses IOKit and GameController from the pinned macOS SDK.
</package_legitimacy_gate>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Qualify one virtual gamepad on the signed recurring macos-26 CI runner</name>
  <precondition>The branch can run the repository's `mac-verification` GitHub Actions job; an authentication failure becomes the workflow's dynamic auth gate, not a local substitute for hosted evidence.</precondition>
  <files>playstead-mac/PlaysteadUITests/Support/VirtualGamepad.swift, playstead-mac/PlaysteadUITests/ControllerHardwareIntegrationTests.swift, playstead-mac/PlaysteadUITests/PlaysteadUITests.entitlements, playstead-mac/TestPlans/UI.xctestplan, playstead-mac/scripts/ci/run-mac-verification.sh</files>
  <behavior>
    - RED: without the entitlement/fixture, creating the virtual gamepad or observing it in the packaged app fails the test with a named stage; it never becomes XCTSkip and never passes on a zero-controller state.
    - GREEN: on the recurring `mac-verification` job's macos-26 runner, with the UI-test bundle signed by the repository harness, a fixed descriptor creates exactly one extended gamepad, the app's production ControllerSettings surface renders it, and a detach removes it within a bounded timeout.
    - GREEN: recreating the fixture produces a reconnect observation through GCController notifications without relaunching the app.
    - Negative control: an intentionally invalid descriptor is rejected by the fixture and cannot satisfy the enumeration oracle.
    - Boundary control: the fixture accepts only its compiled descriptor and named report methods; no path, descriptor bytes, usage page, or arbitrary report arrives from an environment variable.
  </behavior>
  <action>
Write the failing feasibility XCUITest first. Add `VirtualGamepad` under the UI-test target only, backed by IOHIDUserDevice with one compiled, conventional extended-gamepad descriptor covering d-pad, A/B, shoulders, Menu, and Options. Expose named operations (`connect`, `disconnect`, `press`, `release`, directional report) and stage-specific errors; do not expose arbitrary descriptor/report injection. Add the SDK-required `com.apple.developer.hid.virtual.device` entitlement only to PlaysteadUITests.entitlements, preserving the existing unsandboxed-runner explanation and the shipped app's separate D-04 posture.

The tracer launches the real packaged app under an existing deterministic profile, navigates through the existing readiness route to ControllerSettingsView, asserts no controller row before creation, creates the device, waits for the production settings surface to render the controller, detaches it, and reconnects it without relaunch. Use bounded expectations and explicit non-vacuity counts. Add the test class to UI.xctestplan and its exact identifier to the unfiltered UI layer required-test list.

Treat hosted execution as the feasibility gate, not as a later confidence check: the same commit must pass this identifier in `.github/workflows/ci.yml`'s `mac-verification` job on `runs-on: macos-26`, with hosted evidence recording runner image/build, signing posture, discovery count, skip state, and outcome. Extend the complete hosted-evidence manifest validator and its mutation fixtures in run-mac-verification.sh so this exact identifier is mandatory in the UI layer; `.github/workflows/verify-hosted-evidence.yml` then binds it to the completed recurring run. A local pass, SDK exposure, or an unsigned unit fake cannot satisfy the gate. If device creation, entitlement authorization, or GameController enumeration fails there, record the named failing stage, stop before Task 2 and every dependent plan, and escalate G-03-8/G-03-9/G-03-10 for phase replanning around a different OS-supported automation seam or an explicit human-needed disposition; never fall through to fake-source evidence as equivalent coverage. Per D-14, keep keyboard/pointer available throughout; the detailed non-stranding recovery assertion belongs to 03-18 after the banner is composed.
  </action>
  <verify>
    <automated>test -s hosted-evidence/run-record.json &amp;&amp; test -s hosted-evidence/run-view.json &amp;&amp; test -s hosted-evidence/complete-verification-evidence.json &amp;&amp; playstead-mac/scripts/ci/run-mac-verification.sh --verify-hosted-run complete --run-record hosted-evidence/run-record.json --run-view hosted-evidence/run-view.json --manifest hosted-evidence/complete-verification-evidence.json</automated>
  </verify>
  <done>The exact recurring macos-26 signed CI job records the virtual controller identifier discovered once, executed once, non-skipped, and passed through packaged-app enumeration/detach/reconnect; otherwise phase execution halts with the failing feasibility stage and escalation disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate runtime controller identity from persisted mapping identity</name>
  <files>playstead-mac/Playstead/Controller/ControllerHost.swift, playstead-mac/PlaysteadTests/ControllerTests/ControllerHostTests.swift</files>
  <behavior>
    - RED: two equal-model controller descriptors cannot simultaneously preserve distinct runtime assignment identities and one stable persistence key under the current conflated id.
    - GREEN: two simultaneous identical controllers have distinct runtime ids but one documented stable model mapping key; reconnect/relaunch reproduces that mapping key.
    - Negative control: changing run-local object identity cannot change the stable mapping key, while changing normalized public model metadata must change it.
  </behavior>
  <action>
Write the identity tests and observe the current conflated-id failure before changing ControllerDescriptor. Then retain a runtime instance id for assignment among simultaneously connected devices and add a stable model mapping key derived only from normalized public GCController product metadata. Document that identical controller models share a mapping because GameController exposes no durable per-unit serial; test that contract rather than implying per-unit persistence. Keep input-forwarding production code out of this task: 03-22 begins with a failing packaged-app press/release oracle and implements the bridge only after that red state is captured. The hosted feasibility result from Task 1 is a hard prerequisite, so this task must not run if the entitlement/device/enumeration seam failed.
  </action>
  <verify>
    <automated>cd playstead-mac &amp;&amp; xcodebuild test -scheme Playstead -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -testPlan Unit -only-testing:PlaysteadTests/ControllerHostTests</automated>
  </verify>
  <done>Runtime assignment and stable mapping identities are distinct and tested honestly; production input forwarding remains unimplemented until 03-22's packaged production-input oracle is red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner -> macOS HID | Synthetic reports cross into a privileged OS device service. |
| GameController -> app | Untrusted device metadata and lifecycle notifications enter production state. |
| Runtime device identity -> SQLite mapping key | Ephemeral framework objects influence durable user configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17-01 | Elevation of Privilege | VirtualGamepad / entitlement | high | mitigate | Entitlement stays on the non-distributed UI runner; a release-exclusion guard in 03-20/03-21 inspects the shipped app and compile conditions. |
| T-03-17-02 | Tampering | HID report parser | medium | mitigate | Fixed descriptor, named report methods, range checks, de-duplication, and invalid-descriptor negative control. |
| T-03-17-03 | Denial of Service | HID enumeration lifecycle | medium | mitigate | One finite device, bounded attach/detach expectations, deterministic teardown, and no arbitrary report channel. |
| T-03-17-04 | Spoofing | Stable controller key | low | accept | GameController exposes model metadata but no durable serial; identical models intentionally share mappings and runtime assignment still uses distinct instance ids. |
</threat_model>

<verification>
- Unit: ControllerHostTests execute non-zero and prove runtime-versus-persistence identity semantics.
- Integration/E2E: the entitled virtual device enumerates, disconnects, and reconnects through the packaged app without XCTSkip.
- CI: the macos-26 hosted manifest contains the exact feasibility identifier once and verify-hosted-evidence binds it to a successful recurring run.
- Boundary: no production target contains IOHIDUserDevice creation or the virtual-device entitlement.
</verification>

<success_criteria>
- The shared virtual-HID prerequisite for G-03-8/G-03-9/G-03-10 is real, bounded, and recurring in CI.
- Downstream production input work cannot begin until the enumeration seam is proven on its real recurring runner.
- No claim about subjective accessibility experience is made by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-mac-offline-play-vertical-slice/03-17-SUMMARY.md` when done.
</output>
