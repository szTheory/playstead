---
schema_version: 1
open_count: 46
waived_count: 2
fixed_count: 42
total_count: 90
last_updated: 2026-09-15T15:23:30.605Z
---

# Broken Windows Ledger

> Cross-phase defect register. With `workflow.windows_enforce` enabled, `/gsd-ship` blocks while `open_count > 0`.
> Waive with `gsd-tools windows waive <id> "<reason>"` (reason required).
> Mark fixed with `gsd-tools windows fixed <id>`.

| id | phase | kind | file | line | description | status | reason | recorded_at | resolved_at |
|----|-------|------|------|------|-------------|--------|--------|-------------|-------------|
| 1 | 03 | stub | playstead-mac/Playstead/Adapter/AdapterHost.swift |  | verifyInstalledDigest() expects an .install-verify.json sidecar that no plan yet writes (emulator installer is 03-08/03-09 territory) | fixed |  | 2026-08-31T00:10:31.234Z | 2026-08-31T02:44:11.180Z |
| 2 | 03 | unrun-verify | playstead-mac/scripts/sign-and-notarize.sh |  | Notarization + Developer-ID release build could not run (no Developer ID Application cert / paid Apple Developer Program in this environment); notarization deferred per 03-01 owner decision | waived | Deferred by owner 2026-08-31: local development only; no Developer ID / paid Apple Developer Program enrollment this milestone. Notarization is distribution-only and does not affect running a dev-signed build on the owner's own Mac. Revisit before distributing to other machines. | 2026-08-31T00:10:35.783Z | 2026-08-31T05:45:26.950Z |
| 3 | 03 | unrun-verify | playstead-mac/Playstead/Adapter/AdapterHost.swift |  | Task 3 human-check (notarized app, network disabled, Play launches mGBA, quit returns to library) unverified in this session — no installed emulator/downloaded ROM/Developer ID cert available | open |  | 2026-08-31T00:10:40.298Z |  |
| 4 | 03 | deviation | playstead-mac/Playstead/Library/GameCardView.swift |  | DownloadCoordinator progress percent wired but not yet consumed by LibraryViewModel/GameCardView's live rendering path (03-07) | open |  | 2026-08-31T01:42:14.870Z |  |
| 5 | 03 | deviation | playstead-mac/Playstead/App/PlaysteadApp.swift |  | AppEnvironment does not yet construct DownloadQueue/DownloadCoordinator/QuotaManager/PinStore/EvictionPlanner as live app-wide singletons (03-07) | fixed |  | 2026-08-31T01:42:20.733Z | 2026-08-31T12:46:28.600Z |
| 6 | 03 | unrun-verify | playstead-mac/Playstead/Library/StorageView.swift |  | Visual/interactive click-through of DownloadsView/QuotaSettingsView/ReclaimPromptView/StorageView against a live paired server unverified in this headless session (03-07 coverage D6) | open |  | 2026-08-31T01:42:25.363Z |  |
| 7 | 03 | stub | playstead-mac/Playstead/Adapter/BiosStore.swift |  | BiosStore's known-reference digest set has no production default (DI-only, no fabricated evidence); real reference digest wiring is deferred until sourced | open |  | 2026-08-31T02:43:54.600Z |  |
| 8 | 03 | deviation | playstead-mac/Playstead/Adapter/AdapterHost.swift |  | Gatekeeper-held child hangs silently: AdapterHost.launch treats a successful Process.run() as launched, but a quarantined bundle is left suspended at _dyld_start and terminationHandler never fires — the app believes the emulator runs forever with no error surfaced. AdapterInstaller preserves quarantine per D-05. Proven experimentally (signed+quarantined HUNG; signed alone exits 0.15s). A properly notarized mGBA should pass assessment so the happy path is likely fine, but the failure shape is silent and unbounded. Fix requires a design decision: a liveness probe cannot distinguish held from idle (held task reports S not T; Mach suspend count needs task-port access the app lacks), and pre-flighting SecStaticCodeCheckValidity at install would reject the /bin/echo stand-ins both test suites depend on, so a fixture strategy must be decided alongside it. | open |  | 2026-08-31T10:48:44.083Z |  |
| 9 | 03.5 | unrun-verify | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Task 1 exact UI-layer drag verification requires the centrally orchestrated hosted macOS runner; local app launch is prohibited by the no-password safety boundary. | fixed |  | 2026-09-01T06:29:58.866Z | 2026-09-01T18:29:36.711Z |
| 10 | 03.5 | unrun-verify | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Task 2 full CurationInteractionTests UI-layer verification requires the centrally orchestrated hosted macOS runner; local app launch is prohibited by the no-password safety boundary. | fixed |  | 2026-09-01T06:29:58.948Z | 2026-09-01T18:29:41.733Z |
| 11 | 03.5 | deviation | playstead-mac/Playstead/Library/ShelfView.swift |  | Plan path corrected from nonexistent Curation/ShelfView.swift to the production Library/ShelfView.swift location. | open |  | 2026-09-01T06:29:59.030Z |  |
| 12 | 03.5 | deviation | playstead-mac/Playstead/UITesting/DeterministicProfile.swift |  | Added compile-gated UUID-only persisted profile support required to prove process-relaunch durability without touching Plan 07 storage production files. | open |  | 2026-09-01T06:29:59.112Z |  |
| 13 | 03.5 | deviation | playstead-mac/scripts/ci/run-mac-verification.sh |  | Hosted run 33478091423 exposed an EXIT trap that referenced function-local keyboard state after scope unwind and masked the underlying early build result; fixed in a4ef343 with globally initialized guarded cleanup and pre-capture failure regression. | open |  | 2026-09-01T06:44:32.931Z |  |
| 14 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Split broad hosted curation UI identity into three exact stages and changed drag to the deterministic last-row-to-first boundary after run 33481640835. | open |  | 2026-09-01T07:44:47.355Z |  |
| 15 | 03.5 | deviation | playstead-mac/Playstead/UITesting/UITestBootstrap.swift |  | Hosted run 33483731474 exposed redundant fresh-position validation on persisted reorder relaunch plus stale shelf selectors; fixed and split into nine exact curation stages. | open |  | 2026-09-01T08:14:14.831Z |  |
| 16 | 03.5 | deviation | playstead-mac/Playstead/Curation/CollectionsView.swift |  | Hosted run 33486052488 exposed Favorites collapsed-node and Collections child-text routing mismatches; fixed with exact card queries and a stable collection route button. | open |  | 2026-09-01T08:41:10.578Z |  |
| 17 | 03.5 | deviation | playstead-mac/Playstead/Library/GameCardView.swift |  | Hosted run 33488360822 exposed card identity before accessibility collapse and drag gestures on semantic content instead of List cells; fixed and split drag mutation from durability. | open |  | 2026-09-01T09:07:27.559Z |  |
| 18 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Hosted run 33490616418 exposed only aggregate reorder outcomes; split route, cell ownership, drag mutation/durability, keyboard focus/effect/durability and full E2E, with a slow held drag. | open |  | 2026-09-01T09:33:08.765Z |  |
| 19 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Hosted run 33495336534 proved routes and cells but no mutations: switched touch-style press-drag to macOS click-drag and removed duplicate Space after focusContainedAction. | open |  | 2026-09-01T10:33:18.230Z |  |
| 20 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Hosted run 33498048537 proved focusContainedAction is focus-only; restored one Space, isolated Move Up availability/click, and held the macOS drag destination. | open |  | 2026-09-01T11:04:38.825Z |  |
| 21 | 03.5 | deviation | playstead-mac/Playstead/Curation/CollectionsViewModel.swift |  | Hosted run 33500660524 exposed an unobserved SQLite member query; refresh now publishes a member revision so optimistic reorder and bounded evidence invalidate. | open |  | 2026-09-01T11:39:10.783Z |  |
| 22 | 03.5 | deviation | playstead-mac/Playstead/Curation/CollectionDetailView.swift |  | Hosted run 33503640089 isolated keyboard focus ownership; collection and queue reorder actions now bind stable identity and focus on the final button through playsteadFocusable. | open |  | 2026-09-01T12:13:48.616Z |  |
| 23 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Hosted run 33506605120 exposed app-scoped Space dispatch plus a missing combined-path activation; both keyboard paths now send one Space through the exact focused button. | open |  | 2026-09-01T12:48:01.842Z |  |
| 24 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Hosted run 33510034845 exposed an impossible retained-focus assertion on a Move Up button disabled by reaching the first boundary; the keyboard proof now moves last-to-middle while preserving exact effect/order/boundary/durability checks. | open |  | 2026-09-01T13:28:24.085Z |  |
| 25 | 03.5 | deviation | playstead-mac/PlaysteadUITests/CurationInteractionTests.swift |  | Hosted run 33513649409 lacked keyboard stage detail; curation now uses exact-target canary focus detection and bounded safe focus/effect/retention test IDs. | open |  | 2026-09-01T14:17:23.468Z |  |
| 26 | 03.5 | deviation | playstead-mac/scripts/ci/run-mac-verification.sh |  | Hosted run 33518726537 executed but did not report the three curation keyboard stage tests; all are now required evidence before any production command-model change. | open |  | 2026-09-01T14:56:43.305Z |  |
| 27 | 03.5 | deviation | playstead-mac/Playstead/Curation/CollectionDetailView.swift |  | Hosted run 33526574205 proved nested List row buttons are not ordinary Tab stops; keyboard reorder now uses exact List selection plus visible bounded commands through the existing settlement path. | open |  | 2026-09-01T16:09:38.131Z |  |
| 28 | 04 | deviation | playstead-server/lib/playstead_web/controllers/api/v1/saves_controller.ex |  | Save-lane UploadSlots/RateLimiter (D-33) not yet wired into the save routes | open |  | 2026-09-04T03:18:16.081Z |  |
| 29 | 04 | stub | playstead-mac/Playstead/Sync/JournalApplier.swift |  | CacheObjectsSaveBytesPrefetcher.onPrefetchNeeded is a no-op; real byte transfer for D-43 prefetch deferred to a later downloads-lane plan | open |  | 2026-09-04T03:18:16.168Z |  |
| 30 | 04 | deviation | playstead-server/lib/playstead/export/export.ex |  | Real Playstead.Saves revision data is not yet loaded into Export.to_layout_input/1; SavesPlan/Sidecar/BagitWriter pipeline is built+tested against synthetic data only (04-08) FIXED by plan 04-21 (22d92f7/433f7e4/8e32347): Export.load_save_revisions/2 loads real save revisions at the Export context boundary; the real file is playstead-server/lib/playstead/export.ex, not export/export.ex. | fixed |  | 2026-09-04T15:35:17.080Z | 2026-09-06T02:02:52.856Z |
| 31 | 04 | deviation | playstead-server/lib/playstead_web/live/exports_live.ex |  | No console UI control yet sets ExportRecord.saves_scope (defaults to all); field is persisted and fully threaded through Worker (04-08) | fixed |  | 2026-09-04T15:35:17.159Z | 2026-09-04T19:49:47.031Z |
| 32 | 04 | deviation | playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift |  | Comparison sheet is built and tested standalone but not yet wired into a live attention-inbox or game-detail navigation path on the Mac (server console entry point already shipped in 04-10); presentation wiring is left for a later plan STALE, not fixed by 04-23: ConflictComparisonSheet was wired into ReadinessSheetView by plan 04-16 / commit c223218, which postdates this entry; 04-VERIFICATION.md confirmed the wiring directly in source. Closed with c223218 as the resolving commit. | fixed |  | 2026-09-04T20:20:49.523Z | 2026-09-06T02:02:53.323Z |
| 33 | 04 | deviation | playstead-mac/Playstead/Sync/JournalApplier.swift |  | Divergence detection does not yet trigger an eager 32KB prefetch of both sides' artifacts (CacheObjectsSaveBytesPrefetcher.onPrefetchNeeded is still the no-op tracked by WINDOWS #29); comparing/choosing works offline only once both blobs are already locally cached by some other path | open |  | 2026-09-04T20:20:49.612Z |  |
| 34 | 04 | unrun-verify | playstead-mac/PlaysteadUITests/ConflictResolutionInteractionTests.swift |  | UI-layer keyboard-interaction verification requires the centrally orchestrated hosted macOS runner; local execution is disabled by the project's login-Keychain launch guard (mirrors WINDOWS #9/#10 precedent) | open |  | 2026-09-04T20:20:49.696Z |  |
| 35 | 04 | deviation | playstead-mac/Playstead/Saves/OnlyCopyInterruptiveSheet.swift |  | Unpair, sign out, and delete game have no production entry point yet in this codebase; only remove-local-copy (ReclaimPromptView) and eviction (StorageView) are wired to the interruptive gate. The sheet and gate are built, tested, and reusable for whichever future plan adds those three call sites. | open |  | 2026-09-05T01:12:59.522Z |  |
| 36 | 04 | unrun-verify | playstead-mac/PlaysteadUITests/OnlyCopyInterruptionTests.swift |  | UI-layer keyboard-interaction verification requires the centrally orchestrated hosted macOS runner; local execution is disabled by the project's login-Keychain launch guard (mirrors WINDOWS #9/#10/#34 precedent). | open |  | 2026-09-05T01:12:59.615Z |  |
| 37 | 04 | deviation | playstead-mac/Playstead/Library/GameRowView.swift |  | LaunchSavePlanner/SavePlanExecutor are still not wired into GameRowView.play() -- AdapterHost.launch's executeSavePlan closure exists (04-07) but no call site passes one; 04-07-SUMMARY.md named plan 04-13 as the downstream consumer, but 04-13's declared task scope (files_modified) is limited to the two new proof tests, the two xctestplans, and run-mac-verification.sh, so this wiring remains open | fixed |  | 2026-09-05T01:50:48.874Z | 2026-09-05T02:27:54.263Z |
| 38 | 04 | stub | playstead-mac/Playstead/Saves/LaunchSaveEnvironment.swift |  | captureExistingFile commits pre-existing save bytes into the CAS for durability but does not create a SaveStore revision row, so a pre-restore capture on the launch path is not yet visible in SaveHistorySheet (04-14) | open |  | 2026-09-05T02:28:02.845Z |  |
| 39 | 04 | stub | playstead-mac/Playstead/Library/GameRowView.swift |  | SaveLaunchNotice from the launch-path save plan is held on GameRowView's saveLaunchNotice @State but not yet rendered -- no detail view with a Save section exists yet in this codebase to surface it inline into (04-14) | open |  | 2026-09-05T02:28:02.935Z |  |
| 40 | 04 | deviation | playstead-mac/Playstead/Saves/OnlyCopyEscalation.swift |  | OnlyCopyEscalationPanel now has a real call site (ReadinessSheetView) gated by a real only-copy count, but the four unfixable escalation reasons (revokedAuth/capabilitySkew/serverRefusal/compatibilityRejection) still can't fire in production because SaveUploadLane has no wired failure-classification output yet (mac2 review WR-04) (04-16) FIXED by plan 04-19 (fb6ad53). | fixed |  | 2026-09-05T14:17:19.948Z | 2026-09-05T22:18:48.497Z |
| 41 | 04 | deviation | playstead-mac/Playstead/Readiness/ReadinessEngine.swift |  | AppEnvironment.saveReadinessCase(for:) does not compute .serverHasNewer -- distinguishing it from .localOnlyReachable needs per-device session context this client doesn't track yet; every other SaveReadinessCase, including the higher-priority .twoVersions, is computed (04-16) | open |  | 2026-09-05T14:17:20.065Z |  |
| 42 | 04 | deviation | playstead-mac/Playstead/Sync/Outbox.swift |  | AppEnvironment now constructs a shared SaveOutbox/SaveConflictResolver so MC-06's resolution path durably records locally, but no drain trigger (OutboxDrainTrigger-equivalent) is wired for SaveOutbox yet -- a resolved fork is recorded locally but may not reach the server promptly (mac2 review WR-04's other half) (04-16) FIXED by plan 04-23 (5c6439c): SaveOutboxDrainTrigger wired to post-enqueue, reachability-regained, and scene-active triggers. | fixed |  | 2026-09-05T14:17:20.181Z | 2026-09-06T02:02:52.938Z |
| 43 | 04 | deviation | playstead-mac/Playstead/Readiness/ReadinessSheetView.swift |  | saveHistorySessions closure passed to ReadinessSheetView is still never populated from SaveStore (mac2 review CR-04's other half) -- SaveHistorySheet's 'Review versions...' path for a non-diverged line still renders its empty state; out of 04-16's declared MC-01..MC-06 scope FIXED by plan 04-22 (62e0db0): SaveHistorySessionBuilder wired to real SaveStore data at both ReadinessSheetView call sites. | fixed |  | 2026-09-05T14:17:20.303Z | 2026-09-06T02:02:53.019Z |
| 44 | 04 | unmet-truth | playstead-mac/Playstead/Saves/SaveUploadLane.swift |  | SaveUploadLane (the dedicated actor draining locally-captured save revisions to the server, D-16/D-32) is never constructed anywhere in production -- discovered baselining 04-18's reachability sweep. A more severe superset of the already-tracked WINDOWS #40 (which only covered its missing failure-classification output): the WHOLE upload path is unwired, not just one output of it. See 04-18-SUMMARY.md. FIXED by plan 04-19 (fb6ad53). | fixed |  | 2026-09-05T21:54:18.525Z | 2026-09-05T22:18:48.578Z |
| 45 | 04 | unmet-truth | playstead-mac/Playstead/Saves/SaveCapturePoller.swift |  | SaveCapturePoller is constructed in production ONLY inside SaveSessionRecovery.replay, which itself has no production caller -- there is no live, in-play save-capture trigger anywhere in the shipped app. Discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md. FIXED by plan 04-19 (3691ec3). | fixed |  | 2026-09-05T21:54:18.602Z | 2026-09-05T22:18:48.656Z |
| 46 | 04 | unmet-truth | playstead-mac/Playstead/Saves/SaveSessionRecovery.swift |  | SaveSessionRecovery (crash-recovery replay actor, D-05/D-07) is never constructed in production despite its own doc comment saying app launch should call it once per known save line. Discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md. FIXED by plan 04-19 (3691ec3). | fixed |  | 2026-09-05T21:54:18.681Z | 2026-09-05T22:18:48.732Z |
| 47 | 04 | unmet-truth | playstead-mac/Playstead/Saves/SaveRollup.swift |  | SaveRollup.rollup(for:) (D-36's game-level save rollup string) has zero production callers -- not reachable from any shipped UI. Discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md. FIXED by plan 04-22 (24d5c34): AppEnvironment.saveRollupSummary(forAssetSetID:) is SaveRollup.rollup(for:)'s first production caller; reachability-allowlist.txt's SaveRollup line removed. | fixed |  | 2026-09-05T21:54:18.760Z | 2026-09-06T02:02:53.099Z |
| 48 | 04 | unmet-truth | playstead-mac/Playstead/Controller/ControllerRecoveryBanner.swift |  | ControllerRecoveryBanner and ControllerTestView (Controller/) and FirstRunBanner and ShowAllSystemsControl (Library/) are fully built views with zero production call sites -- discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md and scripts/ci/reachability-allowlist.txt for the full list and per-symbol detail. | open |  | 2026-09-05T21:54:18.835Z |  |
| 49 | 04 | stub | playstead-mac/Playstead/Saves/SaveSessionCoordinator.swift |  | A promoted capture's bytes are written to the save-captures directory and recorded as a SaveStore revision, but are NOT committed into the CAS. LaunchSaveContextBuilder derives head candidates' bytesLocal from casManager.contains(blobSHA256), so a locally-captured revision reads as bytes-not-local at the next launch and cannot be restored from history until it has round-tripped through the server. Same-device continuity still works (the live .sav artifact persists), so this is the mirror image of WINDOWS #38 rather than a break in SAVE-01's main path. Out of 04-19's declared scope (capture -> SaveStore -> upload). FIXED by plan 04-20 (10783c4 excludes save blobs from the reclaim menu; de3aec1 commits capture bytes into the CAS). | fixed |  | 2026-09-05T22:19:41.797Z | 2026-09-05T23:05:00.000Z |
| 50 | 04 | stub | playstead-mac/Playstead/Saves/SaveCaptureBlockedState.swift |  | AppEnvironment constructs SaveCaptureBlockedState with no SaveCaptureAlertSink and no SaveDirectUploadTransport, so a blocked capture (D-31) is recorded durably and is queryable, but raises no user-visible alert and has no direct-upload escape hatch in the shipped app. The seams exist and are injected; no attention surface consumes them yet. | open |  | 2026-09-05T22:19:41.878Z |  |
| 51 | 04 | deviation | playstead-mac/Playstead/Saves/SaveCapturePoller.swift |  | The save-captures directory (<root>/save-captures/<assetSetID>/) has no reclamation path: each session leaves one session-<id>.staged.sav plus one <digest>.sav promoted blob, and QuotaManager measures paths.objects only, so capture blobs count against neither the quota nor the free-space floor and are never evicted. Save artifacts are small (tens of KB), so this is slow growth rather than an immediate hazard, but nothing bounds it. | open |  | 2026-09-05T22:19:41.957Z |  |
| 52 | 04 | stub | playstead-mac/Playstead/Saves/SaveSessionRecovery.swift |  | SaveSessionRecovery.replay (D-07 crash-recovery, reachable in production via AppEnvironment.recoverAbandonedSaveSessionsAtLaunch) records a promoted revision but holds no CASManager, so a crash-recovered capture's bytes never enter the CAS and LaunchSaveContextBuilder reports bytesLocal: false for it -- the same hole 04-20 closed on the live-session path (WINDOWS #49), reached via the second capture path. Out of 04-20's declared scope (files_modified named SaveSessionCoordinator only). Fix should extract 04-20's SaveSessionCoordinator.commitCaptureBytes into one shared spelling rather than a second copy. FIXED by plan 04-23 (fd4e2b4): extracted SaveCaptureBytesCommitter, shared by SaveSessionCoordinator and SaveSessionRecovery; recovery replay commits bytes into the CAS before inserting the row. | fixed |  | 2026-09-05T22:36:11.098Z | 2026-09-06T02:02:53.188Z |
| 53 | 04 | deviation | playstead-server/lib/playstead/export.ex |  | Export.load_save_revisions/2 selects only the (battery, slot 0) save line when a content_key has more than one save line; any other lines are silently excluded from export rather than merged (v1 client only ever writes one line, so this is currently unreachable). | open |  | 2026-09-06T01:18:12.481Z |  |
| 54 | 04 | stub | playstead-mac/Playstead/Net/APIClient.swift |  | The Mac client ships no pairing ceremony, so a human cannot pair a Mac at all. Every production PairingCredential construction is a keychain READ (KeychainStore.swift:152); the only writers are UI_TESTING-gated (UITestBootstrap.swift:552, DeterministicProfile.swift:49). There is no pairing view, no code-entry surface, no URL-scheme handler. The server side is complete -- POST /api/v1/device-pairing/requests, GET /requests/:id, POST /requests/:id/redeem, plus the devices approval console -- and the client calls none of it. APIClient.swift:57 states it outright ('this tracer plan does not yet ship the pairing ceremony') and anticipates a future plan writing AppPaths.root/pinned-ca.der; until then APIClient uses default TLS trust rather than the pinned root CA. LibraryShellView.swift:474's empty state instructs the user to 'Pair with your Playstead server to see your library', naming an action that exists nowhere in the app. 03.5-08 proved pairing only through scripts/ci/live-server.sh driving the HTTP ceremony from a shell script and handing a credential to a test build -- the human path was never built. Consequence: every human checkpoint requiring a paired Mac is unperformable, CP7-SAVE-C (UAT tests 107/112/120) included, which its 'blocked_by: physical-device' reason understates. Discovered 2026-09-08 when the owner tried to run CP7-SAVE-C and had no way to pair. Worked around for the owner's Mac only, by driving the production endpoints by hand and writing the server-issued credential into the login keychain (service dev.playstead.mac, device d23b584e); the missing UI itself is unfixed. | fixed |  | 2026-09-08T17:38:16.547Z | 2026-09-09T00:51:44.258Z |
| 55 | 04 | deviation | playstead-mac/Playstead/Saves/SaveUploadLane.swift |  | SHIPPED DEFECT: save uploads 404'd against every real server, so no captured revision has ever reached a server from the shipped app. APIClient.send does credential.baseURL.appendingPathComponent(path) and the paired credential's baseURL is the server ORIGIN, so callers own the whole path. SnapshotClient (/api/v1/snapshot) and ChangesClient (/api/v1/changes) spell it correctly; SaveUploadLane spelled 'saves/uploads/<id>' and 'saves/revisions' without the /api/v1 prefix, at both of its call sites. Server routes are PUT /api/v1/saves/uploads/:command_id and POST /api/v1/saves/revisions (router.ex:285,292). Observed on the owner's machine 2026-09-08: 'PUT /saves/uploads/... Sent 404' twice in the server log while POST /api/v1/play-sessions succeeded; two captured revisions sat at durability='queued' with 0 rows in the server's save_revisions. Capture, promotion and CAS commit all work -- only the upload half was broken. Fixed 2026-09-08 by prefixing both paths; guarded by scripts/ci/tests/api-path-prefix-test.sh, verified to reject the pre-fix source. WHY CI MISSED IT: SaveEndToEndTests guarded three fixture stages with bare 'guard try runFixture(...) else { return }', so a failing stage returned from the test having asserted nothing and XCTest recorded a pass -- the end-to-end proof of this exact path was fail-open. Seven such returns existed across SaveEndToEndTests and LiveServerSnapshotTests; all seven now XCTFail, and four-layer-topology-test.sh line 199 previously PINNED the fail-open shape as required. UAT tests 106 and 117 were marked pass on that evidence and are reverted to issue. | open |  | 2026-09-08T17:49:43.371Z |  |
| 56 | 04 | deviation | playstead-server/lib/playstead_web/controllers/api/v1/saves_controller.ex |  | SHIPPED DEFECT (server): the streamed save-upload route replied on the ORIGINAL conn instead of the one advanced by reading the body. Plug.Conn is immutable and body_stream/1 threads its own conn through Stream.resource, so create_upload/2 responded 200 on a conn that still believed the request body was unread. The connection was then left mid-body, and the NEXT request on it stalled until Bandit's 15s read timeout, surfacing as 'Bandit.HTTPError Read timeout' on the upload's request_id and a 502 EOF at Caddy for the following request. Because SaveUploadLane does PUT bytes then immediately POST /api/v1/saves/revisions on the same connection, the metadata commit ALWAYS died: bytes landed in save_pending_uploads, no revision ever committed, and the client's revision stayed durability='queued' forever. Size-dependent, which is why it was never noticed: reproduced deterministically at 4096 bytes -> next request 0.004s, 65536 -> 15.010s, 131072 -> 15.010s. Fixed 2026-09-08 by capturing the drained conn from body_stream/1 and replying on it; re-verified 0.0017s at both previously-broken sizes. Not reachable by ConnTest (Phoenix.ConnTest never opens a real connection), which is why the 2 existing saves_controller tests pass either way; the live-server e2e test WOULD have caught it had it not been fail-open (WINDOWS #55). Full server suite green after the fix: 1037 tests, 0 failures. | open |  | 2026-09-08T18:22:12.727Z |  |
| 57 | 04 | stub | playstead-mac/Playstead/Saves/SaveSessionCoordinator.swift |  | Capture provenance is never recorded: save_revision.adapter_id and adapter_version are empty on every revision produced by the real Play path, though both columns exist client-side and server-side and SaveUploadLane forwards them when present. Observed on the owner's machine 2026-09-08 across all three revisions captured through the shipped app (Advance Wars 65,536 bytes; Pokemon LeafGreen 131,072 x2) -- adapter_id '', adapter_version '', capture_method 'session'. Consequence: a stored revision cannot say which emulator or which pinned adapter version produced it, so a future adapter upgrade that changes save-format behaviour leaves no way to tell affected revisions from unaffected ones. CP7-SAVE-C's own how-to-verify asks the developer to record the adapter version alongside the observations, which the app itself cannot supply. Not blocking: capture, upload, restore and byte-identity are all proven correct without it. Low severity, but it is provenance for the one artifact class this phase exists to protect. | fixed |  | 2026-09-08T18:34:29.543Z | 2026-09-08T22:15:00.000Z |
| 61 | 04 | deviation | playstead-mac/PlaysteadUITests/SaveEndToEndTests.swift |  | SaveEndToEndTests failed once on hosted run 34281587417 (result file never written within 60s) and passed on 34285679092 with no change to any code the test exercises. The failure is intermittent and its cause is still unknown. Recorded as OPEN rather than closed by the green run: a test that fails one run in N is not fixed by the run where it passes. WINDOWS #60's failure channel and the 120s deadline mean the next occurrence will name which of the four harness steps (quiesce, pairing, upload, sync) failed, and whether it was slow or broken. Do not close without that evidence. | open |  | 2026-09-08T23:50:00.000Z |  |
| 60 | 04 | deviation | playstead-mac/Playstead/UITesting/UITestBootstrap.swift |  | CI GAP (fixed same day, 4903ec8): the save end-to-end harness swallowed every failure, on the stated grounds that the absent result file was itself the signal. It is a signal with no content -- a slow run and a broken upload both surface as 'result was never written within 60s'. Hosted run 34281587417 failed exactly this way one run after the test was promoted to required, and the evidence named only the polling assertion's file:line. Fixed by writing a sanitized reason to a sibling file and giving each known cause its own assertion site, since CI's evidence pipeline keeps file:line and discards messages. Deadline raised 60s -> 120s only alongside the diagnostic. Root cause of the underlying timeout still unknown -- the next occurrence will name it. | fixed |  | 2026-09-08T22:40:00.000Z | 2026-09-08T22:40:00.000Z |
| 58 | 04 | deviation | playstead-mac/PlaysteadUITests/SaveEndToEndTests.swift |  | CI GAP (fixed same day, cc7640d): the phase's only end-to-end save proof had never run on a hosted runner. SaveEndToEndTests resolved its fixture environment from ProcessInfo.processInfo.environment alone; the XCTest process inherits a PATH with no Elixir toolchain, so live-server.sh's 'mix playstead.mac_ci_fixture' died with 'mix: command not found' at provision-domain on every hosted run. The sibling LiveServerSnapshotTests already merged the runner-written live-server-runtime.json (which carries the real PATH) over the inherited environment and documented exactly this hazard; SaveEndToEndTests' doc comment claimed it reused that discipline 'verbatim' and did not. Invisible because the test was fail-open until e6316d5 and because live-server.sh discarded its own stderr until 266015e. Third seam-between-plans defect of this phase after #55 and #56. Guarded by scripts/ci/tests/live-server-env-resolution-test.sh, falsified against the real pre-fix source. | fixed |  | 2026-09-08T20:58:11.764Z | 2026-09-08T20:58:25.364Z |
| 59 | 04 | deviation | playstead-mac/scripts/ci/run-mac-verification.sh |  | CI GAP (fixed same day, 266015e): the hosted UI layer's 1800s deadline sat 9% above its own observed runtime (1631s in the last green run, 50960ba), and run 34264338508 was SIGTERMed at exactly 1800s on a commit whose diff touched no file in the UI test plan. Raised to 2700s with four-layer-topology-test.sh's pinned expectation and ceiling moved with it. Recorded because the failure mode is a gate that reports red for reasons unrelated to the code under test, which trains readers to discount it. | fixed |  | 2026-09-08T20:58:18.772Z | 2026-09-08T20:58:25.464Z |
| 62 | 04.5 | unrun-verify | playstead-mac/PlaysteadUITests/PairingCeremonyTests.swift |  | PairingCeremonyTests (the live-server pairing ceremony proof) is registered in LiveServer.xctestplan and compiles, but has not been executed against a real hosted Phoenix+Postgres mac_ci server in this session; not yet promoted to run-mac-verification.sh's --required-test list per WINDOWS #58's lesson. | open |  | 2026-09-09T00:53:41.765Z |  |
| 63 | 04.5 | stub | playstead-mac/Playstead/Pairing/PinnedCertificateCapture.swift |  | PinnedCertificateCapture has no dedicated unit test — its real trust-anchor capture path only fires on a genuine TLS handshake, which StubURLProtocol never performs. Coverage is structural/code-review only until the live-server proof (window #62) is run. | open |  | 2026-09-09T00:53:50.372Z |  |
| 64 | 03 | stub | playstead-server/lib/playstead_web/live/library_live.ex |  | list-view status_slot only passes queued fact, not the full device-reported status map status_for/2 now returns | open |  | 2026-09-11T04:31:57.324Z |  |
| 65 | 03 | unrun-verify | playstead-server/lib/playstead_web/live/library_live.ex |  | 03-13 backstop truths (500-entry stress toggle, download-in-progress uses existing indicator) not exercised by a dedicated automated fixture | open |  | 2026-09-11T04:31:57.416Z |  |
| 66 | 03 | stub | playstead-mac/Playstead/Cache/AvailabilityReporter.swift |  | Live transfer percent falls back to 0 when no DownloadCoordinator exists yet at report time | open |  | 2026-09-11T05:01:21.768Z |  |
| 67 | 03 | unmet-truth | playstead-mac/PlaysteadUITests/SaveEndToEndTests.swift |  | FLAKY: testOneSaveRoundTripsCaptureUploadAndJournalReturn failed intermittently (2 of 5 hosted runs, ~40%). RESOLVED 2026-09-12, green on main at c3f51e1 (run 34676636969, 6.36s). Do NOT delete the test: it is the only end-to-end proof of capture->upload->journal->sync and previously caught save uploads 404ing against every real server (e6316d5). ROOT CAUSE, found only on the THIRD diagnosis: a RACE, not a timeout and not a transient error. The running app drains save uploads on a reachability transition (PlaysteadApp reachability.onChange -> uploadLane.drainOnce()), which in CI fires right after pairing, exactly while the harness inserts its revision. The harness built its OWN second SaveUploadLane over the same store; drainOnce's doc comment promises 'the lane is an actor, so overlapping calls serialize' and that is true -- but two actor INSTANCES serialize nothing, so whichever lane lost saw an empty pending set and reported sent=0. Run 34663361104 named it exactly (upload-nothing-pending), which is what the three-way diagnostic split was added to distinguish. Fixed by sharing the app's own lane and asserting the OUTCOME (the revision reaches .uploaded) rather than one lane object's counter. TWO EARLIER DIAGNOSES WERE WRONG, kept deliberately rather than quietly swapped: (1) 'a fixed 120s poll deadline on the upload lane' -- wrong; that is the XCTest-side wait for the result file. (2) 'a single un-retried drainOnce pass' -- a real weakness, fixed and unit-pinned by SaveUploadLaneTests.test_oneTransientFailure_makesASinglePassSendNothing_thoughTheNextPassSucceeds (reproduces in under a second: one 503 makes a single pass report sent=0 while the very next pass succeeds), but NOT what was failing in CI. FOLLOW-ON DEFECT introduced by the fix itself and caught by run 34670123715 at save-e2e-harness=upload-server-refused: the retry predicate branched on `lastFailureClassification == .none`, but SaveUploadFailureClassification has seven cases and its own doc says three of them (.none, .offlineQueue, .slowUpload) are the product working correctly. .offlineQueue is the EXPECTED state at pairing time, so the most ordinary outcome was reported as an unfixable server refusal and the retry was skipped. Now gated on OnlyCopyEscalationReason(classification:) -- the same initializer the shipped escalation panel uses -- at all three decision points, and pinned by four falsification probes in four-layer-topology-test.sh. | fixed |  | 2026-09-11T23:23:31.675Z | 2026-09-12T14:57:49.371286Z |
| 68 | 03 | fixme | playstead-mac/scripts/ci/live-server.sh |  | FALSE DIAGNOSTIC: the live-server stage marker is written on entering each stage and never cleared when a fixture action SUCCEEDS, so any later non-fixture failure inherits the last stage entered. Run 34648546919 reported FAILURE_STAGE redeem-pairing for a prepare that had completed fine; the real failure was in the save harness. This actively misdirected the first diagnosis. Clear the marker on successful completion of each action. RESOLVED 2026-09-12 (green on main at c3f51e1): the EXIT trap now calls clear_failure_stage on success, and live-server-stage-marker-test.sh sources the REAL validated_marker/write_failure_stage/clear_failure_stage out of live-server.sh rather than restating them, so the test cannot pass against a drifted copy. It also asserts the clearer is exactly as strict as the writer (a weaker clearer is an arbitrary-path rm) and that the shipped trap still contains the success branch. | fixed |  | 2026-09-11T23:23:31.769Z | 2026-09-12T14:57:49.371286Z |
| 69 | 03.5 | fixme | .github/workflows/verify-hosted-evidence.yml |  | EVIDENCE BINDING IS REF-FRAGILE: the 'verify hosted evidence' workflow fails with 'complete evidence identity mismatch: head_sha' whenever the evidence-producing ci run's head_sha differs from the ref the verify workflow evaluates. TWO distinct triggers observed, so the root cause is broader than the first one recorded: (1) RE-RUN: 'gh run rerun --failed' on a ci run (run 34655069813 against ci run 34648546919). Re-running a flaky job is the standard mitigation, and it trips the evidence-identity guard, so the two controls are in conflict. (2) CROSS-REF, observed 2026-09-12: run 34677705521 on main (then at 363b10d) fired 8s after the BRANCH run 34676636969's mac job completed at 09612a9, and tried to bind that branch evidence to main's tip. Any branch run finishing while main sits at a different commit reproduces it. Decide which identity the manifest binds to across attempts AND across refs; a guard that fails on ordinary branch work is noise that will train people to ignore it. | open |  | 2026-09-11T23:23:31.870Z |  |
| 70 | 03.5 | unmet-truth | playstead-mac/PlaysteadTests/CacheTests/StubURLProtocol.swift |  | CROSS-TEST POLLUTION, unit layer: RelaunchTests.testPreviouslyLaunchableGameStillEvaluatesLaunchableAfterClosingAndReopeningEveryStoreWithNetworkStubbedToFail fails intermittently at RelaunchTests.swift:114, XCTAssertTrue(StubURLProtocol.requestLog.isEmpty). First observed on main, run 34700720746 (2026-09-12); the same tree passed on branch run 34676636969, so this is timing-dependent, not a regression (unit in_test was 120.8s on the red run vs 95.7s on the green one). NOT caused by the awaitExistence/clickWhenHittable work: the unit layer runs no XCUITest. MECHANISM: requestLog is a shared nonisolated(unsafe) static var. RelaunchTests DOES call StubURLProtocol.reset() in setUp, so the log is empty when the test starts -- which means a request arrived DURING the test. reset()'s inFlightGroup.wait() only covers a response already being DELIVERED; it does not cover a URLSessionTask from an earlier test that has not yet issued its request. So a lingering task appends into the next test's log. DO NOT apply the obvious fix: invalidating every session handed out by makeSession() inside reset() is NOT safe, because reset() is called mid-test, and in SaveConflictResolverTests setUp it runs at line 175 AFTER makeSession() at line 174 -- cancelling there would break that whole class. FilterTests:123 also calls reset() mid-test. A correct fix needs to identify the leaking test first, and the failing assertion does not capture the offending URLs; CI keeps file:line and discards assertion messages, so the next occurrence will be equally mute unless the request log is attached as evidence. Fix the diagnostic before guessing at the cause. OBSERVATION 2026-09-12, run 34707257044: PASSED. Expected for a timing-dependent flake and NOT evidence of a fix -- nothing was changed here. Still open. OBSERVATION 2026-09-14, hosted run 34903951673 (branch feat/dev-standup-and-uat7-closure, head 295038f): RED again at RelaunchTests.swift:114, the same XCTAssertTrue(StubURLProtocol.requestLog.isEmpty). This observation is unusually clean and worth keeping for that reason alone: the immediately preceding run (34901279944, head 6e4c1f8) passed the unit layer at 697 executed / 0 failures, and the ONLY difference between the two heads is .planning/WINDOWS.md -- 19 insertions and 5 deletions in a markdown file, not one byte of Swift. So the same compiled test code passed and then failed. That closes off "regression" as a reading of this entry without needing any timing argument, and it also rules out the session's AdapterProcessRegistry / StandInExecutable work (WINDOWS #85, #86) as a cause: that landed in 86c816c, two commits earlier, and the unit layer was green on it and on 6e4c1f8 before going red here with identical sources. TIMING, consistent with this entry's existing evidence that the flake is load-dependent: unit in_test_seconds was 144.4s on this red run against 115.1s on both preceding green ones, the same direction and rough magnitude as the 120.8s-versus-95.7s pair already recorded above. Note the red run is also the one whose live-server layer PASSED, so the runner was not uniformly slow -- the unit layer specifically ran ~25 percent longer. STILL UNDIAGNOSED, and this entry's own standing instruction still applies unchanged: the assertion captured no URLs, so this run says nothing new about WHICH test leaks the lingering task. Fix the diagnostic before guessing at the cause. ONE CONCRETE ROUTE, noted since this entry says the message channel is mute: run-mac-verification.sh does not only keep file:line. Alongside bounded_failure_diagnostic it scans assertion strings for allowlisted token patterns (ui_stage_pattern, live_stage_pattern, audit_pattern, around run-mac-verification.sh:762-771) and surfaces the matches. A bounded token of the same shape -- a fixed, allowlisted vocabulary naming the leaking suite rather than a URL -- would therefore survive the pipeline where a free-text message does not, and would not put a path or query string into published evidence. That is a candidate, not an applied fix. FIXED 2026-09-15, and the mechanism this entry inferred is now reproduced deterministically instead of being waited for. WHAT WAS DONE: makeSession() stamps each session it hands out with a unique X-Playstead-Stub-Session header and opens that stamp in a two-bucket ledger; startLoading drops any request whose stamp is missing or retired, failing it with URLError(.cancelled) BEFORE it can touch responder or requestLog; reset() rotates the buckets. The gate is deliberately fail-CLOSED -- an unstamped request is precisely the leak this entry records, and admitting it "just in case" is what kept the leak invisible. TWO BUCKETS, NOT ONE, and that is the whole difficulty this entry warned about: a session must survive one full reset() before being retired, because reset() is also called MID-test -- SaveConflictResolverTests calls makeSession() at setUp:174 and reset() at :175, and FilterTests resets at :123 partway through a test. Retiring every live session on reset() is the obvious fix this entry explicitly says NOT to apply, and it would have broken exactly those suites. After one full reset() cycle the owning test has ended by construction, so anything the session still emits is stale by definition. PROVEN, not inferred: StubURLProtocolIsolationTests asserts both halves. test_aSessionRetiredByTwoResetsCannotAppendToTheNextTestsLog issues a request on a session retired by two resets -- the boundary between a finished test and the next -- and requires both that it is not served and that requestLog stays empty. test_aSessionSurvivesTheMidTestResetItsOwnSuitePerforms issues one across a single mid-test reset and requires it to succeed, pinning the constraint that makes the naive fix wrong. Falsified by deleting only the rotate() call, which restores the pre-fix behaviour of never retiring a session: the first test then fails on BOTH its clauses -- the retired session's request is served (200, 7 bytes) and does appear in the next test's log -- while the second still passes, confirming the two tests are not testing the same thing. Restore verified byte-exact, not by git checkout. THIS ALSO ANSWERS THE ENTRY'S OWN STANDING INSTRUCTION, "fix the diagnostic before guessing at the cause", in a better way than it asked: rather than making the leaking suite identifiable after the fact, the leak can no longer reach another test's log at all, so there is nothing left to identify. The bounded-token route noted in the previous observation is therefore NOT needed and was not taken. EVIDENCE: the full Unit layer (ReleaseHookAbsenceTests skipped) ran 696 tests with 0 failures with the gate in place, which also demonstrates the stamp propagates through URLSessionConfiguration.httpAdditionalHeaders -- under a fail-closed gate, a stamp that did not reach the protocol would have failed every networked test loudly rather than silently no-opping. Note this entry's rate history: it fired on hosted runs 34700720746 and 34903951673 and passed in between, so a single green hosted run must NOT be read as confirmation; the falsification probe above is the load-bearing evidence, not the rate. | fixed |  | 2026-09-12T16:22:57.379Z | 2026-09-15T14:01:00.017Z |
| 71 | 03.5 | unmet-truth | playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift |  | OBSERVED FAILURE, live-server layer: testPairedFreshMirrorRendersSnapshotBeforeAnyBlobDownloadAndPersistsKeychainAcrossRelaunch failed at LiveServerSnapshotTests.swift:63 on main, run 34700720746 (2026-09-12). It cascaded: SaveEndToEndTests also shows failed with NO diagnostic of its own, and the fixture reported FAILURE_STAGE verify-evidence, both of which follow from the first test dying before its second snapshot -- all three share one Phoenix server, so the live-server layer reports one root failure as three. RULED OUT: the awaitExistence short-circuit racing ahead. The test ran 19.27s on the red run versus 13.22s when green, i.e. the 10s fallback wait burned in full and the row genuinely never appeared. The short-circuit demonstrably did not skip the wait. SUSPECTED: the click on the preceding line. exists is not isHittable, and XCUITest silently DROPS a click on a non-hittable element, so the failure surfaces later at whatever the click should have produced -- pointing at the wrong line. Before awaitExistence, the existence check above each click took ~1s and incidentally gave layout time; that pause was never the contract and depending on it was always a race that simply had not been observed. MITIGATION APPLIED: clickWhenHittable() at all 37 click sites, waiting on exists AND isHittable and forwarding file/line so a stuck click reports at the caller rather than making every one look like the same defect. NOT YET CONFIRMED: one observation, and the suspected mechanism is inferred rather than proven. Keep open until a run reproduces green with the mitigation, and do not close it merely because the next run passes -- this failed 1 of 3 runs. OBSERVATION 2026-09-12, run 34707257044 with clickWhenHittable: GREEN, 12.92s (the failing run was 19.27s, the earlier green one 13.22s), and SaveEndToEndTests passed at 6.20s. STAYS OPEN per this entry's own rule: that is ONE green run against a failure seen 1 in 3, so it is consistent with the fix and equally consistent with the flake simply not firing. The mechanism remains inferred. Close only after several consecutive green live-server runs, and note that the runner's own variance is wide -- the unit layer measured 95.7s, 120.8s and 150.7s across these three runs, which is the same variance that surfaced #70. RED AGAIN 2026-09-14, run 34898512311 (branch feat/dev-standup-and-uat7-closure, head 86c816c), WITH clickWhenHittable in place: same test, same LiveServerSnapshotTests.swift:63, and SaveEndToEndTests again failed carrying no diagnostic of its own. THE MITIGATION IS THEREFORE NOT SUFFICIENT. This entry's closing rule -- "close only after several consecutive green live-server runs" -- had very nearly been met: three consecutive greens (34707257044, 34876938531 addf7ea, 34893420944 5afc6b0) preceded this red. A three-green streak is not evidence of a fix for a failure seen roughly 1 in 4; do not close this on a streak alone, only on a proven mechanism. NOT CAUSED BY 86c816c: that commit changed no file under PlaysteadUITests, touched exactly one production file (AdapterHost.swift, additive injectable defaults), and the unit layer it targets passed this same hosted run at 697 executed tests and 0 failures. TIMING, corroborating this entry's earlier reasoning: the two reds ran 19.27s and 18.12s; the three greens ran 13.22s, 11.75s and 14.15s. Reds and greens do not overlap, which is again consistent with the 10s awaitExistence at :63 burning in full, and again rules out the short-circuit skipping the wait. THE SUSPECTED MECHANISM IS NOW IN DOUBT. The fixture's verify-evidence stage died on its catalogue_entries titles check -- "fresh mirror does not contain exactly both synthetic sentinels" -- and that check reads client-mirror SQLite state only. It depends on no click at all: a dropped, non-hittable click leaves the mirror correct and fails only the UI assertion. So the dropped-click hypothesis does not explain the fixture failure, and this entry's earlier reading of that failure as a mere cascade off ":63 dying before its second snapshot" is not supported -- SaveEndToEndTests makes its OWN both-sentinels assertion via runFixture("verify", ["snapshots-not-asserted-here"]) at SaveEndToEndTests.swift:135, so its failure need not be a cascade either. WHAT THE LOG CANNOT SAY, and why: exactly ONE verify-evidence invocation ran in the entire layer and it failed, yet TWO tests call that stage, and the message named neither the observed titles nor which caller produced it. So it is not even determinable from this run whether the mirror was MISSING a sentinel or carrying an EXTRA row -- two findings with opposite causes. Separately, the evidence pipeline recorded failure_diagnostic_count=1 for two failed tests, and the one it kept is the :63 XCTAssertTrue; a fixture failure also fires an XCTAssertEqual through recordFixtureFailure, so at least one diagnostic did not survive into the evidence. That drop is an open sub-question of this entry, not yet investigated. FIXED THIS SESSION, the diagnostic only: live-server.sh's verify-evidence now gathers every observed fact BEFORE the first check and attaches all of them to whichever check fails -- root, snapshots observed/expected, blob_requests, cursor presence, the observed title set, and the missing/unexpected set difference. The snapshots field also identifies the caller for free, since LiveServerSnapshotTests passes "2" and SaveEndToEndTests passes snapshots-not-asserted-here. Verified by running the extracted block against synthetic mirrors for both ambiguous cases: a missing sentinel reports missing=['Playstead CI Sentinel Two'] unexpected=[], an extra row reports missing=[] unexpected=['Some Other Game'], and the passing case prints the unchanged success line. This does not fix the flake; it makes the NEXT red run diagnostic instead of ambiguous, which is the only way this entry advances given it reproduces only on hosted CI. FORMAT CORRECTION, same session: that diagnostic ships as ONE FACT PER LINE on stderr, not as the single bracketed line first written, because run-mac-verification.sh's own diagnostic printer truncates every line to 200 characters -- and the one-line form measured about 215, losing its tail at exactly the missing/unexpected difference that is the whole point. The same printer rewrites absolute paths to <path> and any 32-or-more character run to <token>, so the caller is identified by its snapshot expectation (caller=asserts-2-snapshots versus caller=snapshots-not-asserted-here) rather than by the run root, whose UUID would have been scrubbed to <token> anyway. Verified by replaying the shipped stderr through a replica of that printer's own regexes: all eight lines survive intact in both the missing-sentinel and extra-row cases, and the passing path still prints its unchanged success line. Filed here because this is the second time in two sessions that the evidence pipeline, not the test, decided what a future reader would be able to conclude. SUB-QUESTION PARTLY INVESTIGATED, same session, with one of its own answers retracted. ESTABLISHED: the fixture's raw stderr does NOT travel through the Swift caller's pipes at all. live-server.sh re-executes fd 2 onto a shared, runner-owned, append-only file at $PLAYSTEAD_LIVE_SERVER_STAGE_ROOT/live-server-fixture-diagnostic (mode 0600, torn down with the native root, never uploaded), and run-mac-verification.sh prints a sanitized bounded tail of THAT file. Consequence, and it is the one that matters for the new diagnostic: the caller= line will reach the job log no matter which of the two tests triggers the failure, including SaveEndToEndTests, whose runFixture sends both standardOutput and standardError to FileHandle.nullDevice. ASYMMETRY WORTH FIXING SEPARATELY, not fixed here: LiveServerSnapshotTests and PairingCeremonyTests both funnel a failed fixture through recordFixtureFailure, which emits one distinct XCTAssertEqual per allowlisted stage so the stage survives as a file:line; SaveEndToEndTests.runFixture instead returns a bare false, so its callers can only say XCTFail("live fixture stage 'verify' failed") and the stage is lost. That is the exact failure mode SaveEndToEndTests' own recordSaveHarnessFailure doc comment argues against, twenty lines above. Left alone deliberately: the live-server layer cannot be run locally, it is the flakiest and most expensive layer, and the fixture-side caller= line already removes the ambiguity that motivated it -- so this is a candidate, not a pending fix. RETRACTED: the guess that SaveEndToEndTests' fixture failure is invisible to the evidence pipeline because XCTFail carries no capturable diagnostic. It is wrong -- run-mac-verification.sh's assertion_pattern explicitly lists XCTFail alongside the XCTAssert family, and XCTFail does record a file and line. So failure_diagnostic_count=1 against two failed tests REMAINS UNEXPLAINED, and no inference should be drawn from it about which caller's verify-evidence was the one that ran. Do not use that count to attribute the titles failure to either test; the next red run's caller= line is what will say. GREEN 2026-09-15, run 34982994861, head 17a7c189 (branch feat/dev-standup-and-uat7-closure): the macOS four-layer job passed in full, 24m20s, so the live-server layer including this test and SaveEndToEndTests was green. THIS ENTRY STAYS OPEN, by its own rule and for the same reason as last time: this is one green after the 2026-09-14 red at run 34898512311, and a failure seen roughly 1 in 4 is not disproved by a single pass. The previous three-green streak was followed by a red, which is exactly why the rule says close only on a proven mechanism, never on a streak. NOTHING NEW WAS LEARNED ABOUT THE MECHANISM from this run, and that is the point worth recording rather than the pass itself: the improved fixture diagnostic committed at 6e4c1f8 -- one fact per line to a runner-owned stderr file, with a `caller=` marker distinguishing the two callers and every line kept under the 200-char truncation -- only emits on a RED run. It has therefore still never been exercised against the failure it was built for. The next red is the one that matters; this green only confirms the diagnostic costs nothing when the test passes. | open |  | 2026-09-12T16:23:11.283Z |  |
| 72 | 03 | stub | playstead-mac/Playstead/Library/LibraryShellView.swift | 424 | Library card availability badge is a hardcoded literal: ShelfView is handed statuses: [.serverOnly] for EVERY entry, so every card in the grid layout shows the 'On server' cloud glyph regardless of what is cached or pinned, and its accessible name reads '<title> is on your server. Choose Download to play it offline.' over content that is downloaded and playable. Found while trying to identify a 'Ready offline' title for UAT item 7 Run A: 7 objects / 38076928 bytes sit in the CAS (including the 16 MiB Max Payne Advance ROM played on 2026-09-13) and no card can say so. The trustworthy signal today is GameRowView's action button (Play vs Download), which derives from the real six-check ReadinessReport via PreflightChecker(cas:). FIXED on branch feat/dev-standup-and-uat7-closure: AppEnvironment.libraryStatuses(for:) is the production construction site, and both the grid and the Favorites shelf now use it. Gated at the front door by StorageInteractionTests.testCachedAndPinnedGameIsNotDescribedAsBeingOnTheServer, which fails with the exact pre-fix sentence when the literal is restored. | fixed |  | 2026-09-13T13:52:01.120Z | 2026-09-13T19:57:04.120Z |
| 73 | 03 | stub | playstead-mac/Playstead/Cache/AvailabilityState.swift | 82 | AvailabilityState.derive/AvailabilityInputs have ZERO production construction sites - grep for 'AvailabilityInputs(' outside PlaysteadTests returns nothing. The six-state D-21 derivation whose own doc comment calls it 'the read-time derivation every card, list row, and rebuild-from-disk test calls' is called by no card and no list row in the shipped app, so that doc comment is currently false. Directly enables the hardcoded .serverOnly badge recorded alongside this. FIXED on branch feat/dev-standup-and-uat7-closure: AvailabilityInputs is now built by AppEnvironment.availabilityInputs(for:) from CASManager/DownloadQueue/PinStore, and LibraryStatusWiringTests drives every state through the real stores rather than constructing inputs by hand. | fixed |  | 2026-09-13T13:52:01.225Z | 2026-09-13T19:57:04.257Z |
| 74 | 03 | stub | playstead-mac/Playstead/Library/GameListView.swift | 43 | GameListView has ZERO production call sites - LibraryShellView's .list layout renders GameRowView instead. GameListView is the only place LibraryStatus.listViewLabel renders, so the strings 'Ready offline', 'On server', 'Downloading - n%', 'Pinned' and 'Queued' never appear anywhere in the shipped app, and the 03-UI-SPEC QUAL-01 guarantee that list view pairs a text label with the glyph (never colour/glyph alone) is unreachable in production. LibraryContractSnapshotTests asserts it against the unreachable view. FIXED IN PART on branch feat/dev-standup-and-uat7-closure: the QUAL-01 guarantee is now satisfied where it is actually rendered -- GameRowView (the row the shipped list layout uses) pairs the status glyph with its listViewLabel text, identifier library.status-label, asserted by the front-door UI test above. What is NOT fixed is GameListView itself and the sortable list it implements, which still has no production call site; that residue is tracked as its own window rather than left inside this one. | fixed |  | 2026-09-13T13:52:01.336Z | 2026-09-13T19:57:04.396Z |
| 75 | 03 | deviation | playstead-mac/Playstead/App/PlaysteadApp.swift | 1344 | The real download path never writes cache_objects. AppEnvironment.attemptDownload(for:) - documented as 'the whole of what GameRowView's Download button does' - calls engine.download directly; the INSERT INTO cache_objects lives only in DownloadCoordinator.handleSuccess (the queue path the button does not use) and is itself a swallowed try?. Evidence on the owner's machine after a successful real download-and-play: cache_objects has 0 rows while objects/ holds 7 files / 38076928 bytes and verify-index.json has all 7. Three consequences, each independently checkable: (1) QuotaManager.usedBytes() sums cache_objects (QuotaManager.swift:67), so the capacity gate attemptDownload runs before every download always sees 0 bytes used and can never block anything - reinstating the unbounded-cache regression GameRowView.swift:308's comment says was fixed - and QuotaSettingsView renders '0 bytes used' over 36 MB on disk; (2) EvictionPlanner enumerates cache_objects, so downloaded content is invisible to the storage view and can never be reclaimed; (3) the pins table holds Pokemon - Version Vert Feuille with no cache_objects row for its required member, the pinned-but-not-cached combination AvailabilityInputs documents as a caller defect. FIXED on branch feat/dev-standup-and-uat7-closure: CASManager records every commit through CacheObjectStore and forgets on removal, so the row no longer depends on which caller performed the download. StorageShellWiringTests' fixture no longer inserts the row by hand, so 15 of its 15 tests fail if commit stops recording. | fixed |  | 2026-09-13T13:52:01.445Z | 2026-09-13T19:57:27.814Z |
| 76 | 03 | deviation | playstead-mac/Playstead/Adapter/AdapterPin.json | 1 | A normal user quit of the emulator classifies as unknown, not clean. PROVEN BY HAND 2026-09-13: the owner pressed Cmd-Q inside mGBA and GameRowView rendered 'Last exit: unknown(status: 0, reason: "exit")'. AdapterPin.json's exit_detection maps clean to terminationStatus 15 / uncaughtSignal - i.e. SIGTERM, which is what the APP sends via Process.terminate() - and the pin's own note says mGBA treats SIGTERM exactly like a crash with no graceful save-and-quit. So no signature covers status 0 / exit, and the single most common real exit falls through to AdapterExit.unknown. Two consequences: 'clean' currently means 'we asked it to stop' rather than 'the user quit normally', and any future save-recovery messaging keyed on .clean will never see a normal quit. The automated suites cannot detect this - AdapterExitTests classify against the same pin table they assert, so the tautology is green. Fix requires an owner decision on the pin (add a status 0 / exit signature as clean, and decide what 15/uncaughtSignal should then be called) rather than a code change alone, since the pin is the frozen contract. FIXED 2026-09-14 on branch feat/dev-standup-and-uat7-closure, with the owner's explicit authorization for option A. exit_detection categories now hold a LIST of signatures (decoding still accepts the old single-object shape, so no pin or fixture had to be migrated): clean = [0/exit], crash = [11/uncaughtSignal], killed = [9/uncaughtSignal, 15/uncaughtSignal]. 15 joins killed rather than staying clean because AdapterHost.terminateAll() sends SIGTERM and escalates to SIGKILL after a 2s grace period -- both are Playstead ending the process, and the pin's own note records that mGBA dies to SIGTERM exactly like a crash, so save-recovery messaging keyed on .clean must never fire for it. Gated by AdapterExitBoundaryTests, which breaks the tautology in both directions: it classifies against AdapterPin.load() (the pin that actually ships, never a literal beside the assertion) and it takes its termination signatures from REAL child processes over {0,9,11,15} x {exit,uncaughtSignal}. Falsified by restoring the old mapping: the boundary suite fails 6 assertions with 'normal exit produced status 0 / exit, which the shipped pin does not describe', while the old fixture-based AdapterPinTests stays 5/5 green -- a direct demonstration of why this survived phase 03. The 03-UAT item 7 exit-classification record is amended (not rewritten) to mark its observations as history and state what the same commands produce now. | fixed |  | 2026-09-13T14:06:28.567Z | 2026-09-14T15:56:42.808Z |
| 77 | 03 | deviation | playstead-mac/Playstead/App/PlaysteadApp.swift | 633 | The curation outbox has no time-based drain trigger, so a server coming back does not drain it. The three wired triggers are post-enqueue, reachability-regained and scene-active. Reachability wraps NWPathMonitor, which reports interface reachability - so a server that goes away and returns without the Mac's network changing (a stopped container, a restarted service, a server reboot) never fires trigger 2, and a pending entry waits for the user to focus the window or for an unrelated enqueue. Observed 2026-09-13: two play_session_record entries queued at 14:02:26Z (attempt_count 2) and 14:03:11Z while caddy was stopped, still queued with delivered=0 several minutes after caddy was answering /healthz again; Outbox.retryDelay(forAttempt: 2) is only 20s, so they were eligible and simply never triggered. CORRECTION to an earlier version of this entry: the two availability_report entries from 2026-09-13T02:25:41Z and 03:07:38Z are NOT stuck and are NOT corroborating evidence - they are state='rejected' with last_error_code='not_found', the terminal casualties of the PUT /api/v1/devices/me/availability route skew against a stale server image, excluded from listPending by design and surfaced in RejectedIntentsView. They read attempt_count 0 only because markRejected does not increment it, which is itself mildly misleading in the table. The real evidence is narrower and still stands: B2CFB20D is state='pending' with next_retry_at 2026-09-13T14:03:31Z (long past) and 9B2AB144 is state='pending' with next_retry_at NULL, so both are eligible under listPending, the server has been answering /healthz for many minutes, and neither has been sent. Nothing has fired a drain since caddy returned. Note also that in-app sidebar navigation did not drain them, so trigger 3 needs a real scene-activation transition, not mere interaction. FIXED on branch feat/dev-standup-and-uat7-closure: added drain trigger 4/4, OutboxDrainTicker -- a plain 60s periodic tick that fires exactly what the reachability edge fires (curation outbox, save outbox, save upload lane). Deliberately a tick rather than a server-reachability probe: a probe guesses when the server came back, while the per-entry backoff already decides eligibility, so a pass with nothing due costs one indexed query and sends nothing. Gated by OutboxDrainTickerTests, including 'an eligible pending entry is delivered with no trigger at all' and 'a server that comes back is noticed without any network change' (a 503, then a wound-back next_retry_at, then a 200, with reachability untouched). Both fail when ticker.start() is removed. | fixed |  | 2026-09-13T14:06:28.685Z | 2026-09-13T20:03:38.188Z |
| 78 | 03 | deviation | playstead-mac/Playstead/Cache/LaunchMaterializer.swift | 44 | Every launch silently destroys the player's emulator save states. LaunchMaterializer.materialize does removeItem(at: directory) on the whole launch/<asset_set_id>/ directory before recreating it, and its doc comment frames this as correct ('the launch directory is always rebuilt from the CAS, the verified source of truth'). That reasoning holds for cache-derived files and fails for emulator-AUTHORED ones. Battery saves are safe because the launch command relocates them via -C savegamePath to saves/<asset_set_id>/, but nothing relocates mGBA save states (.ss1-.ss9), screenshots or cheat files -- mGBA writes those next to the ROM, i.e. inside the directory that is deleted on the next Play. PROVEN 2026-09-13: launch/1687f207-.../Advance Wars (USA) (v1.1).ss1, 55487 bytes, written 14:03:07Z, was gone after the next launch of the same title at about 14:21; the only surviving .ss1 on the machine belongs to Max Payne Advance, the one title not relaunched since. The owner hit the user-visible symptom first - mGBA's Load State list was empty for a game they had state-saved twenty minutes earlier. Save states are a first-class feature of every emulator and a player will reasonably expect them to persist; this is silent data loss on the happy path, not an edge case. Fix needs a decision on where emulator-authored artifacts live (a sibling per-asset-set directory that is NOT rebuilt, alongside saves/), plus whether existing states can be rescued. Catchable cheaply in CI: materialize, write a sentinel file the members list does not name, materialize again, assert the sentinel survives - see SEED-034. FIXED on branch feat/dev-standup-and-uat7-closure: materialize removes only the names its own sidecar manifest records, so emulator-authored files survive. Gated by MaterializationTests.testMaterializeTwiceLeavesAnEmulatorAuthoredFileUntouched, falsified against the restored whole-directory wipe. | fixed |  | 2026-09-13T14:23:16.274Z | 2026-09-13T19:08:42.822Z |
| 79 | 03 | stub | playstead-mac/Playstead/Library/GameListView.swift | 19 | The sortable list view has no production call site. Residue of WINDOWS #74, split out when that window's QUAL-01 half was fixed: GameRowView now renders the status label+glyph pair the shipped list layout was missing, but GameListView itself -- the 'sortable alternative to ShelfView' whose own doc comment promises sorting by title, system and date added, and the only renderer of GameListRow/LibrarySortOption -- is still constructed nowhere in Playstead/. The shipped list layout renders GameRowView, which offers no sort control at all, so the sortable list described in 03-UI-SPEC is an unreachable feature rather than a false claim. AccessibilityAuditTests.testGameListRowAccessibleNameContainsTitleSystemAndStatusSentence asserts a composition rule against that unreachable view (and asserts it by rebuilding the string locally rather than calling the view, so it would pass even if the view were deleted). Decide deliberately: wire a sort control into the shipped list layout, or delete GameListView and its sort enum and drop the spec clause. FIXED IN PART 2026-09-14 on branch feat/dev-standup-and-uat7-closure: the shipped list layout now offers a sort control (Title / System, one button per option beside Cards/List, same playsteadFocusable path) and catalogueList orders its entries through LibrarySortOption.sortedEntries, so the sortable list the spec promises is reachable for the first time. Ordering is case- and diacritic-insensitive with an id tie-break, so equal keys cannot reshuffle between two evaluations of the same body. Gated by LibrarySortTests (the orderings) and CurationInteractionTests.testListLayoutOffersAWorkingSortControl (the control exists where a user can press it, pressing it changes state, and rows come out in that order); removing the control fails the latter with 'the list layout offers no way to sort'. WHAT IS NOT DONE, and needs an owner decision: 'date added' is not offered, because nothing local knows it -- CatalogueEntry has no date and the server's catalogue payload has a FROZEN key set exposing updated_at but no inserted_at. Making it real is a protocol change (one frozen key plus a client field); the alternative is amending 03-UI-SPEC to promise two sort options rather than three. GameListView itself is still unrendered: it is now the only thing that renders GameListRow, and AccessibilityAuditTests asserts a composition rule against it by rebuilding the string locally, so that test would pass even if the view were deleted. Deleting it is the likely right answer once the date-added question is settled. DATE-ADDED QUESTION DECIDED 2026-09-14, owner-authorized, and the dead code it was keeping alive is gone. The decision is NOT to add the ordering: the server's catalogue payload key set is frozen with a test asserting no accidental additions, it exposes updated_at and no inserted_at, and CatalogueEntry carries no date at all, so offering the control would mean inventing an order or fabricating a date. Adding it later is a protocol change -- one frozen key, one client field -- and should be planned as one. Recorded in 03-UI-SPEC under a new List sort order section, which now promises exactly two orderings (Title and System, each with a total tie-break on asset-set id) and states plainly that plans 03-05 and 03-06 both described a third that was never buildable against this protocol. The plans themselves were NOT rewritten; they are historical records. DELETED as a consequence: the LibrarySortOption.dateAdded case, whose sortedEntries branch returned its input unchanged and which selectable excluded -- an unreachable branch kept alive by a comment explaining why it was unreachable; the GameListView struct and GameListRow struct, which nothing had instantiated since the shell took over rendering; and AccessibilityAuditTests/testGameListRowAccessibleNameContainsTitleSystemAndStatusSentence, which was VACUOUS -- it interpolated a label string itself and then asserted that string contained the pieces it had just interpolated, calling no production code. The live list row's status text is covered for real from the front door by StorageInteractionTests reading library.status-label. LibrarySortOption and NoMatchesView were moved into their own files since the file they shared was named for a view that no longer exists. Both stale reachability-allowlist entries were removed: GameListView because it is deleted, NoMatchesView because it is now rendered (WINDOWS #83) -- that entry had said a future plan should either wire it in or delete the file, and both halves are now done. The test that pinned selectable against a hardcoded list was replaced with selectable == allCases, which is strictly stronger: it fails if a case is ever added without being offered, which is the mistake that produced this window. | fixed |  | 2026-09-13T19:58:01.645Z | 2026-09-14T16:07:34.858Z |
| 80 | 03 | deviation | playstead-mac/Playstead/Sync/SyncEngine.swift | 105 | A foreign or re-signed sync cursor strands the client in 'offline' permanently, with no self-healing path. Verified end to end 2026-09-13 by reading both sides: sync_cursor is a single id=1 row with NO server or device identity column (Migrations.swift:57, CursorStore.swift), nothing clears it when pairing changes (PairingCoordinator writes the credential and the pin, and never touches it), and the server's cursor is HMAC-signed with a secret derived from its own secret_key_base (playstead-server/lib/playstead/sync/cursor.ex:55). Playstead.Sync.changes_after/2 therefore returns {:error, :cursor_invalid} for a 'malformed/tampered/FOREIGN' cursor (sync.ex:21, its own wording) which ErrorCodes maps to 400 cursor_invalid. ChangesClient maps that to SyncError.cursorInvalid -- and SyncEngine.syncNow() catches ONLY SyncError.cursorExpired, so cursorInvalid falls into the generic catch, sets state = .offline(since:), leaves the stored cursor byte-identical, and every later pass re-sends the same rejected cursor. The app reports 'offline since <date>' forever while the server is up and answering. The remedy already exists and is correct (forceFullResync / bootstrapFromSnapshot, which replaces the whole catalogue and curation mirror and stores a cursor this server actually issued) but its only caller is OutboxWorker.onDestructiveRejection -- no UI surfaces it, and nothing calls it on an invalid cursor. Two realistic triggers, neither exotic: re-pairing this Mac to a different server, and the same server coming back with a regenerated SECRET_KEY_BASE (a .env rebuild), which invalidates every cursor it ever issued. FIXED on branch feat/dev-standup-and-uat7-closure: syncNow() now treats cursorInvalid exactly as it treats cursorExpired -- handleCursorExpired is renamed handleUnusableCursor and both codes route to it, since a cursor is opaque and positionally meaningless to this client and there is nothing to salvage from one the server refuses. Gated by SyncEngineTests.testCursorInvalidAlsoResetsToFreshSnapshotRatherThanStrandingTheClientOffline, which asserts the rejected cursor is replaced by one this server issued, the other server's mirror is replaced rather than merged, and the app does not read as offline; 3 assertions fail when the catch arm is removed. NOT addressed: forceFullResync() still has no UI entry point, and sync_cursor still carries no server identity column -- the fix makes the dead end recoverable rather than making the cursor self-describing. | fixed |  | 2026-09-13T20:04:59.401Z | 2026-09-13T20:07:13.173Z |
| 81 | 04 | deviation | playstead-server/test/playstead_web/browser/library_restream_test.exs | 111 | PlaysteadWeb.Browser.LibraryRestreamTest is flaky at roughly 1 run in 4, observed repeatedly across full-suite runs on 2026-09-12/13 (and NOT failing in the 1079-test run at the end of that session, which is what a 25 percent rate looks like). Recorded now because it had never been written down, so every green run was being read as evidence. It is a Wallaby feature that samples requestAnimationFrame frames during a filter-chip re-stream of a 500-entry library and asserts, in addition to 'never empty' and 'never busy', that Enum.max(counts) == @library_size and Enum.min(counts) == @ready_offline_size -- exact endpoint counts over a sampled transition, which is the part most likely to be timing-dependent (a sampler that starts a frame late, or a re-stream that settles before the first sample, sees only one endpoint). NOT diagnosed: which assertion actually fails on a bad run was never captured, so the mechanism above is a hypothesis, not a finding. Next step is to capture the failure output (run the feature in a loop until it fails, keeping the message) before changing anything -- the non-vacuity assertions are load-bearing and must not be weakened just to make the test quiet. EVIDENCE 2026-09-14: ran the feature file alone 20 times in a row -- 20/20 green (mix test test/playstead_web/browser/library_restream_test.exs). So it does NOT reproduce in isolation, and the ~25 percent rate observed earlier was under the full suite (max_cases 36, 1079 tests), which points at load/concurrency -- a sampler starved of frames while 36 async cases share the machine and one browser -- rather than at the feature's own logic. The next step is therefore unchanged but better aimed: capture the failure from a FULL-SUITE run (keep the message, not just the exit code), because looping the file alone will not produce one. RATE CORRECTED 2026-09-14, and the earlier '1 in 4' claim does NOT hold up: 28 consecutive green runs -- 20 of the feature file alone, 6 of the FULL suite (max_cases 36, the configuration the flake was attributed to), and 2 hosted `mix precommit` runs on PR #5. At a true 25 percent rate the probability of that is under one in a thousand. Two honest readings, and this window should not be closed until one is chosen: either the original observation was mis-attributed (the failing runs happened while several xcodebuild Mac test runs were saturating the same machine, which is a condition none of these 28 runs reproduced), or the trigger is rarer and more specific than 'under load'. What is now established is that looping it -- alone or as a full suite, on an otherwise idle machine -- will not produce a failure, so the next attempt must either recreate the contention (run the Mac four-layer verification concurrently) or capture the message from a real CI failure when one next occurs. Do not weaken the feature's non-vacuity assertions in the meantime. CONTENTION EXPERIMENT RUN 2026-09-14, which is the step this window has been asking for, and it changes the picture. Six full-suite runs (mix test, max_cases 36, 1079 tests) were executed while a loop of xcodebuild Mac test runs saturated the same machine. LibraryRestreamTest passed all six. That brings it to 33 consecutive green runs across every configuration this window ever blamed -- 20 of the feature file alone, 6 full-suite idle, 2 hosted mix precommit, and now 5 full-suite UNDER the contention that was the last untested hypothesis. Caveat recorded honestly: runs 3 and 4 were partially degraded because a compile error of mine made the load generator fail fast for roughly two minutes instead of building, so treat runs 1, 2, 5 and 6 as the fully contended ones. The experiment DID produce a failure on run 5 -- but in PlaysteadWeb.Plugs.ThrottleTest, not here. That failure is diagnosed, proven and fixed as WINDOWS #84: the throttle test's per-test IP formula dropped bits 8..15 of its unique integer, collapsing 2000 concurrent draws onto 72 distinct addresses, so tests shared per-IP rate-limit buckets whose window outlives the suite. That is a real load-sensitive full-suite flake in the same suite, with the same symptom shape (a green-most-of-the-time full run), and it is now the most likely explanation for what was originally attributed HERE without the failing message ever being captured. This window's first reading -- mis-attribution -- is therefore the one the evidence supports. It stays open only because the original observation's message was never recorded and so cannot be matched against #84's; nothing further should be done to LibraryRestreamTest, and its non-vacuity assertions must not be touched. Close this if #84's fix is followed by no further full-suite flakes, or reopen the investigation with a captured message if one occurs. MESSAGE CAPTURED 2026-09-14, and it CORRECTS the update immediately above this one. That update said mis-attribution was the reading the evidence supported. That was wrong, or at best half right: a second contention run -- this one on a machine cleared of 12 orphaned CPU-burner processes that a different project's session had leaked, which had been confounding the earlier measurements -- produced a real failure in THIS test, with exactly the mechanism the original hypothesis named. The message: "assert Enum.min(counts) == @ready_offline_size / left: 500 / right: 50" at library_restream_test.exs:143. Every sample was stuck at the pre-toggle count of 500, so the requestAnimationFrame sampler never recorded a single frame at the narrowed state. DIAGNOSIS, now a finding rather than a hypothesis: await_count polls the DOM directly via its own js call, so it returns as soon as the stream shows 50 rows. On an idle machine rAF fires every ~16ms and the sampler is certain to catch that state; under load Chrome throttles rAF hard, and read_samples can stop the sampler in a gap before any frame at the settled count was recorded. The failure is therefore a STARVED SAMPLER, not the empty-frame blink the feature exists to catch -- and the assertion message actively misleads about that, which is why this window sat open for days. FIXED by waiting on the sampler rather than the DOM: a new await_sampled_count helper blocks until window.__psSamples contains a frame at the expected count before read_samples runs, on both the narrowing and the widening feature. This weakens nothing and the window's own prohibition is honoured: Enum.min(counts) == @ready_offline_size still fails on a partial refill that paints fewer rows, Enum.min(counts) > 0 and the busy-treatment check are untouched, and all of them become HARDER to satisfy vacuously because the sampler is now guaranteed to span the transition instead of lucky to. Even in the worst case the diagnostics improve: if the sampler is starved past the bound, the failure now reads "timed out waiting for the requestAnimationFrame sampler to record a frame at 50 rows" instead of a row-count mismatch that looks like a rendering defect. What remains UNVERIFIED and should not be claimed: the original "roughly 1 in 4" rate was never reproduced and no failing message from those runs was ever kept, so whether this same mechanism caused them is unknown. WINDOWS #84 was also real and also load-sensitive, so both may have contributed. | fixed |  | 2026-09-13T20:07:13.276Z | 2026-09-14T19:11:08.079Z |
| 82 | 03.5 | deviation | playstead-mac/scripts/ci/run-mac-verification.sh | 1247 | The UI layer's first few tests fail on hittability when the layer starts while the machine is still busy from the previous layer. Observed 2026-09-13 on this Mac: StorageInteractionTests run immediately after the Rendering layer produced 3 failures out of 27 -- testDownloadsPauseResumeFlow, testQuotaEditAndFocusRestoration and testCachedAndPinnedGameIsNotDescribedAsBeingOnTheServer -- every one of them 'control never became hittable, so the click would have been silently dropped', and every one of them among the first tests executed in the run. The identical selection on an idle machine passed 27/27 twice. Nothing about the app changed between the two runs. run_four_layer_verification runs unit -> rendering -> UI back-to-back in one invocation, which is exactly that pattern, and layers.json carries an automatic_retries field that is hard-coded to 0 -- so a slow window-server settle is currently indistinguishable from a real defect in the evidence, and a green run is partly luck about machine load. Options, in preference order: (a) have clickWhenHittable's waiter tolerate a cold start (a longer first-use timeout, or one warm-up hittability wait per test class), (b) settle explicitly between layers rather than sleeping blindly, (c) implement the automatic_retries field honestly -- retry a hittability-classified failure once, record the retry count, and never retry an assertion failure. Do NOT simply widen every timeout: this diagnostic message exists because a dropped click used to pass silently. EVIDENCE 2026-09-14: the UI layer has a second, distinct environmental failure on this Mac, and it is worth telling the two apart. After a run was interrupted, every subsequent UI invocation failed before any test executed with 'The test runner failed to initialize for UI testing. (Underlying Error: Timed out while enabling automation mode.)' -- three consecutive invocations, including a class this session never touched. Killing the long-lived user-owned /usr/libexec/testmanagerd (it respawns on demand) fixed it immediately and the same test then passed. So: 'timed out while enabling automation mode' before any test starts is a wedged testmanagerd, while 'control never became hittable' inside a test is the cold-start load problem this window is about. CI should distinguish them -- the first is worth one automatic retry after clearing the daemon, the second is not the same failure at all. THIRD FAILURE MODE, 2026-09-14, and the session it was found in can no longer verify UI accessibility audits at all: performAccessibilityAudit started failing with 'Error Domain=com.apple.xcode.xctest.accessibilityAudit Code=-56 Audit failed to complete in time' for every audit test (testReclaimPromptActionsPassLiveAudit, testStorageInventoryActionsPassLiveAudit, testDownloadsPauseResumeFlow), reproducibly and in isolation, after the same selection had passed 27/27 an hour earlier. BISECTED AGAINST CODE AND CLEARED: the identical test fails at commit 018ce1a in a clean throwaway worktree -- a tree that predates every UI element added this session -- so the variable is the machine, not the change. Restarting testmanagerd (which fixed the 'timed out while enabling automation mode' mode above) did NOT fix this one. Consequence to state plainly rather than paper over: no accessibility audit could be re-verified after that point in the session, so newly added library elements (the list row's status label and glyph) have unit and front-door coverage but NO audit coverage yet. Re-run the UI layer on a fresh login session before trusting a green audit result, and see #82's other two modes for how to tell the three apart. CLEARED 2026-09-14, THIRD MODE ONLY: the hosted macOS 26 runner ran the full four-layer verification at commit 30710cd (run 34870390370, job 'macOS 26 unit + rendering + UI + live server') and the UI layer PASSED -- 115 executed, 53/53 required verified, zero Code=-56. All three performAccessibilityAudit tests are required gates there (StorageInteractionTests/testReclaimPromptActionsPassLiveAudit, StorageInteractionTests/testStorageInventoryActionsPassLiveAudit, SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit), so they genuinely executed against the library status label and glyph added this session rather than being skipped. The honest consequence recorded above is therefore retired: those elements DO have audit coverage, and the 'Audit failed to complete in time' reproduction is confirmed local to this Mac, matching the bisect at 018ce1a. NOTE the run's workflow-level conclusion reads 'cancelled' because a newer push superseded its concurrency group after the jobs finished -- every job in it concluded success, but do not cite that run id as a green required check. MODES 1 AND 2 REMAIN OPEN and are what this window is now about: hosted CI passing does not disprove the cold-start hittability problem, it only means that runner did not hit it, and automatic_retries is still hard-coded to 0. | open |  | 2026-09-13T20:20:08.595Z |  |
| 83 | 03 | deviation | playstead-mac/Playstead/Library/LibraryShellView.swift | 518 | The "no matches" empty state promised by 03-UI-SPEC's Copywriting Contract (line 235) is computed, unit-tested and snapshot-tested but NEVER RENDERED, and the state the user actually sees in its place is actively wrong. LibraryViewModel.searchResultState correctly returns a SearchResultState whenever a non-empty query matches nothing, FilterTests exercises it, and LibraryContractSnapshotTests builds one at line 126 -- so three layers of evidence say this is covered, and 03-UI-SPEC's own coverage table at line 259 claims "covered". No live view reads searchResultState. Its only renderer, NoMatchesView, sits in GameListView.swift, which nothing has instantiated since the shell took over rendering; grep confirms GameListView, GameListRow and NoMatchesView have no references outside comments and their own file. User-visible consequence, list layout: searching a paired library for a title that does not match falls through to catalogueList's entries.isEmpty overlay and displays "No games yet" with "Pair with your Playstead server to see your library" and a "Pair with Server..." button -- telling a user who is already paired and has a full library to go pair. Cards layout is wrong more mildly: ShelfView's emptyExplanation says "No games match the current search and filters", which is not blank but does not meet the contract either (no query echo in the heading, and no "Clear search" control, so LibraryViewModel.clearSearch is unreachable from the UI in exactly the state it exists for). This is the "plumbed everywhere, proven nowhere" and "backstop truths hide real defects" pattern: the unit and snapshot tests pass against the view model and a directly-constructed state, so neither can see that no view consumes it. Found 2026-09-14 while auditing what was left of GameListView after WINDOWS #79. The fix must render the contract copy on both layouts, wire the clear control, and be gated by a front-door UI test plus a topology guard pinning the shell's use of searchResultState -- not by another view-model test, which is the coverage that already failed to catch it. FIXED 2026-09-14 in the same session it was found. Both layouts now render NoMatchesView from library.searchResultState, and the empty list pane distinguishes three cases where it had one: a search miss (the 03-UI-SPEC Copywriting Contract copy, verbatim), a non-empty library emptied by the active filters, and a genuinely empty library (the pairing prompt, which is the only place it belongs). NoMatchesView carries playstead.surface.no-matches and its button playstead.control.clear-search, so clearSearch() is finally reachable from the UI. Gated by LibraryEmptyStateTests -- a FRONT-DOOR UI test driving the real search field, registered in TestPlans/UI.xctestplan and listed as three --required-test entries in run-mac-verification.sh, deliberately not another view-model test since view-model coverage is exactly what missed this -- plus a four-layer-topology guard that strips comments before matching, because both this shell and the guard file name searchResultState in prose and a whole-file grep would pass over a gutted body. Both probes falsified against the pre-fix tree: removing the cards binding failed with "bindings=1, renders=1, expected 2 and 2", and restoring the pairing prompt as the fall-through failed with the three-way diagnostic; the file was restored byte-identical after each. 03-UI-SPEC's coverage table row that falsely read "covered" was corrected in place rather than deleted. | fixed |  | 2026-09-14T17:20:52.933Z | 2026-09-14T17:42:22.739Z |
| 84 | 04 | deviation | playstead-server/test/playstead_web/plugs/throttle_test.exs | 9 | ThrottleTest's per-test IP was not unique, so its tests shared Hammer per-IP buckets and one arrived pre-throttled. CAPTURED 2026-09-14 by running the full Elixir suite (max_cases 36, 1079 tests) concurrently with a loop of xcodebuild Mac test runs -- the machine contention WINDOWS #81 named as its required next step. Run 5 of 6 failed: "test call/2 denies requests beyond the per-IP limit with a rate_limited problem+json response (PlaysteadWeb.Plugs.ThrottleTest)", throttle_test.exs:34, "Expected false or nil, got true / code: refute conn.halted", with the stacktrace landing on line 41 -- INSIDE the arrange loop, not on the assertion the test is about. That is the tell: the very first request of the test was already halted, so the per-IP bucket was full before the test began. MECHANISM, proven not hypothesised: setup derived the IP as {10, 0, div(unique, 65536) rem 256, rem(unique, 256)}, which drops bits 8..15 of the unique integer entirely. Any two uniques 256 apart map to the SAME address. Measured: over 65536 consecutive uniques the formula reaches 512 distinct IPs, and 2000 draws under 36-way concurrency collapse onto 72 -- a 96 percent collision rate. The per-IP bucket's scale is one minute (throttle.ex @per_ip_scale) while the suite runs for over three, so a collision within the file is a shared limit, and three of its tests each burn exactly their 5-hit limit on their setup IP. The setup's own comment has always claimed counters "never bleed state across tests"; they bled constantly and the comment was the giveaway. FIXED by deriving the address from three bytes instead of two ({10, b2, b3, b4}), which is injective over any 2^24 consecutive uniques against an observed draw spread of about 16k, and by moving the per-account test's hardcoded addresses from 10.1.x to 192.168.x so a generated address can never land on one. Gated by a new test that draws 2000 uniques concurrently and asserts the mapping is injective, plus the specific 1000000/1000256 collision; falsified against the old formula, which fails it 52 distinct of 2000. This is a strictly stronger assertion than the file had, not a weakened one. | fixed |  | 2026-09-14T17:41:58.759Z | 2026-09-14T17:41:58.919Z |
| 85 | 03 | deviation | playstead-mac/PlaysteadTests/CurationTests/PlaySessionTests.swift | 217 | PlaySessionTests/test_launchSucceedsIndependentlyOfPlaySessionRecording fails under machine load on a five-second liveness wait. CAPTURED 2026-09-14 with the message, which is what previous flake windows here lacked: "PlaySessionTests.swift:217: error: -[PlaysteadTests.PlaySessionTests test_launchSucceedsIndependentlyOfPlaySessionRecording] : Asynchronous wait failed: Exceeded timeout of 5 seconds, with unfulfilled expectations: process exits." Reproduced by running the Unit layer while the full Elixir suite (max_cases 36, 1079 tests) saturated the same machine; the layer took 421.7 seconds in that run against its usual 62, and the identical selection passes on an idle machine. The same test had been seen failing once before in this project and was attributed to machine load without the message being kept, so this is the second sighting and the first with evidence. MECHANISM: the test writes a tiny executable, hashes it, decodes a pin, and calls AdapterHost.launch, then waits five seconds for the process-exit callback. The binary exits in milliseconds when the machine is idle; under contention, spawning and reaping it plus the surrounding hashing can exceed the bound. RESOLUTION, and the reasoning matters because it looks superficially like the mistake WINDOWS #82 warns against: the bound was raised to 30 seconds, because the wall-clock is NOT part of this test's criterion. What it asserts is that the exit callback fires at all with no PlaySessionRecorder in the picture -- that is what its name says and what the rest of the test sets up. In #82's hittability case the timeout IS the diagnostic, because a dropped click used to pass silently, so widening it would destroy the assertion; here the assertion is the fulfillment itself and the timeout is only a liveness ceiling. If spawning and reaping a trivial binary ever exceeds 30 seconds that is a genuine hang and should still fail. NOT done, and worth considering if this recurs: the suite has only seven expectation waits in total (five at 5 seconds, one at 1, one at 0), so there is no shared convention to align on, and no CI-load-aware timeout policy exists. FURTHER EVIDENCE 2026-09-14, and the conclusion is deliberately NOT to widen again. With the 30-second bound already in place, this test failed once more during a Unit-layer run that took 2626 seconds against its usual 62 -- a 42x slowdown, self-inflicted, with a Wallaby browser suite and an Elixir full suite running against the same machine at the same time. The identical selection then passed in 0.78 seconds once that load cleared, and the hosted runner executes this layer in about 132 seconds of test time with no failure at all. So the correct reading is that the test rig produced a pathological condition no real CI machine reproduces, not that 30 seconds is still too tight. Raising the bound again to chase a self-inflicted 42x slowdown would be precisely the mistake #82 warns about in a different costume: the number would stop meaning anything and a genuine hang would no longer fail. If this ever fails on hosted CI, that is a different and much more interesting fact, and it should be investigated as a hang rather than answered with a larger constant. DIAGNOSIS RETRACTED 2026-09-14, in the same session that made it. This window is wrong: the failure is NOT machine load. Proof: the test failed again at the raised 30-second bound inside a Unit-layer run that took 69 seconds total on an idle machine, and the 1077-second and 2626-second layer times that were read here as "contention" were one single test, ReleaseHookAbsenceTests/testNonTestingReleaseBinaryAndSymbolsContainNoBootstrapProfileOrEnvironmentKey, which runs a full cold xcodebuild Release build from inside a unit test and took 960 seconds of that on its own; skipping it brings the layer to 38-to-78 seconds, which is the hosted figure. The bound was therefore never the problem, the bigger number did not fix anything, and it has been REVERTED to 5 seconds -- echo exits in milliseconds here, so a spawn that has not called back in five seconds is stuck, not slow. The real cause is recorded in WINDOWS #86: the tests that spawn real processes are coupled through two process-global singletons, AdapterLaunchMutex.shared and AdapterProcessRegistry.shared. The first half is fixed there; the rest is open. Keep this window as the record of a wrong call reached by reasoning from a single correlated observation without isolating the variable -- the layer was slow AND the test failed, and slow was assumed to cause failed. CAUSE FOUND 2026-09-14, and it was never timing. This window's failing test spawns a copy of /bin/echo that was never ad-hoc re-signed. macOS routes the spawn of a file it recognises as an application bundle's main executable through a security assessment, and a system binary's signature is only valid at its platform path -- so the copy fails that assessment, and the failure is silent: Process.run() succeeds, the child is left suspended at _dyld_start forever, and terminationHandler never fires. The test therefore waits on an exit callback for a process that will never exit, which is why 5 seconds failed, 30 seconds failed, and why no bound could ever have worked. Both the load diagnosis and the retraction's "still unexplained" reading are now superseded: the mechanism was already written down in this repo, in AdapterWiringTests.installStandInExecutable's doc comment, which re-signs its own stand-in for exactly this reason and asserts in prose that "PlaySessionTests already does" the same -- it did not. FIXED by routing every stand-in through a single StandInExecutable.install that copies, chmods and ad-hoc re-signs, and that throws a described error rather than letting an unsigned stand-in hang. Five of the suite's six stand-in copy sites were unsigned (PlaySessionTests, LaunchMutexTests x2, SavePlanExecutorTests, PlayPathSaveWiringTests); only AdapterWiringTests signed. The 5-second bound is restored and is correct: echo exits in milliseconds, so a spawn that has not called back in five seconds is stuck, not slow, and the bound should still fail if that ever recurs. This entry's status field has read "fixed" since the retracted change; it is now genuinely fixed, by a different change, for a different reason. See WINDOWS #86 for the second, independent coupling found alongside this one. | fixed |  | 2026-09-14T18:08:03.468Z | 2026-09-14T18:08:20.572Z |
| 86 | 03 | deviation | playstead-mac/Playstead/Adapter/AdapterHost.swift | 101 | Every unit test that spawns a real child process shares two process-global singletons, and the local Unit layer is flaky in that cluster as a result -- 1 to 6 failures varying run to run on an idle machine. NOT FIXED; this needs its own plan. FOUND 2026-09-14 while chasing what WINDOWS #85 wrongly diagnosed as machine load. The affected tests are AdapterWiringTests (testInstallResultVerifiesAndLaunchesThroughARealAdapterHost, testSelectingAnAdapterThroughTheEnvironmentMakesLaunchVerificationSucceedAndActuallyLaunch), LaunchMutexTests (testMutexReleasesOnProcessExitRegardlessOfExitClassification, testRegisteredAdapterProcessIsTerminatedWhenApplicationTerminationHandlerRuns) and PlaySessionTests (test_launchSucceedsIndependentlyOfPlaySessionRecording). The shared symptom is that a spawned child's terminationHandler never fires, so an expectation on process exit times out; one run instead produced "unknown(status: 15, reason: uncaughtSignal)" where clean was expected, meaning the child was SIGTERMed rather than exiting on its own. TWO DISTINCT COUPLINGS, both through globals. First, AdapterLaunchMutex.shared is keyed by asset-set id and released only by the spawned process's termination handler, so ONE launch whose process never exits leaks that key for the rest of the run. Five tests across four suites used the literal "test-asset-set", so that leak turned every later launch into a thrown launchInProgress -- and critically MASKED REAL ASSERTIONS: InstallerTests/testAdapterHostRefusesLaunchWhenInstallStateIsUnverified reported "expected .digestMismatch, got launchInProgress", so a security-relevant refusal silently went unverified while the suite still reported a failure that looked like a locking problem. That half IS FIXED in this session by giving each test a unique id; the four-suite probe went from 6 failures in 16.0s to 0 failures in 1.8s, and AdapterWiringTests from 3-of-10 failing in isolation to 10-of-10 passing. Second, and still open: AdapterProcessRegistry.shared is also process-global, and testRegisteredAdapterProcessIsTerminatedWhenApplicationTerminationHandlerRuns deliberately exercises the app-termination sweep that SIGTERMs every REGISTERED process. A process left registered by an earlier test -- which is exactly what happens when a termination handler never fires -- is therefore killed by a later unrelated test, and a test's own process can be swept while it is still waiting on it. That is the likeliest source of the residual 1-to-6 failures and of the status-15 classification. WHAT IS NOT KNOWN: why a child sometimes fails to exit at all. AdapterWiringTests' own doc comment describes the mechanism for its case -- macOS routes the spawn of an app bundle's main executable through a security assessment, a copy of /bin/echo fails it, and the child is left suspended at _dyld_start forever with terminationHandler never firing -- and that is why it ad-hoc re-signs its stand-in. Whether the re-signing is failing on this machine, or whether syspolicyd is degraded after 16 days of uptime (the same family as #82), was not established. HOSTED CI IS GREEN on this layer (697 executed, 34 required verified, PASSED), so none of this is blocking and the local flakiness may be machine-specific -- but a suite coupled through two mutable globals is flaky by construction, so a green hosted run should be read as luck about timing rather than as evidence the coupling is safe. Next steps, in order: make AdapterProcessRegistry.shared injectable or resettable per test so the termination sweep cannot reach another test's process; then determine why the stand-in child hangs, on a machine rebooted first to rule out a degraded syspolicyd. LEDGER NOTE: WINDOWS #85 still reads status=fixed even though its diagnosis was retracted and its change reverted. gsd-tools windows has no reopen subcommand and refuses to waive an already-fixed entry, so that status field cannot be corrected through the tool; read #85's description, not its status, and treat this window as the live one. CONFOUND FOUND 2026-09-14, and it downgrades part of this window. After a laptop sleep/wake, xcodebuild on this machine began aborting with SIGABRT (exit 134) before compiling anything, logging "DVTFilePathFSEvents: Failed to start fs event stream" and "DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = NSPOSIXErrorDomain Code=5 Input/output error". A bare ps -r also hung for over two minutes while echo returned instantly, with 907 processes live. So the per-user cache/temp directory machinery and FSEvents are broken at the OS level, not in this project. TWO CONSEQUENCES, stated separately because they have different confidence. (1) The shared-mutex half of this window STANDS and is machine-independent: five tests across four suites keying one process-global mutex on the literal "test-asset-set" is a defect by construction, the masking was directly observed as "expected .digestMismatch, got launchInProgress", and the fix took the four-suite probe from 6 failures in 16.0s to 0 in 1.8s and AdapterWiringTests from 3-of-10 failing in isolation to 10-of-10 passing. (2) The residual "1 to 6 failures varying run to run" is NO LONGER attributable to AdapterProcessRegistry.shared with any confidence. A machine whose DARWIN_USER_CACHE_DIR lookup returns EIO can plausibly fail to spawn or reap children, which is the shared symptom, so the registry coupling remains a real design smell worth fixing but is NOT established as the cause of those runs. The same confound may also explain #82's third mode (performAccessibilityAudit failing with Code=-56 reproducibly late in a session) and should be checked against it. REQUIRED BEFORE ANY FURTHER LOCAL CONCLUSION: reboot this Mac and re-run the Unit layer. Until then, hosted CI is the only trustworthy evidence for this project, and no local flake should be attributed to the code. BOTH HALVES NOW CLOSED 2026-09-14. SECOND COUPLING (AdapterProcessRegistry.shared) FIXED: the registry is process-global AND it observed NotificationCenter.default, so the single test that legitimately exercises the app-termination sweep reached every other test's registered child. AdapterProcessRegistry now takes an injectable NotificationCenter (production still uses .default; the app has one termination event and one registry) and removes its observer on deinit; AdapterHost takes an injectable processRegistry defaulting to .shared; all 14 test-side AdapterHost constructions pass .isolatedForTesting(), and RelaunchTests' sweep test drives its own registry on its own private center instead of posting to .default. Injecting the registry alone would NOT have been enough -- every instance still observed the one default center, so a single post swept all of them; the center is the part that had to be injectable. PROVEN, not inferred from flakiness going away: a new test, RelaunchTests/testTerminationSweepReachesOnlyItsOwnRegistrysProcesses, registers one child in each of two registries, sweeps the first, and asserts the second survives. Falsified by restoring pre-fix conditions in place (file backups, NOT git checkout, which would have reverted the uncommitted fix under test) -- it failed on exactly the intended assertion, "a process registered in a different registry must survive another registry's termination sweep", at RelaunchTests.swift:185. THE "WHAT IS NOT KNOWN" IS NOW KNOWN, and it was NOT the registry: after the registry fix, PlaySessionTests still hung. The cause is unsigned stand-in binaries left suspended at _dyld_start -- see WINDOWS #85, fixed in the same session by StandInExecutable. The two couplings were genuinely independent, and the registry fix alone does not close this cluster. REPORTING ARTIFACT worth knowing before reading any future failure list: the baseline failure "-[RelaunchTests testRegisteredAdapterProcessIsTerminatedWhenApplicationTerminationHandlerRuns] : XCTAssertEqual failed: (unknown(status: 15, reason: uncaughtSignal)) is not equal to (clean)" is MISATTRIBUTED. RelaunchTests.swift contains no such assertion; it is LaunchMutexTests' onExit closure firing late and being charged to whichever test happened to be running. Do not chase RelaunchTests on the strength of that line. EVIDENCE, and its limits: on an idle machine the full Unit layer (ReleaseHookAbsenceTests skipped, which alone costs ~960s of cold Release xcodebuild) went from 0, 1 and 6 failures in three baseline runs to the post-fix runs recorded in this session's commit message, at a steady ~45s. That RATE evidence is local and this Mac has NOT been rebooted, so it remains subject to this entry's own standing instruction to distrust local flake measurements until it has been; hosted CI on the branch is the arbiter for the rate. The falsification result above is NOT subject to that caveat -- it is a named assertion flipping on a deliberate pre/post comparison, and the _dyld_start mechanism is documented in this repo's own AdapterWiringTests doc comment rather than inferred from this machine. | fixed |  | 2026-09-14T19:56:00.640Z | 2026-09-14T21:22:58.976Z |
| 87 | 03 | deviation | playstead-mac/Playstead/Saves/SaveUploadLane.swift | 80 | The live-server harness decides "real defect" versus "ordinary retry" by reading a shared last-writer-wins cell that a CONCURRENT drain can overwrite, so an ordinary state can be reported as an unfixable server refusal. OBSERVED 2026-09-14, hosted run 34901279944 (head 6e4c1f8): SaveEndToEndTests/testOneSaveRoundTripsCaptureUploadAndJournalReturn failed at SaveEndToEndTests.swift:185, which is the one-assertion-site-per-cause marker save-e2e-harness=upload-server-refused -- the verdict WINDOWS #67 documents as "a real defect, never a flake". LiveServerSnapshotTests passed in the same run at 11.20s, inside its green band, so this is NOT #71 and NOT the same failure as run 34898512311. MECHANISM, established by reading the code rather than inferred from the run. UITestBootstrap.runSaveEndToEnd branches on TWO values with different lifetimes: drainResult, which belongs to the harness's own drainOnce pass, and lane.lastFailureClassification, which does not. The latter is a nonisolated computed property over SaveUploadLane's single classificationCell (SaveUploadLane.swift:80), written by whichever pass ran last -- set(classify(error)) on failure at :156 and set(.none) on success at :150. Since WINDOWS #67's own fix made the harness share the APP's lane, and the app drains on the reachability transition at pairing time, two passes now write that one cell while the harness reads it out-of-band at three decision points (UITestBootstrap.swift:347, :360, :382). So drainResult.stoppedForRetry can be true for the harness's ordinary .offlineQueue stop while the cell already holds the app pass's .revokedAuth, and isRetryable then returns false and the harness reports "server refused". The contamination runs both ways: the app's success path writes .none, which reads as retryable and would instead misreport an escalating failure as an ordinary one. THIS IS #67's OWN DEFECT ONE LEVEL UP. #67's root cause was asserting on "the lane object I happen to hold incremented sent" instead of the outcome, and its fix was to share the app's lane and assert the revision reaches .uploaded. The uploaded check was correctly moved to the outcome; the failure CLASSIFICATION was not, and sharing the lane is exactly what made that cell cross-contaminated. #67 is recorded status=fixed and gsd-tools has no reopen, hence this separate entry -- see also this ledger's own note that a retracted entry's status field cannot be corrected. NOT CAUSED BY 6e4c1f8 or 86c816c: neither touched Saves, Sync or UITesting, and the unit, rendering and UI layers all passed in this same run (697 / 45 / 118 executed, 0 failures). FIX DIRECTION, not applied: OutboxDrainResult (OutboxWorker.swift:5) already carries sent, rejected and stoppedForRetry but not the classification, which is precisely why the harness has to reach for the shared cell. Carrying the pass's own classification back in that struct makes the harness branch on the pass it actually ran, and is checkable in the unit layer alongside the existing SaveUploadLaneTests rather than only in the live-server layer. NOT ATTEMPTED HERE because it is production code in the save path and the layer that exercises it cannot be run locally; it wants its own plan and a falsification probe, in the style of the four already pinned in four-layer-topology-test.sh. WHAT IS NOT KNOWN: whether the observed run was this race or a genuine .revokedAuth against the freshly provisioned CI server. The harness cannot currently tell those apart, which is itself the finding -- the reason channel carries a fixed literal by design, so the classification that drove the verdict is never reported. Reporting it (it is a closed enum this repo owns, so it is safe on that channel under this file's own stated rule) would settle the next occurrence in one run. FIXED 2026-09-15, and proven rather than inferred. OutboxDrainResult now carries failureClassification, SaveUploadLane.drainOnce records its own pass's verdict there alongside the shared cell, and all three of the harness's decision points read drainResult.failureClassification instead of lane.lastFailureClassification -- so stoppedForRetry and the classification are now guaranteed to describe the same pass. The shared cell is untouched and still serves the shipped escalation panel, which legitimately wants the most recent verdict. UNIT-LEVEL PROOF, in the layer that can actually be run locally: SaveUploadLaneTests/test_aLaterSuccessfulPassDoesNotRewriteTheFailedPassOwnClassification drives one pass to a 401 (.revokedAuth, one of D-40's four unfixable reasons) and then a second pass to success, which resets the cell to .none. Afterwards lane.lastFailureClassification reads .none -- "nothing is wrong" -- while the first pass genuinely was refused. Falsified by deleting the single line that records the classification on the result: the test fails at SaveUploadLaneTests.swift:296 with ("none") is not equal to ("revokedAuth"), which is exactly the misreport this window describes, and the restore was verified byte-exact rather than by git checkout. THREE CONTRACT GUARDS, each falsified in place. four-layer-topology-test.sh now (1) forbids lane.lastFailureClassification anywhere inside runSaveEndToEnd outright, rather than counting correct uses -- reading the lane there IS the defect; (2) requires drainResult.failureClassification at all three decision points; (3) separately requires OutboxDrainResult to declare the field and drainOnce to write it, because the harness-side guard passes perfectly well against a field nobody writes, which would silently reclassify every refusal as retryable and make the whole test unfailable. Probes 1, 2 and 3 each failed on their own intended message and the tree was restored clean. DIAGNOSTIC, closing this entry's "what is not known": the single save-e2e-harness=upload-server-refused verdict is now four -- refused-revoked-auth, refused-capability-skew, refused-server-refusal, refused-compatibility-rejection -- one assertion site each in SaveEndToEndTests, because D-40's four unfixable reasons are four different verdicts and CI keeps only file:line. So the next occurrence says which refusal it was in one run, and since the classification now provably belongs to the failing pass, a recurrence can no longer be dismissed as this window's race. Each remains a fixed literal over a closed enum this repo owns, per UITestBootstrap's own stated rule that an error's own description must never reach the reason channel. NOT YET SEEN GREEN ON THE LIVE-SERVER LAYER: that layer cannot be run locally and reproduces only on hosted CI. The unit layer ran 696 tests with 0 failures with these changes in place, and all static contract guards pass. | fixed |  | 2026-09-14T22:24:05.812Z | 2026-09-15T14:01:00.180Z |
| 88 | 03.5 | deviation | playstead-mac/PlaysteadTests/AdapterTests/StandInExecutable.swift | 1 | THIS MAC CANNOT EXECUTE A COPIED BINARY AT ALL, and every local child-process test failure on it is therefore uninformative about the code. MEASURED 2026-09-15 at 17 days uptime, outside Xcode and outside any test: copy /bin/echo to a fresh path, chmod +x, run it -- it never executes and produces no output. Signed or unsigned makes NO difference (ad-hoc codesign succeeds, exit 0, and the copy still hangs), and neither does the directory: $HOME, /tmp, and the DARWIN_USER_TEMP_DIR that FileManager.temporaryDirectory returns all reproduce it, three trials each on distinct fresh paths, rc=124 under timeout for the signed variant and rc=137 for the unsigned. /bin/echo at its own system path in the same shell returns rc=0 and prints normally, and a copy of /usr/bin/true hangs identically, so it is not specific to one binary. This is a broken macOS execution path -- the AMFI/syspolicyd/trustd family -- not a project defect. METHOD NOTE, because the first run of this probe reported the opposite and would have put a false mechanism in this ledger: re-using ONE path and cp-ing over it makes the result meaningless, because cp reuses the inode and the earlier ad-hoc signature is still cached against it, so an "unsigned" trial silently inherits the previous "signed" trial's state. The corrected probe stages every trial at its own fresh, never-before-signed path. Any future re-measurement must do the same. WHAT THIS EXPLAINS: a full local Unit run on this machine now fails 5 tests -- AdapterWiringTests/testSelectingAnAdapterThroughTheEnvironmentMakesLaunchVerificationSucceedAndActuallyLaunch, LaunchMutexTests/testMutexReleasesOnProcessExitRegardlessOfExitClassification at LaunchMutexTests.swift:193 and :196, and PlaySessionTests/test_launchSucceedsIndependentlyOfPlaySessionRecording at PlaySessionTests.swift:236, each an expectation on a spawned child exiting. It reproduces with only those four suites selected (27 tests, 5 failures), so it is NOT contention from the full suite. WHAT IT DOES NOT MEAN: it is not a regression in WINDOWS #85 or #86. Those were closed against hosted CI, which ran this same Unit layer green three consecutive times at 697 executed / 0 failures on commits 86c816c, 6e4c1f8 and 295038f, and the local commits since then touch only Outbox, Migrations, two PlaysteadApp doc comments and OutboxTests -- nothing in the adapter or stand-in path. #85's mechanism also still stands: ad-hoc signing is the right fix for an unsigned copy of a bundle's main executable on a HEALTHY machine, and this entry is a second, independent failure mode in which signing is simply not the discriminator because nothing copied runs at all. THIS IS THE CONFOUND WINDOWS #86 PREDICTED, now measured rather than suspected. #86 recorded the same machine at 16 days uptime with DVTFilePathFSEvents and DARWIN_USER_CACHE_DIR EIO failures and issued a standing instruction: "REQUIRED BEFORE ANY FURTHER LOCAL CONCLUSION: reboot this Mac and re-run the Unit layer. Until then, hosted CI is the only trustworthy evidence for this project, and no local flake should be attributed to the code." That instruction was correct and is hereby re-asserted with direct evidence behind it. ACTION: reboot this Mac, then re-run the probe above before trusting any local child-process result. Until the probe passes, treat a local Unit run's child-process failures as measuring the machine, and read the layer's other ~698 tests only. Hosted CI remains the arbiter. RETRACTED 2026-09-15, SAME DAY, BY THE AUTHOR OF THE ENTRY. This window does not exist. The machine is fine, and the characterisation above is a probe artefact that I wrote into the ledger as a finding. WHAT ACTUALLY BROKE IT: every trial in the original probe copied a PLATFORM binary -- `/bin/echo` and `/usr/bin/true`, nothing else -- and the conclusion "this Mac cannot execute a copied binary at all" generalised from a single subject class that happens to be the one class where copying genuinely does fail, on every healthy Apple Silicon Mac, by design. The fresh-path discipline the earlier retraction added was necessary and was correctly applied; it simply was not sufficient, because the missing control was a different SUBJECT, not a different path. RE-PROBED ACROSS CLASSES, one command, each trial at its own fresh path: a locally built binary runs both in place and copied (rc=0); a copy of an ordinary third-party binary runs; a copy of `/bin/echo` is SIGKILLed (rc=137); that same copy after `codesign -f -s -` runs and prints its argument (rc=0); that same copy with `--remove-signature` is SIGKILLed again (arm64 requires a signature). That is the ordinary AMFI rule -- a platform binary's signature is only valid inside the platform trust domain, so off its system path it fails validation -- and it is not a fault of any kind. It also directly contradicts this entry's recorded claim that signed copies hang at rc=124: an ad-hoc re-signed copy runs. THE 17-DAY-UPTIME CONFOUND NARRATIVE IS WITHDRAWN WITH IT. Uptime is still 17 days, unchanged, no reboot has happened -- and the full Unit layer now passes 714 of 714 with zero failures, including `AdapterExitBoundaryTests`, whose `testEachRealTerminationClassifiesAsTheRightNamedCase` and `testNoRealChildProcessTerminationClassifiesAsUnknown` are precisely the child-process tests this entry claimed the machine could not run. They pass because the stand-in executable they spawn is LOCALLY BUILT, which was never the failing class. So no reboot is required of the user, and local child-process evidence was never untrustworthy. WHAT THIS ENTRY COST, recorded because the cost is the lesson: it declared local evidence unreliable and asked for a user reboot, both on the strength of a probe with no cross-class control. STILL UNEXPLAINED, and deliberately not folded into this entry: the earlier child-process test failures that prompted the probe in the first place. They are not accounted for by anything here, they did not reproduce in the 714/714 run, and they need their own entry with their own evidence if they recur -- attaching them to a retracted mechanism is exactly how this entry went wrong the first time. | waived | Retracted by the author the same day: probe artefact, not a defect. Every trial copied a platform binary, the one class where copying fails by design on Apple Silicon. Cross-class re-probe: locally built and third-party copies run; an ad-hoc re-signed copy of /bin/echo runs. Unit layer passes 714/714 including the child-process tests this entry claimed were unrunnable. No reboot needed; nothing to fix. | 2026-09-15T14:31:26.880Z | 2026-09-15T15:23:30.605Z |
| 89 | 03 | deviation | playstead-mac/Playstead/Sync/Outbox.swift | 197 | An outbox row left `in_flight` when the app quits is stranded permanently: never retried, never quarantined, never surfaced -- while its optimistic local write stands, so the local model and the server diverge and nothing ever reconciles them. FOUND 2026-09-15 by the independent re-verification of phase 03, as a side observation while checking WR-07's watermark, not by a failing test. MECHANISM, confirmed by direct read of Outbox.swift: `markInFlight` (line 197) is the only writer of `state = 'in_flight'`, and the only paths out of that state are `markDone`, `markRejected` and `markPendingForRetry` -- all three of which are reached only from `OutboxWorker.drainOnce`'s in-process handling of a response. Nothing runs at startup. `listPending` selects `state = 'pending'` exclusively, so a stranded row is invisible to every subsequent drain, and `listQuarantined` never sees it either because quarantine is reached only through `markPendingForRetry`'s attempt counter. So a favourite, a collection rename or an availability report that was on the wire at the moment of a crash, a force-quit, or an ordinary quit mid-request is simply gone from the outbox's point of view, while `applyOptimistically` has already written it locally. THE OBVIOUS FIX IS COUPLED TO WR-07, and that coupling is the reason this is filed rather than left as a note. The natural repair is a startup sweep moving `in_flight` back to `pending`. That sweep is exactly what makes WR-07's cross-restart case REACHABLE: today an `in_flight` row at quit can never revert, so the scenario the delivered-watermark protects against cannot currently occur, and the protection is latent. The moment this window is fixed, that path goes live -- and it is the CONDITIONAL gate on the watermark migration (Migrations.swift, `pragma_table_info(...) WHERE name = 'created_at'`) that then keeps WR-07 from returning, because an unconditional drop would wipe the watermark on the very launch that performs the sweep. Whoever fixes this must not independently "simplify" that gate. `test_theWatermarkSurvivesAnOrdinaryRelaunch` in OutboxWatermarkMigrationTests already pins the gate and will fail if it is removed. NOT YET SIZED: whether a blind startup revert is even correct, since an `in_flight` row is a request that may well have been received and applied by the server. The idempotency key is preserved across attempts (P4-CR-001, pinned by test_retry_sendsTheSameIdempotencyKeyAsTheFirstAttempt), so a replay should be absorbed rather than duplicated -- which suggests the sweep is safe for the curation kinds, but that argument needs checking per kind, and against the server's own replay handling, before it is relied on. FIXED 2026-09-15. `Outbox.recoverStrandedInFlight(at:)` sweeps every `state = 'in_flight'` row, and `AppEnvironment`'s initializer calls it immediately after constructing the `Outbox` -- the one point in the process that runs exactly once, before any of the three drain triggers can fire. THE UNSIZED PREMISE WAS CHECKED FIRST, not assumed: this entry said the replay-safety argument "needs checking per kind, and against the server's own replay handling", so both were read. `idempotencyKey` is `kind:entryID`, generated once in `enqueue` and PERSISTED as outbox_entries column 3, read back by `rows(_:_:)` -- so it is byte-identical across attempts AND across restarts, which is the property that makes reverting a possibly-applied request safe at all. On the server, all 16 real `CurationIntentKind` cases map onto routes that sit in a scope piping through `:idempotency` (every `/api/v1/curation/*` mutation, `/play-sessions`, and `PUT /devices/me/availability`), and that plug holds a per-device receipt keyed on {device_id, idempotency_key} with a unique index (D-20a). A replay is therefore absorbed, not applied twice. RESIDUAL, recorded rather than engineered around: receipts are pruned at ~90 days, so a row stranded longer than that would genuinely re-execute; the sweep running at the very next launch is what bounds this in practice. THE SWEEP GOES THROUGH `markPendingForRetry`, NOT A BARE `UPDATE ... SET state = 'pending'`, and that is the whole non-obvious part of the fix -- it buys three properties a bare revert loses: the attempt counter advances so a row that strands on every launch eventually quarantines instead of replaying forever, backoff stops a launch firing every recovered request at once, and WR-07's supersede check runs so a stale report is dropped rather than revived behind a newer one. FALSIFIED BY MUTATION, two mutants, because the four-round WR-07 lineage showed the defects land in the fix rather than the original. Mutant B, the bare `UPDATE` -- exactly what someone would write not knowing about the counter or the supersede rule -- fails 5 assertions across precisely the two discriminating tests (`...EventuallyQuarantinesRatherThanReplayingForever` and `...IsDroppedRatherThanRevivedBehindANewerDeliveredOne`) and passes the other four, which is the correct discrimination and not a blanket failure. THE COUPLING THIS ENTRY WAS FILED FOR IS NOW LIVE AND PINNED. WR-07's cross-restart case was latent only because an `in_flight` row at quit could never revert; it can now, and `test_aStrandedReportIsDroppedRatherThanRevivedBehindANewerDeliveredOne` is the test that walks it -- stranded report, newer report delivered so `markDone` deleted its row and only the watermark remembers it, then relaunch and sweep. That test passes only because the watermark migration's drop is CONDITIONAL on the legacy `created_at` column; an unconditional drop would wipe the watermark on the very launch that performs the sweep, and `test_theWatermarkSurvivesAnOrdinaryRelaunch` pins that gate. TESTS: `PlaysteadTests/SyncTests/OutboxStrandedInFlightTests.swift`, 6 tests, all against a SECOND `Outbox` over the same `LocalStore`, because that is what a relaunch actually is. Green locally at 34/34 alongside OutboxTests (25) and OutboxWatermarkMigrationTests (3). NOT `try?` at the call site: a failed sweep leaves rows stranded, which is the silent divergence the sweep exists to end, so it is logged via a `startup-recovery` OSLog category rather than swallowed, and is not fatal -- the next launch sweeps again. | fixed |  | 2026-09-15T14:41:04.741Z | 2026-09-15T15:21:37.065Z |
| 90 | 04 | deviation | playstead-mac/Playstead/Persistence/Migrations.swift | 1 | `save_revision.restored_here_at` is the only column in the whole schema declared SOLELY by a defensive `try? ALTER`, with no matching entry in its own `CREATE TABLE` block -- so the statement that creates it on a fresh database is also the statement a future cleanup is most likely to delete as redundant. FOUND 2026-09-15 by the independent re-verification of phase 03, which extracted every CREATE block and every ALTER in Migrations.swift and scanned all 19 commits that have touched the file: 11 of the 12 ALTER-added columns also appear in their CREATE block, and this one does not. SOUND TODAY, and that is precisely the hazard -- the ALTER runs unconditionally on every launch, so every database has the column and nothing is broken right now. The failure is latent and arrives with an ordinary tidy-up: delete the "redundant" ALTER, and a fresh database silently lacks a column that `SaveStore` reads and writes in four places, with the error swallowed by the `try?` that made the line look optional in the first place. RELATED BUT NOT THE SAME MODE AS WR-08. WR-08 was a `CREATE TABLE IF NOT EXISTS` no-opping against an older shape so that a defensive ALTER left the table half-migrated and unwritable. This is the mirror image: the CREATE is the incomplete one, and the ALTER is load-bearing rather than defensive. Both come from the same root cause, which is worth stating plainly because it will keep producing entries: Migrations.swift has NO `user_version` gate anywhere (grep count 0), so every statement in it is written to be idempotent and re-runnable, and there is no syntactic distinction between "this ALTER upgrades an old database" and "this ALTER is the only thing that creates the column". FIX, when phase 04 is touched: add `restored_here_at` to `save_revision`'s CREATE TABLE so the ALTER becomes genuinely redundant, and keep the ALTER for existing databases. Do not simply delete the ALTER. A `user_version` scheme would retire this whole class, but that is a larger change than this entry justifies on its own. FIXED 2026-09-15. `restored_here_at TEXT` is now declared in `save_revision`'s own CREATE TABLE block, so the defensive `try? ALTER` below it is genuinely redundant on a fresh database and load-bearing only for an install predating 04-22; the ALTER is KEPT, and its comment now says so explicitly so the next tidy-up deletes nothing. GUARDED, and this is the part that outlives the one-column fix: `scripts/ci/tests/migration-column-declaration-test.sh` parses Migrations.swift, extracts every CREATE TABLE column list and every `ALTER TABLE ... ADD COLUMN`, and fails when any ALTER-added column is absent from its own CREATE block. It reports 12 ALTER-added columns all declared, which independently reproduces the re-verification's hand count of 12 with 11 previously declared. The guard supplies exactly the syntactic distinction this file lacks -- with no `user_version` gate anywhere, nothing else separates "this ALTER upgrades an old database" from "this ALTER is the only thing that creates the column". FALSIFIED against the pre-fix source: the detector names `undeclared=save_revision.restored_here_at` and exits 1. It also fails closed three ways, each exercised in the guard's own self-tests -- an ALTER-only column, a source containing no ALTER statements at all (a detector that matches nothing must not report success on a file it never understood), and a missing file. Registration needed no action: run_contract_self_tests enumerates tests/*.sh from the directory rather than a hand-kept list, so the guard is picked up by existing. The full contract gate passes 25/25 with it included. NOT FIXED HERE: the absent `user_version` scheme, which is the shared root cause of this entry and WR-08 and will keep producing entries of both shapes. That remains a larger change than either entry justifies. | fixed |  | 2026-09-15T14:41:04.833Z | 2026-09-15T15:21:36.915Z |

````json
[
  {
    "id": 1,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Adapter/AdapterHost.swift",
    "line": null,
    "description": "verifyInstalledDigest() expects an .install-verify.json sidecar that no plan yet writes (emulator installer is 03-08/03-09 territory)",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-31T00:10:31.234Z",
    "resolved_at": "2026-08-31T02:44:11.180Z"
  },
  {
    "id": 2,
    "kind": "unrun-verify",
    "phase": "03",
    "file": "playstead-mac/scripts/sign-and-notarize.sh",
    "line": null,
    "description": "Notarization + Developer-ID release build could not run (no Developer ID Application cert / paid Apple Developer Program in this environment); notarization deferred per 03-01 owner decision",
    "status": "waived",
    "reason": "Deferred by owner 2026-08-31: local development only; no Developer ID / paid Apple Developer Program enrollment this milestone. Notarization is distribution-only and does not affect running a dev-signed build on the owner's own Mac. Revisit before distributing to other machines.",
    "recorded_at": "2026-08-31T00:10:35.783Z",
    "resolved_at": "2026-08-31T05:45:26.950Z"
  },
  {
    "id": 3,
    "kind": "unrun-verify",
    "phase": "03",
    "file": "playstead-mac/Playstead/Adapter/AdapterHost.swift",
    "line": null,
    "description": "Task 3 human-check (notarized app, network disabled, Play launches mGBA, quit returns to library) unverified in this session — no installed emulator/downloaded ROM/Developer ID cert available",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-31T00:10:40.298Z",
    "resolved_at": null
  },
  {
    "id": 4,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Library/GameCardView.swift",
    "line": null,
    "description": "DownloadCoordinator progress percent wired but not yet consumed by LibraryViewModel/GameCardView's live rendering path (03-07)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-31T01:42:14.870Z",
    "resolved_at": null
  },
  {
    "id": 5,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/App/PlaysteadApp.swift",
    "line": null,
    "description": "AppEnvironment does not yet construct DownloadQueue/DownloadCoordinator/QuotaManager/PinStore/EvictionPlanner as live app-wide singletons (03-07)",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-08-31T01:42:20.733Z",
    "resolved_at": "2026-08-31T12:46:28.600Z"
  },
  {
    "id": 6,
    "kind": "unrun-verify",
    "phase": "03",
    "file": "playstead-mac/Playstead/Library/StorageView.swift",
    "line": null,
    "description": "Visual/interactive click-through of DownloadsView/QuotaSettingsView/ReclaimPromptView/StorageView against a live paired server unverified in this headless session (03-07 coverage D6)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-31T01:42:25.363Z",
    "resolved_at": null
  },
  {
    "id": 7,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Adapter/BiosStore.swift",
    "line": null,
    "description": "BiosStore's known-reference digest set has no production default (DI-only, no fabricated evidence); real reference digest wiring is deferred until sourced",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-31T02:43:54.600Z",
    "resolved_at": null
  },
  {
    "id": 8,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Adapter/AdapterHost.swift",
    "line": null,
    "description": "Gatekeeper-held child hangs silently: AdapterHost.launch treats a successful Process.run() as launched, but a quarantined bundle is left suspended at _dyld_start and terminationHandler never fires — the app believes the emulator runs forever with no error surfaced. AdapterInstaller preserves quarantine per D-05. Proven experimentally (signed+quarantined HUNG; signed alone exits 0.15s). A properly notarized mGBA should pass assessment so the happy path is likely fine, but the failure shape is silent and unbounded. Fix requires a design decision: a liveness probe cannot distinguish held from idle (held task reports S not T; Mach suspend count needs task-port access the app lacks), and pre-flighting SecStaticCodeCheckValidity at install would reject the /bin/echo stand-ins both test suites depend on, so a fixture strategy must be decided alongside it.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-08-31T10:48:44.083Z",
    "resolved_at": null
  },
  {
    "id": 9,
    "kind": "unrun-verify",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Task 1 exact UI-layer drag verification requires the centrally orchestrated hosted macOS runner; local app launch is prohibited by the no-password safety boundary.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-01T06:29:58.866Z",
    "resolved_at": "2026-09-01T18:29:36.711Z"
  },
  {
    "id": 10,
    "kind": "unrun-verify",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Task 2 full CurationInteractionTests UI-layer verification requires the centrally orchestrated hosted macOS runner; local app launch is prohibited by the no-password safety boundary.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-01T06:29:58.948Z",
    "resolved_at": "2026-09-01T18:29:41.733Z"
  },
  {
    "id": 11,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/Library/ShelfView.swift",
    "line": null,
    "description": "Plan path corrected from nonexistent Curation/ShelfView.swift to the production Library/ShelfView.swift location.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T06:29:59.030Z",
    "resolved_at": null
  },
  {
    "id": 12,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/UITesting/DeterministicProfile.swift",
    "line": null,
    "description": "Added compile-gated UUID-only persisted profile support required to prove process-relaunch durability without touching Plan 07 storage production files.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T06:29:59.112Z",
    "resolved_at": null
  },
  {
    "id": 13,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/scripts/ci/run-mac-verification.sh",
    "line": null,
    "description": "Hosted run 33478091423 exposed an EXIT trap that referenced function-local keyboard state after scope unwind and masked the underlying early build result; fixed in a4ef343 with globally initialized guarded cleanup and pre-capture failure regression.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T06:44:32.931Z",
    "resolved_at": null
  },
  {
    "id": 14,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Split broad hosted curation UI identity into three exact stages and changed drag to the deterministic last-row-to-first boundary after run 33481640835.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T07:44:47.355Z",
    "resolved_at": null
  },
  {
    "id": 15,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/UITesting/UITestBootstrap.swift",
    "line": null,
    "description": "Hosted run 33483731474 exposed redundant fresh-position validation on persisted reorder relaunch plus stale shelf selectors; fixed and split into nine exact curation stages.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T08:14:14.831Z",
    "resolved_at": null
  },
  {
    "id": 16,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/Curation/CollectionsView.swift",
    "line": null,
    "description": "Hosted run 33486052488 exposed Favorites collapsed-node and Collections child-text routing mismatches; fixed with exact card queries and a stable collection route button.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T08:41:10.578Z",
    "resolved_at": null
  },
  {
    "id": 17,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/Library/GameCardView.swift",
    "line": null,
    "description": "Hosted run 33488360822 exposed card identity before accessibility collapse and drag gestures on semantic content instead of List cells; fixed and split drag mutation from durability.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T09:07:27.559Z",
    "resolved_at": null
  },
  {
    "id": 18,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Hosted run 33490616418 exposed only aggregate reorder outcomes; split route, cell ownership, drag mutation/durability, keyboard focus/effect/durability and full E2E, with a slow held drag.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T09:33:08.765Z",
    "resolved_at": null
  },
  {
    "id": 19,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Hosted run 33495336534 proved routes and cells but no mutations: switched touch-style press-drag to macOS click-drag and removed duplicate Space after focusContainedAction.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T10:33:18.230Z",
    "resolved_at": null
  },
  {
    "id": 20,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Hosted run 33498048537 proved focusContainedAction is focus-only; restored one Space, isolated Move Up availability/click, and held the macOS drag destination.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T11:04:38.825Z",
    "resolved_at": null
  },
  {
    "id": 21,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/Curation/CollectionsViewModel.swift",
    "line": null,
    "description": "Hosted run 33500660524 exposed an unobserved SQLite member query; refresh now publishes a member revision so optimistic reorder and bounded evidence invalidate.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T11:39:10.783Z",
    "resolved_at": null
  },
  {
    "id": 22,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/Curation/CollectionDetailView.swift",
    "line": null,
    "description": "Hosted run 33503640089 isolated keyboard focus ownership; collection and queue reorder actions now bind stable identity and focus on the final button through playsteadFocusable.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T12:13:48.616Z",
    "resolved_at": null
  },
  {
    "id": 23,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Hosted run 33506605120 exposed app-scoped Space dispatch plus a missing combined-path activation; both keyboard paths now send one Space through the exact focused button.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T12:48:01.842Z",
    "resolved_at": null
  },
  {
    "id": 24,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Hosted run 33510034845 exposed an impossible retained-focus assertion on a Move Up button disabled by reaching the first boundary; the keyboard proof now moves last-to-middle while preserving exact effect/order/boundary/durability checks.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T13:28:24.085Z",
    "resolved_at": null
  },
  {
    "id": 25,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/CurationInteractionTests.swift",
    "line": null,
    "description": "Hosted run 33513649409 lacked keyboard stage detail; curation now uses exact-target canary focus detection and bounded safe focus/effect/retention test IDs.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T14:17:23.468Z",
    "resolved_at": null
  },
  {
    "id": 26,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/scripts/ci/run-mac-verification.sh",
    "line": null,
    "description": "Hosted run 33518726537 executed but did not report the three curation keyboard stage tests; all are now required evidence before any production command-model change.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T14:56:43.305Z",
    "resolved_at": null
  },
  {
    "id": 27,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/Playstead/Curation/CollectionDetailView.swift",
    "line": null,
    "description": "Hosted run 33526574205 proved nested List row buttons are not ordinary Tab stops; keyboard reorder now uses exact List selection plus visible bounded commands through the existing settlement path.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-01T16:09:38.131Z",
    "resolved_at": null
  },
  {
    "id": 28,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/lib/playstead_web/controllers/api/v1/saves_controller.ex",
    "line": null,
    "description": "Save-lane UploadSlots/RateLimiter (D-33) not yet wired into the save routes",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-04T03:18:16.081Z",
    "resolved_at": null
  },
  {
    "id": 29,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Sync/JournalApplier.swift",
    "line": null,
    "description": "CacheObjectsSaveBytesPrefetcher.onPrefetchNeeded is a no-op; real byte transfer for D-43 prefetch deferred to a later downloads-lane plan",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-04T03:18:16.168Z",
    "resolved_at": null
  },
  {
    "id": 30,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/lib/playstead/export/export.ex",
    "line": null,
    "description": "Real Playstead.Saves revision data is not yet loaded into Export.to_layout_input/1; SavesPlan/Sidecar/BagitWriter pipeline is built+tested against synthetic data only (04-08) FIXED by plan 04-21 (22d92f7/433f7e4/8e32347): Export.load_save_revisions/2 loads real save revisions at the Export context boundary; the real file is playstead-server/lib/playstead/export.ex, not export/export.ex.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-04T15:35:17.080Z",
    "resolved_at": "2026-09-06T02:02:52.856Z"
  },
  {
    "id": 31,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/lib/playstead_web/live/exports_live.ex",
    "line": null,
    "description": "No console UI control yet sets ExportRecord.saves_scope (defaults to all); field is persisted and fully threaded through Worker (04-08)",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-04T15:35:17.159Z",
    "resolved_at": "2026-09-04T19:49:47.031Z"
  },
  {
    "id": 32,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/ConflictComparisonSheet.swift",
    "line": null,
    "description": "Comparison sheet is built and tested standalone but not yet wired into a live attention-inbox or game-detail navigation path on the Mac (server console entry point already shipped in 04-10); presentation wiring is left for a later plan STALE, not fixed by 04-23: ConflictComparisonSheet was wired into ReadinessSheetView by plan 04-16 / commit c223218, which postdates this entry; 04-VERIFICATION.md confirmed the wiring directly in source. Closed with c223218 as the resolving commit.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-04T20:20:49.523Z",
    "resolved_at": "2026-09-06T02:02:53.323Z"
  },
  {
    "id": 33,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Sync/JournalApplier.swift",
    "line": null,
    "description": "Divergence detection does not yet trigger an eager 32KB prefetch of both sides' artifacts (CacheObjectsSaveBytesPrefetcher.onPrefetchNeeded is still the no-op tracked by WINDOWS #29); comparing/choosing works offline only once both blobs are already locally cached by some other path",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-04T20:20:49.612Z",
    "resolved_at": null
  },
  {
    "id": 34,
    "kind": "unrun-verify",
    "phase": "04",
    "file": "playstead-mac/PlaysteadUITests/ConflictResolutionInteractionTests.swift",
    "line": null,
    "description": "UI-layer keyboard-interaction verification requires the centrally orchestrated hosted macOS runner; local execution is disabled by the project's login-Keychain launch guard (mirrors WINDOWS #9/#10 precedent)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-04T20:20:49.696Z",
    "resolved_at": null
  },
  {
    "id": 35,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/OnlyCopyInterruptiveSheet.swift",
    "line": null,
    "description": "Unpair, sign out, and delete game have no production entry point yet in this codebase; only remove-local-copy (ReclaimPromptView) and eviction (StorageView) are wired to the interruptive gate. The sheet and gate are built, tested, and reusable for whichever future plan adds those three call sites.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T01:12:59.522Z",
    "resolved_at": null
  },
  {
    "id": 36,
    "kind": "unrun-verify",
    "phase": "04",
    "file": "playstead-mac/PlaysteadUITests/OnlyCopyInterruptionTests.swift",
    "line": null,
    "description": "UI-layer keyboard-interaction verification requires the centrally orchestrated hosted macOS runner; local execution is disabled by the project's login-Keychain launch guard (mirrors WINDOWS #9/#10/#34 precedent).",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T01:12:59.615Z",
    "resolved_at": null
  },
  {
    "id": 37,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Library/GameRowView.swift",
    "line": null,
    "description": "LaunchSavePlanner/SavePlanExecutor are still not wired into GameRowView.play() -- AdapterHost.launch's executeSavePlan closure exists (04-07) but no call site passes one; 04-07-SUMMARY.md named plan 04-13 as the downstream consumer, but 04-13's declared task scope (files_modified) is limited to the two new proof tests, the two xctestplans, and run-mac-verification.sh, so this wiring remains open",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T01:50:48.874Z",
    "resolved_at": "2026-09-05T02:27:54.263Z"
  },
  {
    "id": 38,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/LaunchSaveEnvironment.swift",
    "line": null,
    "description": "captureExistingFile commits pre-existing save bytes into the CAS for durability but does not create a SaveStore revision row, so a pre-restore capture on the launch path is not yet visible in SaveHistorySheet (04-14)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T02:28:02.845Z",
    "resolved_at": null
  },
  {
    "id": 39,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Library/GameRowView.swift",
    "line": null,
    "description": "SaveLaunchNotice from the launch-path save plan is held on GameRowView's saveLaunchNotice @State but not yet rendered -- no detail view with a Save section exists yet in this codebase to surface it inline into (04-14)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T02:28:02.935Z",
    "resolved_at": null
  },
  {
    "id": 40,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/OnlyCopyEscalation.swift",
    "line": null,
    "description": "OnlyCopyEscalationPanel now has a real call site (ReadinessSheetView) gated by a real only-copy count, but the four unfixable escalation reasons (revokedAuth/capabilitySkew/serverRefusal/compatibilityRejection) still can't fire in production because SaveUploadLane has no wired failure-classification output yet (mac2 review WR-04) (04-16) FIXED by plan 04-19 (fb6ad53).",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T14:17:19.948Z",
    "resolved_at": "2026-09-05T22:18:48.497Z"
  },
  {
    "id": 41,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Readiness/ReadinessEngine.swift",
    "line": null,
    "description": "AppEnvironment.saveReadinessCase(for:) does not compute .serverHasNewer -- distinguishing it from .localOnlyReachable needs per-device session context this client doesn't track yet; every other SaveReadinessCase, including the higher-priority .twoVersions, is computed (04-16)",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T14:17:20.065Z",
    "resolved_at": null
  },
  {
    "id": 42,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Sync/Outbox.swift",
    "line": null,
    "description": "AppEnvironment now constructs a shared SaveOutbox/SaveConflictResolver so MC-06's resolution path durably records locally, but no drain trigger (OutboxDrainTrigger-equivalent) is wired for SaveOutbox yet -- a resolved fork is recorded locally but may not reach the server promptly (mac2 review WR-04's other half) (04-16) FIXED by plan 04-23 (5c6439c): SaveOutboxDrainTrigger wired to post-enqueue, reachability-regained, and scene-active triggers.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T14:17:20.181Z",
    "resolved_at": "2026-09-06T02:02:52.938Z"
  },
  {
    "id": 43,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Readiness/ReadinessSheetView.swift",
    "line": null,
    "description": "saveHistorySessions closure passed to ReadinessSheetView is still never populated from SaveStore (mac2 review CR-04's other half) -- SaveHistorySheet's 'Review versions...' path for a non-diverged line still renders its empty state; out of 04-16's declared MC-01..MC-06 scope FIXED by plan 04-22 (62e0db0): SaveHistorySessionBuilder wired to real SaveStore data at both ReadinessSheetView call sites.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T14:17:20.303Z",
    "resolved_at": "2026-09-06T02:02:53.019Z"
  },
  {
    "id": 44,
    "kind": "unmet-truth",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveUploadLane.swift",
    "line": null,
    "description": "SaveUploadLane (the dedicated actor draining locally-captured save revisions to the server, D-16/D-32) is never constructed anywhere in production -- discovered baselining 04-18's reachability sweep. A more severe superset of the already-tracked WINDOWS #40 (which only covered its missing failure-classification output): the WHOLE upload path is unwired, not just one output of it. See 04-18-SUMMARY.md. FIXED by plan 04-19 (fb6ad53).",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T21:54:18.525Z",
    "resolved_at": "2026-09-05T22:18:48.578Z"
  },
  {
    "id": 45,
    "kind": "unmet-truth",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveCapturePoller.swift",
    "line": null,
    "description": "SaveCapturePoller is constructed in production ONLY inside SaveSessionRecovery.replay, which itself has no production caller -- there is no live, in-play save-capture trigger anywhere in the shipped app. Discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md. FIXED by plan 04-19 (3691ec3).",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T21:54:18.602Z",
    "resolved_at": "2026-09-05T22:18:48.656Z"
  },
  {
    "id": 46,
    "kind": "unmet-truth",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveSessionRecovery.swift",
    "line": null,
    "description": "SaveSessionRecovery (crash-recovery replay actor, D-05/D-07) is never constructed in production despite its own doc comment saying app launch should call it once per known save line. Discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md. FIXED by plan 04-19 (3691ec3).",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T21:54:18.681Z",
    "resolved_at": "2026-09-05T22:18:48.732Z"
  },
  {
    "id": 47,
    "kind": "unmet-truth",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveRollup.swift",
    "line": null,
    "description": "SaveRollup.rollup(for:) (D-36's game-level save rollup string) has zero production callers -- not reachable from any shipped UI. Discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md. FIXED by plan 04-22 (24d5c34): AppEnvironment.saveRollupSummary(forAssetSetID:) is SaveRollup.rollup(for:)'s first production caller; reachability-allowlist.txt's SaveRollup line removed.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T21:54:18.760Z",
    "resolved_at": "2026-09-06T02:02:53.099Z"
  },
  {
    "id": 48,
    "kind": "unmet-truth",
    "phase": "04",
    "file": "playstead-mac/Playstead/Controller/ControllerRecoveryBanner.swift",
    "line": null,
    "description": "ControllerRecoveryBanner and ControllerTestView (Controller/) and FirstRunBanner and ShowAllSystemsControl (Library/) are fully built views with zero production call sites -- discovered baselining 04-18's reachability sweep. See 04-18-SUMMARY.md and scripts/ci/reachability-allowlist.txt for the full list and per-symbol detail.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T21:54:18.835Z",
    "resolved_at": null
  },
  {
    "id": 49,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveSessionCoordinator.swift",
    "line": null,
    "description": "A promoted capture's bytes are written to the save-captures directory and recorded as a SaveStore revision, but are NOT committed into the CAS. LaunchSaveContextBuilder derives head candidates' bytesLocal from casManager.contains(blobSHA256), so a locally-captured revision reads as bytes-not-local at the next launch and cannot be restored from history until it has round-tripped through the server. Same-device continuity still works (the live .sav artifact persists), so this is the mirror image of WINDOWS #38 rather than a break in SAVE-01's main path. Out of 04-19's declared scope (capture -> SaveStore -> upload). FIXED by plan 04-20 (10783c4 excludes save blobs from the reclaim menu; de3aec1 commits capture bytes into the CAS).",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T22:19:41.797Z",
    "resolved_at": "2026-09-05T23:05:00.000Z"
  },
  {
    "id": 50,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveCaptureBlockedState.swift",
    "line": null,
    "description": "AppEnvironment constructs SaveCaptureBlockedState with no SaveCaptureAlertSink and no SaveDirectUploadTransport, so a blocked capture (D-31) is recorded durably and is queryable, but raises no user-visible alert and has no direct-upload escape hatch in the shipped app. The seams exist and are injected; no attention surface consumes them yet.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T22:19:41.878Z",
    "resolved_at": null
  },
  {
    "id": 51,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveCapturePoller.swift",
    "line": null,
    "description": "The save-captures directory (<root>/save-captures/<assetSetID>/) has no reclamation path: each session leaves one session-<id>.staged.sav plus one <digest>.sav promoted blob, and QuotaManager measures paths.objects only, so capture blobs count against neither the quota nor the free-space floor and are never evicted. Save artifacts are small (tens of KB), so this is slow growth rather than an immediate hazard, but nothing bounds it.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-05T22:19:41.957Z",
    "resolved_at": null
  },
  {
    "id": 52,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveSessionRecovery.swift",
    "line": null,
    "description": "SaveSessionRecovery.replay (D-07 crash-recovery, reachable in production via AppEnvironment.recoverAbandonedSaveSessionsAtLaunch) records a promoted revision but holds no CASManager, so a crash-recovered capture's bytes never enter the CAS and LaunchSaveContextBuilder reports bytesLocal: false for it -- the same hole 04-20 closed on the live-session path (WINDOWS #49), reached via the second capture path. Out of 04-20's declared scope (files_modified named SaveSessionCoordinator only). Fix should extract 04-20's SaveSessionCoordinator.commitCaptureBytes into one shared spelling rather than a second copy. FIXED by plan 04-23 (fd4e2b4): extracted SaveCaptureBytesCommitter, shared by SaveSessionCoordinator and SaveSessionRecovery; recovery replay commits bytes into the CAS before inserting the row.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-05T22:36:11.098Z",
    "resolved_at": "2026-09-06T02:02:53.188Z"
  },
  {
    "id": 53,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/lib/playstead/export.ex",
    "line": null,
    "description": "Export.load_save_revisions/2 selects only the (battery, slot 0) save line when a content_key has more than one save line; any other lines are silently excluded from export rather than merged (v1 client only ever writes one line, so this is currently unreachable).",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-06T01:18:12.481Z",
    "resolved_at": null
  },
  {
    "id": 54,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Net/APIClient.swift",
    "line": null,
    "description": "The Mac client ships no pairing ceremony, so a human cannot pair a Mac at all. Every production PairingCredential construction is a keychain READ (KeychainStore.swift:152); the only writers are UI_TESTING-gated (UITestBootstrap.swift:552, DeterministicProfile.swift:49). There is no pairing view, no code-entry surface, no URL-scheme handler. The server side is complete -- POST /api/v1/device-pairing/requests, GET /requests/:id, POST /requests/:id/redeem, plus the devices approval console -- and the client calls none of it. APIClient.swift:57 states it outright ('this tracer plan does not yet ship the pairing ceremony') and anticipates a future plan writing AppPaths.root/pinned-ca.der; until then APIClient uses default TLS trust rather than the pinned root CA. LibraryShellView.swift:474's empty state instructs the user to 'Pair with your Playstead server to see your library', naming an action that exists nowhere in the app. 03.5-08 proved pairing only through scripts/ci/live-server.sh driving the HTTP ceremony from a shell script and handing a credential to a test build -- the human path was never built. Consequence: every human checkpoint requiring a paired Mac is unperformable, CP7-SAVE-C (UAT tests 107/112/120) included, which its 'blocked_by: physical-device' reason understates. Discovered 2026-09-08 when the owner tried to run CP7-SAVE-C and had no way to pair. Worked around for the owner's Mac only, by driving the production endpoints by hand and writing the server-issued credential into the login keychain (service dev.playstead.mac, device d23b584e); the missing UI itself is unfixed.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-08T17:38:16.547Z",
    "resolved_at": "2026-09-09T00:51:44.258Z"
  },
  {
    "id": 55,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveUploadLane.swift",
    "line": null,
    "description": "SHIPPED DEFECT: save uploads 404'd against every real server, so no captured revision has ever reached a server from the shipped app. APIClient.send does credential.baseURL.appendingPathComponent(path) and the paired credential's baseURL is the server ORIGIN, so callers own the whole path. SnapshotClient (/api/v1/snapshot) and ChangesClient (/api/v1/changes) spell it correctly; SaveUploadLane spelled 'saves/uploads/<id>' and 'saves/revisions' without the /api/v1 prefix, at both of its call sites. Server routes are PUT /api/v1/saves/uploads/:command_id and POST /api/v1/saves/revisions (router.ex:285,292). Observed on the owner's machine 2026-09-08: 'PUT /saves/uploads/... Sent 404' twice in the server log while POST /api/v1/play-sessions succeeded; two captured revisions sat at durability='queued' with 0 rows in the server's save_revisions. Capture, promotion and CAS commit all work -- only the upload half was broken. Fixed 2026-09-08 by prefixing both paths; guarded by scripts/ci/tests/api-path-prefix-test.sh, verified to reject the pre-fix source. WHY CI MISSED IT: SaveEndToEndTests guarded three fixture stages with bare 'guard try runFixture(...) else { return }', so a failing stage returned from the test having asserted nothing and XCTest recorded a pass -- the end-to-end proof of this exact path was fail-open. Seven such returns existed across SaveEndToEndTests and LiveServerSnapshotTests; all seven now XCTFail, and four-layer-topology-test.sh line 199 previously PINNED the fail-open shape as required. UAT tests 106 and 117 were marked pass on that evidence and are reverted to issue.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-08T17:49:43.371Z",
    "resolved_at": null
  },
  {
    "id": 56,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/lib/playstead_web/controllers/api/v1/saves_controller.ex",
    "line": null,
    "description": "SHIPPED DEFECT (server): the streamed save-upload route replied on the ORIGINAL conn instead of the one advanced by reading the body. Plug.Conn is immutable and body_stream/1 threads its own conn through Stream.resource, so create_upload/2 responded 200 on a conn that still believed the request body was unread. The connection was then left mid-body, and the NEXT request on it stalled until Bandit's 15s read timeout, surfacing as 'Bandit.HTTPError Read timeout' on the upload's request_id and a 502 EOF at Caddy for the following request. Because SaveUploadLane does PUT bytes then immediately POST /api/v1/saves/revisions on the same connection, the metadata commit ALWAYS died: bytes landed in save_pending_uploads, no revision ever committed, and the client's revision stayed durability='queued' forever. Size-dependent, which is why it was never noticed: reproduced deterministically at 4096 bytes -> next request 0.004s, 65536 -> 15.010s, 131072 -> 15.010s. Fixed 2026-09-08 by capturing the drained conn from body_stream/1 and replying on it; re-verified 0.0017s at both previously-broken sizes. Not reachable by ConnTest (Phoenix.ConnTest never opens a real connection), which is why the 2 existing saves_controller tests pass either way; the live-server e2e test WOULD have caught it had it not been fail-open (WINDOWS #55). Full server suite green after the fix: 1037 tests, 0 failures.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-08T18:22:12.727Z",
    "resolved_at": null
  },
  {
    "id": 57,
    "kind": "stub",
    "phase": "04",
    "file": "playstead-mac/Playstead/Saves/SaveSessionCoordinator.swift",
    "line": null,
    "description": "Capture provenance is never recorded: save_revision.adapter_id and adapter_version are empty on every revision produced by the real Play path, though both columns exist client-side and server-side and SaveUploadLane forwards them when present. Observed on the owner's machine 2026-09-08 across all three revisions captured through the shipped app (Advance Wars 65,536 bytes; Pokemon LeafGreen 131,072 x2) -- adapter_id '', adapter_version '', capture_method 'session'. Consequence: a stored revision cannot say which emulator or which pinned adapter version produced it, so a future adapter upgrade that changes save-format behaviour leaves no way to tell affected revisions from unaffected ones. CP7-SAVE-C's own how-to-verify asks the developer to record the adapter version alongside the observations, which the app itself cannot supply. Not blocking: capture, upload, restore and byte-identity are all proven correct without it. Low severity, but it is provenance for the one artifact class this phase exists to protect.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-08T18:34:29.543Z",
    "resolved_at": "2026-09-08T22:15:00.000Z"
  },
  {
    "id": 61,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/PlaysteadUITests/SaveEndToEndTests.swift",
    "line": null,
    "description": "SaveEndToEndTests failed once on hosted run 34281587417 (result file never written within 60s) and passed on 34285679092 with no change to any code the test exercises. The failure is intermittent and its cause is still unknown. Recorded as OPEN rather than closed by the green run: a test that fails one run in N is not fixed by the run where it passes. WINDOWS #60's failure channel and the 120s deadline mean the next occurrence will name which of the four harness steps (quiesce, pairing, upload, sync) failed, and whether it was slow or broken. Do not close without that evidence.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-08T23:50:00.000Z",
    "resolved_at": null
  },
  {
    "id": 60,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/UITesting/UITestBootstrap.swift",
    "line": null,
    "description": "CI GAP (fixed same day, 4903ec8): the save end-to-end harness swallowed every failure, on the stated grounds that the absent result file was itself the signal. It is a signal with no content -- a slow run and a broken upload both surface as 'result was never written within 60s'. Hosted run 34281587417 failed exactly this way one run after the test was promoted to required, and the evidence named only the polling assertion's file:line. Fixed by writing a sanitized reason to a sibling file and giving each known cause its own assertion site, since CI's evidence pipeline keeps file:line and discards messages. Deadline raised 60s -> 120s only alongside the diagnostic. Root cause of the underlying timeout still unknown -- the next occurrence will name it.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-08T22:40:00.000Z",
    "resolved_at": "2026-09-08T22:40:00.000Z"
  },
  {
    "id": 58,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/PlaysteadUITests/SaveEndToEndTests.swift",
    "line": null,
    "description": "CI GAP (fixed same day, cc7640d): the phase's only end-to-end save proof had never run on a hosted runner. SaveEndToEndTests resolved its fixture environment from ProcessInfo.processInfo.environment alone; the XCTest process inherits a PATH with no Elixir toolchain, so live-server.sh's 'mix playstead.mac_ci_fixture' died with 'mix: command not found' at provision-domain on every hosted run. The sibling LiveServerSnapshotTests already merged the runner-written live-server-runtime.json (which carries the real PATH) over the inherited environment and documented exactly this hazard; SaveEndToEndTests' doc comment claimed it reused that discipline 'verbatim' and did not. Invisible because the test was fail-open until e6316d5 and because live-server.sh discarded its own stderr until 266015e. Third seam-between-plans defect of this phase after #55 and #56. Guarded by scripts/ci/tests/live-server-env-resolution-test.sh, falsified against the real pre-fix source.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-08T20:58:11.764Z",
    "resolved_at": "2026-09-08T20:58:25.364Z"
  },
  {
    "id": 59,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/scripts/ci/run-mac-verification.sh",
    "line": null,
    "description": "CI GAP (fixed same day, 266015e): the hosted UI layer's 1800s deadline sat 9% above its own observed runtime (1631s in the last green run, 50960ba), and run 34264338508 was SIGTERMed at exactly 1800s on a commit whose diff touched no file in the UI test plan. Raised to 2700s with four-layer-topology-test.sh's pinned expectation and ceiling moved with it. Recorded because the failure mode is a gate that reports red for reasons unrelated to the code under test, which trains readers to discount it.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-08T20:58:18.772Z",
    "resolved_at": "2026-09-08T20:58:25.464Z"
  },
  {
    "id": 62,
    "kind": "unrun-verify",
    "phase": "04.5",
    "file": "playstead-mac/PlaysteadUITests/PairingCeremonyTests.swift",
    "line": null,
    "description": "PairingCeremonyTests (the live-server pairing ceremony proof) is registered in LiveServer.xctestplan and compiles, but has not been executed against a real hosted Phoenix+Postgres mac_ci server in this session; not yet promoted to run-mac-verification.sh's --required-test list per WINDOWS #58's lesson.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-09T00:53:41.765Z",
    "resolved_at": null
  },
  {
    "id": 63,
    "kind": "stub",
    "phase": "04.5",
    "file": "playstead-mac/Playstead/Pairing/PinnedCertificateCapture.swift",
    "line": null,
    "description": "PinnedCertificateCapture has no dedicated unit test — its real trust-anchor capture path only fires on a genuine TLS handshake, which StubURLProtocol never performs. Coverage is structural/code-review only until the live-server proof (window #62) is run.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-09T00:53:50.372Z",
    "resolved_at": null
  },
  {
    "id": 64,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-server/lib/playstead_web/live/library_live.ex",
    "line": null,
    "description": "list-view status_slot only passes queued fact, not the full device-reported status map status_for/2 now returns",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-11T04:31:57.324Z",
    "resolved_at": null
  },
  {
    "id": 65,
    "kind": "unrun-verify",
    "phase": "03",
    "file": "playstead-server/lib/playstead_web/live/library_live.ex",
    "line": null,
    "description": "03-13 backstop truths (500-entry stress toggle, download-in-progress uses existing indicator) not exercised by a dedicated automated fixture",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-11T04:31:57.416Z",
    "resolved_at": null
  },
  {
    "id": 66,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Cache/AvailabilityReporter.swift",
    "line": null,
    "description": "Live transfer percent falls back to 0 when no DownloadCoordinator exists yet at report time",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-11T05:01:21.768Z",
    "resolved_at": null
  },
  {
    "id": 67,
    "kind": "unmet-truth",
    "phase": "03",
    "file": "playstead-mac/PlaysteadUITests/SaveEndToEndTests.swift",
    "line": null,
    "description": "FLAKY: testOneSaveRoundTripsCaptureUploadAndJournalReturn failed intermittently (2 of 5 hosted runs, ~40%). RESOLVED 2026-09-12, green on main at c3f51e1 (run 34676636969, 6.36s). Do NOT delete the test: it is the only end-to-end proof of capture->upload->journal->sync and previously caught save uploads 404ing against every real server (e6316d5). ROOT CAUSE, found only on the THIRD diagnosis: a RACE, not a timeout and not a transient error. The running app drains save uploads on a reachability transition (PlaysteadApp reachability.onChange -> uploadLane.drainOnce()), which in CI fires right after pairing, exactly while the harness inserts its revision. The harness built its OWN second SaveUploadLane over the same store; drainOnce's doc comment promises 'the lane is an actor, so overlapping calls serialize' and that is true -- but two actor INSTANCES serialize nothing, so whichever lane lost saw an empty pending set and reported sent=0. Run 34663361104 named it exactly (upload-nothing-pending), which is what the three-way diagnostic split was added to distinguish. Fixed by sharing the app's own lane and asserting the OUTCOME (the revision reaches .uploaded) rather than one lane object's counter. TWO EARLIER DIAGNOSES WERE WRONG, kept deliberately rather than quietly swapped: (1) 'a fixed 120s poll deadline on the upload lane' -- wrong; that is the XCTest-side wait for the result file. (2) 'a single un-retried drainOnce pass' -- a real weakness, fixed and unit-pinned by SaveUploadLaneTests.test_oneTransientFailure_makesASinglePassSendNothing_thoughTheNextPassSucceeds (reproduces in under a second: one 503 makes a single pass report sent=0 while the very next pass succeeds), but NOT what was failing in CI. FOLLOW-ON DEFECT introduced by the fix itself and caught by run 34670123715 at save-e2e-harness=upload-server-refused: the retry predicate branched on `lastFailureClassification == .none`, but SaveUploadFailureClassification has seven cases and its own doc says three of them (.none, .offlineQueue, .slowUpload) are the product working correctly. .offlineQueue is the EXPECTED state at pairing time, so the most ordinary outcome was reported as an unfixable server refusal and the retry was skipped. Now gated on OnlyCopyEscalationReason(classification:) -- the same initializer the shipped escalation panel uses -- at all three decision points, and pinned by four falsification probes in four-layer-topology-test.sh.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-11T23:23:31.675Z",
    "resolved_at": "2026-09-12T14:57:49.371286Z"
  },
  {
    "id": 68,
    "kind": "fixme",
    "phase": "03",
    "file": "playstead-mac/scripts/ci/live-server.sh",
    "line": null,
    "description": "FALSE DIAGNOSTIC: the live-server stage marker is written on entering each stage and never cleared when a fixture action SUCCEEDS, so any later non-fixture failure inherits the last stage entered. Run 34648546919 reported FAILURE_STAGE redeem-pairing for a prepare that had completed fine; the real failure was in the save harness. This actively misdirected the first diagnosis. Clear the marker on successful completion of each action. RESOLVED 2026-09-12 (green on main at c3f51e1): the EXIT trap now calls clear_failure_stage on success, and live-server-stage-marker-test.sh sources the REAL validated_marker/write_failure_stage/clear_failure_stage out of live-server.sh rather than restating them, so the test cannot pass against a drifted copy. It also asserts the clearer is exactly as strict as the writer (a weaker clearer is an arbitrary-path rm) and that the shipped trap still contains the success branch.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-11T23:23:31.769Z",
    "resolved_at": "2026-09-12T14:57:49.371286Z"
  },
  {
    "id": 69,
    "kind": "fixme",
    "phase": "03.5",
    "file": ".github/workflows/verify-hosted-evidence.yml",
    "line": null,
    "description": "EVIDENCE BINDING IS REF-FRAGILE: the 'verify hosted evidence' workflow fails with 'complete evidence identity mismatch: head_sha' whenever the evidence-producing ci run's head_sha differs from the ref the verify workflow evaluates. TWO distinct triggers observed, so the root cause is broader than the first one recorded: (1) RE-RUN: 'gh run rerun --failed' on a ci run (run 34655069813 against ci run 34648546919). Re-running a flaky job is the standard mitigation, and it trips the evidence-identity guard, so the two controls are in conflict. (2) CROSS-REF, observed 2026-09-12: run 34677705521 on main (then at 363b10d) fired 8s after the BRANCH run 34676636969's mac job completed at 09612a9, and tried to bind that branch evidence to main's tip. Any branch run finishing while main sits at a different commit reproduces it. Decide which identity the manifest binds to across attempts AND across refs; a guard that fails on ordinary branch work is noise that will train people to ignore it.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-11T23:23:31.870Z",
    "resolved_at": null
  },
  {
    "id": 70,
    "kind": "unmet-truth",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadTests/CacheTests/StubURLProtocol.swift",
    "line": null,
    "description": "CROSS-TEST POLLUTION, unit layer: RelaunchTests.testPreviouslyLaunchableGameStillEvaluatesLaunchableAfterClosingAndReopeningEveryStoreWithNetworkStubbedToFail fails intermittently at RelaunchTests.swift:114, XCTAssertTrue(StubURLProtocol.requestLog.isEmpty). First observed on main, run 34700720746 (2026-09-12); the same tree passed on branch run 34676636969, so this is timing-dependent, not a regression (unit in_test was 120.8s on the red run vs 95.7s on the green one). NOT caused by the awaitExistence/clickWhenHittable work: the unit layer runs no XCUITest. MECHANISM: requestLog is a shared nonisolated(unsafe) static var. RelaunchTests DOES call StubURLProtocol.reset() in setUp, so the log is empty when the test starts -- which means a request arrived DURING the test. reset()'s inFlightGroup.wait() only covers a response already being DELIVERED; it does not cover a URLSessionTask from an earlier test that has not yet issued its request. So a lingering task appends into the next test's log. DO NOT apply the obvious fix: invalidating every session handed out by makeSession() inside reset() is NOT safe, because reset() is called mid-test, and in SaveConflictResolverTests setUp it runs at line 175 AFTER makeSession() at line 174 -- cancelling there would break that whole class. FilterTests:123 also calls reset() mid-test. A correct fix needs to identify the leaking test first, and the failing assertion does not capture the offending URLs; CI keeps file:line and discards assertion messages, so the next occurrence will be equally mute unless the request log is attached as evidence. Fix the diagnostic before guessing at the cause. OBSERVATION 2026-09-12, run 34707257044: PASSED. Expected for a timing-dependent flake and NOT evidence of a fix -- nothing was changed here. Still open. OBSERVATION 2026-09-14, hosted run 34903951673 (branch feat/dev-standup-and-uat7-closure, head 295038f): RED again at RelaunchTests.swift:114, the same XCTAssertTrue(StubURLProtocol.requestLog.isEmpty). This observation is unusually clean and worth keeping for that reason alone: the immediately preceding run (34901279944, head 6e4c1f8) passed the unit layer at 697 executed / 0 failures, and the ONLY difference between the two heads is .planning/WINDOWS.md -- 19 insertions and 5 deletions in a markdown file, not one byte of Swift. So the same compiled test code passed and then failed. That closes off \"regression\" as a reading of this entry without needing any timing argument, and it also rules out the session's AdapterProcessRegistry / StandInExecutable work (WINDOWS #85, #86) as a cause: that landed in 86c816c, two commits earlier, and the unit layer was green on it and on 6e4c1f8 before going red here with identical sources. TIMING, consistent with this entry's existing evidence that the flake is load-dependent: unit in_test_seconds was 144.4s on this red run against 115.1s on both preceding green ones, the same direction and rough magnitude as the 120.8s-versus-95.7s pair already recorded above. Note the red run is also the one whose live-server layer PASSED, so the runner was not uniformly slow -- the unit layer specifically ran ~25 percent longer. STILL UNDIAGNOSED, and this entry's own standing instruction still applies unchanged: the assertion captured no URLs, so this run says nothing new about WHICH test leaks the lingering task. Fix the diagnostic before guessing at the cause. ONE CONCRETE ROUTE, noted since this entry says the message channel is mute: run-mac-verification.sh does not only keep file:line. Alongside bounded_failure_diagnostic it scans assertion strings for allowlisted token patterns (ui_stage_pattern, live_stage_pattern, audit_pattern, around run-mac-verification.sh:762-771) and surfaces the matches. A bounded token of the same shape -- a fixed, allowlisted vocabulary naming the leaking suite rather than a URL -- would therefore survive the pipeline where a free-text message does not, and would not put a path or query string into published evidence. That is a candidate, not an applied fix. FIXED 2026-09-15, and the mechanism this entry inferred is now reproduced deterministically instead of being waited for. WHAT WAS DONE: makeSession() stamps each session it hands out with a unique X-Playstead-Stub-Session header and opens that stamp in a two-bucket ledger; startLoading drops any request whose stamp is missing or retired, failing it with URLError(.cancelled) BEFORE it can touch responder or requestLog; reset() rotates the buckets. The gate is deliberately fail-CLOSED -- an unstamped request is precisely the leak this entry records, and admitting it \"just in case\" is what kept the leak invisible. TWO BUCKETS, NOT ONE, and that is the whole difficulty this entry warned about: a session must survive one full reset() before being retired, because reset() is also called MID-test -- SaveConflictResolverTests calls makeSession() at setUp:174 and reset() at :175, and FilterTests resets at :123 partway through a test. Retiring every live session on reset() is the obvious fix this entry explicitly says NOT to apply, and it would have broken exactly those suites. After one full reset() cycle the owning test has ended by construction, so anything the session still emits is stale by definition. PROVEN, not inferred: StubURLProtocolIsolationTests asserts both halves. test_aSessionRetiredByTwoResetsCannotAppendToTheNextTestsLog issues a request on a session retired by two resets -- the boundary between a finished test and the next -- and requires both that it is not served and that requestLog stays empty. test_aSessionSurvivesTheMidTestResetItsOwnSuitePerforms issues one across a single mid-test reset and requires it to succeed, pinning the constraint that makes the naive fix wrong. Falsified by deleting only the rotate() call, which restores the pre-fix behaviour of never retiring a session: the first test then fails on BOTH its clauses -- the retired session's request is served (200, 7 bytes) and does appear in the next test's log -- while the second still passes, confirming the two tests are not testing the same thing. Restore verified byte-exact, not by git checkout. THIS ALSO ANSWERS THE ENTRY'S OWN STANDING INSTRUCTION, \"fix the diagnostic before guessing at the cause\", in a better way than it asked: rather than making the leaking suite identifiable after the fact, the leak can no longer reach another test's log at all, so there is nothing left to identify. The bounded-token route noted in the previous observation is therefore NOT needed and was not taken. EVIDENCE: the full Unit layer (ReleaseHookAbsenceTests skipped) ran 696 tests with 0 failures with the gate in place, which also demonstrates the stamp propagates through URLSessionConfiguration.httpAdditionalHeaders -- under a fail-closed gate, a stamp that did not reach the protocol would have failed every networked test loudly rather than silently no-opping. Note this entry's rate history: it fired on hosted runs 34700720746 and 34903951673 and passed in between, so a single green hosted run must NOT be read as confirmation; the falsification probe above is the load-bearing evidence, not the rate.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-12T16:22:57.379Z",
    "resolved_at": "2026-09-15T14:01:00.017Z"
  },
  {
    "id": 71,
    "kind": "unmet-truth",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadUITests/LiveServerSnapshotTests.swift",
    "line": null,
    "description": "OBSERVED FAILURE, live-server layer: testPairedFreshMirrorRendersSnapshotBeforeAnyBlobDownloadAndPersistsKeychainAcrossRelaunch failed at LiveServerSnapshotTests.swift:63 on main, run 34700720746 (2026-09-12). It cascaded: SaveEndToEndTests also shows failed with NO diagnostic of its own, and the fixture reported FAILURE_STAGE verify-evidence, both of which follow from the first test dying before its second snapshot -- all three share one Phoenix server, so the live-server layer reports one root failure as three. RULED OUT: the awaitExistence short-circuit racing ahead. The test ran 19.27s on the red run versus 13.22s when green, i.e. the 10s fallback wait burned in full and the row genuinely never appeared. The short-circuit demonstrably did not skip the wait. SUSPECTED: the click on the preceding line. exists is not isHittable, and XCUITest silently DROPS a click on a non-hittable element, so the failure surfaces later at whatever the click should have produced -- pointing at the wrong line. Before awaitExistence, the existence check above each click took ~1s and incidentally gave layout time; that pause was never the contract and depending on it was always a race that simply had not been observed. MITIGATION APPLIED: clickWhenHittable() at all 37 click sites, waiting on exists AND isHittable and forwarding file/line so a stuck click reports at the caller rather than making every one look like the same defect. NOT YET CONFIRMED: one observation, and the suspected mechanism is inferred rather than proven. Keep open until a run reproduces green with the mitigation, and do not close it merely because the next run passes -- this failed 1 of 3 runs. OBSERVATION 2026-09-12, run 34707257044 with clickWhenHittable: GREEN, 12.92s (the failing run was 19.27s, the earlier green one 13.22s), and SaveEndToEndTests passed at 6.20s. STAYS OPEN per this entry's own rule: that is ONE green run against a failure seen 1 in 3, so it is consistent with the fix and equally consistent with the flake simply not firing. The mechanism remains inferred. Close only after several consecutive green live-server runs, and note that the runner's own variance is wide -- the unit layer measured 95.7s, 120.8s and 150.7s across these three runs, which is the same variance that surfaced #70. RED AGAIN 2026-09-14, run 34898512311 (branch feat/dev-standup-and-uat7-closure, head 86c816c), WITH clickWhenHittable in place: same test, same LiveServerSnapshotTests.swift:63, and SaveEndToEndTests again failed carrying no diagnostic of its own. THE MITIGATION IS THEREFORE NOT SUFFICIENT. This entry's closing rule -- \"close only after several consecutive green live-server runs\" -- had very nearly been met: three consecutive greens (34707257044, 34876938531 addf7ea, 34893420944 5afc6b0) preceded this red. A three-green streak is not evidence of a fix for a failure seen roughly 1 in 4; do not close this on a streak alone, only on a proven mechanism. NOT CAUSED BY 86c816c: that commit changed no file under PlaysteadUITests, touched exactly one production file (AdapterHost.swift, additive injectable defaults), and the unit layer it targets passed this same hosted run at 697 executed tests and 0 failures. TIMING, corroborating this entry's earlier reasoning: the two reds ran 19.27s and 18.12s; the three greens ran 13.22s, 11.75s and 14.15s. Reds and greens do not overlap, which is again consistent with the 10s awaitExistence at :63 burning in full, and again rules out the short-circuit skipping the wait. THE SUSPECTED MECHANISM IS NOW IN DOUBT. The fixture's verify-evidence stage died on its catalogue_entries titles check -- \"fresh mirror does not contain exactly both synthetic sentinels\" -- and that check reads client-mirror SQLite state only. It depends on no click at all: a dropped, non-hittable click leaves the mirror correct and fails only the UI assertion. So the dropped-click hypothesis does not explain the fixture failure, and this entry's earlier reading of that failure as a mere cascade off \":63 dying before its second snapshot\" is not supported -- SaveEndToEndTests makes its OWN both-sentinels assertion via runFixture(\"verify\", [\"snapshots-not-asserted-here\"]) at SaveEndToEndTests.swift:135, so its failure need not be a cascade either. WHAT THE LOG CANNOT SAY, and why: exactly ONE verify-evidence invocation ran in the entire layer and it failed, yet TWO tests call that stage, and the message named neither the observed titles nor which caller produced it. So it is not even determinable from this run whether the mirror was MISSING a sentinel or carrying an EXTRA row -- two findings with opposite causes. Separately, the evidence pipeline recorded failure_diagnostic_count=1 for two failed tests, and the one it kept is the :63 XCTAssertTrue; a fixture failure also fires an XCTAssertEqual through recordFixtureFailure, so at least one diagnostic did not survive into the evidence. That drop is an open sub-question of this entry, not yet investigated. FIXED THIS SESSION, the diagnostic only: live-server.sh's verify-evidence now gathers every observed fact BEFORE the first check and attaches all of them to whichever check fails -- root, snapshots observed/expected, blob_requests, cursor presence, the observed title set, and the missing/unexpected set difference. The snapshots field also identifies the caller for free, since LiveServerSnapshotTests passes \"2\" and SaveEndToEndTests passes snapshots-not-asserted-here. Verified by running the extracted block against synthetic mirrors for both ambiguous cases: a missing sentinel reports missing=['Playstead CI Sentinel Two'] unexpected=[], an extra row reports missing=[] unexpected=['Some Other Game'], and the passing case prints the unchanged success line. This does not fix the flake; it makes the NEXT red run diagnostic instead of ambiguous, which is the only way this entry advances given it reproduces only on hosted CI. FORMAT CORRECTION, same session: that diagnostic ships as ONE FACT PER LINE on stderr, not as the single bracketed line first written, because run-mac-verification.sh's own diagnostic printer truncates every line to 200 characters -- and the one-line form measured about 215, losing its tail at exactly the missing/unexpected difference that is the whole point. The same printer rewrites absolute paths to <path> and any 32-or-more character run to <token>, so the caller is identified by its snapshot expectation (caller=asserts-2-snapshots versus caller=snapshots-not-asserted-here) rather than by the run root, whose UUID would have been scrubbed to <token> anyway. Verified by replaying the shipped stderr through a replica of that printer's own regexes: all eight lines survive intact in both the missing-sentinel and extra-row cases, and the passing path still prints its unchanged success line. Filed here because this is the second time in two sessions that the evidence pipeline, not the test, decided what a future reader would be able to conclude. SUB-QUESTION PARTLY INVESTIGATED, same session, with one of its own answers retracted. ESTABLISHED: the fixture's raw stderr does NOT travel through the Swift caller's pipes at all. live-server.sh re-executes fd 2 onto a shared, runner-owned, append-only file at $PLAYSTEAD_LIVE_SERVER_STAGE_ROOT/live-server-fixture-diagnostic (mode 0600, torn down with the native root, never uploaded), and run-mac-verification.sh prints a sanitized bounded tail of THAT file. Consequence, and it is the one that matters for the new diagnostic: the caller= line will reach the job log no matter which of the two tests triggers the failure, including SaveEndToEndTests, whose runFixture sends both standardOutput and standardError to FileHandle.nullDevice. ASYMMETRY WORTH FIXING SEPARATELY, not fixed here: LiveServerSnapshotTests and PairingCeremonyTests both funnel a failed fixture through recordFixtureFailure, which emits one distinct XCTAssertEqual per allowlisted stage so the stage survives as a file:line; SaveEndToEndTests.runFixture instead returns a bare false, so its callers can only say XCTFail(\"live fixture stage 'verify' failed\") and the stage is lost. That is the exact failure mode SaveEndToEndTests' own recordSaveHarnessFailure doc comment argues against, twenty lines above. Left alone deliberately: the live-server layer cannot be run locally, it is the flakiest and most expensive layer, and the fixture-side caller= line already removes the ambiguity that motivated it -- so this is a candidate, not a pending fix. RETRACTED: the guess that SaveEndToEndTests' fixture failure is invisible to the evidence pipeline because XCTFail carries no capturable diagnostic. It is wrong -- run-mac-verification.sh's assertion_pattern explicitly lists XCTFail alongside the XCTAssert family, and XCTFail does record a file and line. So failure_diagnostic_count=1 against two failed tests REMAINS UNEXPLAINED, and no inference should be drawn from it about which caller's verify-evidence was the one that ran. Do not use that count to attribute the titles failure to either test; the next red run's caller= line is what will say. GREEN 2026-09-15, run 34982994861, head 17a7c189 (branch feat/dev-standup-and-uat7-closure): the macOS four-layer job passed in full, 24m20s, so the live-server layer including this test and SaveEndToEndTests was green. THIS ENTRY STAYS OPEN, by its own rule and for the same reason as last time: this is one green after the 2026-09-14 red at run 34898512311, and a failure seen roughly 1 in 4 is not disproved by a single pass. The previous three-green streak was followed by a red, which is exactly why the rule says close only on a proven mechanism, never on a streak. NOTHING NEW WAS LEARNED ABOUT THE MECHANISM from this run, and that is the point worth recording rather than the pass itself: the improved fixture diagnostic committed at 6e4c1f8 -- one fact per line to a runner-owned stderr file, with a `caller=` marker distinguishing the two callers and every line kept under the 200-char truncation -- only emits on a RED run. It has therefore still never been exercised against the failure it was built for. The next red is the one that matters; this green only confirms the diagnostic costs nothing when the test passes.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-12T16:23:11.283Z",
    "resolved_at": null
  },
  {
    "id": 72,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Library/LibraryShellView.swift",
    "line": 424,
    "description": "Library card availability badge is a hardcoded literal: ShelfView is handed statuses: [.serverOnly] for EVERY entry, so every card in the grid layout shows the 'On server' cloud glyph regardless of what is cached or pinned, and its accessible name reads '<title> is on your server. Choose Download to play it offline.' over content that is downloaded and playable. Found while trying to identify a 'Ready offline' title for UAT item 7 Run A: 7 objects / 38076928 bytes sit in the CAS (including the 16 MiB Max Payne Advance ROM played on 2026-09-13) and no card can say so. The trustworthy signal today is GameRowView's action button (Play vs Download), which derives from the real six-check ReadinessReport via PreflightChecker(cas:). FIXED on branch feat/dev-standup-and-uat7-closure: AppEnvironment.libraryStatuses(for:) is the production construction site, and both the grid and the Favorites shelf now use it. Gated at the front door by StorageInteractionTests.testCachedAndPinnedGameIsNotDescribedAsBeingOnTheServer, which fails with the exact pre-fix sentence when the literal is restored.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T13:52:01.120Z",
    "resolved_at": "2026-09-13T19:57:04.120Z"
  },
  {
    "id": 73,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Cache/AvailabilityState.swift",
    "line": 82,
    "description": "AvailabilityState.derive/AvailabilityInputs have ZERO production construction sites - grep for 'AvailabilityInputs(' outside PlaysteadTests returns nothing. The six-state D-21 derivation whose own doc comment calls it 'the read-time derivation every card, list row, and rebuild-from-disk test calls' is called by no card and no list row in the shipped app, so that doc comment is currently false. Directly enables the hardcoded .serverOnly badge recorded alongside this. FIXED on branch feat/dev-standup-and-uat7-closure: AvailabilityInputs is now built by AppEnvironment.availabilityInputs(for:) from CASManager/DownloadQueue/PinStore, and LibraryStatusWiringTests drives every state through the real stores rather than constructing inputs by hand.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T13:52:01.225Z",
    "resolved_at": "2026-09-13T19:57:04.257Z"
  },
  {
    "id": 74,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Library/GameListView.swift",
    "line": 43,
    "description": "GameListView has ZERO production call sites - LibraryShellView's .list layout renders GameRowView instead. GameListView is the only place LibraryStatus.listViewLabel renders, so the strings 'Ready offline', 'On server', 'Downloading - n%', 'Pinned' and 'Queued' never appear anywhere in the shipped app, and the 03-UI-SPEC QUAL-01 guarantee that list view pairs a text label with the glyph (never colour/glyph alone) is unreachable in production. LibraryContractSnapshotTests asserts it against the unreachable view. FIXED IN PART on branch feat/dev-standup-and-uat7-closure: the QUAL-01 guarantee is now satisfied where it is actually rendered -- GameRowView (the row the shipped list layout uses) pairs the status glyph with its listViewLabel text, identifier library.status-label, asserted by the front-door UI test above. What is NOT fixed is GameListView itself and the sortable list it implements, which still has no production call site; that residue is tracked as its own window rather than left inside this one.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T13:52:01.336Z",
    "resolved_at": "2026-09-13T19:57:04.396Z"
  },
  {
    "id": 75,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/App/PlaysteadApp.swift",
    "line": 1344,
    "description": "The real download path never writes cache_objects. AppEnvironment.attemptDownload(for:) - documented as 'the whole of what GameRowView's Download button does' - calls engine.download directly; the INSERT INTO cache_objects lives only in DownloadCoordinator.handleSuccess (the queue path the button does not use) and is itself a swallowed try?. Evidence on the owner's machine after a successful real download-and-play: cache_objects has 0 rows while objects/ holds 7 files / 38076928 bytes and verify-index.json has all 7. Three consequences, each independently checkable: (1) QuotaManager.usedBytes() sums cache_objects (QuotaManager.swift:67), so the capacity gate attemptDownload runs before every download always sees 0 bytes used and can never block anything - reinstating the unbounded-cache regression GameRowView.swift:308's comment says was fixed - and QuotaSettingsView renders '0 bytes used' over 36 MB on disk; (2) EvictionPlanner enumerates cache_objects, so downloaded content is invisible to the storage view and can never be reclaimed; (3) the pins table holds Pokemon - Version Vert Feuille with no cache_objects row for its required member, the pinned-but-not-cached combination AvailabilityInputs documents as a caller defect. FIXED on branch feat/dev-standup-and-uat7-closure: CASManager records every commit through CacheObjectStore and forgets on removal, so the row no longer depends on which caller performed the download. StorageShellWiringTests' fixture no longer inserts the row by hand, so 15 of its 15 tests fail if commit stops recording.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T13:52:01.445Z",
    "resolved_at": "2026-09-13T19:57:27.814Z"
  },
  {
    "id": 76,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Adapter/AdapterPin.json",
    "line": 1,
    "description": "A normal user quit of the emulator classifies as unknown, not clean. PROVEN BY HAND 2026-09-13: the owner pressed Cmd-Q inside mGBA and GameRowView rendered 'Last exit: unknown(status: 0, reason: \"exit\")'. AdapterPin.json's exit_detection maps clean to terminationStatus 15 / uncaughtSignal - i.e. SIGTERM, which is what the APP sends via Process.terminate() - and the pin's own note says mGBA treats SIGTERM exactly like a crash with no graceful save-and-quit. So no signature covers status 0 / exit, and the single most common real exit falls through to AdapterExit.unknown. Two consequences: 'clean' currently means 'we asked it to stop' rather than 'the user quit normally', and any future save-recovery messaging keyed on .clean will never see a normal quit. The automated suites cannot detect this - AdapterExitTests classify against the same pin table they assert, so the tautology is green. Fix requires an owner decision on the pin (add a status 0 / exit signature as clean, and decide what 15/uncaughtSignal should then be called) rather than a code change alone, since the pin is the frozen contract. FIXED 2026-09-14 on branch feat/dev-standup-and-uat7-closure, with the owner's explicit authorization for option A. exit_detection categories now hold a LIST of signatures (decoding still accepts the old single-object shape, so no pin or fixture had to be migrated): clean = [0/exit], crash = [11/uncaughtSignal], killed = [9/uncaughtSignal, 15/uncaughtSignal]. 15 joins killed rather than staying clean because AdapterHost.terminateAll() sends SIGTERM and escalates to SIGKILL after a 2s grace period -- both are Playstead ending the process, and the pin's own note records that mGBA dies to SIGTERM exactly like a crash, so save-recovery messaging keyed on .clean must never fire for it. Gated by AdapterExitBoundaryTests, which breaks the tautology in both directions: it classifies against AdapterPin.load() (the pin that actually ships, never a literal beside the assertion) and it takes its termination signatures from REAL child processes over {0,9,11,15} x {exit,uncaughtSignal}. Falsified by restoring the old mapping: the boundary suite fails 6 assertions with 'normal exit produced status 0 / exit, which the shipped pin does not describe', while the old fixture-based AdapterPinTests stays 5/5 green -- a direct demonstration of why this survived phase 03. The 03-UAT item 7 exit-classification record is amended (not rewritten) to mark its observations as history and state what the same commands produce now.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T14:06:28.567Z",
    "resolved_at": "2026-09-14T15:56:42.808Z"
  },
  {
    "id": 77,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/App/PlaysteadApp.swift",
    "line": 633,
    "description": "The curation outbox has no time-based drain trigger, so a server coming back does not drain it. The three wired triggers are post-enqueue, reachability-regained and scene-active. Reachability wraps NWPathMonitor, which reports interface reachability - so a server that goes away and returns without the Mac's network changing (a stopped container, a restarted service, a server reboot) never fires trigger 2, and a pending entry waits for the user to focus the window or for an unrelated enqueue. Observed 2026-09-13: two play_session_record entries queued at 14:02:26Z (attempt_count 2) and 14:03:11Z while caddy was stopped, still queued with delivered=0 several minutes after caddy was answering /healthz again; Outbox.retryDelay(forAttempt: 2) is only 20s, so they were eligible and simply never triggered. CORRECTION to an earlier version of this entry: the two availability_report entries from 2026-09-13T02:25:41Z and 03:07:38Z are NOT stuck and are NOT corroborating evidence - they are state='rejected' with last_error_code='not_found', the terminal casualties of the PUT /api/v1/devices/me/availability route skew against a stale server image, excluded from listPending by design and surfaced in RejectedIntentsView. They read attempt_count 0 only because markRejected does not increment it, which is itself mildly misleading in the table. The real evidence is narrower and still stands: B2CFB20D is state='pending' with next_retry_at 2026-09-13T14:03:31Z (long past) and 9B2AB144 is state='pending' with next_retry_at NULL, so both are eligible under listPending, the server has been answering /healthz for many minutes, and neither has been sent. Nothing has fired a drain since caddy returned. Note also that in-app sidebar navigation did not drain them, so trigger 3 needs a real scene-activation transition, not mere interaction. FIXED on branch feat/dev-standup-and-uat7-closure: added drain trigger 4/4, OutboxDrainTicker -- a plain 60s periodic tick that fires exactly what the reachability edge fires (curation outbox, save outbox, save upload lane). Deliberately a tick rather than a server-reachability probe: a probe guesses when the server came back, while the per-entry backoff already decides eligibility, so a pass with nothing due costs one indexed query and sends nothing. Gated by OutboxDrainTickerTests, including 'an eligible pending entry is delivered with no trigger at all' and 'a server that comes back is noticed without any network change' (a 503, then a wound-back next_retry_at, then a 200, with reachability untouched). Both fail when ticker.start() is removed.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T14:06:28.685Z",
    "resolved_at": "2026-09-13T20:03:38.188Z"
  },
  {
    "id": 78,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Cache/LaunchMaterializer.swift",
    "line": 44,
    "description": "Every launch silently destroys the player's emulator save states. LaunchMaterializer.materialize does removeItem(at: directory) on the whole launch/<asset_set_id>/ directory before recreating it, and its doc comment frames this as correct ('the launch directory is always rebuilt from the CAS, the verified source of truth'). That reasoning holds for cache-derived files and fails for emulator-AUTHORED ones. Battery saves are safe because the launch command relocates them via -C savegamePath to saves/<asset_set_id>/, but nothing relocates mGBA save states (.ss1-.ss9), screenshots or cheat files -- mGBA writes those next to the ROM, i.e. inside the directory that is deleted on the next Play. PROVEN 2026-09-13: launch/1687f207-.../Advance Wars (USA) (v1.1).ss1, 55487 bytes, written 14:03:07Z, was gone after the next launch of the same title at about 14:21; the only surviving .ss1 on the machine belongs to Max Payne Advance, the one title not relaunched since. The owner hit the user-visible symptom first - mGBA's Load State list was empty for a game they had state-saved twenty minutes earlier. Save states are a first-class feature of every emulator and a player will reasonably expect them to persist; this is silent data loss on the happy path, not an edge case. Fix needs a decision on where emulator-authored artifacts live (a sibling per-asset-set directory that is NOT rebuilt, alongside saves/), plus whether existing states can be rescued. Catchable cheaply in CI: materialize, write a sentinel file the members list does not name, materialize again, assert the sentinel survives - see SEED-034. FIXED on branch feat/dev-standup-and-uat7-closure: materialize removes only the names its own sidecar manifest records, so emulator-authored files survive. Gated by MaterializationTests.testMaterializeTwiceLeavesAnEmulatorAuthoredFileUntouched, falsified against the restored whole-directory wipe.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T14:23:16.274Z",
    "resolved_at": "2026-09-13T19:08:42.822Z"
  },
  {
    "id": 79,
    "kind": "stub",
    "phase": "03",
    "file": "playstead-mac/Playstead/Library/GameListView.swift",
    "line": 19,
    "description": "The sortable list view has no production call site. Residue of WINDOWS #74, split out when that window's QUAL-01 half was fixed: GameRowView now renders the status label+glyph pair the shipped list layout was missing, but GameListView itself -- the 'sortable alternative to ShelfView' whose own doc comment promises sorting by title, system and date added, and the only renderer of GameListRow/LibrarySortOption -- is still constructed nowhere in Playstead/. The shipped list layout renders GameRowView, which offers no sort control at all, so the sortable list described in 03-UI-SPEC is an unreachable feature rather than a false claim. AccessibilityAuditTests.testGameListRowAccessibleNameContainsTitleSystemAndStatusSentence asserts a composition rule against that unreachable view (and asserts it by rebuilding the string locally rather than calling the view, so it would pass even if the view were deleted). Decide deliberately: wire a sort control into the shipped list layout, or delete GameListView and its sort enum and drop the spec clause. FIXED IN PART 2026-09-14 on branch feat/dev-standup-and-uat7-closure: the shipped list layout now offers a sort control (Title / System, one button per option beside Cards/List, same playsteadFocusable path) and catalogueList orders its entries through LibrarySortOption.sortedEntries, so the sortable list the spec promises is reachable for the first time. Ordering is case- and diacritic-insensitive with an id tie-break, so equal keys cannot reshuffle between two evaluations of the same body. Gated by LibrarySortTests (the orderings) and CurationInteractionTests.testListLayoutOffersAWorkingSortControl (the control exists where a user can press it, pressing it changes state, and rows come out in that order); removing the control fails the latter with 'the list layout offers no way to sort'. WHAT IS NOT DONE, and needs an owner decision: 'date added' is not offered, because nothing local knows it -- CatalogueEntry has no date and the server's catalogue payload has a FROZEN key set exposing updated_at but no inserted_at. Making it real is a protocol change (one frozen key plus a client field); the alternative is amending 03-UI-SPEC to promise two sort options rather than three. GameListView itself is still unrendered: it is now the only thing that renders GameListRow, and AccessibilityAuditTests asserts a composition rule against it by rebuilding the string locally, so that test would pass even if the view were deleted. Deleting it is the likely right answer once the date-added question is settled. DATE-ADDED QUESTION DECIDED 2026-09-14, owner-authorized, and the dead code it was keeping alive is gone. The decision is NOT to add the ordering: the server's catalogue payload key set is frozen with a test asserting no accidental additions, it exposes updated_at and no inserted_at, and CatalogueEntry carries no date at all, so offering the control would mean inventing an order or fabricating a date. Adding it later is a protocol change -- one frozen key, one client field -- and should be planned as one. Recorded in 03-UI-SPEC under a new List sort order section, which now promises exactly two orderings (Title and System, each with a total tie-break on asset-set id) and states plainly that plans 03-05 and 03-06 both described a third that was never buildable against this protocol. The plans themselves were NOT rewritten; they are historical records. DELETED as a consequence: the LibrarySortOption.dateAdded case, whose sortedEntries branch returned its input unchanged and which selectable excluded -- an unreachable branch kept alive by a comment explaining why it was unreachable; the GameListView struct and GameListRow struct, which nothing had instantiated since the shell took over rendering; and AccessibilityAuditTests/testGameListRowAccessibleNameContainsTitleSystemAndStatusSentence, which was VACUOUS -- it interpolated a label string itself and then asserted that string contained the pieces it had just interpolated, calling no production code. The live list row's status text is covered for real from the front door by StorageInteractionTests reading library.status-label. LibrarySortOption and NoMatchesView were moved into their own files since the file they shared was named for a view that no longer exists. Both stale reachability-allowlist entries were removed: GameListView because it is deleted, NoMatchesView because it is now rendered (WINDOWS #83) -- that entry had said a future plan should either wire it in or delete the file, and both halves are now done. The test that pinned selectable against a hardcoded list was replaced with selectable == allCases, which is strictly stronger: it fails if a case is ever added without being offered, which is the mistake that produced this window.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T19:58:01.645Z",
    "resolved_at": "2026-09-14T16:07:34.858Z"
  },
  {
    "id": 80,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Sync/SyncEngine.swift",
    "line": 105,
    "description": "A foreign or re-signed sync cursor strands the client in 'offline' permanently, with no self-healing path. Verified end to end 2026-09-13 by reading both sides: sync_cursor is a single id=1 row with NO server or device identity column (Migrations.swift:57, CursorStore.swift), nothing clears it when pairing changes (PairingCoordinator writes the credential and the pin, and never touches it), and the server's cursor is HMAC-signed with a secret derived from its own secret_key_base (playstead-server/lib/playstead/sync/cursor.ex:55). Playstead.Sync.changes_after/2 therefore returns {:error, :cursor_invalid} for a 'malformed/tampered/FOREIGN' cursor (sync.ex:21, its own wording) which ErrorCodes maps to 400 cursor_invalid. ChangesClient maps that to SyncError.cursorInvalid -- and SyncEngine.syncNow() catches ONLY SyncError.cursorExpired, so cursorInvalid falls into the generic catch, sets state = .offline(since:), leaves the stored cursor byte-identical, and every later pass re-sends the same rejected cursor. The app reports 'offline since <date>' forever while the server is up and answering. The remedy already exists and is correct (forceFullResync / bootstrapFromSnapshot, which replaces the whole catalogue and curation mirror and stores a cursor this server actually issued) but its only caller is OutboxWorker.onDestructiveRejection -- no UI surfaces it, and nothing calls it on an invalid cursor. Two realistic triggers, neither exotic: re-pairing this Mac to a different server, and the same server coming back with a regenerated SECRET_KEY_BASE (a .env rebuild), which invalidates every cursor it ever issued. FIXED on branch feat/dev-standup-and-uat7-closure: syncNow() now treats cursorInvalid exactly as it treats cursorExpired -- handleCursorExpired is renamed handleUnusableCursor and both codes route to it, since a cursor is opaque and positionally meaningless to this client and there is nothing to salvage from one the server refuses. Gated by SyncEngineTests.testCursorInvalidAlsoResetsToFreshSnapshotRatherThanStrandingTheClientOffline, which asserts the rejected cursor is replaced by one this server issued, the other server's mirror is replaced rather than merged, and the app does not read as offline; 3 assertions fail when the catch arm is removed. NOT addressed: forceFullResync() still has no UI entry point, and sync_cursor still carries no server identity column -- the fix makes the dead end recoverable rather than making the cursor self-describing.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T20:04:59.401Z",
    "resolved_at": "2026-09-13T20:07:13.173Z"
  },
  {
    "id": 81,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/test/playstead_web/browser/library_restream_test.exs",
    "line": 111,
    "description": "PlaysteadWeb.Browser.LibraryRestreamTest is flaky at roughly 1 run in 4, observed repeatedly across full-suite runs on 2026-09-12/13 (and NOT failing in the 1079-test run at the end of that session, which is what a 25 percent rate looks like). Recorded now because it had never been written down, so every green run was being read as evidence. It is a Wallaby feature that samples requestAnimationFrame frames during a filter-chip re-stream of a 500-entry library and asserts, in addition to 'never empty' and 'never busy', that Enum.max(counts) == @library_size and Enum.min(counts) == @ready_offline_size -- exact endpoint counts over a sampled transition, which is the part most likely to be timing-dependent (a sampler that starts a frame late, or a re-stream that settles before the first sample, sees only one endpoint). NOT diagnosed: which assertion actually fails on a bad run was never captured, so the mechanism above is a hypothesis, not a finding. Next step is to capture the failure output (run the feature in a loop until it fails, keeping the message) before changing anything -- the non-vacuity assertions are load-bearing and must not be weakened just to make the test quiet. EVIDENCE 2026-09-14: ran the feature file alone 20 times in a row -- 20/20 green (mix test test/playstead_web/browser/library_restream_test.exs). So it does NOT reproduce in isolation, and the ~25 percent rate observed earlier was under the full suite (max_cases 36, 1079 tests), which points at load/concurrency -- a sampler starved of frames while 36 async cases share the machine and one browser -- rather than at the feature's own logic. The next step is therefore unchanged but better aimed: capture the failure from a FULL-SUITE run (keep the message, not just the exit code), because looping the file alone will not produce one. RATE CORRECTED 2026-09-14, and the earlier '1 in 4' claim does NOT hold up: 28 consecutive green runs -- 20 of the feature file alone, 6 of the FULL suite (max_cases 36, the configuration the flake was attributed to), and 2 hosted `mix precommit` runs on PR #5. At a true 25 percent rate the probability of that is under one in a thousand. Two honest readings, and this window should not be closed until one is chosen: either the original observation was mis-attributed (the failing runs happened while several xcodebuild Mac test runs were saturating the same machine, which is a condition none of these 28 runs reproduced), or the trigger is rarer and more specific than 'under load'. What is now established is that looping it -- alone or as a full suite, on an otherwise idle machine -- will not produce a failure, so the next attempt must either recreate the contention (run the Mac four-layer verification concurrently) or capture the message from a real CI failure when one next occurs. Do not weaken the feature's non-vacuity assertions in the meantime. CONTENTION EXPERIMENT RUN 2026-09-14, which is the step this window has been asking for, and it changes the picture. Six full-suite runs (mix test, max_cases 36, 1079 tests) were executed while a loop of xcodebuild Mac test runs saturated the same machine. LibraryRestreamTest passed all six. That brings it to 33 consecutive green runs across every configuration this window ever blamed -- 20 of the feature file alone, 6 full-suite idle, 2 hosted mix precommit, and now 5 full-suite UNDER the contention that was the last untested hypothesis. Caveat recorded honestly: runs 3 and 4 were partially degraded because a compile error of mine made the load generator fail fast for roughly two minutes instead of building, so treat runs 1, 2, 5 and 6 as the fully contended ones. The experiment DID produce a failure on run 5 -- but in PlaysteadWeb.Plugs.ThrottleTest, not here. That failure is diagnosed, proven and fixed as WINDOWS #84: the throttle test's per-test IP formula dropped bits 8..15 of its unique integer, collapsing 2000 concurrent draws onto 72 distinct addresses, so tests shared per-IP rate-limit buckets whose window outlives the suite. That is a real load-sensitive full-suite flake in the same suite, with the same symptom shape (a green-most-of-the-time full run), and it is now the most likely explanation for what was originally attributed HERE without the failing message ever being captured. This window's first reading -- mis-attribution -- is therefore the one the evidence supports. It stays open only because the original observation's message was never recorded and so cannot be matched against #84's; nothing further should be done to LibraryRestreamTest, and its non-vacuity assertions must not be touched. Close this if #84's fix is followed by no further full-suite flakes, or reopen the investigation with a captured message if one occurs. MESSAGE CAPTURED 2026-09-14, and it CORRECTS the update immediately above this one. That update said mis-attribution was the reading the evidence supported. That was wrong, or at best half right: a second contention run -- this one on a machine cleared of 12 orphaned CPU-burner processes that a different project's session had leaked, which had been confounding the earlier measurements -- produced a real failure in THIS test, with exactly the mechanism the original hypothesis named. The message: \"assert Enum.min(counts) == @ready_offline_size / left: 500 / right: 50\" at library_restream_test.exs:143. Every sample was stuck at the pre-toggle count of 500, so the requestAnimationFrame sampler never recorded a single frame at the narrowed state. DIAGNOSIS, now a finding rather than a hypothesis: await_count polls the DOM directly via its own js call, so it returns as soon as the stream shows 50 rows. On an idle machine rAF fires every ~16ms and the sampler is certain to catch that state; under load Chrome throttles rAF hard, and read_samples can stop the sampler in a gap before any frame at the settled count was recorded. The failure is therefore a STARVED SAMPLER, not the empty-frame blink the feature exists to catch -- and the assertion message actively misleads about that, which is why this window sat open for days. FIXED by waiting on the sampler rather than the DOM: a new await_sampled_count helper blocks until window.__psSamples contains a frame at the expected count before read_samples runs, on both the narrowing and the widening feature. This weakens nothing and the window's own prohibition is honoured: Enum.min(counts) == @ready_offline_size still fails on a partial refill that paints fewer rows, Enum.min(counts) > 0 and the busy-treatment check are untouched, and all of them become HARDER to satisfy vacuously because the sampler is now guaranteed to span the transition instead of lucky to. Even in the worst case the diagnostics improve: if the sampler is starved past the bound, the failure now reads \"timed out waiting for the requestAnimationFrame sampler to record a frame at 50 rows\" instead of a row-count mismatch that looks like a rendering defect. What remains UNVERIFIED and should not be claimed: the original \"roughly 1 in 4\" rate was never reproduced and no failing message from those runs was ever kept, so whether this same mechanism caused them is unknown. WINDOWS #84 was also real and also load-sensitive, so both may have contributed.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-13T20:07:13.276Z",
    "resolved_at": "2026-09-14T19:11:08.079Z"
  },
  {
    "id": 82,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/scripts/ci/run-mac-verification.sh",
    "line": 1247,
    "description": "The UI layer's first few tests fail on hittability when the layer starts while the machine is still busy from the previous layer. Observed 2026-09-13 on this Mac: StorageInteractionTests run immediately after the Rendering layer produced 3 failures out of 27 -- testDownloadsPauseResumeFlow, testQuotaEditAndFocusRestoration and testCachedAndPinnedGameIsNotDescribedAsBeingOnTheServer -- every one of them 'control never became hittable, so the click would have been silently dropped', and every one of them among the first tests executed in the run. The identical selection on an idle machine passed 27/27 twice. Nothing about the app changed between the two runs. run_four_layer_verification runs unit -> rendering -> UI back-to-back in one invocation, which is exactly that pattern, and layers.json carries an automatic_retries field that is hard-coded to 0 -- so a slow window-server settle is currently indistinguishable from a real defect in the evidence, and a green run is partly luck about machine load. Options, in preference order: (a) have clickWhenHittable's waiter tolerate a cold start (a longer first-use timeout, or one warm-up hittability wait per test class), (b) settle explicitly between layers rather than sleeping blindly, (c) implement the automatic_retries field honestly -- retry a hittability-classified failure once, record the retry count, and never retry an assertion failure. Do NOT simply widen every timeout: this diagnostic message exists because a dropped click used to pass silently. EVIDENCE 2026-09-14: the UI layer has a second, distinct environmental failure on this Mac, and it is worth telling the two apart. After a run was interrupted, every subsequent UI invocation failed before any test executed with 'The test runner failed to initialize for UI testing. (Underlying Error: Timed out while enabling automation mode.)' -- three consecutive invocations, including a class this session never touched. Killing the long-lived user-owned /usr/libexec/testmanagerd (it respawns on demand) fixed it immediately and the same test then passed. So: 'timed out while enabling automation mode' before any test starts is a wedged testmanagerd, while 'control never became hittable' inside a test is the cold-start load problem this window is about. CI should distinguish them -- the first is worth one automatic retry after clearing the daemon, the second is not the same failure at all. THIRD FAILURE MODE, 2026-09-14, and the session it was found in can no longer verify UI accessibility audits at all: performAccessibilityAudit started failing with 'Error Domain=com.apple.xcode.xctest.accessibilityAudit Code=-56 Audit failed to complete in time' for every audit test (testReclaimPromptActionsPassLiveAudit, testStorageInventoryActionsPassLiveAudit, testDownloadsPauseResumeFlow), reproducibly and in isolation, after the same selection had passed 27/27 an hour earlier. BISECTED AGAINST CODE AND CLEARED: the identical test fails at commit 018ce1a in a clean throwaway worktree -- a tree that predates every UI element added this session -- so the variable is the machine, not the change. Restarting testmanagerd (which fixed the 'timed out while enabling automation mode' mode above) did NOT fix this one. Consequence to state plainly rather than paper over: no accessibility audit could be re-verified after that point in the session, so newly added library elements (the list row's status label and glyph) have unit and front-door coverage but NO audit coverage yet. Re-run the UI layer on a fresh login session before trusting a green audit result, and see #82's other two modes for how to tell the three apart. CLEARED 2026-09-14, THIRD MODE ONLY: the hosted macOS 26 runner ran the full four-layer verification at commit 30710cd (run 34870390370, job 'macOS 26 unit + rendering + UI + live server') and the UI layer PASSED -- 115 executed, 53/53 required verified, zero Code=-56. All three performAccessibilityAudit tests are required gates there (StorageInteractionTests/testReclaimPromptActionsPassLiveAudit, StorageInteractionTests/testStorageInventoryActionsPassLiveAudit, SurfaceAccessibilityTests/testKeyboardOnlySurfaceInventoryAndLiveAudit), so they genuinely executed against the library status label and glyph added this session rather than being skipped. The honest consequence recorded above is therefore retired: those elements DO have audit coverage, and the 'Audit failed to complete in time' reproduction is confirmed local to this Mac, matching the bisect at 018ce1a. NOTE the run's workflow-level conclusion reads 'cancelled' because a newer push superseded its concurrency group after the jobs finished -- every job in it concluded success, but do not cite that run id as a green required check. MODES 1 AND 2 REMAIN OPEN and are what this window is now about: hosted CI passing does not disprove the cold-start hittability problem, it only means that runner did not hit it, and automatic_retries is still hard-coded to 0.",
    "status": "open",
    "reason": "",
    "recorded_at": "2026-09-13T20:20:08.595Z",
    "resolved_at": null
  },
  {
    "id": 83,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Library/LibraryShellView.swift",
    "line": 518,
    "description": "The \"no matches\" empty state promised by 03-UI-SPEC's Copywriting Contract (line 235) is computed, unit-tested and snapshot-tested but NEVER RENDERED, and the state the user actually sees in its place is actively wrong. LibraryViewModel.searchResultState correctly returns a SearchResultState whenever a non-empty query matches nothing, FilterTests exercises it, and LibraryContractSnapshotTests builds one at line 126 -- so three layers of evidence say this is covered, and 03-UI-SPEC's own coverage table at line 259 claims \"covered\". No live view reads searchResultState. Its only renderer, NoMatchesView, sits in GameListView.swift, which nothing has instantiated since the shell took over rendering; grep confirms GameListView, GameListRow and NoMatchesView have no references outside comments and their own file. User-visible consequence, list layout: searching a paired library for a title that does not match falls through to catalogueList's entries.isEmpty overlay and displays \"No games yet\" with \"Pair with your Playstead server to see your library\" and a \"Pair with Server...\" button -- telling a user who is already paired and has a full library to go pair. Cards layout is wrong more mildly: ShelfView's emptyExplanation says \"No games match the current search and filters\", which is not blank but does not meet the contract either (no query echo in the heading, and no \"Clear search\" control, so LibraryViewModel.clearSearch is unreachable from the UI in exactly the state it exists for). This is the \"plumbed everywhere, proven nowhere\" and \"backstop truths hide real defects\" pattern: the unit and snapshot tests pass against the view model and a directly-constructed state, so neither can see that no view consumes it. Found 2026-09-14 while auditing what was left of GameListView after WINDOWS #79. The fix must render the contract copy on both layouts, wire the clear control, and be gated by a front-door UI test plus a topology guard pinning the shell's use of searchResultState -- not by another view-model test, which is the coverage that already failed to catch it. FIXED 2026-09-14 in the same session it was found. Both layouts now render NoMatchesView from library.searchResultState, and the empty list pane distinguishes three cases where it had one: a search miss (the 03-UI-SPEC Copywriting Contract copy, verbatim), a non-empty library emptied by the active filters, and a genuinely empty library (the pairing prompt, which is the only place it belongs). NoMatchesView carries playstead.surface.no-matches and its button playstead.control.clear-search, so clearSearch() is finally reachable from the UI. Gated by LibraryEmptyStateTests -- a FRONT-DOOR UI test driving the real search field, registered in TestPlans/UI.xctestplan and listed as three --required-test entries in run-mac-verification.sh, deliberately not another view-model test since view-model coverage is exactly what missed this -- plus a four-layer-topology guard that strips comments before matching, because both this shell and the guard file name searchResultState in prose and a whole-file grep would pass over a gutted body. Both probes falsified against the pre-fix tree: removing the cards binding failed with \"bindings=1, renders=1, expected 2 and 2\", and restoring the pairing prompt as the fall-through failed with the three-way diagnostic; the file was restored byte-identical after each. 03-UI-SPEC's coverage table row that falsely read \"covered\" was corrected in place rather than deleted.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-14T17:20:52.933Z",
    "resolved_at": "2026-09-14T17:42:22.739Z"
  },
  {
    "id": 84,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-server/test/playstead_web/plugs/throttle_test.exs",
    "line": 9,
    "description": "ThrottleTest's per-test IP was not unique, so its tests shared Hammer per-IP buckets and one arrived pre-throttled. CAPTURED 2026-09-14 by running the full Elixir suite (max_cases 36, 1079 tests) concurrently with a loop of xcodebuild Mac test runs -- the machine contention WINDOWS #81 named as its required next step. Run 5 of 6 failed: \"test call/2 denies requests beyond the per-IP limit with a rate_limited problem+json response (PlaysteadWeb.Plugs.ThrottleTest)\", throttle_test.exs:34, \"Expected false or nil, got true / code: refute conn.halted\", with the stacktrace landing on line 41 -- INSIDE the arrange loop, not on the assertion the test is about. That is the tell: the very first request of the test was already halted, so the per-IP bucket was full before the test began. MECHANISM, proven not hypothesised: setup derived the IP as {10, 0, div(unique, 65536) rem 256, rem(unique, 256)}, which drops bits 8..15 of the unique integer entirely. Any two uniques 256 apart map to the SAME address. Measured: over 65536 consecutive uniques the formula reaches 512 distinct IPs, and 2000 draws under 36-way concurrency collapse onto 72 -- a 96 percent collision rate. The per-IP bucket's scale is one minute (throttle.ex @per_ip_scale) while the suite runs for over three, so a collision within the file is a shared limit, and three of its tests each burn exactly their 5-hit limit on their setup IP. The setup's own comment has always claimed counters \"never bleed state across tests\"; they bled constantly and the comment was the giveaway. FIXED by deriving the address from three bytes instead of two ({10, b2, b3, b4}), which is injective over any 2^24 consecutive uniques against an observed draw spread of about 16k, and by moving the per-account test's hardcoded addresses from 10.1.x to 192.168.x so a generated address can never land on one. Gated by a new test that draws 2000 uniques concurrently and asserts the mapping is injective, plus the specific 1000000/1000256 collision; falsified against the old formula, which fails it 52 distinct of 2000. This is a strictly stronger assertion than the file had, not a weakened one.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-14T17:41:58.759Z",
    "resolved_at": "2026-09-14T17:41:58.919Z"
  },
  {
    "id": 85,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/PlaysteadTests/CurationTests/PlaySessionTests.swift",
    "line": 217,
    "description": "PlaySessionTests/test_launchSucceedsIndependentlyOfPlaySessionRecording fails under machine load on a five-second liveness wait. CAPTURED 2026-09-14 with the message, which is what previous flake windows here lacked: \"PlaySessionTests.swift:217: error: -[PlaysteadTests.PlaySessionTests test_launchSucceedsIndependentlyOfPlaySessionRecording] : Asynchronous wait failed: Exceeded timeout of 5 seconds, with unfulfilled expectations: process exits.\" Reproduced by running the Unit layer while the full Elixir suite (max_cases 36, 1079 tests) saturated the same machine; the layer took 421.7 seconds in that run against its usual 62, and the identical selection passes on an idle machine. The same test had been seen failing once before in this project and was attributed to machine load without the message being kept, so this is the second sighting and the first with evidence. MECHANISM: the test writes a tiny executable, hashes it, decodes a pin, and calls AdapterHost.launch, then waits five seconds for the process-exit callback. The binary exits in milliseconds when the machine is idle; under contention, spawning and reaping it plus the surrounding hashing can exceed the bound. RESOLUTION, and the reasoning matters because it looks superficially like the mistake WINDOWS #82 warns against: the bound was raised to 30 seconds, because the wall-clock is NOT part of this test's criterion. What it asserts is that the exit callback fires at all with no PlaySessionRecorder in the picture -- that is what its name says and what the rest of the test sets up. In #82's hittability case the timeout IS the diagnostic, because a dropped click used to pass silently, so widening it would destroy the assertion; here the assertion is the fulfillment itself and the timeout is only a liveness ceiling. If spawning and reaping a trivial binary ever exceeds 30 seconds that is a genuine hang and should still fail. NOT done, and worth considering if this recurs: the suite has only seven expectation waits in total (five at 5 seconds, one at 1, one at 0), so there is no shared convention to align on, and no CI-load-aware timeout policy exists. FURTHER EVIDENCE 2026-09-14, and the conclusion is deliberately NOT to widen again. With the 30-second bound already in place, this test failed once more during a Unit-layer run that took 2626 seconds against its usual 62 -- a 42x slowdown, self-inflicted, with a Wallaby browser suite and an Elixir full suite running against the same machine at the same time. The identical selection then passed in 0.78 seconds once that load cleared, and the hosted runner executes this layer in about 132 seconds of test time with no failure at all. So the correct reading is that the test rig produced a pathological condition no real CI machine reproduces, not that 30 seconds is still too tight. Raising the bound again to chase a self-inflicted 42x slowdown would be precisely the mistake #82 warns about in a different costume: the number would stop meaning anything and a genuine hang would no longer fail. If this ever fails on hosted CI, that is a different and much more interesting fact, and it should be investigated as a hang rather than answered with a larger constant. DIAGNOSIS RETRACTED 2026-09-14, in the same session that made it. This window is wrong: the failure is NOT machine load. Proof: the test failed again at the raised 30-second bound inside a Unit-layer run that took 69 seconds total on an idle machine, and the 1077-second and 2626-second layer times that were read here as \"contention\" were one single test, ReleaseHookAbsenceTests/testNonTestingReleaseBinaryAndSymbolsContainNoBootstrapProfileOrEnvironmentKey, which runs a full cold xcodebuild Release build from inside a unit test and took 960 seconds of that on its own; skipping it brings the layer to 38-to-78 seconds, which is the hosted figure. The bound was therefore never the problem, the bigger number did not fix anything, and it has been REVERTED to 5 seconds -- echo exits in milliseconds here, so a spawn that has not called back in five seconds is stuck, not slow. The real cause is recorded in WINDOWS #86: the tests that spawn real processes are coupled through two process-global singletons, AdapterLaunchMutex.shared and AdapterProcessRegistry.shared. The first half is fixed there; the rest is open. Keep this window as the record of a wrong call reached by reasoning from a single correlated observation without isolating the variable -- the layer was slow AND the test failed, and slow was assumed to cause failed. CAUSE FOUND 2026-09-14, and it was never timing. This window's failing test spawns a copy of /bin/echo that was never ad-hoc re-signed. macOS routes the spawn of a file it recognises as an application bundle's main executable through a security assessment, and a system binary's signature is only valid at its platform path -- so the copy fails that assessment, and the failure is silent: Process.run() succeeds, the child is left suspended at _dyld_start forever, and terminationHandler never fires. The test therefore waits on an exit callback for a process that will never exit, which is why 5 seconds failed, 30 seconds failed, and why no bound could ever have worked. Both the load diagnosis and the retraction's \"still unexplained\" reading are now superseded: the mechanism was already written down in this repo, in AdapterWiringTests.installStandInExecutable's doc comment, which re-signs its own stand-in for exactly this reason and asserts in prose that \"PlaySessionTests already does\" the same -- it did not. FIXED by routing every stand-in through a single StandInExecutable.install that copies, chmods and ad-hoc re-signs, and that throws a described error rather than letting an unsigned stand-in hang. Five of the suite's six stand-in copy sites were unsigned (PlaySessionTests, LaunchMutexTests x2, SavePlanExecutorTests, PlayPathSaveWiringTests); only AdapterWiringTests signed. The 5-second bound is restored and is correct: echo exits in milliseconds, so a spawn that has not called back in five seconds is stuck, not slow, and the bound should still fail if that ever recurs. This entry's status field has read \"fixed\" since the retracted change; it is now genuinely fixed, by a different change, for a different reason. See WINDOWS #86 for the second, independent coupling found alongside this one.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-14T18:08:03.468Z",
    "resolved_at": "2026-09-14T18:08:20.572Z"
  },
  {
    "id": 86,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Adapter/AdapterHost.swift",
    "line": 101,
    "description": "Every unit test that spawns a real child process shares two process-global singletons, and the local Unit layer is flaky in that cluster as a result -- 1 to 6 failures varying run to run on an idle machine. NOT FIXED; this needs its own plan. FOUND 2026-09-14 while chasing what WINDOWS #85 wrongly diagnosed as machine load. The affected tests are AdapterWiringTests (testInstallResultVerifiesAndLaunchesThroughARealAdapterHost, testSelectingAnAdapterThroughTheEnvironmentMakesLaunchVerificationSucceedAndActuallyLaunch), LaunchMutexTests (testMutexReleasesOnProcessExitRegardlessOfExitClassification, testRegisteredAdapterProcessIsTerminatedWhenApplicationTerminationHandlerRuns) and PlaySessionTests (test_launchSucceedsIndependentlyOfPlaySessionRecording). The shared symptom is that a spawned child's terminationHandler never fires, so an expectation on process exit times out; one run instead produced \"unknown(status: 15, reason: uncaughtSignal)\" where clean was expected, meaning the child was SIGTERMed rather than exiting on its own. TWO DISTINCT COUPLINGS, both through globals. First, AdapterLaunchMutex.shared is keyed by asset-set id and released only by the spawned process's termination handler, so ONE launch whose process never exits leaks that key for the rest of the run. Five tests across four suites used the literal \"test-asset-set\", so that leak turned every later launch into a thrown launchInProgress -- and critically MASKED REAL ASSERTIONS: InstallerTests/testAdapterHostRefusesLaunchWhenInstallStateIsUnverified reported \"expected .digestMismatch, got launchInProgress\", so a security-relevant refusal silently went unverified while the suite still reported a failure that looked like a locking problem. That half IS FIXED in this session by giving each test a unique id; the four-suite probe went from 6 failures in 16.0s to 0 failures in 1.8s, and AdapterWiringTests from 3-of-10 failing in isolation to 10-of-10 passing. Second, and still open: AdapterProcessRegistry.shared is also process-global, and testRegisteredAdapterProcessIsTerminatedWhenApplicationTerminationHandlerRuns deliberately exercises the app-termination sweep that SIGTERMs every REGISTERED process. A process left registered by an earlier test -- which is exactly what happens when a termination handler never fires -- is therefore killed by a later unrelated test, and a test's own process can be swept while it is still waiting on it. That is the likeliest source of the residual 1-to-6 failures and of the status-15 classification. WHAT IS NOT KNOWN: why a child sometimes fails to exit at all. AdapterWiringTests' own doc comment describes the mechanism for its case -- macOS routes the spawn of an app bundle's main executable through a security assessment, a copy of /bin/echo fails it, and the child is left suspended at _dyld_start forever with terminationHandler never firing -- and that is why it ad-hoc re-signs its stand-in. Whether the re-signing is failing on this machine, or whether syspolicyd is degraded after 16 days of uptime (the same family as #82), was not established. HOSTED CI IS GREEN on this layer (697 executed, 34 required verified, PASSED), so none of this is blocking and the local flakiness may be machine-specific -- but a suite coupled through two mutable globals is flaky by construction, so a green hosted run should be read as luck about timing rather than as evidence the coupling is safe. Next steps, in order: make AdapterProcessRegistry.shared injectable or resettable per test so the termination sweep cannot reach another test's process; then determine why the stand-in child hangs, on a machine rebooted first to rule out a degraded syspolicyd. LEDGER NOTE: WINDOWS #85 still reads status=fixed even though its diagnosis was retracted and its change reverted. gsd-tools windows has no reopen subcommand and refuses to waive an already-fixed entry, so that status field cannot be corrected through the tool; read #85's description, not its status, and treat this window as the live one. CONFOUND FOUND 2026-09-14, and it downgrades part of this window. After a laptop sleep/wake, xcodebuild on this machine began aborting with SIGABRT (exit 134) before compiling anything, logging \"DVTFilePathFSEvents: Failed to start fs event stream\" and \"DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = NSPOSIXErrorDomain Code=5 Input/output error\". A bare ps -r also hung for over two minutes while echo returned instantly, with 907 processes live. So the per-user cache/temp directory machinery and FSEvents are broken at the OS level, not in this project. TWO CONSEQUENCES, stated separately because they have different confidence. (1) The shared-mutex half of this window STANDS and is machine-independent: five tests across four suites keying one process-global mutex on the literal \"test-asset-set\" is a defect by construction, the masking was directly observed as \"expected .digestMismatch, got launchInProgress\", and the fix took the four-suite probe from 6 failures in 16.0s to 0 in 1.8s and AdapterWiringTests from 3-of-10 failing in isolation to 10-of-10 passing. (2) The residual \"1 to 6 failures varying run to run\" is NO LONGER attributable to AdapterProcessRegistry.shared with any confidence. A machine whose DARWIN_USER_CACHE_DIR lookup returns EIO can plausibly fail to spawn or reap children, which is the shared symptom, so the registry coupling remains a real design smell worth fixing but is NOT established as the cause of those runs. The same confound may also explain #82's third mode (performAccessibilityAudit failing with Code=-56 reproducibly late in a session) and should be checked against it. REQUIRED BEFORE ANY FURTHER LOCAL CONCLUSION: reboot this Mac and re-run the Unit layer. Until then, hosted CI is the only trustworthy evidence for this project, and no local flake should be attributed to the code. BOTH HALVES NOW CLOSED 2026-09-14. SECOND COUPLING (AdapterProcessRegistry.shared) FIXED: the registry is process-global AND it observed NotificationCenter.default, so the single test that legitimately exercises the app-termination sweep reached every other test's registered child. AdapterProcessRegistry now takes an injectable NotificationCenter (production still uses .default; the app has one termination event and one registry) and removes its observer on deinit; AdapterHost takes an injectable processRegistry defaulting to .shared; all 14 test-side AdapterHost constructions pass .isolatedForTesting(), and RelaunchTests' sweep test drives its own registry on its own private center instead of posting to .default. Injecting the registry alone would NOT have been enough -- every instance still observed the one default center, so a single post swept all of them; the center is the part that had to be injectable. PROVEN, not inferred from flakiness going away: a new test, RelaunchTests/testTerminationSweepReachesOnlyItsOwnRegistrysProcesses, registers one child in each of two registries, sweeps the first, and asserts the second survives. Falsified by restoring pre-fix conditions in place (file backups, NOT git checkout, which would have reverted the uncommitted fix under test) -- it failed on exactly the intended assertion, \"a process registered in a different registry must survive another registry's termination sweep\", at RelaunchTests.swift:185. THE \"WHAT IS NOT KNOWN\" IS NOW KNOWN, and it was NOT the registry: after the registry fix, PlaySessionTests still hung. The cause is unsigned stand-in binaries left suspended at _dyld_start -- see WINDOWS #85, fixed in the same session by StandInExecutable. The two couplings were genuinely independent, and the registry fix alone does not close this cluster. REPORTING ARTIFACT worth knowing before reading any future failure list: the baseline failure \"-[RelaunchTests testRegisteredAdapterProcessIsTerminatedWhenApplicationTerminationHandlerRuns] : XCTAssertEqual failed: (unknown(status: 15, reason: uncaughtSignal)) is not equal to (clean)\" is MISATTRIBUTED. RelaunchTests.swift contains no such assertion; it is LaunchMutexTests' onExit closure firing late and being charged to whichever test happened to be running. Do not chase RelaunchTests on the strength of that line. EVIDENCE, and its limits: on an idle machine the full Unit layer (ReleaseHookAbsenceTests skipped, which alone costs ~960s of cold Release xcodebuild) went from 0, 1 and 6 failures in three baseline runs to the post-fix runs recorded in this session's commit message, at a steady ~45s. That RATE evidence is local and this Mac has NOT been rebooted, so it remains subject to this entry's own standing instruction to distrust local flake measurements until it has been; hosted CI on the branch is the arbiter for the rate. The falsification result above is NOT subject to that caveat -- it is a named assertion flipping on a deliberate pre/post comparison, and the _dyld_start mechanism is documented in this repo's own AdapterWiringTests doc comment rather than inferred from this machine.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-14T19:56:00.640Z",
    "resolved_at": "2026-09-14T21:22:58.976Z"
  },
  {
    "id": 87,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Saves/SaveUploadLane.swift",
    "line": 80,
    "description": "The live-server harness decides \"real defect\" versus \"ordinary retry\" by reading a shared last-writer-wins cell that a CONCURRENT drain can overwrite, so an ordinary state can be reported as an unfixable server refusal. OBSERVED 2026-09-14, hosted run 34901279944 (head 6e4c1f8): SaveEndToEndTests/testOneSaveRoundTripsCaptureUploadAndJournalReturn failed at SaveEndToEndTests.swift:185, which is the one-assertion-site-per-cause marker save-e2e-harness=upload-server-refused -- the verdict WINDOWS #67 documents as \"a real defect, never a flake\". LiveServerSnapshotTests passed in the same run at 11.20s, inside its green band, so this is NOT #71 and NOT the same failure as run 34898512311. MECHANISM, established by reading the code rather than inferred from the run. UITestBootstrap.runSaveEndToEnd branches on TWO values with different lifetimes: drainResult, which belongs to the harness's own drainOnce pass, and lane.lastFailureClassification, which does not. The latter is a nonisolated computed property over SaveUploadLane's single classificationCell (SaveUploadLane.swift:80), written by whichever pass ran last -- set(classify(error)) on failure at :156 and set(.none) on success at :150. Since WINDOWS #67's own fix made the harness share the APP's lane, and the app drains on the reachability transition at pairing time, two passes now write that one cell while the harness reads it out-of-band at three decision points (UITestBootstrap.swift:347, :360, :382). So drainResult.stoppedForRetry can be true for the harness's ordinary .offlineQueue stop while the cell already holds the app pass's .revokedAuth, and isRetryable then returns false and the harness reports \"server refused\". The contamination runs both ways: the app's success path writes .none, which reads as retryable and would instead misreport an escalating failure as an ordinary one. THIS IS #67's OWN DEFECT ONE LEVEL UP. #67's root cause was asserting on \"the lane object I happen to hold incremented sent\" instead of the outcome, and its fix was to share the app's lane and assert the revision reaches .uploaded. The uploaded check was correctly moved to the outcome; the failure CLASSIFICATION was not, and sharing the lane is exactly what made that cell cross-contaminated. #67 is recorded status=fixed and gsd-tools has no reopen, hence this separate entry -- see also this ledger's own note that a retracted entry's status field cannot be corrected. NOT CAUSED BY 6e4c1f8 or 86c816c: neither touched Saves, Sync or UITesting, and the unit, rendering and UI layers all passed in this same run (697 / 45 / 118 executed, 0 failures). FIX DIRECTION, not applied: OutboxDrainResult (OutboxWorker.swift:5) already carries sent, rejected and stoppedForRetry but not the classification, which is precisely why the harness has to reach for the shared cell. Carrying the pass's own classification back in that struct makes the harness branch on the pass it actually ran, and is checkable in the unit layer alongside the existing SaveUploadLaneTests rather than only in the live-server layer. NOT ATTEMPTED HERE because it is production code in the save path and the layer that exercises it cannot be run locally; it wants its own plan and a falsification probe, in the style of the four already pinned in four-layer-topology-test.sh. WHAT IS NOT KNOWN: whether the observed run was this race or a genuine .revokedAuth against the freshly provisioned CI server. The harness cannot currently tell those apart, which is itself the finding -- the reason channel carries a fixed literal by design, so the classification that drove the verdict is never reported. Reporting it (it is a closed enum this repo owns, so it is safe on that channel under this file's own stated rule) would settle the next occurrence in one run. FIXED 2026-09-15, and proven rather than inferred. OutboxDrainResult now carries failureClassification, SaveUploadLane.drainOnce records its own pass's verdict there alongside the shared cell, and all three of the harness's decision points read drainResult.failureClassification instead of lane.lastFailureClassification -- so stoppedForRetry and the classification are now guaranteed to describe the same pass. The shared cell is untouched and still serves the shipped escalation panel, which legitimately wants the most recent verdict. UNIT-LEVEL PROOF, in the layer that can actually be run locally: SaveUploadLaneTests/test_aLaterSuccessfulPassDoesNotRewriteTheFailedPassOwnClassification drives one pass to a 401 (.revokedAuth, one of D-40's four unfixable reasons) and then a second pass to success, which resets the cell to .none. Afterwards lane.lastFailureClassification reads .none -- \"nothing is wrong\" -- while the first pass genuinely was refused. Falsified by deleting the single line that records the classification on the result: the test fails at SaveUploadLaneTests.swift:296 with (\"none\") is not equal to (\"revokedAuth\"), which is exactly the misreport this window describes, and the restore was verified byte-exact rather than by git checkout. THREE CONTRACT GUARDS, each falsified in place. four-layer-topology-test.sh now (1) forbids lane.lastFailureClassification anywhere inside runSaveEndToEnd outright, rather than counting correct uses -- reading the lane there IS the defect; (2) requires drainResult.failureClassification at all three decision points; (3) separately requires OutboxDrainResult to declare the field and drainOnce to write it, because the harness-side guard passes perfectly well against a field nobody writes, which would silently reclassify every refusal as retryable and make the whole test unfailable. Probes 1, 2 and 3 each failed on their own intended message and the tree was restored clean. DIAGNOSTIC, closing this entry's \"what is not known\": the single save-e2e-harness=upload-server-refused verdict is now four -- refused-revoked-auth, refused-capability-skew, refused-server-refusal, refused-compatibility-rejection -- one assertion site each in SaveEndToEndTests, because D-40's four unfixable reasons are four different verdicts and CI keeps only file:line. So the next occurrence says which refusal it was in one run, and since the classification now provably belongs to the failing pass, a recurrence can no longer be dismissed as this window's race. Each remains a fixed literal over a closed enum this repo owns, per UITestBootstrap's own stated rule that an error's own description must never reach the reason channel. NOT YET SEEN GREEN ON THE LIVE-SERVER LAYER: that layer cannot be run locally and reproduces only on hosted CI. The unit layer ran 696 tests with 0 failures with these changes in place, and all static contract guards pass.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-14T22:24:05.812Z",
    "resolved_at": "2026-09-15T14:01:00.180Z",
    "milestone": "v1.0"
  },
  {
    "id": 88,
    "kind": "deviation",
    "phase": "03.5",
    "file": "playstead-mac/PlaysteadTests/AdapterTests/StandInExecutable.swift",
    "line": 1,
    "description": "THIS MAC CANNOT EXECUTE A COPIED BINARY AT ALL, and every local child-process test failure on it is therefore uninformative about the code. MEASURED 2026-09-15 at 17 days uptime, outside Xcode and outside any test: copy /bin/echo to a fresh path, chmod +x, run it -- it never executes and produces no output. Signed or unsigned makes NO difference (ad-hoc codesign succeeds, exit 0, and the copy still hangs), and neither does the directory: $HOME, /tmp, and the DARWIN_USER_TEMP_DIR that FileManager.temporaryDirectory returns all reproduce it, three trials each on distinct fresh paths, rc=124 under timeout for the signed variant and rc=137 for the unsigned. /bin/echo at its own system path in the same shell returns rc=0 and prints normally, and a copy of /usr/bin/true hangs identically, so it is not specific to one binary. This is a broken macOS execution path -- the AMFI/syspolicyd/trustd family -- not a project defect. METHOD NOTE, because the first run of this probe reported the opposite and would have put a false mechanism in this ledger: re-using ONE path and cp-ing over it makes the result meaningless, because cp reuses the inode and the earlier ad-hoc signature is still cached against it, so an \"unsigned\" trial silently inherits the previous \"signed\" trial's state. The corrected probe stages every trial at its own fresh, never-before-signed path. Any future re-measurement must do the same. WHAT THIS EXPLAINS: a full local Unit run on this machine now fails 5 tests -- AdapterWiringTests/testSelectingAnAdapterThroughTheEnvironmentMakesLaunchVerificationSucceedAndActuallyLaunch, LaunchMutexTests/testMutexReleasesOnProcessExitRegardlessOfExitClassification at LaunchMutexTests.swift:193 and :196, and PlaySessionTests/test_launchSucceedsIndependentlyOfPlaySessionRecording at PlaySessionTests.swift:236, each an expectation on a spawned child exiting. It reproduces with only those four suites selected (27 tests, 5 failures), so it is NOT contention from the full suite. WHAT IT DOES NOT MEAN: it is not a regression in WINDOWS #85 or #86. Those were closed against hosted CI, which ran this same Unit layer green three consecutive times at 697 executed / 0 failures on commits 86c816c, 6e4c1f8 and 295038f, and the local commits since then touch only Outbox, Migrations, two PlaysteadApp doc comments and OutboxTests -- nothing in the adapter or stand-in path. #85's mechanism also still stands: ad-hoc signing is the right fix for an unsigned copy of a bundle's main executable on a HEALTHY machine, and this entry is a second, independent failure mode in which signing is simply not the discriminator because nothing copied runs at all. THIS IS THE CONFOUND WINDOWS #86 PREDICTED, now measured rather than suspected. #86 recorded the same machine at 16 days uptime with DVTFilePathFSEvents and DARWIN_USER_CACHE_DIR EIO failures and issued a standing instruction: \"REQUIRED BEFORE ANY FURTHER LOCAL CONCLUSION: reboot this Mac and re-run the Unit layer. Until then, hosted CI is the only trustworthy evidence for this project, and no local flake should be attributed to the code.\" That instruction was correct and is hereby re-asserted with direct evidence behind it. ACTION: reboot this Mac, then re-run the probe above before trusting any local child-process result. Until the probe passes, treat a local Unit run's child-process failures as measuring the machine, and read the layer's other ~698 tests only. Hosted CI remains the arbiter. RETRACTED 2026-09-15, SAME DAY, BY THE AUTHOR OF THE ENTRY. This window does not exist. The machine is fine, and the characterisation above is a probe artefact that I wrote into the ledger as a finding. WHAT ACTUALLY BROKE IT: every trial in the original probe copied a PLATFORM binary -- `/bin/echo` and `/usr/bin/true`, nothing else -- and the conclusion \"this Mac cannot execute a copied binary at all\" generalised from a single subject class that happens to be the one class where copying genuinely does fail, on every healthy Apple Silicon Mac, by design. The fresh-path discipline the earlier retraction added was necessary and was correctly applied; it simply was not sufficient, because the missing control was a different SUBJECT, not a different path. RE-PROBED ACROSS CLASSES, one command, each trial at its own fresh path: a locally built binary runs both in place and copied (rc=0); a copy of an ordinary third-party binary runs; a copy of `/bin/echo` is SIGKILLed (rc=137); that same copy after `codesign -f -s -` runs and prints its argument (rc=0); that same copy with `--remove-signature` is SIGKILLed again (arm64 requires a signature). That is the ordinary AMFI rule -- a platform binary's signature is only valid inside the platform trust domain, so off its system path it fails validation -- and it is not a fault of any kind. It also directly contradicts this entry's recorded claim that signed copies hang at rc=124: an ad-hoc re-signed copy runs. THE 17-DAY-UPTIME CONFOUND NARRATIVE IS WITHDRAWN WITH IT. Uptime is still 17 days, unchanged, no reboot has happened -- and the full Unit layer now passes 714 of 714 with zero failures, including `AdapterExitBoundaryTests`, whose `testEachRealTerminationClassifiesAsTheRightNamedCase` and `testNoRealChildProcessTerminationClassifiesAsUnknown` are precisely the child-process tests this entry claimed the machine could not run. They pass because the stand-in executable they spawn is LOCALLY BUILT, which was never the failing class. So no reboot is required of the user, and local child-process evidence was never untrustworthy. WHAT THIS ENTRY COST, recorded because the cost is the lesson: it declared local evidence unreliable and asked for a user reboot, both on the strength of a probe with no cross-class control. STILL UNEXPLAINED, and deliberately not folded into this entry: the earlier child-process test failures that prompted the probe in the first place. They are not accounted for by anything here, they did not reproduce in the 714/714 run, and they need their own entry with their own evidence if they recur -- attaching them to a retracted mechanism is exactly how this entry went wrong the first time.",
    "status": "waived",
    "reason": "Retracted by the author the same day: probe artefact, not a defect. Every trial copied a platform binary, the one class where copying fails by design on Apple Silicon. Cross-class re-probe: locally built and third-party copies run; an ad-hoc re-signed copy of /bin/echo runs. Unit layer passes 714/714 including the child-process tests this entry claimed were unrunnable. No reboot needed; nothing to fix.",
    "recorded_at": "2026-09-15T14:31:26.880Z",
    "resolved_at": "2026-09-15T15:23:30.605Z",
    "milestone": "v1.0"
  },
  {
    "id": 89,
    "kind": "deviation",
    "phase": "03",
    "file": "playstead-mac/Playstead/Sync/Outbox.swift",
    "line": 197,
    "description": "An outbox row left `in_flight` when the app quits is stranded permanently: never retried, never quarantined, never surfaced -- while its optimistic local write stands, so the local model and the server diverge and nothing ever reconciles them. FOUND 2026-09-15 by the independent re-verification of phase 03, as a side observation while checking WR-07's watermark, not by a failing test. MECHANISM, confirmed by direct read of Outbox.swift: `markInFlight` (line 197) is the only writer of `state = 'in_flight'`, and the only paths out of that state are `markDone`, `markRejected` and `markPendingForRetry` -- all three of which are reached only from `OutboxWorker.drainOnce`'s in-process handling of a response. Nothing runs at startup. `listPending` selects `state = 'pending'` exclusively, so a stranded row is invisible to every subsequent drain, and `listQuarantined` never sees it either because quarantine is reached only through `markPendingForRetry`'s attempt counter. So a favourite, a collection rename or an availability report that was on the wire at the moment of a crash, a force-quit, or an ordinary quit mid-request is simply gone from the outbox's point of view, while `applyOptimistically` has already written it locally. THE OBVIOUS FIX IS COUPLED TO WR-07, and that coupling is the reason this is filed rather than left as a note. The natural repair is a startup sweep moving `in_flight` back to `pending`. That sweep is exactly what makes WR-07's cross-restart case REACHABLE: today an `in_flight` row at quit can never revert, so the scenario the delivered-watermark protects against cannot currently occur, and the protection is latent. The moment this window is fixed, that path goes live -- and it is the CONDITIONAL gate on the watermark migration (Migrations.swift, `pragma_table_info(...) WHERE name = 'created_at'`) that then keeps WR-07 from returning, because an unconditional drop would wipe the watermark on the very launch that performs the sweep. Whoever fixes this must not independently \"simplify\" that gate. `test_theWatermarkSurvivesAnOrdinaryRelaunch` in OutboxWatermarkMigrationTests already pins the gate and will fail if it is removed. NOT YET SIZED: whether a blind startup revert is even correct, since an `in_flight` row is a request that may well have been received and applied by the server. The idempotency key is preserved across attempts (P4-CR-001, pinned by test_retry_sendsTheSameIdempotencyKeyAsTheFirstAttempt), so a replay should be absorbed rather than duplicated -- which suggests the sweep is safe for the curation kinds, but that argument needs checking per kind, and against the server's own replay handling, before it is relied on. FIXED 2026-09-15. `Outbox.recoverStrandedInFlight(at:)` sweeps every `state = 'in_flight'` row, and `AppEnvironment`'s initializer calls it immediately after constructing the `Outbox` -- the one point in the process that runs exactly once, before any of the three drain triggers can fire. THE UNSIZED PREMISE WAS CHECKED FIRST, not assumed: this entry said the replay-safety argument \"needs checking per kind, and against the server's own replay handling\", so both were read. `idempotencyKey` is `kind:entryID`, generated once in `enqueue` and PERSISTED as outbox_entries column 3, read back by `rows(_:_:)` -- so it is byte-identical across attempts AND across restarts, which is the property that makes reverting a possibly-applied request safe at all. On the server, all 16 real `CurationIntentKind` cases map onto routes that sit in a scope piping through `:idempotency` (every `/api/v1/curation/*` mutation, `/play-sessions`, and `PUT /devices/me/availability`), and that plug holds a per-device receipt keyed on {device_id, idempotency_key} with a unique index (D-20a). A replay is therefore absorbed, not applied twice. RESIDUAL, recorded rather than engineered around: receipts are pruned at ~90 days, so a row stranded longer than that would genuinely re-execute; the sweep running at the very next launch is what bounds this in practice. THE SWEEP GOES THROUGH `markPendingForRetry`, NOT A BARE `UPDATE ... SET state = 'pending'`, and that is the whole non-obvious part of the fix -- it buys three properties a bare revert loses: the attempt counter advances so a row that strands on every launch eventually quarantines instead of replaying forever, backoff stops a launch firing every recovered request at once, and WR-07's supersede check runs so a stale report is dropped rather than revived behind a newer one. FALSIFIED BY MUTATION, two mutants, because the four-round WR-07 lineage showed the defects land in the fix rather than the original. Mutant B, the bare `UPDATE` -- exactly what someone would write not knowing about the counter or the supersede rule -- fails 5 assertions across precisely the two discriminating tests (`...EventuallyQuarantinesRatherThanReplayingForever` and `...IsDroppedRatherThanRevivedBehindANewerDeliveredOne`) and passes the other four, which is the correct discrimination and not a blanket failure. THE COUPLING THIS ENTRY WAS FILED FOR IS NOW LIVE AND PINNED. WR-07's cross-restart case was latent only because an `in_flight` row at quit could never revert; it can now, and `test_aStrandedReportIsDroppedRatherThanRevivedBehindANewerDeliveredOne` is the test that walks it -- stranded report, newer report delivered so `markDone` deleted its row and only the watermark remembers it, then relaunch and sweep. That test passes only because the watermark migration's drop is CONDITIONAL on the legacy `created_at` column; an unconditional drop would wipe the watermark on the very launch that performs the sweep, and `test_theWatermarkSurvivesAnOrdinaryRelaunch` pins that gate. TESTS: `PlaysteadTests/SyncTests/OutboxStrandedInFlightTests.swift`, 6 tests, all against a SECOND `Outbox` over the same `LocalStore`, because that is what a relaunch actually is. Green locally at 34/34 alongside OutboxTests (25) and OutboxWatermarkMigrationTests (3). NOT `try?` at the call site: a failed sweep leaves rows stranded, which is the silent divergence the sweep exists to end, so it is logged via a `startup-recovery` OSLog category rather than swallowed, and is not fatal -- the next launch sweeps again.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-15T14:41:04.741Z",
    "resolved_at": "2026-09-15T15:21:37.065Z",
    "milestone": "v1.0"
  },
  {
    "id": 90,
    "kind": "deviation",
    "phase": "04",
    "file": "playstead-mac/Playstead/Persistence/Migrations.swift",
    "line": 1,
    "description": "`save_revision.restored_here_at` is the only column in the whole schema declared SOLELY by a defensive `try? ALTER`, with no matching entry in its own `CREATE TABLE` block -- so the statement that creates it on a fresh database is also the statement a future cleanup is most likely to delete as redundant. FOUND 2026-09-15 by the independent re-verification of phase 03, which extracted every CREATE block and every ALTER in Migrations.swift and scanned all 19 commits that have touched the file: 11 of the 12 ALTER-added columns also appear in their CREATE block, and this one does not. SOUND TODAY, and that is precisely the hazard -- the ALTER runs unconditionally on every launch, so every database has the column and nothing is broken right now. The failure is latent and arrives with an ordinary tidy-up: delete the \"redundant\" ALTER, and a fresh database silently lacks a column that `SaveStore` reads and writes in four places, with the error swallowed by the `try?` that made the line look optional in the first place. RELATED BUT NOT THE SAME MODE AS WR-08. WR-08 was a `CREATE TABLE IF NOT EXISTS` no-opping against an older shape so that a defensive ALTER left the table half-migrated and unwritable. This is the mirror image: the CREATE is the incomplete one, and the ALTER is load-bearing rather than defensive. Both come from the same root cause, which is worth stating plainly because it will keep producing entries: Migrations.swift has NO `user_version` gate anywhere (grep count 0), so every statement in it is written to be idempotent and re-runnable, and there is no syntactic distinction between \"this ALTER upgrades an old database\" and \"this ALTER is the only thing that creates the column\". FIX, when phase 04 is touched: add `restored_here_at` to `save_revision`'s CREATE TABLE so the ALTER becomes genuinely redundant, and keep the ALTER for existing databases. Do not simply delete the ALTER. A `user_version` scheme would retire this whole class, but that is a larger change than this entry justifies on its own. FIXED 2026-09-15. `restored_here_at TEXT` is now declared in `save_revision`'s own CREATE TABLE block, so the defensive `try? ALTER` below it is genuinely redundant on a fresh database and load-bearing only for an install predating 04-22; the ALTER is KEPT, and its comment now says so explicitly so the next tidy-up deletes nothing. GUARDED, and this is the part that outlives the one-column fix: `scripts/ci/tests/migration-column-declaration-test.sh` parses Migrations.swift, extracts every CREATE TABLE column list and every `ALTER TABLE ... ADD COLUMN`, and fails when any ALTER-added column is absent from its own CREATE block. It reports 12 ALTER-added columns all declared, which independently reproduces the re-verification's hand count of 12 with 11 previously declared. The guard supplies exactly the syntactic distinction this file lacks -- with no `user_version` gate anywhere, nothing else separates \"this ALTER upgrades an old database\" from \"this ALTER is the only thing that creates the column\". FALSIFIED against the pre-fix source: the detector names `undeclared=save_revision.restored_here_at` and exits 1. It also fails closed three ways, each exercised in the guard's own self-tests -- an ALTER-only column, a source containing no ALTER statements at all (a detector that matches nothing must not report success on a file it never understood), and a missing file. Registration needed no action: run_contract_self_tests enumerates tests/*.sh from the directory rather than a hand-kept list, so the guard is picked up by existing. The full contract gate passes 25/25 with it included. NOT FIXED HERE: the absent `user_version` scheme, which is the shared root cause of this entry and WR-08 and will keep producing entries of both shapes. That remains a larger change than either entry justifies.",
    "status": "fixed",
    "reason": "",
    "recorded_at": "2026-09-15T14:41:04.833Z",
    "resolved_at": "2026-09-15T15:21:36.915Z",
    "milestone": "v1.0"
  }
]
````
